package ca.spottedleaf.concurrentutil.map;

/**
 * COMPILE-ONLY STUB (chunksend netty-compile-stub precedent): the real class
 * ships with the server's concurrentutil module at runtime, not in the kernel
 * jar. Only erased signatures the ChunkSchedOps bridge invokes are declared —
 * the stub is NEVER packaged into the blob.
 */
public final class ConcurrentLong2ReferenceChainedHashTable<V> {
    public V put(long key, V value) { throw new UnsupportedOperationException("stub"); }
    public V remove(long key) { throw new UnsupportedOperationException("stub"); }
    public V get(long key) { throw new UnsupportedOperationException("stub"); }
}
