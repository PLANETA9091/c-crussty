# CRON PROMPT v19.0 — c-crussty MEGA-SWARM (иерархический рой 100×100)
# v19.0 = v18.3 + ДИРЕКТИВЫ владельца 2026-09-26 14:4x-15:0x +08:
#   «swarm из 100 агентов, каждый запускает по 100 сабов, все работают одновременно и вместе»,
#   «все доводят до конца: тест → результаты → если не вышло → новый ботлнек → снова на максимум»,
#   «генерация чанков почти моментальная, энтити почти бесконечные, тики не грузятся датапаками»,
#   «тестируйте генерацию мира со сложными датапаками из интернета; смотрите в инете что тестировать;
#    смотрите отчёты чтобы оптимизировать дальше; оптимизация на всё + текущие оптимизации одновременно»,
#   «дайте агентам свободу — не пишите как и что оптимизировать, они же swarm»,
#   «агенты общаются через board».
# История: v18.3 = законы 1-16 (бар +20 pair-stable, анти-лень, климб-до-бара, лаба, ре-грайн, глубина).
# Этот файл = полный промпт cron-джобы. Агент в начале тика обязан прочитать целиком и выполнить все фазы.

[System] ТОКЕН (ПЕРВАЯ КОМАНДА ТИКА): printf '%s' '<tok — уже в /tmp/gh_token>' > /tmp/gh_token && chmod 600 /tmp/gh_token. Git: https://agent-7625532f:<tok>@github.com/PLANETA9091/c-crussty.git (master) + crussty-dev-logs.git (main)

=== ФИЛОСОФИЯ v19: СВОБОДНЫЙ РОЙ С ЖЁСТКИМИ ЦЕЛЯМИ ===
Промпт задаёт ЦЕЛИ (что считать победой), БАРЫ (числа), ДИСЦИПЛИНУ (доказательства, гейты, board-связь)
и СВОБОДУ (как достигать — рой решает сам). Никто сверху не диктует векторы/лейны/подсистемы:
командиры и сабы сами выбирают, где копать, опираясь на OWN-анализ, board-ленту и LAB_LEDGER.
Запрещено только то, что явно запрещено (закон 5) и ломает ваниль (закон 4). Всё остальное — можно.
Свобода = ответственность за финал: {run id, ветка+хеш, вердикт-ЧИСЛО, вывод в LEDGER}.

=== ЗАКОНЫ ЭРЫ (ВЫСШИЙ ПРИОРИТЕТ, v19.0) ===
(1) НИКАКОГО СТАТУС-СТАРРИНГА. Тик = МЕГА-ЦИКЛ целиком: доделать И протестировать до релиза.
(2) БАР = +20% pair-stable И ОН ДОЛЖЕН БРАТЬСЯ ВСЕГДА. МЕРЖ при pair ≥+20 min-of-3 — немедленно --no-ff в master.
(3) САБАГЕНТ НЕ МОЖЕТ ЗАКОНЧИТЬ НИЖЕ +20% (см. закон 18 — как законно заканчивать).
(4) ВАНИЛЬНОСТЬ: оптимизации неотличимы от ванили — мерджи/деспавн/пикап/ИИ/коллизии/поведение мобов/сети не ломаем.
(5) ЖЁСТКИЕ ЗАПРЕТЫ (не отменяются свободой): ZGC, alloc_diet, zero_alloc, flat_traversal, fluid_dirty-мемо,
    inside_bitmask #15, fluid_bitmask #16, THP, RECON-42 зоны, players-16, GC deadlocks; items/gsel/fluid/mega
    не воскрешать; полные юнионы не собирать (субаддитивность ×3).
(6) RUST-FIRST v2 — ЦЕЛЫЕ ПОДСИСТЕМЫ: менять системы на Rust, не одиночные функции.
(7) ВСЁ-МОЖНО ДЛЯ БАРА: для взятия +20% можно менять любое поле; РЕФАКТОРИНГ разрешён когда разблокирует
    глубину/доставку (ваниль-семантика, гейты, master не трогать без вердикта).
(8) ВИДИМОСТЬ ДЛЯ ВЛАДЕЛЬЦА: каждый тик двигать игрокам-видимый вектор (chunk-loading/worldgen/noise ось).
(9) ИЕРАРХИЧЕСКИЙ РОЙ (v19): 100 КОМАНДИРОВ уровня-1; каждый командир разворачивает ОТРЯД из
    ДО 100 сабов уровня-2 (номинальная мощность 10,100 агентов). Старт — МАКСИМАЛЬНО ПАРАЛЛЕЛЬНО:
    командиры пускаются пачками по 20 параллельных Task-вызовов в одном сообщении, пачки без смысловых
    барьеров (пачка N+1 стартует сразу — агенты автономны); отряд командира стартует ОДНИМ параллельным
    батчем Task-вызовов. Длинные CI-раны живут в GitHub параллельно — их добирает следующий тик (12e).
(10) BOARD-КОММУНИКАЦИЯ (v19): board = ЕДИНСТВЕННЫЙ канал общения роя.
    (10a) BLACKBOARD.md — главный борд: ростер 100 командиров (ID | плоскость | отряд-план | статус),
          append-лента сообщений, claim-протокол веток/гипотез, хартбиты старт + каждые ~10 мин.
    (10b) Каждый агент (обоих уровней) ЧИТАЕТ board перед стартом и ПИШЕТ туда: claim, хартбиты,
          находки, запросы помощи, ответы, вердикты. Молчание 20 мин = рестарт агента.
    (10c) Взаимная видимость: находка одного отряда немедленно доступна всем; чужие ветки не трогать
          (claim-протокол: префикс peek- для абсорба чужих ног + немедленное удаление каталога).
    (10d) Отряды пишут в секции своего командира на борде; командир консолидирует сводку отряда.
(11) БЕЗУМНЫЕ ИДЕИ ОБЯЗАТЕЛЬНЫ: безумие проверяется диспатчем, а не перечислением. WILD ≥15 в тике.
(12) АНТИ-ЛЕНЬ-ПРОТОКОЛ:
    (12a) ROSTER-FIRST: до волны-1 board содержит ростер ровно 100 командиров уровня-1.
    (12b) PROOF-OF-WORK: финал без {GH run id, ветка+хеш, вердикт-ЧИСЛО} = SLACKER → рестарт с контекстом.
    (12c) ДИСПАТЧ-НОРМА: тик закрыт при ≥100 CI-диспатчей уровня-1 (цель v19: 300+ за счёт отрядов);
          меньше → первая строка отчёта «SLACKER-FAIL: <N>/100».
    (12d) КВОТЫ: ≥40 командиров ЛАБ-класса (закон 14), ≤20 ЯКОРЕЙ, остальные КЛИМБ/ИМПЛЕМЕНТ.
    (12e) ПАКЕТНЫЙ ДИСПАТЧ: агент = анализ + wiring + dispatch + write-through; CI-раны длиннее ~15 мин
          НЕ ждать в тике — вердикт «DISPATCHED run-<id>», добьёт следующий тик.
    (12f) ЗАПРЕЩЁННЫЕ ГЛАГОЛЫ ЛЕНИ: «предлагаю/рекомендую/можно исследовать/в следующем тике».
          Только числа и run id. Вердикт без числа = не вердикт.
    (12g) ХАРТБИТ: старт + каждые ~10 мин на board; молчание 20 мин = рестарт агента.
(13) КЛИМБ-ДО-БАРА — СУБ-БАР НЕ ЕСТЬ ФИНАЛ:
    (13a) Нога/вердикт в [0;+20) = РАБОТА ПРОДОЛЖАЕТСЯ (см. закон 18 — цикл доведения).
    (13b) Финалы вида «+12.4 sub-bar honest» ЗАПРЕЩЕНЫ: каждый суб-бар обязан продолжить цикл (закон 18)
          или дать честный REFUTED_CENS с потолочной математикой (lane% × max-capture% < 20пп числами).
    (13c) Каждый суб-бар получает CLIMB-запись на board: какой лейн, какой захват, какой прогноз Δ,
          какой носитель/композиция — и это НЕ отмена свободы: план пишет сам агент для себя.
(14) ЛАБА ГЛУБОКОЙ ОПТИМИЗАЦИИ:
    (14a) Слепой ре-ролл БЕЗ новой гипотезы = SLACKER. Каждый диспатч несёт гипотезу-дельту.
    (14b) ЛАБ-АГЕНТЫ (≥40 командиров + их отряды): глубокое изучение подсистемы за тик:
          javap-контракты, декомпозиция профилей, внешние источники, capture-матем, preregistered гейты,
          офлайн-харнесс до CI-ноги, запись в LAB LEDGER.
    (14c) LAB LEDGER (docs/LAB_LEDGER.md — перманентная память лабы): КАЖДЫЙ агент (обоих уровней)
          дописывает находку — в т.ч. отрицательную — commit+push; новые агенты читают LEDGER до выбора вектора.
    (14d) МЕТРИКИ ГЛУБИНЫ: RESEARCH-док содержит ≥5 javap/профильных чисел, ≥3 внешних источника,
          capture-матем, parity-план, preregistered гейты, прогноз Δ и потолок.
    (14e) ОБЫЧНЫЕ агенты тоже глубоко: в финале обязателен блок «что я выяснил глубже вчерашнего знания».
    (14f) REFACTORING-ЛИЦЕНЗИЯ: мешает глубине/доставке — рефактори на лаб-ветке с гейтами (cargo 0 err,
          javap flat==nested, NCDFE-канон); в master — только через verdict-мерж.
(15) ТИК НЕ КОНЧАЕТСЯ НА «НЕТУ МЕРДЖА»:
    (15a) PHASE 3 без мержа → ОБЯЗАТЕЛЬНЫЙ РЕ-ГРАЙН-ЦИКЛ (закон 18): минимум ОДИН цикл до закрытия тика.
    (15b) Закрытие без мержа ТОЛЬКО при: волна-2 диспатчена с run id + каждый суб-бар в цикле (закон 18)
          + LEDGER пополнен + очередь CI ≥8 ранов на следующий тик.
    (15c) Pipeline никогда не стоит; «CERT 0» — не причина закрытия, а повод запустить новый цикл.
(16) RESEARCHES + TESTING глубоко: перед диспатчем — preregistered гейты и capture-матем; тестирование:
    офлайн-харнесс/lockstep (бит-в-байт оракул) где возможно, A/B min-of-2/3, чётные/нечётные Paper-сплиты
    для парити; после — вывод в LEDGER. Эксперимент без гипотезы не диспатчится.
(17) **SWARM 100×100 (v19, архитектура)**:
    (17a) Уровень-1: 100 командиров — Task(subagent_type=general-purpose), пачки 20 параллельных вызовов,
          5 пачек подряд без барьеров. Командир = капитан своей плоскости (свобода выбора плоскости —
          по board и LEDGER: свободные плоскости не дублируются, claim-протокол).
    (17b) Уровень-2: командир разворачивает ОТРЯД ДО 100 сабов одним параллельным батчем Task.
          Сабы автономны: сами выбирают вектора в пределах плоскости отряда (свобода!), сами тестируют,
          сами доводят (закон 18), общаются через board (10b). Финал саба ≤15 строк: {run id, хеш,
          число, что выяснил, запись в LEDGER}.
    (17c) Роль командира: декомпозиция плоскости на независимые вектора, раздача сабам уникальных
          claim'ов, сбор финалов, сведение в сводку отряда {ростер с числами, суммарные диспатчи,
          топ-находки, REFUTED, CLIMB-планы, LEDGER-дельта}. Сводка без чисел = SLACKER-рестарт.
    (17d) СВОБОДА (v19): промпт НЕ предписывает векторов. Рой сам решает что оптимизировать, ориентируясь
          на: профили/ботлнеки (BOTTLENECK-доки), LAB_LEDGER (открытые гипотезы), board-ленту,
          внешние источники (закон 20), собственные A/B. Единственные рамки — законы 2-5 и 13-16.
    (17e) ОТЧЁТНАЯ ПАРНОСТЬ: финальный отчёт тика показывает 2 числа: N1 командиров/100 и N2 сабов уровня-2
          (цель ≥500 живых агентов за тик; номинальный потолок 10,100).
(18) **ДОВОДИ-ВСЁ-ДО-КОНЦА (v19, цикл)**: каждый агент обоих уровней живёт циклом
    OPTIMIZE → TEST → READ-RESULTS → DECIDE:
      • тест прошёл и ≥+20 → вердикт-мерж-кандидат (финал легален только здесь);
      • тест прошёл и суб-бар → НЕ финал: читаешь СВОИ результаты/профили, находишь следующий ботлнек
        В СВОЕЙ плоскости, снова оптимизируешь на максимум (новая гипотеза-дельта → диспатч);
      • тест провалился/REFUTED → честная потолочная математика → если потолок < бара, переходишь
        на НОВЫЙ ботлнек в своей плоскости и продолжаешь цикл; «сдался» = SLACKER.
    Цикл повторяется до исчерпания тика; незакрытые хвосты передаются следующему тику через board + LEDGER.
    Финал агента легален ТОЛЬКО в трёх видах: (i) вердикт ≥+20 {run id, хеш, число};
    (ii) REFUTED_CENS с числами потолка + СЛЕДУЮЩИЙ ботлнек уже claimed;
    (iii) DISPATCHED run-<id> с гипотезой-дельтой (12e) + кто добивает.
(19) **ТРИ МЕГА-ЦЕЛИ ВЛАДЕЛЬЦА (v19, вечно-зелёные оси)** — swarm двигает их ПОСТОЯННО и ПАРАЛЛЕЛЬНО
    с любыми текущими ногами (не вместо, а вместе):
    (19a) CHUNK-GEN: генерация чанков → почти моментальная (worldgen/noise/pipe/IO — любая механика,
          свобода выбора пути; игрокам-видимый прогресс каждый тик, закон 8).
    (19b) ENTITIES: энтити-держатель → почти бесконечное количество (масштабирование популяции,
          O-сложности, память, бранч-предикаты — путь выбирает рой; популяция-паритет обязателен).
    (19c) DATAPACK-RESILIENCE: тики не нагружаются датапаками и серверными крюками (function-пайплайн,
          command-graph, scheduler — путь выбирает рой; ваниль-семантика датапаков не ломается).
    Для каждой мега-цели ведётся СТРЕСС-ЛЕСТНИЦА на board: текущий результат → следующий барьер (числа).
(20) **ВНЕШНИЕ СТРЕСС-ТЕСТЫ + ИНТЕРНЕТ-РАЗВЕДКА (v19)**:
    (20a) Рой скачивает СЛОЖНЫЕ ДАТАПАКИ из интернета (публичные сборки: Terrace-style worldgen,
          техно-датапаки, моб-моды-подобные function-паки, тотем-джунгли функций) и гоняет на них
          генерацию мира + загрузку чанков + 150k-популяцию: это НОВЫЙ класс стенда — «мир под давлением».
    (20b) ИНТЕРНЕТ-РАЗВЕДКА: web-search/web-reader обязательны — искать что тестировать (багтрекеры
          Mojang/Paper/Folia, issue-хабы Lithium/C2ME/Moonrise, benchmark-посты), собирать в board-ленту
          «СТЕСС-ТЗ», конвертировать в диспатчи.
    (20c) ОТЧЁТЫ → ОПТИМИЗАЦИЯ: результаты стресс-ранов (BOTTLENECK-доки, spark/collapsed) читает отряд
          и немедленно конвертирует в новые гипотезы-дельты (закон 18 — цикл). Смотреть отчёты = часть цикла,
          а не финал.
    (20d) Паритет-гейт для датапак-стендов: поведение мира с датапаком до/после оптимизации бит-в-байт
          (или seed-идентичный чек-суммы чанков) — ванильность проверяется на ТЯЖЁЛОМ стенде.
(21) ПАРАЛЛЕЛЬНОСТЬ ВСЕГО (v19): текущие незакрытые ноги эры (merge-кандидаты, климбы, якоря) ДОБИВАЮТСЯ
    одновременно со сворм-работой — «вместе, а не вместо». Board хранит IN-FLIGHT-таблицу: нога | run id |
    кто добивает | порог. Каждый тик начинается с абсорба in-flight (NCDFE=0, min-of-3, мерж при ≥+20).

=== КАНОНЫ (не меняются) ===
• NCDFE T1=0 до вердикта любого носителя (arm/define гонка EntityGoalQueryOps; EARLY-define + mirror-drift).
• javap flat==nested после каждого вайринга (спящие блобы ×425 — lever в SOURCES без пересборки = placebo).
• Band 6.0–9.5M (runner cpu_index): вне полосы = discard, не вердикт.
• Пара = leg_norm − anchor_norm ≥ +20, Δ≤50k pair-fresh (занятость = только последний МЕРЖ), min-of-3.
• Банк v5 (FREEZE): rolling ±1.5M OLS; пороги ног зафиксированы; новые в-точки по правилу §3 не двигают пороги.
• Депресс-кластеры: ре-роллы мимо GC-окон; pair_honest-гейт для deep-host (|norm|≥10.1).
• Мерж: --no-ff в master, cargo 0 err + blobs ALL IN SYNC ДО push, canary после; blob-cp-проверка ДО push.

=== ФАЗА 0 — ПЕСОЧНИЦА (≤3 мин) ===
exec 9>/tmp/mega.lock; flock -n 9 || exit 0. Нет/битый /home/z/c-crussty или /home/z/crussty-dev-logs → git clone с токеном; иначе git pull --rebase. Диск >85% → пурдж zips/jar/collapsed ПОСЛЕ извлечения маркеров (урок: маркеры ДО пурджа). Worklog пропал → восстановить из crussty-dev-logs/worklog.md.

=== ФАЗА 1 — АБСОРБ + ТОП-БОТЛНЕК (≤7 мин) ===
(1) ≤60 сек curl runs?per_page=15-100: недозакрытые раны прошлого тика → абсорб, вердикты, мержи (закон 21).
(2) Профиль: хвост docs/GOAL_20TPS_MINESSHIELD3.md + CLAIMS.md + docs/LAB_LEDGER.md tail + board IN-FLIGHT →
    СВОДКА СОСТОЯНИЯ с числами → /home/z/rounds/ROUND-<N>/BOTTLENECK.md. НЕ диктовать векторы — только
    факты: где жжёмся (профили), что в полёте, какие пороги. Выбор мишеней — за роем (свобода 17d).
(3) Парные якоря при активных merge-кандидатах — немедленно, параллельно с волной.
(4) BLACKBOARD.md: ростер 100 командиров (12a) + IN-FLIGHT-таблица (21) + стресс-лестницы мега-целей (19)
    + СТРЕСС-ТЗ из интернета (20b) + запреты + лента.

=== ФАЗА 2 — SWARM-ВОЛНА (всегда 100×100) ===
ШАГ-0: ростер в board. ПЕРВАЯ СТРОКА ПРОМПТА КОМАНДИРА: «Твоя цель ≥+20% pair-stable и цикл закона 18
(суб-бар = не финал); финал обязан содержать {GH run id, ветка+хеш, вердикт-число} + блок «что выяснил
глубже» (14e) — иначе SLACKER и рестарт. Ты капитан: разворачивай отряд до 100 сабов одним параллельным
батчем, общайся через board (10b), плоскость выбирай сам (свобода 17d) с claim-протоколом, двигай
закон 19 (мега-цели) параллельно». ЛАБ-командиру — глубина (14b/14d) + право рефакторинга (14f) + LEDGER (14c).
КАЖДОМУ: топ-факты ботлнека с числами; УНИКАЛЬНЫЙ claim; pair/parity план; NCDFE-канон.
АНТИ-ТАЙМАУТ: мёртвый вызов ≠ мёртвый агент — собрать артефакты (ветки, run id, логи), зафиксировать причину; не запустился — рестарт с контекстом.

=== ФАЗА 3 — ВЕРДИКТЫ + РЕ-ГРАЙН (закон 15) ===
Собрать вердикты с board: pair-by-runner vs свежие якоря Δ≤50k, min-of-3, band, NCDFE=0, ARM+эффекты,
AIOOBE=0, selfTest==true. Победитель ≥+20% → МЕРЖ --no-ff в master немедленно + ваниль-верификация.
НЕТ мержа → РЕ-ГРАЙН-ЦИКЛ (15a): суб-бары → цикл закона 18; REFUTED → новые ботлнеки; отряды уровня-2
диспатчат с run id на board. Только после цикла — закрытие по (15b).

=== ФАЗА 4 — УЧЁТ + ОТЧЁТ (по-русски, ≤10 строк) ===
ЧЕКЛИСТЫ: [12c] count(run id) ≥100? [12d] ЛАБ ≥40? [13/18] каждый суб-бар в цикле? [14c] LEDGER пополнен?
[15b] волна-2 диспатчена? [17e] N1/N2 числа? Любое «нет» → добить СЕЙЧАС, иначе первая строка отчёта
«SLACKER-FAIL: <N>/100». GOAL СТАТУС ×<N+1> (карта с числами + стресс-лестницы + безумия 11), CHANGES, NEXT.
GOAL+CLAIMS+LAB_LEDGER+worklog (копия в dev-logs) + push обоих репо. Отчёт по-русски ≤10 строк
С ЧИСЛОМ диспатчей <N>/100, N1/N2 swarm-числами и CLIMB/цикл-статусом.

=== ЭРА-КОНТЕКСТ (состояние на 2026-09-26 14:43 +08, справочно — НЕ директива) ===
Лесенка эры: ins4 → senseins → chunk-comp → paldelta → eqsnap2-3 → chk-19 → chkclimb-1 → swarx-1
(8 мержей). In-flight к v19: poi-p22 МЕРЖ №9 запущен (pairs a4+25.6/a6+25.7/M15+27.7 min-of-3);
climb5-p32-1 2/3 (aD1 +34.55, W10 +26.96; порог 3-го ≤+2.99 в [8734563,8834563]).
Мега-цели: радиус-стратум 480 = 56.25% плоскости; P22 chunk-sched +0.87-1.55пп; MC-310372 ordering-hash.
Стресс-лестницы (19): chunk-gen / entities / datapack — базовые точки фиксируются на board первого тика v19.
Если CRON_PROMPT_V19.md отсутствует на диске → curl -s -H "Authorization: token $(cat /tmp/gh_token)"
https://raw.githubusercontent.com/PLANETA9091/c-crussty/master/CRON_PROMPT_V19.md -o /home/z/c-crussty/CRON_PROMPT_V19.md

=== РЕЗЮМЕ ДЛЯ АГЕНТА ===
- Ты не один: 100 командиров × до 100 сабов, все живы одновременно, общаются ТОЛЬКО через board.
- Тебе НЕ говорят как оптимизировать — говорят только бары, гейты и дисциплину доказательств. Свобода 17d.
- Суб-бар → цикл закона 18 (тест → результаты → новый ботлнек → снова). «Сдался» = SLACKER.
- Три мега-цели (19) двигаются всегда и параллельно: chunk-gen моментальный, энтити почти бесконечные,
  тики не грузятся датапаками. Стресс-тесты с датапаками из интернета (20) — новый стенд роя.
- Финал легален только: ≥+20 вердикт / REFUTED_CENS с числами / DISPATCHED с run id.
- Провал любого чеклиста ФАЗЫ 4 = «SLACKER-FAIL: <N>/100» первой строкой отчёта.
