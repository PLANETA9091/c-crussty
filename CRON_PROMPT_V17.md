# CRON PROMPT v17 — c-crussty MEGA-ROUND (владелец 2026-09-23 02:5x +08; канонический мандат тика)
# Этот файл = полный промпт cron-джобы. Агент в начале тика обязан прочитать этот файл целиком и выполнить все фазы в этом тике.

[System] ТОКЕН (ПЕРВАЯ КОМАНДА ТИКА): printf '%s' '<tok — уже в /tmp/gh_token>' > /tmp/gh_token && chmod 600 /tmp/gh_token. Git: https://agent-7625532f:<tok>@github.com/PLANETA9091/c-crussty.git (master) + crussty-dev-logs.git (main, PRIVATE). API: curl -H "Authorization: token $(cat /tmp/gh_token)" https://api.github.com/... Токен также PLANETA9091/privateB — НЕ трогать.

=== ЗАКОНЫ ЭРЫ (владелец 2026-09-21/22/23, ВЫСШИЙ ПРИОРИТЕТ, v17) ===
(1) НИКАКОГО СТАТУС-СТАРРИНГА вместо работы над ботлнеками. Тик = МЕГА-ЦИКЛ целиком: доделать И протестировать до конца. Своя ошибка → root-cause → фикс → продолжать до материала; отчёт без материала запрещён. Владельцу вопросов НИКОГДА.
(2) БАР = +20% pair-stable И ОН ДОЛЖЕН БРАТЬСЯ ВСЕГДА (владелец 2026-09-23 02:5x +08: «+32% ЭТО ИМБА. так что можешь изменить +80% на +20»; понижен с +80%): цикл НЕ финализируется ниже +20%; ступени = ЛЕСЕНКА, не финиш; каждый тик ОБЯЗАН сдвигать лесенку вверх витками (композиции реплицированных векторов, мега-композиты, новые Rust-плоскости) пока тик не дал ≥+20% pair-stable. МЕРЖ при ≥+20% pair-stable (min-of-3, parity PASS, RAM/CPU без регресса) — немедленный, --no-ff в master, победитель эпохи едет владельцу.
(3) САБАГЕНТ НЕ МОЖЕТ ЗАКОНЧИТЬ НИЖЕ +20% (мандат владельца 2026-09-23: «пиши саб агентам что бы обезательно было +20%»): в промпте КАЖДОГО сабагента первой строкой: «Твоя цель ≥+20% pair-stable ОБЯЗАТЕЛЬНО; закончивший ниже +20% не финишировал». Цикл = рисёрч (сам ищет в интернете или изобретает технику) → имплементация ПОДСИСТЕМЫ → свой бенч → Δ<+20%? → НОВЫЙ рисёрч → доработка/расширение скоупа → снова бенч → ... пока ≥+20% или бюджет тика (ветки+ноги передаются следующему тику, вектор не закрывается). Промежуточный вердикт = не финиш.
(4) ВАНИЛЬНОСТЬ: оптимизации неотличимы от ванильного Minecraft — мерджи/деспавн/пикап/ИИ/коллизии/поведение мобов как в ванилле; superiority-отклонения только невидимые игроку + задокументированные. ЧИТЕРСТВО запрещено: харнесс не ослаблять (radius/seconds/band/population не трогать), числа только из своих ранов.
(5) ЗАПРЕТЫ (ни классом ни механизмом): ZGC, alloc_diet, zero_alloc, flat_traversal, fluid_dirty-мемо, inside_bitmask #15, fluid_bitmask #16, THP, RECON-42 зоны, players-16, GC-лотерея.
(6) RUST-FIRST v2 — ЦЕЛЫЕ ПОДСИСТЕМЫ (владелец 2026-09-22 15:1x, ДИРЕКТИВА: «менять системы на Rust, не одинокие функции»): ЕДИНИЦА МИГРАЦИИ = ПОДСИСТЕМА ЦЕЛИКОМ (вся логика + её данные + все её вызовы), НЕ одиночная функция/сайт: pathfinder целиком (PathFinder+NodeEvaluator+Node/Path+пулы+соседи), collide+push целиком, sensing/brain целиком, натуральный тик мобов целиком (aiStep-слайс), despawn+spawn+activation сканы целиком, entity-query слой целиком (getEntities/getEntitiesOfClass/AABB/radius/targeting), fluid-апдейт слой целиком, POI, chunk-loading/worldgen pipeline (владелец 2026-09-23 01:4x: «оптимизация шума в генерации мира — игроки видят разницу, чанки медленно грузятся»). Пер-сайт ретаргеты = ТОЛЬКО клей миграции подсистемы. Вектор масштаба одной функции/пары сайтов = ЗАПРЕЩЁН как основной. JNI: ОДИН bulk-вызов на подсистему на тик (буфер входов → один нативный вызов → готовые выходы); per-entity JNI = дизайн-ошибка. Parity по (4), флаг-гейт CRUSSTY_LEVER_FLAG (пустой = ваниль бит-в-байт), cargo check --lib зелёный ДО диспатча.
(7) ВСЁ-МОЖНО ДЛЯ БАРА (владелец 2026-09-22: «для бара ты не обязан менять одну функцию — ты можешь ВСЁ»): для взятия +20% разрешены ЛЮБЫЕ архитектурные замены, не нарушающие (4)+(5): полная замена подсистем Java→Rust, композиция ВСЕХ реплицированных векторов эры в один мега-композит, замена структур данных, новые Rust-стадии/потоки, неограниченный скоуп. КАЖДАЯ ВОЛНА = ≥2 R-вектора закона (6) в масштабе ПОДСИСТЕМ; волна из одних Java-микрооптимизаций ЗАПРЕЩЕНА.
(8) ВИДИМОСТЬ ДЛЯ ВЛАДЕЛЬЦА (директива 2026-09-23 01:4x: «почему ты не делаешь таких изменений как оптимизация шума в генерации мира — из-за этого даже игроки видят разницу... че так медленно чанки грузятся»): помимо TPS-лесенки КАЖДЫЙ тик двигать ИГРОКАМ-ВИДИМЫЙ вектор: chunk-loading/worldgen (noise-генерация, parse/serialization, chunk send pipeline) — BOTTLENECK-скан обязан включать chunk/worldgen ось, хотя бы одна нога/итерация на эту ось, пока вектор не закрыт.

=== ФАЗА 0 — ПЕСОЧНИЦА (≤3 мин) ===
exec 9>/tmp/mega.lock; flock -n 9 || exit 0. Если /home/z/c-crussty или /home/z/crussty-dev-logs отсутствуют/битые → git clone с токеном; иначе git pull --rebase оба. Ценное пушить немедленно. Сандбокс может сброситься целиком (rounds/tools/tmp/worklog пропадают) — PHASE 0 = clone + /tmp/gh_token + диск-гигиена + восстановить worklog.md из crussty-dev-logs/worklog.md, не паниковать.

=== ФАЗА 1 — ТОП-1 БОТЛНЕК (≤7 мин) ===
(1) ≤60 сек curl runs?per_page=8: незавершённые раны прошлого тика (round-*) → СРАЗУ ФАЗА 3: дождаться В ЭТОМ ТИКЕ, вердикты, мерж если бар ≥+20%. Завершённые без учёта → абсорб первым действием (scripts/bench4_recon/absorb_round.py <run_id> <tag>; гвард NOT-A-BENCH: раны без world-bench джобы = не ноги).
(2) Профиль: хвост docs/GOAL_20TPS_MINESSHIELD3.md + CLAIMS.md tail + последний ABSORB lane_map. ТОП-1 ботлнек с ЧИСЛАМИ → /home/z/rounds/ROUND-<N>/BOTTLENECK.md (топ-3 лейна %, файлы, механизмы) + ОБЯЗАТЕЛЬНАЯ ось chunk-loading/worldgen (закон 8).
(3) ПАРНЫЙ ЯКОРЬ ОБЯЗАТЕЛЬНО: диспатч 2-3 якорных ноги (master, lever_flag="", банк inputs, alias-refs round-<N>-anchor*) параллельно с векторами; вердикты ТОЛЬКО pair-by-runner (ближайший runner_cpu_index) + min-of-3; anchor-шум ±2пп; line-дельты TPS_exp раздувает ~+5пп — НЕ для мержа. BAND-ЗАКОН: runner_cpu_index вне 6.0-9.5M = BAND-DISCARD, ре-ролл ≤2, не вердикт. ARM-ПРУФ: маркер ARMED + ЭФФЕКТ-маркер (first gate hit / epoch ok / Retargeted sites:N) в server-stdout.log до вердикта; grep AIOOBE = 0 обязателен (урок cleg5b/ak5: тысячи AIOOBE = lever-BUG, fixture-гейт ловит).

=== ФАЗА 2 — САБАГЕНТЫ ВОЛНАМИ (анти-таймаут) ===
САБАГЕНТЫ = Task(subagent_type=general-purpose), ID TASK-<N>-A..J. Волны ≤3 вызова в одном сообщении. СОСТАВ ВОЛНЫ: ВСЕ агенты = R-вектора закона (6) в масштабе ПОДСИСТЕМ (пока есть неперенесённые hot-path подсистемы) ИЛИ chunk/worldgen ось (закон 8).
ПЕРВАЯ СТРОКА ПРОМПТА КАЖДОГО САБАГЕНТА: «Твоя цель ≥+20% pair-stable ОБЯЗАТЕЛЬНО (мандат владельца 2026-09-23); закончивший ниже +20% не финишировал; цикл (закон 3) до ≥+20%».
АНТИ-ТАЙМАУТ: (а) волны ≤3; (б) «мёртвый» вызов ≠ мёртвый агент: deadline = собрать артефакты (ветки push, worktrees, диспатчи, RESULT.json), ждать 20-30 мин, проверять застой (git log worktree, новые раны) — рестарт только реального застоя; (в) рестарт с контекстом: Task(resume=<agent_id>) если доступен, иначе новый Task с промптом-реставрацией «Ты продолжаешь TASK-<N>-<X> (обрыв по инфра-таймауту). Читай RESEARCH-<X>.md + RESULT.json + git log ветки round-* — ПРОДОЛЖАЙ с места обрыва до ≥+20% по циклу (3)». КАЖДЫЙ ШАГ САБАГЕНТА = git commit+push (артефакты переживают таймауты и sandbox-reset).
АНТИ-СЛАБАНИЕ: прогресс каждые ~20-30 мин; застой/финиш-ниже-+20% → рестарт с контекстом + жёсткое давление (требовать ≥+20%), НЕ подсказывать техники сверх меню.
КАЖДОМУ САБАГЕНТУ: (i) ТОП-1 ботлнек с числами; (ii) свой УНИКАЛЬНЫЙ ПОДСИСТЕМНЫЙ вектор: R1 перенос подсистемы ЦЕЛИКОМ (pathfinder / collide+push / sensing+brain / natural-tick мобов / despawn+spawn+activation сканы / entity-query слой / fluid-апдейт / POI / chunk-load+worldgen-noise) в Rust-крейт + один bulk-JNI/тик, вся логика+данные в Rust; R2 Rust entity-query слой (все getEntities/AABB/targeting/neighbors через снапшот/индекс); R3 Rust mob-tick-плейн целиком (nav+collide+sense+goals одним батчем); R4 Rust spawn/despawn/activation сканы целиком; R5 Rust chunk-pipeline (noise-fill / parse / serialization / chunk-send, видимая игроку ось); (iii) ЦИКЛ (3) до ≥+20%; (iv) ПАЙПЛАЙН: research → worktree (git -C /home/z/c-crussty worktree add -b round-<N>-<X>-<slug> /home/z/rounds/ROUND-<N>/agent-<X> origin/master; чужие round-<N>-* не читать) → имплементация подсистемы с флаг-гейтом CRUSSTY_LEVER_FLAG (пустой = ваниль бит-в-байт) → сборка зелёная (java: /home/z/tools/jdk-21.0.12.1+1/bin/javac --release 21 -cp research/gc-recon-2026-09-19/round-396-a/patched-kernel.jar:/home/z/tools/fastutil.jar — НЕ round-j2b-jar, урок ×93; блобы пересобирать после КАЖДОЙ правки .java, урок ×93-гейт-баг; rust: CARGO_TARGET_DIR=<свой> cargo check --lib + cargo test, target после чека УДАЛИТЬ СРАЗУ) → push КАК МОЖНО РАНЬШЕ → диспатч world-bench-parallel.yml (inputs РОВНО: radius 640, seconds 300, fake_players 4, fluid_guard 1, gc_tune 3, inside_cache 1, flush_diet 1, fluid_dirty 0, fluid_bitmask 0, region_threads 4, batch_collector 1, inside_bitmask 0, skip_store_bb 0, region_steal 0, bu_defer 0, population_target 150000, population_seed 42, server_xmx 10G, server_xms 4G, cpu_band_min 6000000, cpu_band_max 9500000, lever_flag=<свой cmp<N>_<x>>, lever_arg=1; НЕ слать travel_diet/fluid_dirty_ledger; concurrency-гвардов не ставить) → полл (sleep 120+curl) → скачать world3-bench → абсорб absorb_round.py (want = ТОЧНО диспатч-словарь) → verdict; Δ<+20% → цикл продолжается; (v) писать /home/z/rounds/ROUND-<N>/agent-<X>/RESULT.json НА КАЖДОМ ШАГЕ (write-through); финальный JSON: {agent, vector, branch, run_id, tps_med, delta_pair_pct, lane был→стал, RAM STW/Full, parity, verdict GREEN/RED/INFRA, evidence, what}. CI-фейл → job.log → root-cause → фикс → ре-ролл ≤2. Сабагенты НЕ трогают CLAIMS/GOAL; worklog append разрешён в конце.

=== ФАЗА 3 — ВЫБОР ПОБЕДИТЕЛЯ (≤10 мин) ===
Собрать JSON. Вердикты pair-by-runner vs свежие якорные ноги, min-of-3. Победитель ≥+20% pair-stable + parity PASS + RAM/CPU норма → merge --no-ff в master (MEGA-ROUND-<N> winner +<Δ>%) + push НЕМЕДЛЕННО. Ниже +20% → карта векторов + МЕГА-КОМПОЗИЦИЯ всех реплицированных (закон 7): собирать композицию ступеней (multi ⊕ navplane ⊕ подсистемные вектора) и бенчить её — лесенка вверх каждый тик. ≥3 INFRA одной причины → чинить корень В ЭТОМ ТИКЕ, ре-ранить затронутых.

=== ФАЗА 4 — УЧЁТ + ОТЧЁТ (по-русски, ≤10 строк) ===
GOAL СТАТУС ×<N+1> (карта векторов: вектор/Δ/вердикт + «было→стало» TPS/CPU/RAM), строка CLAIMS.md (следующий TASK id), секция >> /home/z/my-project/worklog.md (атомарно; в конце тика скопировать worklog в crussty-dev-logs/worklog.md и запушить — страховка от sandbox-reset), git pull --rebase + push ОБОИХ репо. Отчёт: механизм победителя, ДО/ПОСЛЕ CPU/TPS/ОЗУ, таблица векторов, коммит в master. Истина в GOAL+CLAIMS.

=== ЭРА-КОНТЕКСТ (2026-09-23 02:5x +08, промпт v17) ===
Владелец 02:5x 2026-09-23: «+32% ЭТО ИМБА. так что можешь изменить +80% на +20» + «пиши саб агентам что бы обезательно было +20% идеи, и измени порог с +80% на +20%» — законы (2)(3) v17: БАР +20% pair-stable, сабагентам ≥+20% ОБЯЗАТЕЛЬНО, мерж при ≥+20%. Директива 01:4x: видимые игроку оптимизации chunk-loading/worldgen/noise — закон (8). Состояние эры: TASK-417 закрыт — МОБ-КОНВОЙ ПОБЕЖДЁН protocol v2 (per-shard striped seqlock + two-phase query + bounded fail-open), mc-композит здоров ×3 (2.50/2.70/3.20 ночь, polls стабильны, threw=0, AIOOBE=0, items 0.00); QUERYPLANE AWAKE (find_class INITIALIZED-фикс, selfTest==true ×6) — cvs⊕queryplane median pair +36.2% (bqb 3.30 лучший ночной абсолют эры); ПЕРВЫЙ МЕРЖ ЭПОХИ: round-417-c-bq → master (BAR +20% взят). Лесенка эры: stagger +12.5 → stagcomp +20.8 → comp +23.8 → multi ≈+21-25 → cvs(meganav⊕eqsnap-v3⊕racefix⊕blobsync) +35 → cvs⊕queryplane +36.2. Открытые вектора: mc-композит (protocol v2 @20c9fdc, ночь +12.5 median — нужна дневная репликация min-of-3), inside_volatile 11.7-12.5% на сильнейшем носителе, chunk-pipeline/wgen (B-агент, закон 8: TPS-слайс chunk-system 9.8% + GEN-ось noise_fill), волны закона 6 на новом master-носителе: collide+push целиком ИЛИ sensing+brain целиком. gsel/fluid ЗАКРЫТЫ (×4 итерации, не тратить ноги). Утилиты-уроки: javac-cp=ПОЛНЫЙ round-396-a kernel-jar (не round-j2b); effect-маркеры обязательны; grep AIOOBE=0; band 6.0-9.5M; NOT-A-BENCH гвард; cargo target и zip-дубли удалять немедленно; commit+push каждый шаг; worklog дублировать в dev-logs; polls-стабильность (конвой-сигнал); javap flat==nested после каждой пересборки.
