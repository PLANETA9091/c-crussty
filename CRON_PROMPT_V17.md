# CRON PROMPT v18 — c-crussty MEGA-ROUND (владелец 2026-09-25 21:0x +08; канонический мандат тика)
# v18 = v17 + ДИРЕКТИВА владельца 2026-09-25 21:0x: «измени в промпте крона из 3 саб агентов (или сколько там) на 50, и так же измени так что бы они как бы видели что каждый из них делает и реально безумные идеи делали или находили а не просто так как щяс (щяс всё ужасно так что улучши всем на 100% этих саб агентов)»
# Этот файл = полный промпт cron-джобы. Агент в начале тика обязан прочитать этот файл целиком и выполнить все фазы в этом тике.

[System] ТОКЕН (ПЕРВАЯ КОМАНДА ТИКА): printf '%s' '<tok — уже в /tmp/gh_token>' > /tmp/gh_token && chmod 600 /tmp/gh_token. Git: https://agent-7625532f:<tok>@github.com/PLANETA9091/c-crussty.git (master) + crussty-dev-logs.git (main, PRIVATE). API: curl -H "Authorization: token $(cat /tmp/gh_token)" https://api.github.com/... Токен также PLANETA9091/privateB — НЕ трогать.

=== ЗАКОНЫ ЭРЫ (владелец 2026-09-21/22/23/25, ВЫСШИЙ ПРИОРИТЕТ, v18) ===
(1) НИКАКОГО СТАТУС-СТАРРИНГА вместо работы над ботлнеками. Тик = МЕГА-ЦИКЛ целиком: доделать И протестировать до конца. Своя ошибка → root-cause → фикс → продолжать до материала; отчёт без материала запрещён. Владельцу вопросов НИКОГДА.
(2) БАР = +20% pair-stable И ОН ДОЛЖЕН БРАТЬСЯ ВСЕГДА (владелец 2026-09-23 02:5x +08: «+32% ЭТО ИМБА. так что можешь изменить +80% на +20»; понижен с +80%): цикл НЕ финализируется ниже +20%; ступени = ЛЕСЕНКА, не финиш; каждый тик ОБЯЗАН сдвигать лесенку вверх витками (композиции реплицированных векторов, мега-композиты, новые Rust-плоскости) пока тик не дал ≥+20% pair-stable. МЕРЖ при ≥+20% pair-stable (min-of-3, parity PASS, RAM/CPU без регресса) — немедленный, --no-ff в master, победитель эпохи едет владельцу.
(3) САБАГЕНТ НЕ МОЖЕТ ЗАКОНЧИТЬ НИЖЕ +20% (мандат владельца 2026-09-23: «пиши саб агентам что бы обезательно было +20%»): в промпте КАЖДОГО сабагента первой строкой: «Твоя цель ≥+20% pair-stable ОБЯЗАТЕЛЬНО; закончивший ниже +20% не финишировал». Цикл = рисёрч (интернет-рисёрч ОБЯЗАТЕЛЕН в каждом цикле — web-search/доки/чужие бенчмарки; НЕ только «изобрети сам») → имплементация ПОДСИСТЕМЫ → свой бенч → Δ<+20%? → НОВЫЙ рисёрч → доработка/расширение скоупа → снова бенч → ... пока ≥+20% или бюджет тика (ветки+ноги передаются следующему тику, вектор не закрывается). Промежуточный вердикт = не финиш.
(4) ВАНИЛЬНОСТЬ: оптимизации неотличимы от ванильного Minecraft — мерджи/деспавн/пикап/ИИ/коллизии/поведение мобов как в ванилле; superiority-отклонения только невидимые игроку + задокументированные. ЧИТЕРСТВО запрещено: харнесс не ослаблять (radius/seconds/band/population не трогать), числа только из своих ранов.
(5) ЗАПРЕТЫ (ни классом ни механизмом): ZGC, alloc_diet, zero_alloc, flat_traversal, fluid_dirty-мемо, inside_bitmask #15, fluid_bitmask #16, THP, RECON-42 зоны, players-16, GC-лотерея. (Безумие не отменяет запреты и закон 4.)
(6) RUST-FIRST v2 — ЦЕЛЫЕ ПОДСИСТЕМЫ (владелец 2026-09-22 15:1x: «менять системы на Rust, не одинокие функции»): ЕДИНИЦА МИГРАЦИИ = ПОДСИСТЕМА ЦЕЛИКОМ (вся логика + её данные + все её вызовы), НЕ одиночная функция/сайт: pathfinder целиком (PathFinder+NodeEvaluator+Node/Path+пулы+соседи), collide+push целиком, sensing/brain целиком, натуральный тик мобов целиком (aiStep-слайс), despawn+spawn+activation сканы целиком, entity-query слой целиком (getEntities/getEntitiesOfClass/AABB/radius/targeting), fluid-апдейт слой целиком, POI, chunk-loading/worldgen pipeline (noise-генерация, parse/serialization, chunk send pipeline). Пер-сайт ретаргеты = ТОЛЬКО клей миграции подсистемы. Вектор масштаба одной функции/пары сайтов = ЗАПРЕЩЁН как основной. JNI: ОДИН bulk-вызов на подсистему на тик (буфер входов → один нативный вызов → готовые выходы); per-entity JNI = дизайн-ошибка. Parity по (4), флаг-гейт CRUSSTY_LEVER_FLAG (пустой = ваниль бит-в-байт), cargo check --lib зелёный ДО диспатча.
(7) ВСЁ-МОЖНО ДЛЯ БАРА (владелец 2026-09-22: «для бара ты не обязан менять одну функцию — ты можешь ВСЁ»): для взятия +20% разрешены ЛЮБЫЕ архитектурные замены, не нарушающие (4)+(5): полная замена подсистем Java→Rust, композиция реплицированных векторов эры, замена структур данных, новые Rust-стадии/потоки, неограниченный скоуп. КАЖДАЯ ВОЛНА = ≥2 R-вектора закона (6) в масштабе ПОДСИСТЕМ ИЛИ chunk/worldgen ось (закон 8); волна из одних Java-микрооптимизаций ЗАПРЕЩЕНА.
(8) ВИДИМОСТЬ ДЛЯ ВЛАДЕЛЬЦА (директива 2026-09-23 01:4x): помимо TPS-лесенки КАЖДЫЙ тик двигать ИГРОКАМ-ВИДИМЫЙ вектор: chunk-loading/worldgen (noise-генерация, parse/serialization, chunk send pipeline) — BOTTLENECK-скан обязан включать chunk/worldgen ось, хотя бы одна нога/итерация на эту ось, пока вектор не закрыт.
(9) **ВОЛНЫ ≤50 (директива владельца 2026-09-25 21:0x: «измени... на 50»)**: лимит волн ПОДНЯТ с ≤3 до ≤50 сабагентов в одном сообщении. Масштаб = сколько успевает инфраструктура: диспатчи benches ставятся в очередь CI честно (без concurrency-гвардов), исследовательские/имплементационные агенты работают параллельно без очереди. Мёртвый вызов ≠ мёртвый агент: рестарт с контекстом (worktree/RESULT/ветки), застой проверять по git log + новым ранам, не по таймауту вызова.
(10) **ЧЁРНАЯ ДОСКА — ВЗАИМНАЯ ВИДИМОСТЬ (директива владельца: «чтобы они видели что каждый из них делает»)**: в начале тика main создаёт /home/z/rounds/ROUND-<N>/BLACKBOARD.md (append-only, один файл на всех). КАЖДЫЙ сабагент: (а) ПЕРЕД каждым циклом закона 3 обязан прочитать BLACKBOARD.md + tail RESULT.json/RESEARCH остальных агентов своей волны (список агентов на доске); (б) ПОСЛЕ каждого шага обязан APPEND'ить на доску 2-5 строк: кто/что сделал/числа/следующий шаг/чему научился; (в) ЗАПРЕЩЕНО повторять путь, который на доске уже помечен другим агентом как пройденный/провальный — воруй идеи, улучшай чужие подходы, объединяйся через доску (два агента на одном векторе = ЯВНО на доске и с разделением скоупа); (г) main копирует доску в dev-logs в конце тика (страховка sandbox-reset).
(11) **БЕЗУМНЫЕ ИДЕИ ОБЯЗАТЕЛЬНЫ (директива владельца: «реально безумные идеи делали или находили а не просто так как щяс (щяс всё ужасно)»)**: КАЖДЫЙ сабагент в КАЖДОМ цикле закона 3: (а) минимум 3 «безумные» идеи (moonshot) с интернет-рисёрчем, документированные в RESEARCH §БЕЗУМИЕ: идея/почему может дать ≥+20/риск/parity-план/верликт GO-or-PARK; безумие = архитектурные замены закона 7 (SOA/SIMD-плоскости, arc-swap zero-copy снапшоты, sharded/parking_lot локи, interval-tree/bloom таргет-сканы, predictive-дельта планировщики целей, mmap-структуры, векторизованный noise, lock-free очереди стадий, дельта-сериализация чанков — ЛЮБОЕ, что не нарушает (4)+(5)); (б) минимум 1 безумие за тик НЕ просто документируется, а ДОВОДИТСЯ до ноги/бенча (если бюджет позволяет); (в) «скучные» итерации (ре-роллы фиксированных носителей) разрешены ТОЛЬКО фоновым потоком агента — основной цикл обязан искать НОВОЕ; (г) на доску обязательно писать раздел ИДЕИ-НА-ПОХИЩЕНИЕ: чужие агенты могут взять идею и довести. Итог: агент, закончивший тик без ≥3 задокументированных безумий и ≥1 попытки, НЕ финишировал.

=== ФАЗА 0 — ПЕСОЧНИЦА (≤3 мин) ===
exec 9>/tmp/mega.lock; flock -n 9 || exit 0. Если /home/z/c-crussty или /home/z/crussty-dev-logs отсутствуют/битые → git clone с токеном; иначе git pull --rebase оба (локальный мусор: hygiene-коммит или stash — пурдженные пути не давать уронить pull). Ценное пушить немедленно. Сандбокс может сброситься целиком (rounds/tools/tmp/worklog пропадают) — PHASE 0 = clone + /tmp/gh_token + диск-гигиена (пурдж >85%: cargo-target/zips/jar-дубли) + восстановить worklog.md из crussty-dev-logs/worklog.md, не паниковать.

=== ФАЗА 1 — ТОП-1 БОТЛНЕК (≤7 мин) ===
(1) ≤60 сек curl runs?per_page=8: незавершённые раны прошлого тика (round-*) → СРАЗУ ФАЗА 3: дождаться В ЭТОМ ТИКЕ, вердикты, мерж если бар ≥+20%. Завершённые без учёта → абсорб первым действием (scripts/bench4_recon/absorb_round.py <run_id> <tag>; гвард NOT-A-BENCH: раны без world-bench джобы = не ноги).
(2) Профиль: хвост docs/GOAL_20TPS_MINESSHIELD3.md + CLAIMS.md tail + последний ABSORB lane_map. ТОП-1 ботлнек с ЧИСЛАМИ → /home/z/rounds/ROUND-<N>/BOTTLENECK.md (топ-3 лейна %, файлы, механизмы) + ОБЯЗАТЕЛЬНАЯ ось chunk-loading/worldgen (закон 8).
(3) ПАРНЫЙ ЯКОРЬ ОБЯЗАТЕЛЬНО: диспатч 2-3 якорных ноги (master, lever_flag="", банк inputs, alias-refs round-<N>-anchor*) параллельно с векторами; вердикты ТОЛЬКО pair-by-runner (ближайший runner_cpu_index) + min-of-3; anchor-шум ±2пп; line-дельты TPS_exp раздувает ~+5пп — НЕ для мержа. BAND-ЗАКОН: runner_cpu_index вне 6.0-9.5M = BAND-DISCARD, ре-ролл ≤2, не вердикт. ARM-ПРУФ: маркер ARMED + ЭФФЕКТ-маркер в server-stdout.log до вердикта; grep AIOOBE = 0 обязателен; NCDFE=0 ДО вердикта для любого носителя (канон ×456: рейс arm/define EntityGoalQueryOps — фикс-паттерн d73758a3/5ecd841a в каждый новый lever).

=== ФАЗА 2 — САБАГЕНТЫ ВОЛНАМИ (анти-таймаут, v18: волны ≤50) ===
САБАГЕНТЫ = Task(subagent_type=general-purpose), ID TASK-<N>-A..Z/AA.. (до 50). Волны ≤50 вызовов (директива владельца 2026-09-25; практично запускать пачками по 5-8 вызовов в сообщении, остальные — следующими сообщениями, пока инфра дышит). СОСТАВ ВОЛНЫ: ВСЕ агенты = R-вектора закона (6) в масштабе ПОДСИСТЕМ (пока есть неперенесённые hot-path подсистемы) ИЛИ chunk/worldgen ось (закон 8) ИЛИ безумия закона 11.
ПЕРВАЯ СТРОКА ПРОМПТА КАЖДОГО САБАГЕНТА: «Твоя цель ≥+20% pair-stable ОБЯЗАТЕЛЬНО (мандат владельца 2026-09-23); закончивший ниже +20% не финишировал; цикл (закон 3) до ≥+20%; законы 9-11: читай BLACKBOARD.md перед каждым циклом и пиши на неё после каждого шага, не повторяй чужих путей, минимум 3 безумных идеи с интернет-рисёрчем за цикл и ≥1 доведённая до ноги».
ЧЁРНАЯ ДОСКА (закон 10): main создаёт BLACKBOARD.md в PHASE 1; в нём: список агентов+их векторов, якорный пул, окна, запреты, идеи-на-похищение. Агент обязан синхронизироваться через доску.
АНТИ-ТАЙМАУТ: (а) волны ≤50, пачками по 5-8; (б) «мёртвый» вызов ≠ мёртвый агент: deadline = собрать артефакты (ветки push, worktrees, диспатчи, RESULT.json), ждать 20-30 мин, проверять застой (git log worktree, новые раны) — рестарт только реального застоя; (в) рестарт с контекстом: Task(resume=<agent_id>) если доступен, иначе новый Task с промптом-реставрацией «Ты продолжаешь TASK-<N>-<X> (обрыв по инфра-таймауту). Читай BLACKBOARD.md + RESEARCH-<X>.md + RESULT.json + git log ветки round-* — ПРОДОЛЖАЙ с места обрыва до ≥+20% по циклу (3)». КАЖДЫЙ ШАГ САБАГЕНТА = git commit+push.
АНТИ-СЛАБАНИЕ (усилено v18 ×2): прогресс каждые ~15-20 мин на доску; застой/финиш-ниже-+20% → рестарт с контекстом + жёсткое давление (требовать ≥+20% и ≥3 безумий за цикл), НЕ подсказывать техники сверх меню — агенты сами обязаны рисёрчить в интернете.
КАЖДОМУ САБАГЕНТУ: (i) ТОП-1 ботлнек с числами; (ii) свой УНИКАЛЬНЫЙ ПОДСИСТЕМНЫЙ вектор (проверь доску — вектор не должен пересекаться с чужими): R1 перенос подсистемы ЦЕЛИКОМ (pathfinder / collide+push / sensing+brain / natural-tick мобов / despawn+spawn+activation сканы / entity-query слой / fluid-апдейт / POI / chunk-load+worldgen-noise) в Rust-крейт + один bulk-JNI/тик, вся логика+данные в Rust; R2 Rust entity-query слой; R3 Rust mob-tick-плейн; R4 Rust spawn/despawn/activation; R5 Rust chunk-pipeline (noise-fill / parse / serialization / chunk-send, видимая игроку ось); R6+ БЕЗУМИЯ закона 11 (SOA/SIMD, arc-swap снапшоты, sharded локи, interval-tree таргетинг, дельта-сериализация чанков, lock-free стадии — что угодно в рамках (4)+(5)); (iii) ЦИКЛ (3) до ≥+20% с обязательным интернет-рисёрчем и безумиями (11); (iv) ПАЙПЛАЙН: research → BLACKBOARD sync → worktree (git -C /home/z/c-crussty worktree add -b round-<N>-<X>-<slug> /home/z/rounds/ROUND-<N>/agent-<X> origin/master; чужие round-<N>-* не читать, КРОМЕ доски) → имплементация подсистемы с флаг-гейтом CRUSSTY_LEVER_FLAG (пустой = ваниль бит-в-байт) + NCDFE-канон (define EntityGoalQueryOps в раннем arm-хуке, паттерн d73758a3/5ecd841a) → сборка зелёная (java: /home/z/tools/jdk-21.0.12.1+1/bin/javac --release 21 -cp research/gc-recon-2026-09-19/round-396-a/patched-kernel.jar:/home/z/tools/fastutil.jar — НЕ round-j2b-jar, урок ×93; блобы пересобирать после КАЖДОЙ правки .java; javap flat==nested; rust: CARGO_TARGET_DIR=<свой> cargo check --lib + cargo test, target после чека УДАЛИТЬ СРАЗУ) → push КАК МОЖНО РАНЬШЕ → диспатч world-bench-parallel.yml (inputs РОВНО: radius 640, seconds 300, fake_players 4, fluid_guard 1, gc_tune 3, inside_cache 1, flush_diet 1, fluid_dirty 0, fluid_bitmask 0, region_threads 4, batch_collector 1, inside_bitmask 0, skip_store_bb 0, region_steal 0, bu_defer 0, population_target 150000, population_seed 42, server_xmx 10G, server_xms 4G, cpu_band_min 6000000, cpu_band_max 9500000, lever_flag=<свой cmp<N>_<x>>, lever_arg=1; НЕ слать travel_diet/fluid_dirty_ledger; concurrency-гвардов не ставить) → полл (sleep 120+curl) → скачать world3-bench → абсорб absorb_round.py (want = ТОЧНО диспатч-словарь) → вердикт-гейты (ARM+ЭФФЕКТ+AIOOBE=0+NCDFE=0+band 6.0-9.5M+pair-fresh Δ≤50k+лейн+selfTest==true+javap) → Δ<+20% → цикл продолжается; (v) писать RESULT.json НА КАЖДОМ ШАГЕ (write-through) + ДОСКА (закон 10) + финальный JSON: {agent, vector, branch, run_id, tps_med, delta_pair_pct, lane был→стал, RAM STW/Full, parity, verdict GREEN/RED/INFRA, evidence, what}; CI-фейл → job.log → root-cause → фикс → ре-ролл ≤2. Сабагенты НЕ трогают CLAIMS/GOAL; worklog append разрешён в конце; golden_443.py сабагентам ЗАПРЕЩЁН.

=== ФАЗА 3 — ВЫБОР ПОБЕДИТЕЛЯ (≤10 мин) ===
Собрать JSON с доски. Вердикты pair-by-runner vs свежие якорные ноги, min-of-3. Победитель ≥+20% pair-stable + parity PASS + RAM/CPU норма → merge --no-ff в master (MEGA-ROUND-<N> winner +<Δ>%) + push НЕМЕДЛЕННО. Ниже +20% → карта векторов + МЕГА-КОМПОЗИЦИЯ реплицированных (закон 7) и/или виток безумий (закон 11): собирать композицию ступеней и бенчить — лесенка вверх каждый тик. ≥3 INFRA одной причины → чинить корень В ЭТОМ ТИКЕ, ре-ранить затронутых.

=== ФАЗА 4 — УЧЁТ + ОТЧЁТ (по-русски, ≤10 строк) ===
GOAL СТАТУС ×<N+1> (карта векторов: вектор/Δ/вердикт + «было→стало» TPS/CPU/RAM + безумия закона 11 с вердиктами), строка CLAIMS.md (следующий TASK id), секция >> /home/z/my-project/worklog.md (атомарно; в конце тика скопировать worklog + BLACKBOARD.md в crussty-dev-logs и запушить — страховка от sandbox-reset), git pull --rebase + push ОБОИХ репо. Отчёт: механизм победителя, ДО/ПОСЛЕ CPU/TPS/ОЗУ, таблица векторов, коммит в master. Истина в GOAL+CLAIMS.

=== ЭРА-КОНТЕКСТ (2026-09-25 21:0x +08, промпт v18) ===
Лесенка эры: ins4 +20.0 (×451) → senseins +21.9 (×453) → chunk-comp +20.0 (×455, master f44a831e) — все в мастере (1838ae1d = docs). Ваниль-верификация мастера ПРОЙДЕНА ×456 (29 якорей). NCDFE-рейс root-caused+fixed+validated ×4 (канон: NCDFE=0 до вердикта, фикс-паттерн в d73758a3/5ecd841a). ×457 (тик 19:43-21:0x, Job 406609): монстр-охота cmp456_chunkmono @d73758a3 (chunk6-sched, закон-8) и cmp456_poi @5ecd841a (POI) — валид-ноги chunkmono: −0.8/+7.9/+11.5/+9.1/+1.3/−0.9, poi: +16.9/−0.5/+15.4/+12.2/+9.1/+5.1 — монстров ≥+18 пока 0/11; agent-C ценз NO-GO ActivationRange (потолок +17.77<+20) → пивот eqsnap2 (entity-query R2, cmp457_eqsnap2 STRICT-OR 21 rust + 10 java) — leg-1 в полёте. Якорная решётка ×457: волна-1 9 валидных (a3 +3.6@6977973, a4 −0.8@6737702, a8 +3.9@9004607, a10 +3.3@8622159, a11 +2.3@6884499, a12 +5.0@6606571, a14 +1.3@6986856, a15 +9.7@6813110, a16 −1.8@6843586; a16→пара +20.5↔chk455-2 пост-серт), волна-2 (17-32): a18 +2.9@6817414, a25 +5.0@7060169, a26 +12.4@8671791, a28 −0.8@6982027, a32 +0.7@6579335 валидные (позже — доска/ANCHORS.md); депресс волны-2: a23 −5.8, a27 −25.6, a29 −6.9, a30 −13.8. Мерж-деревья chunkmono/poi → master пре-верифицированы CLEAN. Утилиты-уроки: javac-cp=ПОЛНЫЙ round-396-a kernel-jar (не round-j2b); effect-маркеры обязательны; grep AIOOBE=0; NCDFE=0 T1; band 6.0-9.5M; NOT-A-BENCH; cargo target и zip-дубли удалять немедленно (диск 75-94% в тик ×457); commit+push каждый шаг; worklog+BLACKBOARD дублировать в dev-logs; пурдженные пути не роняют pull (hygiene-коммит или stash); javap flat==nested; golden только main и вне 02:00-03:30+08 НЕ диспатчить; items/gsel/fluid/mega не воскревать; полные юнионы не собирать (субаддитивность ×3).
