# TASK-132 — C3 HEALTH CADENCE #3: FIRST V2-GATED RUN — PASS ×BOTH GATES (2026-09-09)

**Agent:** agent-7625532f · claim a223ce6 → done · canonical rig `run_task129_pure_inject.sh` · N=1
**Gate:** v2 = `max(R0 × 1.10, R0 + 100 MB)` per `docs/C3_GATE_V2_PROTOCOL_2026-09-09.md` §5 (binding pre-registration) — this is the designated first v2-gated cadence run.
**Law:** INJECTS-ONLY — stock Temurin 21.0.12.1 + ONLY `-agentpath`, zero JVM options; diagnostics flag-free (jcmd GC.heap_info / GC.class_histogram, /proc smaps_rollup).
**Config-of-record verified pre-run:** runtime.so mtime Sep 8 15:14, purpur jar Sep 8 15:06, engine 4f5d5ea — unchanged.

## Verdict

| metric | value |
|---|---|
| boot | **Done (16.636s)** — pure-inject parity band (third boot: 16.27 / 15.96 / 16.64) |
| R0 idle | **916 MB** (used 482.5M, committed 580.6M, smapRss 919 / Pss 916) |
| R1 post-add (64-chunk forceload 3200..3327) | 965 MB (used 474.0M, committed 580.6M) |
| R2 remove-end | **977 MB** (used 490.4M, committed 582.7M) |
| residue | **+61 MB** vs R0 |
| v1 gate (R0×1.10 = 1007.6) | 977 ≤ 1007.6 → **PASS** (GC.run not needed) |
| **v2 gate (max(1007.6, 1016) = 1016)** | 977 ≤ 1016 → **PASS** — pre-registered expectation met (residue ≤ +100 MB) |
| hs_err delta | 0 |
| inject functional | `[crussty-runtime] v2.0.0 loaded` marker present |

## Mandatory commentary (gate v2 §5 — reported regardless of verdict)

**Trajectory shape: PLATEAU.** 13 samples over 120 s (RAW `run_R1/rss_after_remove.traj`):
965 → 970 → 970 → 974 → 974 ×5 → 976 ×3 → 977. Monotone saturating drift of +12 MB total with
shrinking deltas (+5, 0, +4, 0×4, +2, +2, +1) — no creep, no linear climb; the curve converges,
which is the benign signature.

**Used-heap discriminator (authoritative benign/leak signal):** used heap 482.5 MB @baseline →
490.4 MB @remove-end (**+1.6%**, flat within boot-variance noise); G1 committed 580.6 → 582.7 MB
(+2.1 MB). No live-object growth. The +61 MB RSS residue is therefore committed-flat and
used-flat → **native structural class** (C2 code-cache + metaspace from the 64-chunk churn);
TASK-126 NMT attribution measured exactly this class at ~57 MB on the lean config — the two
agree within 4 MB. Branch-(a)/branch-(b) benign framing re-confirmed; no agent-arena signature
(no escalation), no leak-investigation trigger.

## Notes

- R0 = 916 MB sits +27..39 MB above the TASK-129 pair (877–889) — within the per-boot committed
  variance law (±13% observed); same-boot pairing discipline applied (v2 gate computed from THIS
  boot's R0, never cross-boot).
- Rig internal tree intentionally left at v1 semantics (`R2 > R0×1.10`); v2 applied at reporting
  over the measured numbers per the gate doc's "no new instruments" clause. Rig was NOT modified.
  Open tail: a future zero-risk bookkeeping tick may flip the rig's `over_threshold` to v2 so the
  internal label and the protocol agree mechanically (proven-rig law: documented, not rewritten —
  hence deferred, not done silently).
- RAW evidence: `bench/graal_ab/RAW_TASK132_C3V2_20260909_080614/` (results.tsv, run.log,
  run_R1/{boot.log, hist_R2.txt, rss_after_remove.traj}); fifo stub removed at banking per
  fifo-hygiene law.
- Sibling/keep-out: twin lane S7-79 (attempt-17) untouched; boots serialized via BENCH-MUTEX flock.
