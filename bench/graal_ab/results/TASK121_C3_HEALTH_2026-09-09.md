# TASK-121 — Periodic C3 Health Re-Probe (first cadence run)

**Agent:** agent-7625532f · 2026-09-09 · claim 401c271 · RAW `bench/graal_ab/RAW_TASK119_C3_20260909_044330/`

## Purpose

Standing periodic check established at TASK-119 closeout (ledger §48/ADDENDUM-41): re-run the C3
reclaim-probe rig at cadence to verify the RECLAIM-PASS class still holds on the live box state —
new boots from other work accumulate between probes, and the reclaim property is only as good as
its most recent observation. First cadence run, N=1 (one R0/R1/R2 triple).

## Config

- Rig reused **UNCHANGED**: `bench/graal_ab/run_task119_c3_probe.sh`, `N=1`.
- Operator-of-record config: GraalVM CE 21.0.2 + `-XX:+UseJVMCICompiler` + runtime agent
  (env-unset), G1 2G (`-Xms512M -Xmx2G -XX:+UseG1GC`).
- Band: `forceload add 3200 3200 3327 3327` (64 chunks), 120s remove-settle with 10s RSS traj.
- Pre-registered tree (identical to TASK-119): `R2 <= R0*1.10` ⇒ PASS; else `jcmd GC.run` ⇒
  R3 ≤ 1.10 ⇒ PASS-LAZY; else FAIL → leak investigation protocol.
- BENCH-MUTEX flock taken by rig (fd 9, journal start/done pair); hs_err counter checked post-run.

## Result

| run | R0 (MB) | R1 post-add (MB) | R2 post-remove (MB) | R2 delta vs R0 | verdict |
|-----|---------|------------------|---------------------|----------------|---------|
| R1  | 1200    | 1240 (+3.3%)     | 1205                | **+0.4%**      | **PASS** |

- `jcmd GC.run` **not needed** (R3=NA) — no G1 laziness; reclaim completed within the settle window.
- Heap occupancy trace confirms live-set release: baseline last-pause `321M→321M(800M)`,
  post-add `446M→446M(800M)`, remove-end `730M→330M(800M)` — occupancy back at baseline level
  (330M vs 321M), same shape as all three TASK-119 main runs.
- Boot wall 16.455s — inside the established flat band (15.6–16.1s nominal, ±3s swing law).
- hs_err delta = 0. RSS trajectory in `rss_after_remove.traj` (13 samples, monotone decay to 1205).

## Verdict

**RECLAIM-PASS (1/1 PASS-class).** The TASK-119 refinement (TASK-117 D1 override) still holds on
today's box state — accumulation-while-held (+3.3%) and release-on-remove (back to +0.4% of paired
baseline, tighter than the TASK-119 range of +0.7..+5.9%) both reproduced. No drift, no leak
signature. Next cadence probe may run whenever a tick has no queue work; escalate to N=3 only on
any non-PASS observation.

## Banked notes

- Rig reuse without edits worked as designed (reentrancy not needed — fresh RAW stamp).
- Dead `console.fifo` removed from RAW pre-commit (git cannot store fifos; recurring hygiene step).
- CLAIMS union-collision with twin's S7-69 claim (7th this session) — resolved by union law,
  both entries preserved verbatim.
