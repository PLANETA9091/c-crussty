name: BenchPopulation
version: 0.1.0
main: bench.population.BenchPopulationPlugin
api-version: '1.21'
description: 'BENCH-ONLY population-injection fixture (BENCH-X150K, S7-128): deterministic living-scene injection via vanilla addEntity path. Not for production.'
authors: [agent-7625532f]
commands:
  benchpop:
    description: 'BENCH-ONLY: inject the deterministic living population (benchpop inject <target> [seed])'
    usage: '/benchpop inject <target> [seed]'
