Rows 1-2 of the first series (1,A=2.40 / 2,B=2.57) were measured on the
EXACT-BITS-KEY build (12:16-12:3x .so): pilot_exactbits_key proved that key
scheme never hits for ground items (AABB bits rebuilt every tick by friction
drift), so those rows measure guard-overhead, not the guard mechanism.
The definitive series (fresh summary.csv, runs 1-10, order A B B A A B B A A B)
runs on the FINAL cell-quantized-key build (both agents converged on identical
bits, deployed 12:3x). Coordination: TASK-80 note in crussty-dev-logs CLAIMS.
