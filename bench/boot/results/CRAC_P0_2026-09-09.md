# CRaC PROBE P0/P1 — Purpur 1.21.10 under Zulu CRaC JRE 21 (TASK-115, S7-54, 2026-09-09)

Design source: TASK-114 §30 (pre-registered CRaC-P0..P3 ladder) + CLAIM ff84d80. Verification-only session; no deltas banked.

## Setup

- Platform: Azul Zulu **JRE** 21.52+203-CRaC-CA (build 21.0.12.1+1-LTS) — same OpenJDK version line as the box's Temurin (21.0.12.1). Staged `/home/z/crac-jdk` (54MB tarball, Azul metadata API URL, foreground download per setsid-bg law).
- Workload: vanilla Purpur 1.21.10 boot, anchor-restored world, `-Xms512M -Xmx2G -XX:+UseG1GC`, `--nogui`; P1 adds inert `-XX:CRaCCheckpointTo=/tmp/crac_img` + `jcmd JDK.checkpoint` after Done+quiet.
- Launch discipline: script-file background (`bench/boot/crac_p1_probe.sh`, committed) — inline setsid-bg dies at tool-call boundary (4th independent confirmation this session, see lessons).

## Results

| Run | Launch | stdin | Boot | Post-Done survival |
|---|---|---|---|---|
| 1 (P0) | inline setsid-bg | closed/EOF | **Done (16.625s)**, clean log | dead ≤13s after Done |
| 2 (P1 attempt 1) | script-file bg | closed/EOF | **Done (15.772s)**, clean log | dead before checkpoint trigger (≤20s) |
| 3 (P1 attempt 2, instrumented) | script-file bg | **held open** (`tail -f /dev/null` pipe) | **Done (16.144s)**, clean log | **dead ≤2s** (first liveness sample `alive=0` at Done+2s) |

- All three boots: zero exceptions, zero hs_err (count stayed 4/0), RCON up, normal `Done (Xs)!` marker.
- Checkpoint never triggered in any run (no image files) — the **pre-checkpoint exit kills the probe before the pre-registered socket-kill could even be tested**.
- Refuted hypotheses: (a) kernel/cgroup OOM — `memory.events oom_kill=0`, dmesg clean; (b) JVM fatal — hs_err 4/0; (c) console-stdin EOF shutdown — refuted by run 3 (stdin held open, still died); (d) tool-boundary launch artifact — refuted by runs 2/3 (deaths inside a single script execution). (e) workload/wold state — same jar/world boots fine under Temurin and GraalVM for minutes (TASK-110/111/113 survivors).
- Exit cause: **unknown** — exit code not captured (process reaped by subshell before instrumentation).

## Verdict (honest)

**P0 flag-acceptance = FAIL-as-is**: the platform boots Purpur cleanly 3/3 but the JVM **self-terminates within ≤2s of Done** under this CRaC build in this environment — unusable as a server runtime as-is. P1 (checkpoint) **unreachable** — blocked by the pre-checkpoint exit, NOT by the predicted listening-socket refusal (that kill-condition remains untested). CRaC channel status: stays **OPEN but gated** on a new blocking defect.

## NEXT (mitigation ladder, cheapest first)

1. **Capture exit code + `mkdir -p $IMG` before boot** — `CRaCCheckpointTo` pointed at a non-existent directory is the top suspect for a CRaC-patched lazy-init abort; exit code discriminates clean `System.exit` vs signal.
2. Full **JDK** variant (not JRE) of Zulu CRaC 21 — JRE packaging is the second suspect (trimmed jcmd/jdk.crac internals).
3. If 1-2 fail: `-XX:CRaCCheckpointTo` unset boot (pure CRaC-JRE-without-flag) isolates whether the flag itself triggers the exit.
4. If still dying: channel downgraded to BLOCKED-environment; re-probe only after a CRaC build change.

## Lessons (class: probe hygiene)

- 5th instance of the silent-background family: my probe's own background subshells held the tool stdout pipe → 300s tool-call timeout despite the script finishing its logical path; redirect/detach ALL background fds, not just the server's.
- Liveness monitor (2s sampling from Done) is the pattern that turned "process mysteriously gone" into a bounded survival window — reuse for any flaky-exit probe.

## PHASE-2 CORRECTION (S7-55, 2026-09-09) — P0 verdict REFUTED as measurement artifact

**The "self-exit <=2s post-Done" never happened for run-3.** Direct evidence found this session: the run-3 JVM (started 2026-09-08 23:45:34, PID 4366) was ALIVE 8+ hours post-boot (PPid=1 orphan, State=S, listening on 25565+25575). Cause of the false death report: **Zulu CRaC JVM's comm = "exe" (not "java")** -> `pgrep -x java` (the P1 rig's liveness monitor) was structurally BLIND to it. Runs 1-2 deaths remain real (ports were free for later runs to bind; their exit codes were never captured). Additionally, run-3's orphan held BENCH-MUTEX via inherited fd 9 (script 4363 + tail 4365 also orphans) -> three zombies reaped this session; two LOCK-BUSY probes + one port-collision boot (EXIT_CODE=0, Netty "Address already in use" crash-report) spent before the lane was truly free.

**Clean re-test (rig bench/boot/crac_p2_exit.sh, direct-PID liveness via kill -0):**
- Mode A (mkdir -p CRaCCheckpointTo BEFORE boot + flag): **Done 19s, SURVIVED 60s+ post-Done** -> mkdir/flag hypotheses eliminated; platform boots AND lives. P0 flag-acceptance effectively re-opens as PASS-corrected.
- Checkpoint retry (P1-with-mkdir): jcmd delivered rc=0, **CheckpointException raised, 0 image files, server SURVIVED the refusal**. Suppressed exceptions = mechanical blocker inventory: CheckpointOpenFileException(purpur-1.21.10.jar), CheckpointOpenFileException(logs/latest.log), CheckpointOpenSocketException(localport=25575, +25565) — **the pre-registered socket-kill fired, plus open-file blockers** (more than predicted: sockets AND file handles).
- Exit codes: run A-collision = 0 (graceful Netty-bind-fail shutdown); clean-boot exit code not captured (process survived -> reaped by graceful rcon stop).

## Corrected verdict
CRaC channel: **OPEN** — platform boots Purpur cleanly and lives indefinitely (comm-blind pgrep was the ghost). Checkpoint blocked by an EMPIRICALLY ENUMERATED fd set (2 sockets + 2 open files) = exactly the P2 probe's target (org.crac hooks / socket pre-close / file-handle discipline). Restore = untested (no image exists; blocked upstream of it).

## New probe laws (class: CRaC measurement hygiene)
1. Liveness on CRaC lanes = PID-tracked `kill -0 $PID` ONLY; `pgrep -x java` is comm-blind to CRaC JVMs (comm="exe").
2. LANE-BUSY gates must check PORTS (ss :25565/:25575), not just process names.
3. Backgrounded probe subshells INHERIT the flock fd -> an orphaned probe holds BENCH-MUTEX forever; close fd in bg children or verify lane with the port check.
