# BENCH-X150K — спека сценария «прайм» (эра архитектурных бустов)

> Owner directive 2026-09-17 (~21:10-21:15 +08): «надо x100 делать чтобы все
> форс чанки были и всё жило, а тпс и мспт — 20 и минимальный; ты все
> боттленки ебнуть должен — что за микро фиксы; тут архитектуры надо делать
> и реальные бусты» → поправка: «x100 это мало, твой прайм было **x150000**»
> → «по русски».
>
> Статус спеки: DRAFT v1 (S7-127) — два пункта помечены «ЗАПРОС ВЛАДЕЛЬЦУ».

## 1. Что фиксируем (north star НЕ меняется)

- Цель: **20 TPS / минимальный MSPT** на MineShield-3 при ПОЛНОЙ живой сцене.
- «Всё жило»: спавн И деспавн мобов активны «как будто игроки есть», вся
  энтити-жизнь прайма (фермы, предметы, мобы) тикает.
- Все форс-чанки: 9216 (radius 640) — уже так в BENCH-4; сохраняем.
- Запреты в силе: NO fallbacks, NO config-wins/JVM-флаги; только реальный код.
- Метод измерения остаётся честным: A/B min-of-2, median-exact parity,
  world pin, paired по runner_cpu_index (закон S7-96d), preregistration.

## 2. Сцена X150K (главное изменение)

**Рабочая гипотеза юнита**: x150000 = **~150 000 живых сущностей** на прайме
реального сервера (фермы Season 3: предметы + мобы). **ЗАПРОС ВЛАДЕЛЬЦУ #1**:
подтверди юнит — 150k сущностей? (альтернативы: 150k мобов; 150k тиков
чего-то; множитель нагрузки).

Компоненты сцены (все — BENCH-ONLY fixtures, серверный код 100% ваниль —
прецедент BenchFakePlayersPlugin/task170):

| компонент | сейчас (BENCH-4) | X150K |
|---|---|---|
| форс-чанки | 9216 (r640) | 9216 (без изменений) |
| фейк-игроки | 4 | 4–16 (покрыть спавн-линии; **ЗАПРОС ВЛАДЕЛЬЦУ #2**: сколько игроков на прайме?) |
| спавн-циклы | summon_sweeps 0/1 | активный естественный спавн + периодические console-свипы |
| живые сущности на буте | ~1-3k равновесие | **инъекция популяции до таргета ~150k** |
| окно | 900s | 900s (профиль) / отдельные соаки |

**Population-injection fixture** (новый, bench/population): на буте после
Done заливаем популяцию до таргета детерминированно (фиксированный сид):
смесь по классам (items/хостилы/пассивы — распределение согласовать с
реальным прайм-снапшотом снапшотом мира; если нет данных — дефолт
70% items / 20% hostiles / 10% passives), размещение по чанкам фермерских
зон + равномерно по остальным. Инъекция = NBT/addEntity путь сервера,
НЕ правка логики тика. Честность: инъекция до старта окна замера,
далее сервер живёт своей ванильной жизнью (деспавны, мерджи, AI).

## 3. Протокол перебазирования (X150K-база)

1. Реализовать fixture → CI-бут X150K на **pre-pack** теге и на **master**
   (A/B, min-of-2, пары по cpu-классу).
2. Новый базовый банк X150K: 2 arms pre-pack ⇒ окно/порог по закону §125-A1
   (пересечение ±2%, медиана×0.97).
3. Свежий BOTTLENECKS-профиль на X150K ⇒ **ARCH-ATTACK очередь** —
   архитектурные рычаги (не линзы): Rust/JNI порты горячего пути
   (энтити-тик, Brain, палитры, коллизии, item-merge), планировщики,
   батч-операции. Каждый рычаг: STEP-0 kill-gate (javap-контракт) →
   имплементация → гейт ≥3% **на X150K-базе**.
4. Вердикты X150K-эры: pack-банк §125-A1 (−4.52%, LANDS) остаётся в ledger
   как историческая запись старой сцены; новые замеры — только на X150K.

## 4. Ожидаемый профиль (гипотеза, проверить профилем)

На 150k сущностей доминанты сместятся: entity tick (Brain/AI/goals),
item merge + подборе, коллизии/движение, hopper/container-логика фермерских
зон, случайный тик по 9216 чанкам, сеть-серриализация за фейк-игроков.
Текущие топы fp=4-сцены (PalettedContainer.get 3.6%, RandomTickOps 3.5%,
Brain.tick 6.75%) станут ЛИБО шумом, ЛИБО вырастут кратно — решит профиль.

## 5. План работ (порядок)

- [ ] S7-128: population fixture MVP (инъекция N сущностей, сид,
      распределение; сборка в bench-world3 артефакт) + CI smoke на 10k
- [ ] S7-129: масштаб до 150k (память/время бута), soak-стабильность
- [ ] S7-130: X150K база A/B (pre-pack vs master) + свежий профиль
- [ ] S7-131+: ARCH-ATTACK по очереди (первый кандидат решит профиль)
