# GOAL — 20 TPS на MineShield-3 (north star)

> Зафиксировано владельцем 2026-09-16: «твоя задача сделать 20 тпс, минимальный
> количество мспт на этом майншилд 3 сервере со всеми чанками форс лоадами и что
> бы и мобы спавнились, и деспавнились, когбудто бы игроки есть».
> Ранее: «никаких фоллбеков, всё должно быть ускорено а не просто менять конфиги».

## binding directive (правила ранга рычагов)

1. **NO FALLBACKS**: нативный путь либо работает и побеждает, либо рычаг REFUTED.
   Никаких «фоллбек-веток» в хот-лупе; переключение — на уровне гейта.
2. **NO CONFIG-WINS**: любые «+TPS» от правки конфигов/JVM-флагов — НЕ ускорение
   и не засчитываются в ledger. Только реальный код: Rust/JNI, ASM-patch,
   алгоритм/структуры данных, побеждающий с медианно-точной паритетом.
3. Один рычаг за раунд; STEP-0 kill-gate до кода; гейт >= 3.0% MSPT, CI A/B min-of-2.
4. INJECTS-ONLY в sandbox; CI-буты санкционированы.

## сценарий (scenario fidelity)

- Мир: реальный MineShield-3 (Purpur 1.21.10), все чанки force-load.
- **Требование владельца**: мобы спавнятся И деспавнятся, «как будто игроки есть».
  Сейчас natural spawning не активен (0 игроков в бенче) — сущности мира живы
  (Brain.tick 6.85% подтверждает), но цикл спавна не нагружает тик.
  => BENCH-SCENARIO upgrade (task166+): fake-player注入 для per-player spawn
  алгоритма (без сети, серверные ServerPlayer-заглушки). Это сценарий бенча,
  НЕ игровая правка; фиксируется как требование к world-bench-4.

## база и цель

| метрика | run#10 | run#11 | run#15 (sweeps=1) | run#16 (paired) | run#17 (BENCH-4 fp=4, leg1) | run#18 (BENCH-4 fp=4, leg2) | цель |
|---|---|---|---|---|---|---|---|
| MSPT avg | 80.86ms | 84.47ms (разброс ~4% => A/B paired обязателен) | **49.6ms steady (tickmonitor, 8 окон)** | **74-80ms steady** | **76.98ms headline / 72.1ms [⚡]-окна** | **85.24ms headline / 81.7ms [⚡]-окна** | **<= 50ms** |
| TPS | ~13.5 | ~13.3 | **20.0 стабильных ~13 мин** | **12.5-13.5** | **12.8-14.6 steady** | **11.6-12.7 steady** | **20.0** |
| нужно срезать | | | | | **BENCH-4 база min-of-2 = run17: ~-40% CPU** | |

> **RUNNER-VARIANCE LAW (run#16, S7-96d)**: 20 TPS run#15 REFUTED min-of-2 —
> идентичный снапшот (item_frame 2714(160)), идентичные входы, но 12.5-13.5 TPS /
> 74-80ms => shared-runner CPU variance доминирует кросс-рановые базовые линии
> (±30-35% MSPT). Все будущие A/B — только same-boot или paired по
> (world_sha256, runner_cpu_index) — run-env.txt пишется в каждый прогон.
> North star 20 TPS остаётся ОТКРЫТ; базовая линия = 12.5-13.5 TPS / 74-80ms.

> **run#15 (S7-96c, дубль-агент)**: первый прогон канонического условия владельца
> (summon_sweeps=1: 75 summons, F4 churn ACTIVE — дельта 629 сущностей, drowned
> 11->47, item 148->772) — **20.0 TPS / ~49.6ms MSPT steady**. НО: никаких модульных
> оптимизаций в этом прогоне не шипилось (только observability), а run#14 на том же
> мире/forceload/модуле дал 13-14 TPS / ~68-75ms. Ведущая гипотеза дельты:
> **вариативность живого снапшота мира** (world URL = живой экспорт сервера
> владельца: равновесие item/mob-накопления меняется между скачиваниями; run#15
> boot: item 148 -> 772 за прогон = свежий снапшот, накопление в реальном времени).
> => north star НЕ объявляется достигнутым до min-of-2: run#16 (те же входы)
> деспатчен 19:53Z. Если run#16 ~50ms — снапшот-вариативность подтверждена как
> ДОМИНИРУЮЩИЙ фактор базовой линии, и все будущие A/B обязаны качать мир один раз
> на пару прогонов (paired download discipline) либо использовать pinned-снапшот.

> **run#17 (S7-100, BENCH-4 fp=4, id 35156292165, sha 8a6988d)**: первый
> ФИКСТУР-ВАЛИДНЫЙ прогон канонического условия владельца — fake_players=4
> (BenchFake-0..3, детерминированные UUID, кольцо ±320), gates 1a/1b/1c PASS:
> spawnable 289 чанков; churn ACTIVE с summons=0 (polls=15, дельта 774 сущностей:
> item 163->814, ocelot 4->100, zombie 67->96, creeper 80->105, bee 2->19) —
> спавн/деспавн «как будто игроки есть» УПРАЖНЯЕТСЯ. База: 76.98ms headline /
> 72.1ms steady, TPS 12.8-14.6. GC: duty 0.70% wall (321 паузы, avg 19.6ms) —
> GC-FAMILY law сохраняется. Кросс-ран с run#16 НЕ парится (cpu_idx 9080657,
> у world3_art run-env нет) — структурное сравнение профилей только.
> Fresh recon (research/bench4-recon-2026-09-17/run17): профиль структурно
> стабилен vs run#16 (kernel:other 21.5/22.0, entities 12.7/12.5, chunk 10.4/9.8);
> spawn-лейн суммарно ~0.6% (NaturalSpawner 0.31 + createState 0.17 +
> checkDespawn 0.05) — owner-сценарий НЕ взрывает спавн-лейн; новинки top-leaves:
> ServerEntity.sendChanges 0.77% (visibility фейк-игроков), Entity.setDeltaMovement
> 1.10% (топ entity-лейна), frem+fmod ~1.13%. ЗАМЕНЯЕМЫХ СОЛО >= 3% В НОВОМ
> ПРОФИЛЕ НЕ ОБНАРУЖЕНО (PalettedContainer.get 3.62% — closed chunk lane).
> => run#18 35159240368 (leg 2, fp=4, master f3c82b3) деспатчен 22:45Z —
> bench-4 база min-of-2 paired; выбор следующего рычага — после absorb run#18.

> **run#18 (S7-102, leg 2, id 35159240368, sha 7747a8f-era master)**: SUCCESS
> 23:10:26Z; FIXTURE-VALIDITY VALID (churn дельта 816 / 9.4%, summons=0;
> alive-check стабилен). MSPT 85.24ms headline / 81.7ms окна, TPS 11.6-12.7,
> cpu_idx 6746569 (run#17: 9080657) — ДРУГОЙ runner, leg spread 10.7%.
> => **BENCH-4 BASELINE MIN-OF-2 = run#17: 76.98ms / TPS 12.8-14.6** (консервативная
> планка для всех будущих A/B; спред подтверждает runner-variance law).
> Профиль leg1/leg2 структурно стабилен (research/bench4-recon-2026-09-17/run18):
> PalettedContainer.get 3.42/3.62 (closed lane), optimiseRandomTick 1.96/2.56
> (refuted), advanceSeed 1.53/1.29 (refuted solo), spawn-лейн 0.73/0.6%;
> заменимых соло >=3% в min-of-2 профиле НЕТ.
> => run#19 35163894978 деспатчен 23:48Z = N=16 SCALING PROBE (сценарий, не
> модульный вин): метрики роста network/visibility (sendChanges 0.77% при N=4) +
> spawn-proximity (LocalMobCapCalculator) с N; absorb next tick — если lane
> >=3% replaceable при реалистичном N => следующий рычаг.

> **run#19 (S7-103/104, N=16 SCALING PROBE, id 35163894978)**: SUCCESS 00:13:11Z,
> FIXTURE-VALIDITY VALID (churn 747/8.4%, summons=0, alive-check стабилен), MSPT
> 57.01ms, TPS steady 16.9-20.3, cpu_idx 10088241 = САМЫЙ быстрый runner серии
> (абсолютный MSPT кросс-ран невалиден — variance law; вердикт по долям профиля).
> **N-SCALING VERDICT: REFUTED как источник рычага** — профиль N-инвариантен:
> network lane 1.47->0.85% (SHRINK при 4x игроков; sendChanges total
> 2180->1258 = 1.57->0.89%; outbound дешёв при discard-handler), spawn-лейн
> 0.6->0.7% (суб-линейный рост, NaturalSpawner 0.25->0.38%), единственный рост
> = GC-лейны +2.6pp (G1 4.48->5.99, barriers 4.50->5.61) — но alloc-shape
> закрыт GC-FAMILY LAW (STW duty 0.80% wall). ЗАМЕНЯЕМЫХ СОЛО >=3% ПРИ N=16 НЕТ.
> **НАБЛЮДЕНИЕ для владельца (не вердикт, 1 нога)**: на самом быстром runner
> серии текущий kernel держит ~19-20 TPS steady при N=16 — гипотеза «20 TPS
> блокирует контеншн shared-runner, а не kernel» требует min-of-2/pinned-runner
> для проверки. Оставшиеся пути к 20 TPS: (2) семейные агрегаты <3% патчей
> (owner-санкция на пересмотр гейта), (3) pinned/dedicated runner (owner-инфра),
> (4) N-scaling REFUTED — профиль N-инвариантен.

## MSPT budget ledger (run#11, % от CPU тика, обновляется каждый раунд)

| лейн / кластер | presence | replaceable-ядро (верифицировано) | статус |
|---|---|---|---|
| entity/mobs всего | 43.3% | — | главный резерв |
| Brain.tick кластер | 6.85% | STEP-0 сделан (S7-98): replaceable ядро = iterators 0.47-0.80 + getNode 0.16-0.18 + views 0.08-0.20 + половина SELF ~0.15-0.25 = **0.9-1.5%**; itable-диспатч 0.45-0.48 и canStart-тела (PalettedContainer/PathTypeCache/Long2Object) НЕЗАМЕНЯЕМЫ | **REFUTED (task168) — 0.9-1.5% << 3%** |
| GC (STW + barriers) | 9.5% | **CLOSED (S7-97 GC-FAMILY LAW)**: 9.5% = concurrent worker CPU (G1CM/RebuildRemSet), НЕ MSPT; STW duty 0.50-0.56% wall (gc.log run#12/15: eden 2.4GB, 412-598 MB/s, young-GC каждые 4-6s); relief = alloc_share x 0.5% => BlockPos 2-3% alloc => <=0.015% MSPT; Brain-LHM 0.4-0.69% => <=0.005% | **REFUTED (task166) — alloc-shape семейство закрыто** |
| random-tick lane | 5.46% | advanceSeed 1.57% + BlockPos-alloc (hit-only, 2-3% alloc => <=0.015% MSPT) + хвост SELF; batch-RNG REFUTED соло | REFUTED соло (task165); BlockPos-слайс закрыт GC-law (task166) |
| scheduled-tick drain (LevelTicks.tick) | 8.28/11.55% | STEP-0 сделан (S7-97): tickBlock контракт верифицирован; decompose: reads 2.65/3.77 + signal 1.18/1.43 + queue 0.5-0.76 + glue 0.29/0.62 + mid-tick 1.57/1.83 + tail — ВСЕ <3% соло | REFUTED-solo (task167); family-bank parked |
| minecarts | **2.15/2.58%** (measure run#12/15; старые ~5.3% = bucket mirage) | STEP-0 сделан (S7-98): весь лейн под гейтом — move 0.55/0.73 + applyEffectsFromBlocks 0.40/0.46 + hopper 0.18/0.21 + push/pickup 0.12/0.17 + fluid-push 0.12/0.14; perfect-lens bound < 3% | **REFUTED (task169) — whole-lane < gate** |
| Villager | 3.47% | пересекается с Brain | через Brain |
| chunk lane | 9.8% | per-get lens REFUTED; batch-lens REFUTED (3.3% потолок) | closed |
| worldgen | 0.0% | — | closed (измерено) |
| boats (owned) | 0.76% | < 4% гейта | REFUTED (task164) |
| ENT-BP соло | 1.5-1.8% | < 3% | parked (инфра для vehicle A/B) |

## арифметика честного пути к 20 TPS

Сумма всех ЗАКРЫТЫХ честных соло-рычагов сегодня ~5-7% MSPT — мало.
Путь к 40%: только **агрегатные семейства** (каждый патч проходит свой гейт):
- ~~GC-SHAPE~~ **CLOSED (S7-97)**: GC-family law — alloc-shape relief = alloc_share x STW-duty 0.5%; верхний потолок всего семейства < 0.5% MSPT
- ~~REDSTONE-LENS~~ **REFUTED-solo (S7-97)**: drain-lane 8.28/11.55% decompose => все слайсы <3%; family-bank parked
- ~~BRAIN-LENS~~ **REFUTED (S7-98)**: replaceable ядро 0.9-1.5% (dispatch + canStart-тела незаменимы)
- ~~minecarts~~ **REFUTED (S7-98)**: весь лейн 2.15/2.58% < гейта (mirage ~5.3% исправлен)
- **BENCH-4 FAKE-PLAYERS (task170, S7-99 = IMPLEMENTED, S7-100 = VALIDATED)**: каноническое условие владельца (спавн/деспавн как при игроках) + честная база для всех будущих A/B. STEP-0 контракт оффлайн-верифицирован (research/bench4-recon-2026-09-17: placeNewPlayer public + внутренний SGPL 4-arg; doSendPacket isConnected-safe; Connection.tick не тикается для самодельного Connection; keepalive 15s timeout закрыт публичным handleKeepAlive через EmbeddedChannel-стаб; Dec-2025 kernel: PlayerMobDistanceMap заменён на LocalMobCapCalculator.playersNearChunk — то же требование к фикстуре). BenchFakePlayersPlugin (real ServerPlayer, детерминированные UUID, N=4 кольцо, alive-check heartbeat) + harness FAKE_PLAYERS + report FIXTURE-VALIDITY gate. VALIDATION: run#17 35156292165 absorbed — FIXTURE-VALIDITY VALID (gates 1a/1b/1c PASS, churn дельта 774 при summons=0); база leg 1 = 76.98ms / TPS 12.8-14.6; run#18 35159240368 (leg 2, fp=4) in flight — min-of-2 paired база bench-4 эры
- entity_mirror infrastructure A/B (vehicle-dense, ENT-BP infra)
Каждый шаг — паритет-банкованный; сводные A/B после каждого семейства.

> **run#21 (S7-106, PAIR-LEG, id 35169715709)**: SUCCESS 01:37:36Z; FIXTURE-VALIDITY
> VALID; MSPT **76.01ms**, fp=4, cpu_idx 8914646 (band [8636000,9525000] — первая
> нога, прошедшая band-gate после 4 fast-fail отверганий: 6981619/7086411/
> 9958944/вне банды — каждый за ~30с вместо 25 мин).
> **ПЕРВАЯ ЛЕГАЛЬНАЯ ПАРА (S7-96d law, pair_hunter.py)**: world_sha MATCH
> (afb3a0b3ba78) + fp 4/4 + fixture VALID обе ноги + cpu Δ 1.86% <= 2% tol:
> **runA 35169715709 cpu 8914646 MSPT 76.01 | runB 35156292165 (run#17) cpu
> 9080657 MSPT 76.98 => SPREAD 1.3%** (кросс-ран спред был 10.7% — pairing
> схлопывает шум на порядок; runner-variance law подтверждена количественно).
> Парный профиль (lanes_vs_run17): структурно стабилен — все kernel-лейны в
> ±1.6pp (kernel:other -1.61, entities -0.76, network -0.37, chunk -0.18),
> шевелятся только GC-лейны (+1.3/+0.4pp, закрытая семья) и unpooled "other"
> (+2.06pp) — ЗАМЕНИМЫХ СОЛО >=3% НЕТ и на паре. BASELINE НА ПАРЕ = 76.01ms.
> Стоимость охоты: 4×~30s fast-fail + 1 полная нога = самодостаточный legal
> min-of-2 без owner-hardware (task171 доставлен end-to-end).

> **СТАТУС 20 TPS (S7-109)**: СОЛО-ЭРА ЗАВЕРШЕНА (6 подтверждений: run#11,
> N=4 min-of-2, N=16, легальная пара, run#22 кросс-класс, run#23 кросс-класс) —
> восемь STEP-0 киллов + профиль без заменимых соло >=3% на всех классах
> железа + N-SCALING REFUTED + парная проверка. BENCH-4 база: mid-класс пара
> run#17×run#21 = 76.01/76.98 (spread 1.3%); slow-класс 3 ноги = 85.24/83.74/
> 83.96 (spread 1.8%, CLASS-BIMODALITY: MSPT кластеризуется по типу VM —
> LCG шумит ±7% внутри класса, cpu-дельта внутри класса НЕ конвертируется в
> MSPT-дельту); межклассовая сепарация ~10% (slow 84 / mid 76.5 / fast 57).
> Вердиктный гейт паринга = пререгистрированное 2% правило (консервативно);
> class-paired = owner-facing гипотеза. ПУТИ ВПЕРЁД (все owner-gated):
> (2) семейные агрегаты <3% (санкция на пересмотр >=3% правила); (3) pinned/
> dedicated runner класса fast/mid (оценка железного рычага ~10-25% MSPT);
> (4) смена сценария. МОДУЛЬНЫХ РЫЧАГОВ >=3% НЕТ; инфра честных A/B
> самодостаточна и строга (2% вердикт, harness-cpu паринг, sequential).

> **СТАТУС 20 TPS (S7-110)**: FAMILY-AGG PREREGISTRATION — путь (2)
> КВАНТИФИЦИРОВАН как агрегатный рычаг (docs/FAMILY_AGG_PREREGISTRATION.md,
> STEP-0 бумажно, ДО кода). Разрез по НЕПЕРЕСЕКАЮЩИМСЯ лейнам парного профиля
> (sum ~96%), все числа из banked STEP-0: Tier A (только строго верифицированные
> ядра с bit-exact/median-exact паритетом: BATCH-RNG 1.6-1.9 + BRAIN 0.9-1.5) =
> 2.5-3.4%; Tier B (+ LevelTicks parity-safe слайсы reads 0.3-0.5 + queue <=0.5;
> signal lens 1.5-2.5 ТОЛЬКО при bit-exact доказательстве) = **3.3-6.9% >= 3%
> гейта** — первый GO-кандидат эры. Tier C (+ minecarts-assumption 0.6-1.3 +
> ENT-BP parked 1.5-1.8) = 5.4-10.0% (не пререгистрировано). План без пересмотра
> гейта: pack = ОДИН рычаг {F1 batch-RNG, F2 Brain, F3-safe}; билд по чану с
> паритет-банкингом (F1 -> F2 -> F3-reads -> F3-queue; signal — условно); ОДИН
> агрегатный A/B min-of-2 (baseline-нога = банк легальной пары 76.01/76.98,
> pack-ноги = hunt_leg_b машинерией); если pack < 3% — НИЧЕГО не landится,
> агрегат REFUTED, модульная повестка пуста. Исключены по паритет-риску:
> neighbor-glue skip, mid-tick reshape. Гипотеза «один рычаг = агрегат-пак»
> помечена для owner (одно слово = отмена, нулевой риск). Pair-машинария тёплая:
> формальная пара #2 leg dispatched (35179585066, fp=4, band-gated).

## калибровка профилировщика (banked, task165)

- **alloc-collapsed leaves НЕ равны new-сайтам**: `AABB.intersects` (чистая
  математика, 0 new байткодом) — топ-лист; `PalettedContainer.get` — 0 new.
  Интерпретация: alloc-профиль = давление корреляции (TLAB-refill окна),
  НЕ карты сайтов. Verifed-site метод: javap `new`-скан по телу метода.
- CPU leaf после JIT-инлайна = нижний физический фрейм (nextInt -> advanceSeed).

> **СТАТУС 20 TPS (S7-111)**: FAMILY-AGG билд стартовал — F1 BATCH-RNG реализован
> с parity-банкингом (первый инкремент по §125 пререгистрации; ничего не
> landится до агрегатного A/B). RandomTickOps.java: helper с инлайн-LCG (локальный
> seed, регистр-резидент; reject-pick = ноль dispatch/field-трафик), синхронизация
> Unsafe get/put на private `value` только на границах body-вызовов, setSeed
> bypass (gaussian-reset side-effect найден cfdump'ом и обойдён корректно).
> Бит-точный паритет: PASS (320K attempts, 8 сидов, 160K hit-интерливов вкл.
> nextGaussian). Следующий tick: Rust byte hook (classfile.rs surgery + retransform
> по area_map-образцу) + runtime self-test, затем F2 Brain-итераторы. Пара #2:
> 3 band-reject (один в 0.2% от band'а), 4-я нога in flight. INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-113)**: F2 BRAIN-ИТЕРАТОРЫ BUILT — `randomtick/src/BrainOps.java`:
> flat-snapshot lens поверх setup-stable структуры Brain
> (availableBehaviorsByPriority: OUTER=TreeMap/INNER=LinkedHashMap/SET=LinkedHashSet —
> CP-факты; мутации ТОЛЬКО {ctor, computeIfAbsent+Set.add, clear} — все 5 getfield'ов
> просвечены). groupStart[] воспроизводит LIVE-contains РОВНО в местах vanilla
> (раз на группу; одинаковая activity в двух приоритетах = две проверки);
> getStatus/tryStart — не тронутые LIVE-вызовы; gameTime — один раз. FINGERPRINT:
> 5 семейств O(1)-проб (2 identity + 3 size), 0 итераторных аллокаций на hot path;
> cache = WeakHashMap+IdKey (AbstractMap.equals TRAP закрыт S7-тестом).
> PARITY BANK на РЕАЛЬНОМ production entry (ServerLevel через Unsafe.allocateInstance +
> WritableLevelData-прокси; SharedConstants+Bootstrap.bootStrap = статические данные,
> НЕ бут): **PASS — 4828 вызовов / 3083 order-exact событий / 1740 мутаций / 8 сценариев**
> (S7 EQUALS-TRAP + S9 fuzz 60×40 kernel-размера). Byte hook = 14-байтовая прямая
> строка (следующий тик, getfield СВОИХ private-полей = verifier-легален, helper без
> Unsafe). Пара #2: нога#6/7 band-reject (~30s, без трат), нога#8 SUCCESS но harness
> +4.8% вне окна => честный discard, нога#9 35187305900 in flight. Ничего не landится —
> вердикт у агрегатного A/B (§125/§128). INJECTS-ONLY цел (0 sandbox boots).

> **СТАТУС 20 TPS (S7-112)**: F1 BYTE HOOK ГОТОВ — Rust-хук `randomtick.rs`
> + `classfile.rs::patch_optimise_random_tick` (образец patch_update): тело
> optimiseRandomTick заменено на 11-байтовую прямую строку
> `aload/aload/iload/aload/getfield simpleRandom/invokestatic
> RandomTickOps.run/return` (без ветвлений => ПУСТОЙ StackMapTable, 0 кадров).
> Append-only CP, дедуп => patch(patch(x))==patch(x) (идемпотентность
> доказана тестом); fail-closed: чужой класс/обрезанные байты => Err без
> паники (14 prefix-срезов fixture). ВЕРИФИКАТОР-ГЕЙТ на РЕАЛЬНОМ HotSpot:
> VerifyPatched (resolveClass = link-time verification, major 65) —
> **VERIFY-OK** (research-скрипт randomtick/verify_patched.sh; байт-тесты
> cargo: 82 passed). Активация по area_map-образцу: poll ServerLevel ->
> define RandomTickOps в loader kernel'а -> READY -> retransform; маркер-цепочка
> defined/armed/rc/PATCHED; семантический self-test = CI-буты (санкционированы,
> каждый smoke с миром гоняет patched body). Ничего не landится — следующий
> шаг билда: F2 Brain-итераторы, затем F3-reads; агрегатный A/B против банка
> пары 76.01/76.98 решает ВСЁ (§125 протокол). Пара #2: нога#5 35181833283
> OUT-of-window (+0.7%) — честный discard, absorb run#25: 86.65ms при
> harness 6574725 => 4-я slow-нога, класс-спред n=4 = **3.5%** (83.74-86.65;
> было 1.8% n=3 — честная коррекция owner-числа; межкласс по-прежнему ~10-25%).
> Нога#6 35183885492 in flight. INJECTS-ONLY цел (0 sandbox boots).

> **СТАТУС 20 TPS (S7-114)**: F2 BYTE HOOK ГОТОВ — `classfile.rs::patch_brain_start_each`
> (образец patch_optimise_random_tick): тело startEachNonRunningBehavior
> (0x0002, vanilla len=178) заменено на 14-байтовую прямую строку
> `aload_0/getfield availableBehaviorsByPriority/aload_0/getfield
> activeActivities/aload_1/aload_2/invokestatic
> BrainOps.startEachNonRunning:(Map;Set;ServerLevel;LivingEntity;)V/return`
> (без ветвлений => ПУСТОЙ StackMapTable; max_stack 5 / max_locals 3; оба
> getfield — СВОИ private-поля внутри Brain.class => verifier-легально,
> helper БЕЗ Unsafe). Append-only CP + дедуп => идемпотентность тестом;
> fail-closed (14 prefix-срезов + чужие классы). Runtime wiring `brainhook.rs`:
> poll Brain -> define ВЕРНОМ порядке {BrainOps$IdKey, BrainOps$Snapshot,
> BrainOps} в loader kernel'а (nested-first: ленивое разрешение через defining
> loader иначе промахнулось бы по classpath kernel'а) -> READY -> retransform;
> маркер-цепочка defined/armed/rc/PATCHED. ВЕРИФИКАТОР-ГЕЙТ: VerifyBrain на
> реальном HotSpot — child-first loader {patched Brain + helper trio},
> kernel-jar parent-first => **VERIFY-OK** (brain 31966B major 65 + ops 5469B
> слинкованы; randomtick/verify_brain_patched.sh; cargo: 85 passed). Семантический
> self-test = CI-буты (каждый smoke с мобами гоняет patched body; parity-контракт
> уже banked §128: 4828 вызовов PASS). ПАКЕТ: F1 hook ✓ + F2 hook ✓ — следующая
> единица билда: F3-reads (task167 slices, parity-banking), затем ОДИН
> агрегатный A/B против банка пары 76.01/76.98 (§125 протокол) решает всё.
> Пара #2: нога#9 35187305900 SUCCESS но harness 6966037 (+6.7% вне окна)
> => честный discard (5-й подряд честный reject — ворота строгие, ни одной
> ложной пары); нога#10 35189275270 dispatched in flight. INJECTS-ONLY цел
> (0 sandbox boots; verify = link-time только, без <clinit>).

> **СТАТУС 20 TPS (S7-115)**: F3-READS BUILT — `randomtick/src/TickBlockOps.java`
> (package net.minecraft.server.level): ЧЕТЫРЁХСЛОЙНАЯ анатомия cfdump'ами
> run21 (Level.getBlockState @0-71: capture-ветка ПЕРВАЯ + isOutsideBuildHeight
> → VOID_AIR + getChunk(FULL,true) + ChunkAccess.getBlockState;
> LevelChunk.getBlockStateFinal: nonEmptyBlockCount==0 → AIR-шорткат; дрен
> LevelTicks.runCollectedTicks @0-76 С QUIRКОМ: set.remove под guard'ом
> !toRunThisTickSet.isEmpty(); tickBlock @0-53: is→tick→counter&7→mid-tick).
> Дизайн: дрен-хелпер = байт-точное зеркало ванили (Unsafe-read приватных
> final-полей — одна выборка эквивалентна getfield на каждой итерации) + окно
> ThreadLocal section→chunk кэша; readBlockState-линза: ТОЛЬКО getChunk-хоп
> заменён (первый тик секции платит точный ванильный getChunk, остальные секции
> — кэш-хит; ChunkAccess.getBlockState остаётся реальным вызовом); ВНЕ окна —
> точный ванильный путь (tickBlock достижим только из дрен-лямбды — семантика
> идентична, просто не батчится). Mid-tick ВЫЗОВ байт-идентичен по построению
> (тот же invokeinterface на том же поле), в банке не исполняется — счётчик
> держится под кратными 8, арифметика ветки сверяется точно. PARITY BANK на
> РЕАЛЬНЫХ production entry (LevelTicks/LevelChunk/ServerChunkCache/
> PalettedContainer — все allocateInstance+preseed реальных структур; counting
> ConcurrentLong2ReferenceChainedHashTable-стаб; CraftBlockState.getHandle
> реальным диспатчем): **PASS — S1 дрен порядок/книгучёт, S2 quirk, S3 10/10
> чтений (палитра/AIR-шорткат/VOID_AIR/отрицательные координаты), S4 30/30
> счётчик+ветка, S5 capture-ветка, S6 идентичность стрима чтений + ВЫИГРЫШ
> RESOLUTIONS REF=6 → NEW=2 (per-pos → per-section), S9 fuzz 40/40**.
> Рантайм-швы устранены в банке: chunkSource на ServerLevel (НЕ Level!),
> section-резолюция через ChunkAccess.levelHeightAccessor (НЕ minSection),
> -20>>4=-2 floor-деление. Byte hooks СЛЕДУЮЩИЙ тик: patch_run_collected_ticks
> (6 байтов: aload_0/aload_1/invokestatic/return) + patch_tick_block (11
> байтов: aload_0×3/invokestatic/return) — оба без ветвлений => пустой
> StackMapTable. ПАКЕТ: F1 ✓ + F2 ✓ + F3-reads built (hooks next) — затем
> F3-queue (≤0.5%) и ОДИН агрегатный A/B (§125). Пара #2: нога#10 gate-reject
> (8869954, ~30s), нога#11 gate-reject (6852134 у нижней кромки, ~30s) — 7-й
> подряд честный reject; нога#12 35193865177 dispatched in flight.
> INJECTS-ONLY цел (0 sandbox boots).

> **СТАТУС 20 TPS (S7-116)**: F3 BYTE HOOKS ГОТОВЫ — `classfile.rs::patch_run_collected_ticks` (6 байтов:
> aload_0/aload_1/invokestatic TickBlockOps.runCollectedTicks/return, max_stack 2/max_locals 2) +
> `patch_tick_block` (7 байтов: aload_0×3/invokestatic/return, max_stack 3/max_locals 3 — GOAL-заметка
> S7-115 «11 байтов» была арифметической опечаткой, запечатано debug_assert + тестом). Оба тела
> без ветвлений => ПУСТОЙ StackMapTable; append-only CP + дедуп => идемпотентность; fail-closed.
> **F1+F3 COHABITATION (новый шов закрыт)**: оба хука на ServerLevel; JVMTI retransform подаёт
> ORIGINAL bytes, F1 one-shot guard молчит на повторных dispatch => tickBlock-only образ УНИЧТОЖИЛ
> бы optimiseRandomTick swap; решение — F3 ServerLevel-колбэк КОМПОЗИЦИОННЫЙ (ре-apply idempotent F1
> перед tickBlock), итоговый образ order-independent + cycle-stable (тест
> f3_serverlevel_composes_with_f1: оба тела живы, compose deterministic и idempotent на composed
> input — обе byte-модели retransform покрыты). Runtime `src/tickhook.rs`: poll BOTH {ServerLevel,
> LevelTicks} + force-load => define TickBlockOps ОДИН класс (без nested) => READY => retransform ×2
> => маркеры F3 ARMED ×2. Тесты: cargo **89 passed** (85+4; REAL LevelTicks fixture 18923B sha
> ba7dce5e…). ВЕРИФИКАТОР-ГЕЙТ: VerifyF3 на реальном HotSpot — child-first {patched LevelTicks
> 18848B + COMPOSED ServerLevel F1F3 142243B + TickBlockOps 5325B}, kernel parent-first,
> link-time без <clinit> => **VERIFY-OK major=65**. CI 46aa80b SUCCESS (маркер-grep job-логов —
> token 401, отложено; bench runner-лог ноги#13 несёт маркеры бесплатно). ПАКЕТ: F1 hook ✓ + F2
> hook ✓ + F3-reads hooks ✓ — следующий блок билда: F3-queue (≤0.5%, task167) или сразу ОДИН
> агрегатный A/B против банка пары 76.01/76.98 (Tier B floor 3.3% достигается без queue — решение
> по §125 протоколу на следующем тике). Пара #2: нога#12 gate-reject (8875106 >> band, ~30s) —
> 8-й подряд честный reject; нога#13 35196354695 dispatched in flight. INJECTS-ONLY цел
> (0 sandbox boots).

> **СТАТУС 20 TPS (S7-117)**: F3-QUEUE BUILT+HOOKED — TIER B ПАКЕТ СОБРАН ПОЛНОСТЬЮ.
> `TickBlockOps.collectTicks` = байт-точное зеркало collectTicks (sort+counter+
> drain+reschedule, run21 cfdump) с инлайном gate-машинерии (canScheduleMoreTicks
> 3 virtual calls/тик → field compare; scheduleForThisTick → прямой add; 4
> Unsafe-выборки полей на пайплайн). Паритет — САМЫЙ СИЛЬНЫЙ REF эры: РЕАЛЬНЫЙ
> vanilla приватный collectTicks через reflection (нулевой mirror-copy риск):
> **PASS** SQ1 sort-ветки / SQ2 gate boundary / SQ3 frozen-innerHead — наблюдаемый
> порядок [A5,B1,A7] подтвердил INTRA_TICK_DRAIN_ORDER cross-container fairness
> quirk (comparator = priority→subTickOrder, innerHead заморожен до цикла) /
> SQ4 re-keying lifecycle / SQ5 fuzz 30/30. Ловушки банка: LevelChunkTicks.schedule
> дедуп по (pos,type); updateContainerScheduling ПЕРЕКЛЮЧАЕТ ключ на позицию тика
> (контейнер осиротеет, если tick-chunk ≠ container key — реальная vanilla
> семантика). Byte hook: patch_collect_ticks — 9B (wide aload 4, max_stack 5/
> locals 5, пустой StackMapTable); tickhook LevelTicks callback композитный
> (drain 6B + queue 9B в одном образе). cargo **91 passed**; VerifyF3 **VERIFY-OK
> major=65** (composed LevelTicks 18837B + ServerLevel F1F3 142243B + TickBlockOps
> 8389B). ПАКЕТ (§125): F1 ✓ + F2 ✓ + F3-reads ✓ + F3-queue ✓ — signal lens
> dropped по §5.3 (floor 3.3% держится). **СЛЕДУЮЩИЙ ТИК = ОДИН АГРЕГАТНЫЙ A/B**
> против банка пары 76.01/76.98 (min-of-2, gate ≥3.0% MSPT) — решает ВСЁ: при
> <3% НИЧЕГО не landится, модульная повестка пуста (owner-gated пути: pinned
> runner, сценарий). Пара #2: нога#13 gate-reject (6786413 < floor, ~30s) — 9-й
> подряд честный reject; нога#14 35198344256 dispatched in flight. INJECTS-ONLY
> цел (0 sandbox boots).

> **СТАТУС 20 TPS (S7-118)**: РЕКОН БАГ#3 ИСПРАВЛЕН — охота была невыигрываемой
> по конструкции. v2-окно hunt_leg_b было заякорено на run22 (cpu 6401514, SLOW
> класс, MSPT 83.74), тогда как baseline-нога §125 = банк MID-класс пары
> run#17×run#21 (9080657/8914646, 76.98/76.01) — кросс-класс сравнение нелегально
> по S7-96d (class-bimodality ~10%); хуже: gate band [6870000,7030000] НЕ
> ПЕРЕСЕКАЕТСЯ с окном [6273484,6529544] — band-проходные ноги (~6.9M, leg#9
> +6.7%) всегда мимо окна, оконные ноги gate-убиты (leg#13 6786413 < floor).
> 11 подряд честных reject объяснены. v3: WINDOW = [8899044, 9092939] — точное
> пересечение ±2% терпимости вокруг ОБЕИХ banked ног (легальная пара с обоими
> baseline-arms), BAND = [8850000,9120000] (+skew slack), WORLD_SHA pin
> afb3a0b3… (7/7 ног консистентны; drift → discard + re-baseline flag).
> **F1 ARMATION НА BENCH ДОКАЗАНА** (профиль-грейп leg#9 35187305900, kernel
> e326ab3-эры): BOTTLENECKS CPU + WALL таблицы — `RandomTickOps.run` leaf
> **3.0% (4175 samples) / 3.2% (5799)** — swapped body ИСПОЛНЯЕТСЯ на bench
> runner; random-tick phase 4.4-4.8%. Кавет: helper self-leaf 3.0-3.3% vs
> vanilla optimiseRandomTick 1.96-2.56% — подозрение на attribution skew
> (inlining-коллапс callees за invokestatic границей); НЕ вердикт — решает
> только агрегатный A/B. Пара-охота: нога#14 gate-reject (11482771, ~30s) —
> 10-й честный; нога#15 gate-reject (9764130, ~30s) — 11-й честный; нога#16 =
> 35202368357 dispatched на 2c5c40e (FULL PACK kernel F1+F2+F3) с v3-машинерией.
> Вердиктный порог §125: pack median ≤ **74.20ms** (= 76.495 × 0.97) при 2
> in-window ногах. INJECTS-ONLY цел (0 sandbox boots; marker-grep = download
> лога, не бут).

> **СТАТУС 20 TPS (S7-119)**: §125 ПОПРАВКА-1 — SLOW-ТРЕК БАЗЫ; охота v3 снова
> невыигрываемая на практике, но по НОВОЙ причине: сдвиг популяции раннеров.
> Перепись 43 ног с cpu_idx: mid-окно [8899044,9092939] hit только самими банками
> (run#17/run#21, эра 22:10Z/01:13Z); с тех пор **0/14 in-window** (две mid-ноги
> 8869954/8875106 промахнулись на ~0.3%); медленный класс 6.57-6.87M доминирует
> (~30% выдач). Банк MID-класса невозобновляем → reuse банка против свежих ног
> мёртв. ПОПРАВКА (все owner-гейты целы: ≥3% MSPT, min-of-2, ±2% паринг, world
> pin): baseline arm#1 = **run#18 35159240368** (cpu 6746569, MSPT 85.24,
> FIXTURE-VALID S7-102, PRE-PACK kernel f3c82b3-эры — до S7-111/112, world
> afb3a0b3 подтверждён) — бесплатный банк; arm#2 (B1) = свежий диспатч на
> **pre-pack тег `pre-pack-962fc9f`** (S7-111: RandomTickOps.java в банке, но
> НЕ hooked ⇒ нулевое pack-поведение); PACK WINDOW = пересечение ±2% вокруг
> обеих arms (пусто ⇒ честный discard B1, редиспатч); pack-ноги на master в
> окне; ВЕРДИКТ: pack median ≤ median(85.24, B1_mspt)×0.97 при 2 in-window
> ногах ⇒ pack lands, иначе REFUTED row, ноль лендинга. Уроки S7-119 (пойманы
> живьём): workflow-dispatch API НЕ принимает SHA-ref (HTTP 422) ⇒ аудит-тег
> запушен; v4 добавила dispatch-verify (head_branch+status) — слепой захват
> свежайшего run id чуть не записал pack-ногу#18 как baseline state. ОХОТА:
> нога#16 gate-reject (6604889) — 12-й честный; нога#17 (7094750) — 13-й;
> нога#18 (6593031) — 14-й; B1 попытки#1-5 gate-reject (10165007, 8820009,
> 8744984, 7137698, 8780971 — пул ротируется: 8.7-8.8M всплыли, 6.6-6.9M были
> 40 мин назад); **B1 попытка#6 = 35205343087 IN FLIGHT** на pre-pack теге
> (gate пройден, full bench). v4 = scripts/bench4_recon/hunt_leg_b_v4.py
> (one-transition/call, resumable, world pin, early-cancel). INJECTS-ONLY цел
> (0 sandbox boots).

> **СТАТУС 20 TPS (S7-120)**: §125-A1 охота продолжается — B1 35205343087
> (pre-pack тег) в полёте, boot >20 мин (echo ещё нет; полный bench 25-40 мин).
> Инженерия тика: **verdict_a1.py** — калькулятор вердикта §125-A1 (экстрактор
> `MSPT: avg **X**ms` из zip-логов = ТА ЖЕ headline-метрика, что у банка
> 85.24/76.98/76.01; проверка window/world/VALID на каждой ноге; threshold =
> median(arms)×0.97, pack median из 2 in-window ног ⇒ LANDS/REFUTED + запись
> verdict_a1.json) — вердиктный тик становится механическим. **НАБЛЮДЕНИЕ
> (не вердикт, 1 пара)**: leg#9 (F1-only эра e326ab3) 81.64ms cpu 6966037
> ↔ run#23 (pre-pack) 83.96ms cpu 6979464 — дистанция cpu 0.19%, тот же мир
> afb3a0b3, оба VALID, fp=4 (первоисточники перегреплены) ⇒ **−2.76% для F1
> ОДНОГО**. Направление совпадает с preregistered F1-оценкой 1.6-1.9% (solo,
> refuted против 3% гейта) и поддерживает Tier-B floor 3.3% пакета (F2+F3
> добавят); НО: одиночная пара, slow-класс спред n=4 = 3.5% ⇒ шумовые бары
> широкие — решает только агрегатный A/B §125-A1. INJECTS-ONLY цел (0 boots).

> **СТАТУС 20 TPS (S7-121)**: **РЕКОН БАГ#4** — дрейф gate-vs-final harness
> cpu. Band-gate семплирует cpu на ~30s, а закон паринга требует ФИНАЛЬНОЕ
> значение (run-env echo). Замер по парам ИЗ ОДНОГО лога: run#24 **−5.5%**,
> leg#8 −2.0%, leg#9 +0.3%, run#23 +1.5%, run#21(банк!) **+1.9%**, B1#6 +3.4%
> ⇒ дрейф неконстантный до ±5.5%. Дымящий пистолет: gate-значение run#21
> 8745625 БЫЛО БЫ убито v3-band [8850000,9120000], тогда как его финал
> 8914646 — ВНУТРИ v3-окна ⇒ гейт отклонял легально-парируемые финалы; и
> наоборот — B1#6 прошёл узкий band [6611637,6881500], а финал 7057150
> (+2.5% над окном) сгорел в честный discard. ФИКС v4.1: drift-компенсация
> band = [win_lo/(1+0.034), win_hi/(1−0.055)] — гейт пропускает все gate-
> значения, чьи финалы МОГУТ попасть в окно; финальная проверка остаётся
> точным окном (честные discard остальных, ~3 boots/ногу). **B1#6
> ПЕРЕРАБОТАН**: 35205343087 (pre-pack тег, SUCCESS, VALID, мир afb3a0b3,
> MSPT 83.40, финал 7057150) = baseline arm#1 §125-A1 — окно заякорено на
> ЖИВОЙ класс сегодняшнего пула [6916007,7198293] (run#18 arm mothballed:
> 6.75M класс остыл). **B2 = 35209341660 dispatched** (pre-pack тег,
> drift-компенсированный band [6688594,7617241]) — poll следующего тика:
> финал в окне ⇒ BASELINE COMPLETE ⇒ pack-фаза (master, тот же band-метод,
> точное окно) ⇒ 2 in-window pack-ноги ⇒ **ВЕРДИКТ**: pack median ≤
> median(83.40, B2_mspt)×0.97. Все owner-гейты целы. INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-122)**: **BASELINE COMPLETE + РЕКОН БАГ#5 + pack arm#1
> в полёте.** Среда песочницы ПОЛНОСТЬЮ стёрта между тиками (3 repo,
> scripts/, creds — всё отсутствовало) — восстановлено из /tmp-снапшота
> (bootstrap_tick.sh с запечённым токеном спас: creds + 3 клона за один
> прогон; c-crussty f97c84d, dev-logs TASK-257, CRUSSTY 1f4c06a нетронут).
> (1) **B2 = 35209341660 IN-WINDOW SUCCESS**: pre-pack тег, FIXTURE-VALIDITY
> VALID, мир afb3a0b3, MSPT avg **83.51**, финал cpu **6996405** ∈ окно
> arm#1 [6916007,7198293] ⇒ §125-A1 BASELINE COMPLETE: arm#1 = B1#6
> (35205343087, 83.40 @ 7057150), arm#2 = B2 (83.51 @ 6996405) ⇒ pack
> window = [max×0.98, min×1.02] = **[6916007, 7136333]**; ВЕРДИКТНЫЙ ПОРОГ
> = median(83.40, 83.51)×0.97 = **80.95ms** — pack median 2 in-window ног
> ≤ 80.95 ⇒ LANDS, иначе REFUTED. (2) **БАГ#5 (state machine)**: v4.1
> ветка pack-диспатча требовала pack_legs truthy ([]) ⇒ pack arm#1 упал
> в fresh-baseline ветку и переdispатчил PRE-PACK ногу 35213099261 на
> неверный ref, clobbered pack state — поймано инспекцией state, cancel
> pre-bench (~3 мин, ноль bench-стоимости), state восстановлен из
> leg_b_v4.json; фикс **v4.2**: pack-диспатч = (phase=pack AND run_id is
> None) — покрывает arm#1 и arm#2. (3) **PACK ARM#1 = 35213299343
> dispatched на master** (f97c84d, FULL F1+F2+F3 pack kernel, drift band
> [6688594,7551675], финал = точное окно [6916007,7136333]) — poll
> следующего тика: in-window ⇒ leg#1 banked ⇒ автодиспатч arm#2 ⇒ 2 ноги
> ⇒ verdict_a1 ⇒ **§125-A1 ВЕРДИКТ**. (4) verdict_a1.py extracts fix:
> CI-скрипт-литерал `echo "::error::... INVALID"` в КАЖДОМ логе ронял
> valid-флаг (False на здоровых прогонах) — фикс: позитивный маркер
> `- **FIXTURE-VALIDITY: VALID**` достаточен (grep-q gate семантика).
> Пайплайн --check зелёный: обе arms mspt/window/world/valid ✓. F2/F3
> маркеры — на первой завершённой pack-ноге. INJECTS-ONLY цел (0 sandbox
> boots; cancel не boot).

> **СТАТУС 20 TPS (S7-123)**: **pack arm#1 попытка#1 = honest DISCARD; bug#6+
> bug#7 пойманы и закрыты (v4.3); попытка#3 = 35216066889 в полёте.** (1)
> 35213299343 (master f97c84d, FULL pack kernel): SUCCESS, VALID, мир
> afb3a0b3, MSPT 94.14 — НО финал cpu **6832640** НИЖЕ pack window
> [6916007,7136333] (и ниже arm2-lo 6856477 ⇒ не парируется НИ с одной
> armой) ⇒ честный discard; MSPT 94.14 не пригоден для вердикта (другой
> класс раннера — подтверждение закона парирования). (2) **БАГ#6**: логи
> CI-рана НЕДОСТУПНЫ короткое окно сразу после завершения (zip не
> финализирован) — log_text возвращал "", run падал в reject-ветку и
> state СТИРАЛСЯ (пойман live на этой ноге; исход совпал с discard'ом, но
> классификация была неверной). Фикс v4.3: пустые логи при concl=success ⇒
> retry (exit 4), state цел. (3) **БАГ#7 (семья bug#5)**: все pack-фазные
> discard/cancel/reject пути звали clear_state() — терялись phase/window/
> baseline, следующий dispatch падал в fresh-baseline ветку (не тот kernel
> на не том ref). Фикс v4.3: restore_pack_or_clear() — pack-фаза
> ВОССТАНАВЛИВАЕТСЯ (run_id=None), baseline чистится как раньше. (4)
> Попытка#2: gate cpu **10054256** (10M быстрый класс) далеко вне band
> [6688594,7551675] ⇒ ~30s fast-fail reject — dispatch-verify корректно
> отказался сохранять state завершённого рана (нота для v4.4: отличать
> свой fast-fail от чужого рана в окне 25s). (5) **Попытка#3 = 35216066889
> IN FLIGHT** (master fb7d8cb, POST 204, band [6688594,7551675], финал =
> точное окно [6916007,7136333]) — poll следующего тика: in-window ⇒
> leg#1 banked ⇒ v4.3 автодиспатчит arm#2 ⇒ 2 ноги ⇒ verdict_a1 ⇒
> **§125-A1 ВЕРДИКТ** (порог median(83.40,83.51)×0.97 = **80.95ms**
> неизменен). INJECTS-ONLY цел (0 sandbox boots).

> **СТАТУС 20 TPS (S7-124)**: **pack arm#1 attempt#3 = honest DISCARD
> (cpu 6691832 ниже окна); v4.4 dispatch-verify own-fast-fail fix
> (боевое применение); attempt#5 = 35218943354 in flight.** (1) Poll
> 35216066889 (master fb7d8cb, FULL pack kernel): SUCCESS, мир afb3a0b3,
> финал cpu **6691832** — НИЖЕ pack window [6916007,7136333] на 3.2% ⇒
> честный discard (второй: попытка#1 6832640, попытка#3 6691832). Пул
> раннеров дрейфует: наблюдаемые gate значения 6.69M..10.05M при окне
> 3.1% — терпеливая лотерея с drift band [6688594,7551675] + точный
> финальный чек (санкционированный протокол). v4.3
> restore_pack_or_clear сработал: state восстановлен (pack, run_id=None,
> окно цело). (2) **ФИКС v4.4 (диспатч-верифай слепое пятно, нота
> S7-123)**: dispatch() спит 25s перед верификацией — свой gate
> fast-fail (~13-30s) завершается ВНУТРИ окна и читался как false
> MISMATCH без сохранения state (попытка#2 повисла руками). Фикс: top
> run с ref=наш, status=completed, conclusion=failure/cancelled,
> created_at >= t0-5 ⇒ ЭТО НАШ fast-fail — state сохраняется, следующий
> poll классифицирует reject штатно. **Боевое применение в этом же
> тике**: attempt#4 35218775640 — gate cpu 8094573 (8.09M mid класс)
> выше band hi ⇒ ~13s fast-fail, v4.4 сохранил state, reject
> классифицирован штатно (exit 1), автопередиспатч. (3) **ATTEMPT#5 =
> 35218943354 IN FLIGHT** (master 7bf98fc, verify увидел in-flight на
> +25s = past gate, boot/download), band [6688594,7551675], финал =
> точное окно [6916007,7136333] — poll следующего тика: in-window ⇒
> leg#1 banked ⇒ v4.3 автодиспатчит arm#2 ⇒ 2 ноги ⇒ verdict_a1 ⇒
> **§125-A1 ВЕРДИКТ** (порог median(83.40,83.51)×0.97 = **80.95ms**
> неизменен). F2/F3 маркеры — на первой in-window ноге. INJECTS-ONLY
> цел (0 sandbox boots; gate fast-fail не boot).

> **СТАТУС 20 TPS (S7-125)**: **PACK LEG#1 BANKED (первая in-window нога) +
> F2/F3 SMOKE-ТЕСТ ЗАКРЫТ — ВСЕ 4 ХУКА ARMED на bench; arm#2 = 35221359818
> в полёте.** (1) Poll 35218943354 (master, FULL pack kernel): SUCCESS,
> мир afb3a0b3, финал cpu **6998277** ∈ [6916007,7136333] ⇒ **PACK LEG#1
> BANKED** (третья попытка — после discard'ов 6832640/6691832).
> Headline MSPT **85.44ms** (spark tick-monitor). (2) **F2/F3 SMOKE
> TEST ЗАКРЫТ** (дефер S7-116→S7-118): workflow-логи НЕ содержат
> console-маркеров (они в artifact) — скачан world3-bench artifact
> (49.5MB; 302→Azure требует no-redirect opener — новый
> scripts/bench4_recon/fetch_artifact.py) → server-stdout.log: `randomtick:
> F1 ARMED (optimiseRandomTick -> RandomTickOps.run, bit-exact LCG batch)`,
> `brainhook: F2 ARMED (startEachNonRunningBehavior -> BrainOps.
> startEachNonRunning, flat-snapshot lens)`, `tickhook: F3 ARMED ×2
> (runCollectedTicks drain mirror + tickBlock readBlockState lens, F1
> composed)` — **FULL PACK KERNEL АКТИВЕН на bench-ноге**; profile-кадры
> согласны (RandomTickOps.run 3.5-3.6%, Brain.tick 6.75%). (3) Пара-нота:
> leg#1 cpu 6998277 ≈ arm#2 6996405 (Δ 0.27%) — легальная A/B пара;
> leg#1 MSPT 85.44 vs B2 83.51 = pack +2.3% на этой паре (single-pair
> наблюдение, НЕ вердикт; F1-одиночка давала −2.76% на leg#9 — lens-хуки
> F2/F3 могут съедать выигрыш F1; вердикт механический). (4) **ARM#2 =
> 35221359818 auto-dispatched** (master, band [6688594,7551675], финал =
> точное окно) — in-window ⇒ 2 ноги ⇒ verdict_a1 ⇒ **§125-A1 ВЕРДИКТ**:
> pack median ≤ **80.95ms** ⇒ LANDS, иначе REFUTED (ноль посадок). Порог
> неизменен. INJECTS-ONLY цел (0 sandbox boots).

> **СТАТУС 20 TPS (S7-126)**: **pack arm#2 attempt#1 = honest DISCARD #3
> (cpu 6731204 ниже окна 2.7%); затем серия gate fast-fail — пул
> бимодальный 6.37M/9.86M, скачет ЧЕРЕЗ band [6688594,7551675];
> последний dispatch 35224751782 past-gate in flight.** (1) Poll
> 35221359818: SUCCESS, мир afb3a0b3, финал cpu **6731204** ниже окна
> [6916007,7136333] на 2.7% ⇒ честный discard #3 (6832640 / 6691832 /
> 6731204 — все ниже окна; pool дрейфует вниз с утра). v4.3 восстановил
> pack state (leg#1 6998277 сохранён). (2) **Серия fast-fail** (v4.4
> ноль-ручных обработок, каждый ~13-30s): gate значения **6372300 /
> 6372622 / 9864892 / 8261589 / 6645024** — бимодальная карусель вокруг
> band, 6 подряд честных reject (всего 21). Окно НЕ двигаем
> (preregistered §125-A1: окно = пересечение ±2% вокруг двух banked
> arms; перенос окна = перенос базы = owner-amendment). (3) **Последний
> dispatch 35224751782 past-gate in flight** (master, band
> [6688594,7551675], финал = точное окно) — poll следующего тика:
> in-window ⇒ leg#2 ⇒ verdict_a1 ⇒ **§125-A1 ВЕРДИКТ** (порог
> median(83.40,83.51)×0.97 = **80.95ms**; leg#1 85.44 уже в bank — при
> median(85.44, leg2) ≤ 80.95 ⇒ LANDS, иначе REFUTED). F2/F3 маркеры
> закрыты (S7-125, 4/4 ARMED). INJECTS-ONLY цел (0 sandbox boots).

> **СТАТУС 20 TPS (S7-127) — ВЕРДИКТ §125-A1: LANDS + OWNER REDIRECT
> (архитектурная эра).** (1) **§125-A1 ЗАКРЫТ механически**: leg#2 =
> 35224751782 (poll догнал в этом тике; commit-context отрезал прошлый
> poll, но hunt отработал) — финал cpu **6929321** ∈ окно, MSPT
> **73.92ms**, мир afb3a0b3, VALID ⇒ pack ноги = [85.44 @ 6998277,
> 73.92 @ 6929321], pack_median = **79.68ms** ≤ порог 80.95ms ⇒
> **LANDS, Δ = −4.52%** (гейт ≥3% пройден, min-of-2, парный закон,
> INJECTS-ONLY). Пак (F1+F2+F3-reads+F3-queue) ОСТАЁТСЯ на master — он
> уже там; pre-pack тег остаётся базовой ссылкой. verдиктный JSON:
> scripts/bench4_recon/verdict_a1.json. (2) **OWNER REDIRECT
> (2026-09-17 ~21:10-21:15 +08, прямой эфир)**: «надо x100... чтобы все
> форс чанки были и всё жило, а тпс и мспт — 20 и минимальный; ты все
> боттленки ебнуть должен — что за микро фиксы; тут архитектуры надо
> делать и реальные бусты» + поправка: «x100 это мало, твой прайм было
> **x150000**» + «по русски». ЧИТАЮ ТАК: сцена = ПРАЙМ реального
> MineShield-3 — **все форс-чанки (9216) + всё ЖИВОЕ на прайм-масштабе
> (рабочая гипотеза: ~150k живых сущностей — ЗАПРОС ПОДТВЕРЖДЕНИЯ
> ВЛАДЕЛЬЦУ: юнит x150000 = сущности?)**, спавн/деспавн как с
> игроками; таргет остаётся 20 TPS / минимальный MSPT; **микро-рычаги
> (2-3% линзы) — стоп**; арсенал = АРХИТЕКТУРА + реальные бусты, бить
> ВСЕ бутылочные горлышки. (3) Следствие: сцена-апгрейд ДЕЛАЕТ
> недействительной базу §125 (все MSPT сняты на fp=4 полуживой сцене)
> ⇒ охота в окно ЗАМОРОЖЕНА (свежий pre-pack baseline-ран 35228087782
> отменён 202, hunt state очищен; банк остаётся исторической записью).
> (4) **НОВАЯ ЭРА — BENCH-X150K (спека: docs/BENCH_X150K_SCENARIO.md)**:
> population-injection fixture (bench-only, сервер-код 100% ваниль —
> прецедент BenchFake), цель ~150k живых сущностей на буте, все чанки
> форс-лоад, фейк-игроки гоняют спавн-линии; потом свежий профиль ⇒
> ARCH-ATTACK очередь (архитектурные Rust/JNI порты горячего пути,
> планировщики, батчи) — preregistered гейты остаются для ЧЕСТНОГО
> замера бустов (median-exact, A/B min-of-2), но планка = «реальный
> буст», не 2%.

> **СТАТУС 20 TPS (S7-128) — КРОН ИЗМЕНЁН (владелец) + ЮНИТ x150000 УТОЧНЁН
> ВЛАДЕЛЬЦЕМ + ARCH-ATTACK #1: guard-wave wave-1 ARMED на bench.** (1)
> **Живое уточнение владельца (~21:2x +08)**: «ну так ты просто смотри на
> боттлнеки и сделай чтобы они не нагружали, и цель это 20 тпс и минимальное
> количество мспт, но всё работает так же как в ваниле (измени крон)» +
> «x150000 это к скорости выполнения какой то функции из боттлнеков которые
> топ 1 например по изпользованию» + «всем похуй на набор и вердикт уже
> твой». ЧИТАЮ: (а) юнит x150000 = планка ускорения СКОРОСТИ ВЫПОЛНЕНИЯ
> топ-1 функции-горлышка (по использованию) — гипотеза «150k сущностей»
> DRAFT v1 REFUTED владельцем; (б) вердикт и выбор рычага делегированы
> агенту (лотерея окон мертва навсегда); (в) поведение = ванильное.
> (2) **КРОН ИЗМЕНЁН**: Job 390768 (чартер застрял в эре S7-96 — 30+ тиков
> устаревания) УДАЛЁН; создан **Job 393012 c-crussty-module-loop-v4-arch-
> attack** (тот же график :08/:43 каждые 35 мин, Asia/Shanghai, priority
> 10): чартер архитектурной эры БЕЗ вшитого состояния (истина всегда в
> GOAL+worklog), по-русски. (3) **АРХИТЕКТУРНЫЙ ВЕРДИКТ (мой)**: топ-1
> функция по использованию = PalettedContainer.get (3.62% self, стабильна
> min-of-2 3.42/3.62/3.7) — НО анализ честных механизмов её ускорения:
> per-call плоский декод-кэш секций = +32KB×24×9216 ≈ 7GB (memory-
> неосуществимо) и пер-call win 30-50% = микро-фикс класс (запрещён
> владельцем) ⇒ архитектурная атака переносится на УСТРАНЕНИЕ ИЗБЫТОЧНОЙ
> ПЕРЕКОМПУТАЦИИ в entity-физике: **guard-wave wave-1 = same-state
> fluid-push guard (TASK-80, готов + live-verified hit-rate 96.4%)** —
> функция на гард-пути исчезает из профиля (класс >100x на хите, планка
> «горлышко не нагружает»); под сцену «всё живое» entity-лейн масштабируется
> линейно с мобами — удар туда даёт реальный буст. (4) **ВНЕДРЕНО**: bench/
> world3/run_world3.sh — FLUID_GUARD default 1 (export CRUSSTY_FLUID_PUSH_
> GUARD, самодокументация в run-env.txt «fluid_guard: …», A/B оверрайд
> FLUID_GUARD=0); workflow world-bench input fluid_guard (default '1') —
> гард стал частью измеряемого пака bench (банк §125 — историческая запись
> старой сцены, перебаза на X150K запланирована). (5) **Верификационный ран
> диспатчен**: см. CLAIMS TASK-264 / worklog S7-128 — absorb следующего
> тика: console-маркер fluid_guard armed + профиль (updateFluidHeightAndDo
> FluidPushing self → коллапс) + run-env fluid_guard: 1. (6) NEXT: S7-129 =
> guard-wave wave-2 STEP-0 (checkInsideBlocks 3.4% census / InsideBlock
> EffectApplier.flushStep 786 сэмплов run17) + X150K population fixture MVP;
> следом — повторный профиль X150K ⇒ очередь ARCH-ATTACK по свежим данным.
> INJECTS-ONLY цел (CI-буты санкционированы).

> **СТАТУС 20 TPS (S7-129) — X150K POPULATION FIXTURE MVP РЕАЛИЗОВАН (плагин +
> раннер + workflow + локальная javac21-проверка контракта), сцену «всё живое»
> можно теперь ИЗМЕРЯТЬ.** (1) Абсорбция прерванного sibling-раунда S7-128:
> TASK-264 докоммичен verbatim; hunt-state leg_b_v4_state.json нейтрализован
> (FROZEN-BY-OWNER-REDIRECT-X150K — возобновлять ЗАПРЕЩЕНО); runs_index
> дополнен. (2) **BenchPopulationPlugin** (bench/world3/population/, BENCH-ONLY,
> ванильный addEntity-путь — прецедент task170): инъекция живой сцены
> 70% items / 20% hostiles / 10% passives; детерминизм Random(seed) +
> сортировка чанков (x,z) + бюджет 1500/тик; items = фермы (pickup-delay max,
> кластеры 5% чанков), hostiles/passives setPersistent (ферм-сток), НАТУРАЛЬНЫЙ
> спавн/деспавн остаётся ванильным; TOPUP каждые 600 тиков восполняет
> ванильно-деспавннутые items ⇒ item-лейны (тик/мердж/деспавн) горячие всё окно.
> (3) **Харнес**: benchpop inject ПОСЛЕ forceload, ожидание «POPULATION INJECT
> DONE» ДО старта профилировщиков (спека §2 «инъекция до старта окна замера»);
> run-env self-doc population_target/seed; workflow inputs + гейт FIXTURE-VALIDITY
> ≥90%. (4) **Контракт проверен ДО CI**: javac --release 21 против
> paper-api 1.21.10-R0.1-SNAPSHOT + patched-kernel.jar = COMPILE OK (риск
> roundtrip-провала снят). (5) Профиль leg#2 разобран функционально
> (profile_rank.py): entity-лейн 43.8% inclusive (aiStep 18.3%, Mob.tick 22.5%,
> Brain.tick 6.14%), block/redstone 8.9%, leaf top-1 PalettedContainer.get
> 3.37% (вердикт S7-128 неизменен) — на X150K сцена сместит топы, решит свежий
> профиль. (6) NEXT: S7-129b CI smoke 10k (после завершения вериф-рана
> 35231195756 — concurrency cancel-in-progress, не мешать) ⇒ S7-130 масштаб
> 150k + soak ⇒ S7-131 X150K база A/B + свежий профиль ⇒ топ-1 ARCH-ATTACK
> (планка x150000 = скорость выполнения топ-1 функции). INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-129b) — X150K SMOKE 10k = SUCCESS (run 35234643616,
> оба гейта зелёные) — ЖИВАЯ СЦЕНА ИЗМЕРЕНА ПЕРВОЙ В ИСТОРИИ ПРОЕКТА.**
> (1) Fixture: INJECT DONE **10000/10000** (7000 items / 2000 hostiles /
> 1000 passives) за **2572 мс**, FIXTURE-VALIDITY: VALID, farmClusters=500;
> TOPUP активен (deficit=0 steady). (2) Сцена живая: ~**24.9k сущностей**
> (натуральные спавны у 4 фейк-игроков АКТИВНЫ: skeleton 584 / drowned 468 /
> chicken 465 / sheep 442 / creeper 403 / zombie 313 / cow 311) на 9216
> форс-чанках. (3) Нагрузка: MSPT avg **125.63ms** (TPS ~7.6; первый poll
> 20.0 — до инъекции), GC 0 Full GC / high-water 3951MB — прайм-масштаб
> ДАЛЕКО от 20 TPS ⇒ ARCH-ATTACK имеет реальную работу. (4) Профиль leaf:
> **топ-1 PalettedContainer.get 4.20%** (та же цель, что вердикт S7-128),
> RandomTickOps 2.27%, guard-хуки bump 1.03% + slow 0.92% (оверхед
> инструментации виден; net-эффект guard'а — мерить A/B на X150K-базе),
> InsideBlockEffectApplier.flushStep 1.16% (кандидат wave-2 подтверждён).
> (5) Известный баг (фикс S7-130): стартовая инъекция не пишется в
> itemSpawnLog ⇒ первый topup удвоил items (~14k, self-correcting);
> topup-seed привязать к Δt = ft−T0 для строгого replay. (6) NEXT S7-130:
> фикс topup-логирования → масштаб 150k (оценка: инъекция ~39s при 1500/тик,
> heap ~+3-4GB ⇒ возможно Xmx tuning в харнесе) → soak → S7-131 база A/B
> pre-pack vs master на X150K + свежий профиль ⇒ топ-1 ARCH-ATTACK
> (планка владельца x150000 = скорость выполнения топ-1 функции).
> INJECTS-ONLY цел (0 sandbox boots; CI-буты санкционированы).

> **СТАТУС 20 TPS (S7-130) — X150K МАСШТАБ 150k: topup-фикс внедрён + SERVER_XMX проводка; prime-ран 35238931413 ДИСПАТЧЕН (master 7956a2e), absorb S7-131.**
> (1) **Topup-фикс (баг S7-129b закрыт)**: стартовая инъекция теперь логируется в itemSpawnLog ПО ТИКАМ (itemsThisSlice → запись [fullTime, count] на каждом инжекционном тике; ранее только topup-спавны логировались ⇒ первый topup видел aliveEst=0 и удваивал items до ~6600 тика); topup-seed привязан к Δt = ft − T0 (t0FullTime = fullTime в момент finishInjection — якорь replay-детерминизма: (target, seed) → тот же topup-поток независимо от дрейфа бута); INJECT DONE маркер расширен t0FullTime для верификации якоря.
> (2) **SERVER_XMX проводка** (подготовка prime-масштаба; оценка S7-129b «heap +3-4GB» против 6G-потолка при high-water 3951MB на 10k): run_world3.sh SERVER_XMX env (default 6G = историческое поведение), java line -Xmx"$SERVER_XMX", run-env self-doc «server_xmx: …»; workflow input server_xmx (default '6G') + env pass-through. Это инфраструктура харнеса (BENCH-ONLY), не серверный код и не config-win — запрет владельца не затронут.
> (3) **Локальная контракт-проверка ДО CI** (прецедент S7-129): javac --release 21 против paper-api+adventure+bungeecord classpath = COMPILE OK; bash -n + YAML parse OK; фикс не добавляет новых API.
> (4) **Prime-ран диспатчен**: run **35238931413** (master 7956a2e, fp=4, population_target=150000, seed=42, server_xmx=10G, sweeps=0, 300s, guard=1) — concurrency guard проверен перед диспатчем; финал = absorb S7-131: гейты (INJECT DONE 150000/150000, FIXTURE-VALIDITY) + MSPT/TPS при 150k + topup-корректность (первый topup на Δt≈600t должен иметь deficit=0 при живой стартовой инъекции) + свежий профиль leaf ⇒ подтверждение/смена топ-1 ARCH-ATTACK.
> (5) Риски prime-масштаба (мониторинг в absorb): инъекция 150k при 1500/тик ≈ 100 тиков ≈ 39s+ (POP_INJECT_TIMEOUT=900s с запасом); heap 10G против оценки ~7-8GB (GC-лог артефакт покажет high-water); TPS при инъекции временно упадёт — инъекция ДО окна замера (спека §2 соблюдена харнесом).
> (6) NEXT S7-131: absorb 35238931413 ⇒ если зелёный — X150K база A/B (pre-pack vs master, min-of-2 по cpu-парам) + свежий профиль 150k ⇒ топ-1 ARCH-ATTACK (планка владельца x150000 = скорость выполнения топ-1 функции; операционализация §2 спеки: функция должна исчезнуть из профиля). Если OOM/тайм-аут — Xmx/бюджет-тюнинг харнеса первым делом (инфраструктура, не логика). INJECTS-ONLY цел (0 sandbox boots; CI-буты санкционированы).

> **СТАТУС 20 TPS (S7-130b) — ПЕРВЫЙ УСПЕШНЫЙ PRIME-SCALE ЗАМЕР В ИСТОРИИ ПРОЕКТА: 150k живых на 9216 форс-чанках, все гейты зелёные (run 35245032701, master 6c3c20c).**
> (1) **Путь к зелёному**: диспатч #1 35238931413 (7956a2e) FAILED — мой баг: uniform-лейн инъекции one-pass по 9948 чанкам, на 150k нужно ~87k размещений ⇒ cursor исчерпан, инъекция молча замерла на ~11k (сервер ЗДОРОВ: heap 2.2/10G, сцена жила 20 мин; stop по POP_INJECT_TIMEOUT). FIX a88d204: лейн по кругу (modulo) + INJECT STALL warn (placed=0 никогда не молчит). Диспатч #2 35242595837 (a88d204): инъекция 150k DONE за 86.5s + population-гейты ПРОЙДЕНЫ, но FAILED BENCH-4 gate-1c — alive-heartbeat фейк-плагина период 1200 тиков (60s@20TPS) при TPS 0.7-1.0 = 20+ мин wall ⇒ ноль heartbeat'ов в 300s окне (fixture здоров: игроки 4/4, churn ACTIVE). FIX 6c3c20c: heartbeat 100 тиков. Диспатч #3 35245032701 (6c3c20c) = **SUCCESS, ВСЕ ГЕЙТЫ**.
> (2) **Замер**: INJECT DONE 150000/150000 (105000 items / 30000 hostiles / 15000 passives) за 100.4s, t0FullTime=225986013; живая сцена **148589/148397/148246** сущностей (натуральный спавн/деспавн ACTIVE: skeleton 4902 / zombie 4708 / creeper 4691 / husk 4671 / drowned 4595 / item 100398); alive-check 4/4 ×3 (фикс heartbeat верифицирован); GC 0 Full GC, high-water **6914MB** @ Xmx10G (запас есть; 174 паузы / 13.6s суммарно).
> (3) **Нагрузка**: TPS 0.6-0.9 ⇒ implied MSPT **~1405ms avg** — прайм-масштаб КРАЙНЕ далеко от 20 TPS; поле ARCH-ATTACK подтверждено количественно (нужно ~×150 к скорости тика для 20 TPS... «x150000» — планка топ-1 функции).
> (4) **СВЕЖИЙ ПРОФИЛЬ 150k (первый в истории, 52552 cpu-сэмплов, spark yVyl0LQNDk)**: фаза entity tick **54.2%** — доминанта; kernel-топ-1 leaf **PalettedContainer.get 3.3%** (стабилен: 4.2%@10k smoke / 3.62%@fp4 / 3.3%@150k) + readPalette 0.8% + SimpleBitStorage.get 0.8% = палитро-лейн ~4.9%; **FluidPushGuardHook.slow 2.3% + bump 0.9% = 3.2% lens-оверхед** (сам fluid-push ИЗЧЕЗ из профиля — guard wave-1 работает, платим за lens; архитектурная переделка lens = кандидат); G1 GC 13.4% + oop barriers 12.0% ≈ **25.4%** (производная аллокаций entity-лэйна); flushStep 1.0% (wave-2 подтверждён).
> (5) **ВЕРДИКТ S7-130b / ОЧЕРЕДЬ ARCH-ATTACK S7-131** (автономия агента, свежие данные): (a) палитро-лейн PalettedContainer (get+readPalette+SimpleBitStorage) — kernel-топ-1 по использованию, планка владельца x150000 = скорость топ-1 функции; (b) relens guard-хуков (устранить собственный 3.2% оверхед — «не нагружать» по директиве); (c) аллокационная диета entity-лэйна (GC 25.4% — производная); (d) flushStep wave-2. Порядок атаки решит S7-131 (STEP-0 census первым: палитро-лейн имеет O(чанки×секции×тикающие-блоки) структуру — есть ли избыточная перекопутация, как в fluid-push?).
> (6) NEXT S7-131: absorb стабилен, база A/B pre-pack vs master на X150K (min-of-2, пары по cpu) + STEP-0 палитро-лейна ⇒ первый ARCH-ATTACK рычаг. INJECTS-ONLY цел (0 sandbox boots; CI-буты санкционированы).

> **СТАТУС 20 TPS (S7-131) — ARCH-ATTACK рычаг #1 PALETTED-DEMUX РЕАЛИЗОВАН И ВЕРИФИЦИРОВАН ОФЛАЙН (parity ALL PASS), диспатч A/B leg #1 на X150K-базе.**
> (1) **STEP-0 census палитро-лейна** (collapsed stacks 150k, 52552 сэмплов): PalettedContainer.get 1746 самплов (3.32%) бьют (a) fluid-чтения ~56% (guard slow 473 + cellsUnchanged 82 + hasSameAbove/getFlow 363 + прочие), (b) коллизии ~17% (CollisionUtil через noCollision/move), (c) прямые getBlockState ~15% (checkInsideBlocks/getOnPos/pathfinding/redstone/BE). ВСЕ пути — один контейнер LevelChunkSection.states (fluid производен от BlockState) ⇒ демукс на уровне контейнера ускоряет ВСЕ три лейна разом.
> (2) **Дизайн PALETTED-DEMUX**: field-injection ×4 (crusstySnap/snapGen/gen/miss, public volatile) в PalettedContainer на ПЕРВОЙ загрузке (retransform невозможен — смена формы) + get(int) быстрый путь `vals[demux[index]]` (минуя бит-декод и палитру) с fallback в Ops.get (ванильно-эквивалентное чтение + heat-счётчик ленивой материализации 4096 ячеек) + охраняемые мутаторы getAndSet/set (prologue: release+gen++→odd; epilogue: gen++→even) — one-writer инвариант уже гарантирован moonrise region-lock (тем же, что лицензирует несинхронный getAndSetUnchecked). read(FriendlyByteBuf)=server-never — не патчится; onResize идёт внутри охраняемого odd-окна.
> (3) **Путь к верификации**: ручной SMT-фрейм отвергнут верификатором (bad offset; эмпирика: first-frame offset_delta = target) ⇒ переход на ASM COMPUTE_FRAMES build-time патчер (paletted/tools/PalettedPatchTool.java, офлайн — НЕ бут); баг ISTORE-vs-ASTORE и фантомный декремент refcount (начальный snapGen=0 неотличим от «освобождён») закрыты протоколом v2: валидность = snapGen == gen+1, uncounted = 0, release в прологе.
> (4) **Парити-харнесс (research/paletted-demux-2026-09-18/parity_harness_output.txt) = ALL PASS**: 20000 случайных операций (set/getAndSet/get/getAndSetUnchecked) в локстепе vanilla-vs-patched на реальных классах ядра, полное сравнение 4096 ячеек на каждой партии + старые значения getAndSet 4883/4883; лестница resize палитры (40 состояний: linear→hashmap) под охраняемыми мутаторами; lifecycle demux (heat-материализация → published fresh → fast-path parity → write unmark + refcount 1→0 → re-materialize → parity); конкурентный смоук 3 читателя + 1 писатель 500ms без исключений. Верификатор JVM принял пропатченный класс.
> (5) **Ожидания от A/B (X150K-база)**: гейт спеки §3 ≥3% MSPT (лейн ~4.9% CPU ⇒ верхняя граница выигрыша ~4.6-4.9% + снижение GC-давления от дропа вспомогательных аллокаций в fluid-лэйне); планка владельца x150000 = PalettedContainer.get ИСЧЕЗАЕТ из профиля (self-time < ~0.3%), Ops.get ~0.2-0.5% — новая субстрат-цена; риск-режим: ARMED-маркер отсутствует (race READY-vs-load) → vanilla, диагностика по маркерам "paletted: pristine sighting".
> (6) NEXT S7-132: absorb A/B leg #1 (+leg #2 для min-of-2 по cpu-парам) → вердикт рычага → очередь: (b) relens guard-хуков (3.2% оверхеда, теперь частично уже амортизирован демуксом), (c) аллокационная диета entity-лэйна (GC 25.4%), (d) flushStep wave-2. INJECTS-ONLY цел (0 sandbox boots; CI-буты санкционированы).

> **СТАТУС 20 TPS (S7-131b) — PALETTED-DEMUX: 3 ноги (все SUCCESS, ARMED+PATCHED), вердикт REFUTED-BY-ECONOMICS на X150K; дефолт дизарм, инфраструктура банкуется; S7-132 = relens guard.**
> (1) Leg #1 35256298212: порог 16384 чтений/контейнер недостижим (~5-15/тик на десятках тысяч контейнеров) — fast path ни разу, get 5.7% (регресс). Leg #1' 35258976251: порог 64 — билды маргинальных секций не окупаются (лейн 2707 vs 2573 экв. сэмплов). Leg #1'' 35261386323: экономика v3 (4096/16384/блэклист×2) — лейн ≈ 5.3% vs 4.9% базовых. (2) СТРУКТУРНАЯ ПРИЧИНА: X150K-чтения = 100k items × 1-2/тик, размазаны по десяткам тысяч контейнеров; точка окупаемости снапшота >6k будущих чтений/контейнер — распределение на порядки ниже. (3) Честный вердикт (дисциплина TASK-77/S7-128): СОЛО-гейт ≥3% НЕ достигнут, дефолт 0 (fail-closed vanilla), код сохранён как верифицированный субстрат (gen-протокол, parity-харнесс, ASM-пайплайн, fingerprint-serve) для combo: guard-v2 region-gen валидация (убьёт cellsUnchanged перечтения БЕЗ per-container demux). (4) NEXT S7-132: relens guard-хуков (bump 1.0-1.4% чистой инструментации → сэмплирование; slow-path аллокации → GC-диета), затем аллокационная диета entity-лэйна (GC 25.4%), flushStep wave-2. INJECTS-ONLY цел (0 sandbox boots; 5 санкционированных CI-бутов за раунд).

> **СТАТУС 20 TPS (S7-132, TASK-268) — guard relens реализован (inline-счётчики + zero-alloc hit/slow), combo-нога (relens + demux v3) 35264319982 в полёте; absorb S7-133.**
> (1) bump() (~1.0-1.4% self на ~400k вызовов/тик) → inline plain HITS/MISSES + холодный logStats (контракт TASK-77 §7 цел). (2) cellsUnchanged/slow: per-call массивы (~200k аллокаций/тик) → ThreadLocal grow-only (регионный тик однопоточен, entry владеет копией только при cacheable). (3) Диспатч = комбо-нога (relens+demux v3, paletted_demux=1) — тест гипотезы S7-131b «демукс ускоряет собственные свипы guard'а» на той же X150K-сцене; база 35245032701. (4) Preregistered: bump → ≤0.2%, guard-лейн ≤2.5%, hit_rate ~78% ±3, fixture зелёные. (5) NEXT S7-133: absorb → если guard-лейн упал и комбо ≥ базы — чистая нога (demux=0) для изоляции + вердикт; иначе — калибровка. INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-132b, TASK-268 absorb) — combo-leg SUCCESS: bump ИСЧЕЗ из профиля, guard-лейн 3.4→2.5% (−27%), high-water −768MB; hit_rate 73.3% (динамика сцены, честно); S7-133 = аллокационная диета entity-лэйна.**
> (1) Реленс механически выполнен: inline-счётчики сняли bump с горячего пути полностью; ThreadLocal-буферы срезали ~200k аллокаций/тик (high-water 6914→6146MB). (2) Комбо relens+demux: guard-лейн −0.9pp, get-лейн 4.7% (чуть лучше соло 5.3%) — демукс остаётся нейтральным субстратом, не соло-рычагом. (3) MSPT межраново шумовая (cpu 7056716 vs 6679335) — лейн-абсолюты банкуются. (4) NEXT S7-133: аллокационная диета entity-лэйна (GC+барьеры 27.1% — крупнейшая адресуемая производная после entity tick: AABB/Vec3/буферы в move/collide/baseTick) → flushStep wave-2; чистая relens-нога — по требованию изоляции. INJECTS-ONLY цел (0 sandbox boots; 2 CI-бута за тик).

> **СТАТУС 20 TPS (S7-133, TASK-269) — ARCH-ATTACK рычаг #2 ALLOC-DIET реализован и офлайн-верифицирован (ALL PASS), диспатч A/B leg #1 на X150K-базе.**
> (1) **STEP-0 javap-контракт аллокационного лейна** (материализованное ядро purpur-1.21.10, INJECTS-ONLY): (a) `LivingEntity.pushEntities` → `Level.getPushableEntities` → `Level.getEntities(Entity,AABB,Predicate)`: на КАЖДЫЙ вызов `new ArrayList` (dead guava-список, результат не читается НИ ОДНОЙ инструкцией) + `new ArrayList` (fill) + барьеры заполнения — ~45k+ живых сущностей/тик; (b) `ItemEntity.mergeWithNeighbours` ГЕЙТИТСЯ `isMergable()` и bench-предметы (pickupDelay=32767) выходят ДО запроса — merge-запрос на сцене НЕ выполняется (census честно снял merge с очереди); (c) `CollisionUtil.getCollisionsForBlocksOrWorldBorder` — БЕЗУСЛОВНЫЙ `new MutableBlockPos; dup; <init>:()V` до всех веток + `new LazyEntityCollisionContext` — ~250k+ запросов/тик (148k move + 100k item noPhysics noCollision). (2) **Дизайн ALLOC-DIET**: два call-site ретаргета на статический бридж `EntityQueryOps` (net.minecraft.world.entity, kernel-loader): push-обёртка → `pushables(Level,Entity,AABB)` (тот же deep-fill EntityLookup.getEntities + PlatformHooks.addToGetEntities + Profiler-счётчик — последовательность сущностей бит-в-бит ванильная, исчезают 2 ArrayList/вызов), ctor-сплайс → `mutablePos()` (кольцо 8 переиспользуемых MutableBlockPos, re-init set(0,0,0) = точное состояние свежего ctor). Оба сплайса СОХРАНЯЮТ ДЛИНУ (3B→3B опкод+операнд; 7B→7B ctor-блок на 4 nop) — ноль сдвигов веток/StackMapTable. Кольца ротационные (8 слотов/поток) — вложенные запросы из колбэков событий до глубины 7 безопасны, глубже — громкий CME, не тихая порча. LazyEntityCollisionContext-пул отложен в wave-2 (приватные final-поля базового класса; безопасная ре-инициализация требует Unsafe/MH — отвергнуто за цену на горячем пути). (3) **Путь верификации**: rust-патчеры на РЕАЛЬНЫХ байтах (LivingEntity 186570B / CollisionUtil 45439B фикстуры): Retargeted{1} в обоих, идемпотентность (repatch byte-identical), fail-closed на чужих классах; 101 тест зелёный; офлайн-харнесс (allocdiet/harness): defineClass пропатченных байтов = полный проход верификатора JVM ✓ + ретаргет pushEntities подтверждён с JVM-стороны (0xb8 pc=75 → EntityQueryOps#pushables) + кольцо mutablePos 8 ротируемых обнулённых экземпляров + ванильный ctor-якорь = **ALLOC-DIET OFFLINE PASS** (research/alloc-diet-2026-09-18/). (4) **Диспатч leg #1** vs база 35245032701 (guard=1, demux=0, diet=0): X150K 150000/seed42/xmx10G/fp4/300s, diet=1. Preregistered: fixture-гейты зелёные (behavioral parity), ARMED-маркеры («defined EntityQueryOps», «hook serve»), GC-лейн (G1 14.4% + барьеры 12.7% = 27.1% база) ↓ по лейн-абсолютам равных окон (закон S7-96d: MSPT межраново шумовая), high-water ↓ (база 6914/комбо 6146MB), pause-total ↓ (база 15782ms), entity-фаза без регрессии >0.5pp. Оценка объёма диеты честно: ~10-17MB young-gen/тик удалённых аллокаций (push ~3.2MB + MutableBlockPos ~8MB + мёртвые барьеры заполнения). (5) NEXT S7-134: absorb leg #1 → leg #2 min-of-2 → вердикт рычага (гейт: GC-лейн ↓ ≥10% иначе REFUTED-BY-ECONOMICS, дефолт 0) → wave-2 диеты (LazyEntityCollisionContext, refreshDirtyAttributes, InsideBlockEffectApplier) или flushStep. INJECTS-ONLY цел (0 sandbox boots; 1-2 CI-бута за тик санкционированы).

> **СТАТУС 20 TPS (S7-133b, TASK-269 absorb) — ALLOC-DIET leg #1 SUCCESS/ARMED живьём: high-water −20% (−1374MB), но гейт GC-лейна провален с обратным знаком (+21% абсолюта) ⇒ REFUTED как GC-CPU рычаг; дефолт 0, код банкуется; S7-134 = истинный alloc-профиль + рычаг old-gen мутации.**
> (1) Run 35271475494 (f44d9ce): ARMED живьём (Retargeted{1}×2 бит-в-бит офлайну, serve+retransform rc=0), fixture VALID/alive/0 tick-behind — поведенческая парити на живой 148.5k-сцене полная. (2) Лейн-счёт: GC-лейн 27.1→30.6% (+21% абсолют; add_card +58%, Refine +38%, RemSet +31%, CM +16%), entity-фаза плоско (−0.4%) — гейт «GC-лейн ↓≥10%» НЕ достигнут, знак обратный ⇒ REFUTED (leg #2 избыточен: все суб-лейна согласованно вверх — структурная причина, не шум). (3) Банкуется: high-water 6914→5540MB, pause-avg −4%, pause-max −18%, парити живьём, сплайс-механика. (4) УРОК: срезанные ~10-17MB/тик — малая доля истинного чёрна (сотни MB/тик); GC-лейн гонится old-gen МУТАЦИЕЙ (section-движения ChunkEntitySlices, спавн/деспавн churn, entity-data записи), не wrapper-чёрном; слепые диеты без alloc-профиля запрещены. (5) NEXT S7-134: async-profiler alloc-mode в харнес (инфраструктура) ⇒ ранжирование чёрна по байтам ⇒ архитектурный рычаг old-gen мутации (batched section-moves / deferred dirty updates — ванильная парити), flushStep wave-2 в очереди. INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-134, TASK-270) — ИНФРАСТРУКТУРА ИЗМЕРЕНИЙ: ROOT-CAUSE профилировщика v2 найден и исправлен (asprof v4.x `dump` НЕ останавливает сессию — alloc-профиль не собирался НИ РАЗУ в истории проекта), v3 stop-based; ценз-ран 35275967738 в полёте.**
> (1) **ROOT-CAUSE (вскрыт по ap.log-сигнатуре)**: v2 строила окна цепочкой dump→start, но в asprof v4.x `dump` выгружает данные БЕЗ остановки сессии (останавливает только `stop`) ⇒ первая cpu-сессия жила весь соак, старты wall/alloc падали («[ERROR] Profiler already started» ×3 — подтверждено в run-s7131-lever{,2,3}, run-s7132-combo, run-diet-leg1 — ВСЕ раны), а cpu-/wall-/alloc-collapsed.txt были кумулятивными CPU-редампами. Косвенный признак стоял в артефактах всё время: «alloc»-листья = G1 oop-closures/C2-фреймы — невозможные листья для alloc-события. (2) **Валидность прежних вердиктов**: лейн-анализы S7-131..133 валидны (cpu-collapsed + gc.log — CPU-событие, живые данные); WALL/ALLOC секции BOTTLENECKS_3 были CPU-загрязнены — помечены, перевзводятся ценз-раном. (3) **v3-фикс (run_world3.sh)**: окна завершаются `stop` (stop == stop+dump); cpu 0-55% / wall 55-80% / alloc 80-100% (alloc-веса = БАЙТЫ — ценз чёрна S7-133b); asprof_guard_start с orphan-rescue (застрявшая сессия → orphan-collapsed.txt → retry — молча съесть окно больше невозможно); flamegraph = короткая свежая cpu-сессия 20s; run-env.txt + `seconds:` (длина alloc-окна для MB/s). (4) **report_world3.py**: ALLOC-единицы = BYTES во всех таблицах (bucket/phase/leaf) + примечание семантики; F2 = top-10 по байтам + **F2 alloc-churn rate** (MB/s по окну 20%×seconds, total GB). Оффлайн-валидация: синтетический WORK (byte-веса, 1.8GB/60s → ~30MB/s расчёт корректен) + реальный артефакт run-diet-leg1 (парсинг без краша; артефакт-отчёт восстановлен git checkout после смоука — дисциплина артефактов). (5) **Диспатч ценза**: run **35275967738** (master 662738e) — базовая сцена X150K (150000/seed42/xmx10G/fp4/guard1/**demux0/diet0**/300s), НЕ A/B-нога, а ценз: первый истинный alloc-профиль + первый истинный wall-профиль + свежий cpu-профиль мастера. Preregistered гейты абсорба: ap.log чист (0 «already started»/«not active»); alloc-листья = аллокационные сайты (<init>-доминанта, НЕ G1/C2); порядок сотен GB на 60s-окно (оценка S7-133b: сотни MB/тик × 1200 тиков); wall ≠ cpu; fixture-гейты зелёные. (6) NEXT S7-134b: absorb ценза ⇒ ранжирование чёрна по байтам ⇒ выбор архитектурного рычага old-gen мутации (кандидаты: batched section-moves ChunkEntitySlices, deferred dirty updates, EntityTickList churn, Long2Reference rehash) ⇒ S7-135 имплементация; flushStep wave-2 в очереди. INJECTS-ONLY цел (0 sandbox boots; 1 CI-бут ценза санкционирован).

> **СТАТУС 20 TPS (S7-134b, TASK-270 absorb) — ПЕРВЫЙ ИСТИННЫЙ ALLOC-ЦЕНЗ X150K: все инфра-гейты PASS (4 профилировочные сессии впервые реально сменились), истинный чёрн 25.6GB/60s = 21.4MB/тик (оценка S7-133b завышена ~15× — refuted честно); 66% чёрна = movement-геометрия + inside-blocks; топ-1 CPU-функция PalettedContainer.get питается теми же путями; S7-135 = мемоизация inside-blocks/fluid (STEP-0 javap первым).**
> (1) Run 35275967738 (master 662738e, базовая сцена demux=0/diet=0/guard=1) SUCCESS: ap.log = 4×«Profiling started», 0 ошибок — cpu/wall/alloc/flamegraph впервые реально сменились (v3 stop-based работает); alloc-листья = чистые аллокационные сайты (0 G1/C2-фреймов — grep=0); wall ≠ cpu (wall-only waiters: Watchdog/ReferenceHandler/DedicatedServer$1/PrioritisedQueueExecutor); fixture VALID (INJECT DONE 150000/150000 за 105s, alive 4/4). (2) **ИСТИННЫЙ ЧЁРН**: gc.log-границы (14 young GC в 60s-окне × ~245 eden-регионов × 8MB) = **25.6GB/60s = 427MB/s = 21.4MB/тик**; оценка S7-133b «сотни MB/тик» REFUTED (~15× завышена — считала эвакуацию выживших, не eden-потребление); интервал ap-alloc = 3.56MB/сэмпл (выведен, внутренне согласован). (3) **РАНЖИРОВАНИЕ ЧЁРНА ПО БАЙТАМ**: movement/collision-геометрия **43.9%** (топ-1 сайта ценза Vec3.add 9.5% через collidedWithFluid→AABB.collidedAlongVector; AABB makeBoundingBox 5.8%, inflate 3.9%, Fluid.getAABB 3.0%); inside-blocks **22.1%** (LongOpenHashSet long[] 6.1% — dedup-множество НА СУЩНОСТЬ НА ТИК в forEachBlockIntersectedBetween; BlockPos$6 corner-lambda 5.2%; InsideBlockEffectApplier.flushStep→Arrays.copyOf 4.6%); JVM/other 17.3% (вкл. CgroupUtil 6.3% — JVM-внутренний, вне досягаемости, не-цель); fluid 5.5%; остальное шум. (4) **КРОСС-СВЯЗКА**: топ-1 kernel-функция CPU = PalettedContainer.get 3.1% (+readPalette 0.8%) питается ТЕМИ ЖЕ путями (getBlockState из checkInsideBlocks + updateFluidHeightAndDoFluidPushing по 4 углам) — один архитектурный рычаг бьёт И чёрн, И топ-1 CPU-функцию. (5) **ОЧЕРЕДЬ S7-135+**: (a) INSIDE-BLOCKS/FLUID per-entity мемоизация с event-driven dirty-флагом (Δpos=0 + ревизия block-state секции в ключе; механика кэш-гварда, прецедент fluid_guard TASK-80; median-exact parity) — бьёт 22.1%+часть 43.9% чёрна + долю PalettedContainer.get; (b) collidedWithFluid геометрия (Vec3.add 9.5%) — STEP-0 javap-контракт collidedAlongVector; (c) InsideBlockEffectApplier.flushStep (4.6%) — rotating pool на субстрате EntityQueryOps. INJECTS-ONLY цел (0 sandbox boots; 1 CI-бут ценза). (6) NEXT S7-135: STEP-0 javap-контракт checkInsideBlocks/updateFluidHeightAndDoFluidPushing/forEachBlockIntersectedBetween (материализованное ядро, офлайн) ⇒ дизайн мемоизации с ревизией секции ⇒ имплементация (rust classfile + офлайн-харнесс ALL PASS) ⇒ диспатч leg #1 на X150K-базе. Гейт preregistered решит S7-135. flushStep wave-2 в очереди.

> **СТАТУС 20 TPS (S7-135, TASK-271) — ARCH-ATTACK рычаг #3 INSIDE-CACHE реализован и офлайн-верифицирован (rust 106 ✓ + INSIDE-CACHE OFFLINE PASS), диспатч leg #1 35282003292.**
> (1) **STEP-0 javap-контракт**: гейт-сайт isAffectedByBlocks offset 1 в checkInsideBlocks(List,Collector) — единственный; анатомия визитора декодирована (hitShape/inFluid/effectful/intersected-семантика, fluid-ветка ПОСЛЕ block); ванила на статике исполняет ДВЕ traversal/тик (вторая — чистый visitedBlocks-дюп). (2) **Рычаг**: единственный 3B→3B ретаргет isAffectedByBlocks→InsideBlockOps.gate(Entity)Z; bridge обслуживает статику целиком (HIT: верификация state-id + replay ванильных вызовов эффектов; MISS: mirror-traversal теми же примитивами + capture), нестатика — ваниль. Кэш = плоские примитивные массивы 131072×12 (урок §155: ноль oop-массивов). (3) **Офлайн**: rust 106 ✓; JVM-харнесс OFFLINE PASS (ARMED=true на реальном ядре; лов identity-форка загрузчика задокументирован). (4) **Диспатч**: run 35282003292 (master 1bd7f52) vs база-ценз 35275967738; preregistered гейты: fixture зелёные, ARMED-маркеры, alloc-семьи ↓30/25%, PalettedContainer.get ↓15%, young GC ↓, entity-фаза без регрессии >0.5pp; частичный = калибровка leg #2, промах = REFUTED-BY-ECONOMICS дефолт 0, проход = leg #2 min-of-2. (5) NEXT S7-135b: absorb leg #1 → вердикт по лейн-абсолютам равных окон (S7-96d) → leg #2/вердикт; очередь после INSIDE-CACHE: collidedWithFluid-геометрия (Vec3.add 9.5%, если останется), flushStep rotating pool (4.6%), updateFluidHeightAndDoFluidPushing corners (leg #2 INSIDE-CACHE). INJECTS-ONLY цел (0 sandbox boots; 1 CI-бут leg #1 санкционирован).

> **СТАТУС 20 TPS (S7-135b, TASK-271 absorb) — legs #1/#2 INSIDE-CACHE: leg #1 ИНЕРТЕН (capture недостижим по построению + delta-чек не проходил — рычаг НЕ РАБОТАЛ, принцип НЕ опровергнут); bridge v2 (xo-статик-детектор + bootstrap capture); leg #2 РАН НЕВАЛИДЕН (сцена коллапсировала 150k→~70k при VALID-маркере, crawl с инъекции MSPT 430, артефакт без stdout; bridge 0.4% CPU); lever BANKED default 0; S7-136 = ping-pong hardening + чистый re-run.**
> (1) Leg #1 (35282003292): ARMED полн, fixture VALID, НО gate никогда не обслуживал — два механических дефекта моста: capture был достижим только через инвалидацию несуществующего кэша; deltaMovement покоящихся предметов ≠ 0 (гравитационный остаток) — статик-чек не проходил. Лейны = база в шуме (S7-96d). (2) Bridge v2 (d8453b3): статик-детектор xo==x,yo==y,zo==z (from==to==pos; поле Entity.xo публичное, javap ✓) + bootstrap capture на пустом слоте; офлайн OFFLINE PASS повторён. (3) Leg #2 (35284069355): РАН НЕВАЛИДЕН — сцена 67-74k живых (база 148k) при VALID-маркере инъекции (популяция распалась после), TPS 1.5-3.3 / MSPT 430 с первого пост-инъекционного полла, артефакт без server-stdout.log; bridge сам по себе 0.4% CPU — самовзрыва нет, но per-entity entity-фаза ~9× — не изолировано; главный подозреваемый ping-pong слотов (150k последовательных id в 131072 слотах ⇒ ~19k пар делят слот, вечный mirror + Recorder-аллокации + card-marks в old-gen слот-массивы), второй — runner-контеншн (wall 84.5% idle). (4) ВЕРДИКТ: принцип мемоизации не опровергнут; код = ARMED-CODE-BANKED default 0 (fail-closed vanilla); сравнение по leg2 нечестно (сцена развалилась). (5) NEXT S7-136: ping-pong hardening (bootstrap capture только при SLOT_EID[slot]==0 — занятый слот ⇒ ваниль; ~13% сущностей ванильны при 150k>131072 — допустимо) ⇒ чистый re-run leg #2' (полный артефакт) ⇒ вердикт по preregistered гейтам §156; если лейны снова мертвы при живом HIT — JIT/деопт-телеметрия retransform; после INSIDE-CACHE: collidedWithFluid-геометрия / flushStep pool / updateFluidHeight corners. INJECTS-ONLY цел (0 sandbox boots; 2 CI-бута санкционированы).

> **СТАТУС 20 TPS (S7-136, частичный — CREDS-BLOCKED) — ping-pong hardening моста INSIDE-CACHE реализован и офлайн-верифицирован (rust 106 ✓ + INSIDE-CACHE OFFLINE PASS на свежем материализованном ядре); диспатч leg #2' ЗАБЛОКИРОВАН: git-creds утеряны вместе с песочницей (WIPE), bootstrap_tick.sh отсутствует.**
> (1) **Харденинг (урок leg #2 35284069355)**: capture теперь ТОЛЬКО из пустого слота (`SLOT_EID[slot]==0`); слот, занятый чужим eid (живым или мёртвым), больше не перехватывается mirror'ом — чистая ваниль без инвалидации чужого штампа. Механика ping-pong устранена по построению: 150k живых > 2^17 слотов ⇒ ~19k пар eid делят слот; прежний перехват давал вечный A-capture⇔B-capture (полная traversal + Recorder + 6 arraycopy card-marks каждый тик у обеих сущностей, ноль HIT) — главный подозреваемый коллапса сцены leg2 (150k→70k, MSPT 430). Цена: ~13% сущностей остаются ванильными — принято по preregistered плану S7-135b. (2) **Офлайн-верификация повторена на ВОССТАНОВЛЕННОМ окружении** (песочница пересоздана; purpur paperclip качался заново, eula-less pass materialized kernel 29.4MB + joml libraries; JDK21 temurin): build EntityQueryOps + InsideBlockOps(hardened) javac --release 21 OK; cargo rebuild подхватил include_bytes; suite 107 = 106 ✓ + 1 ignored; JVM-харнесс: structural pass 205522B (байт-в-бит прежнему — ядро/патчер не менялись), retarget-проверка, ARMED=true на реальном ядре, массивы 131072×12 ⇒ **INSIDE-CACHE OFFLINE PASS**. Hardened-классы забанкованы (research/inside-cache-2026-09-18/InsideBlockOps_s7136.class + sha256 append-only). (3) **БЛОКЕР**: ~/.git-credentials и bootstrap_tick.sh утеряны (WIPE песочницы между 05:08 и 07:43 +08); push c-crussty и dispatch workflow невозможны анонимно; локальный коммит + патч в /home/z/my-project/download/ сделаны. Требуется владельцу: восстановить bootstrap_tick.sh (baked token) — после этого следующий тик делает push + диспатч leg #2' по preregistered гейтам §156 (fixture зелёные, ARMED, alloc-семьи ↓30/25%, PalettedContainer.get ↓15%, young GC ↓) ⇒ вердикт INSIDE-CACHE. (4) Дополнительный риск leg #2' задокументирован: мёртвые eid занимают слоты навсегда (нет эвикции) — на 300s-ране затухание HIT-доли ожидаемо умеренное (churn-спавны попадают в занятые слоты → ваниль), измеримо по hit-телеметрии; LRU-эвикция сознательно НЕ внесена (минимальный hardening, дисциплина канона). INJECTS-ONLY цел (0 sandbox boots; 0 CI-бутов за тик — диспатч заблокирован creds).

> **СТАТУС 20 TPS (S7-137, частичный — CREDS-BLOCKED) — ARCH-ATTACK рычаг #4 FLUSH-DIET реализован и офлайн-верифицирован (rust 112=111✓+1 ignored + FLUSH-DIET OFFLINE PASS на реальном ядре); dup-рычаг collidedWithFluid ОТМЕНЁН по STEP-0 (весь лейн — под гейтом INSIDE-CACHE); push/dispatch leg #2' по-прежнему заблокированы (bootstrap_tick.sh не восстановлен).**
> (1) **STEP-0 пересборка очереди**: collidedWithFluid-геометрия (Vec3.add 9.5% чёрна, топ-1 сайт ценза) — javap Entity показал: единственный caller = визитор lambda$checkInsideBlocks$2 (fluid-ветка, offset 135; collidedWithShapeMovingFrom = getAABB + List.of + makeBoundingBox + subtract + collidedAlongVector). ВЕСЬ лейн исполняется ПОД гейтом isAffectedByBlocks ⇒ покрывается INSIDE-CACHE leg #2' — дублирующий рычаг не нужен; полный потенциал leg #2' пересчитан: inside-blocks 22.1% + collidedWithFluid-доля ~22.7% + доля PalettedContainer.get ≈ 45% чёрна под одним гейтом.
> (2) **FLUSH-DIET STEP-0** (кандидат из очереди §5 S7-134b, независимый от INSIDE-CACHE — applyAndClear крутится и на HIT-пути): ванильный flushStep() переносит before/after списки в finalEffects через List.addAll; ArrayList.addAll резолвит c.toArray() ДО проверки пустоты ⇒ каждый ПУСТОЙ вызов аллоцирует new Object[0] через Arrays.copyOf; ценз 35275967738: 336 сэмплов (4.6% истинного чёрна) ровно на advanceStep→flushStep→ArrayList.addAll→ArrayList.toArray→Arrays.copyOf→Object[]; advanceStep стреляет на каждом блок-степе traversal (~300k/тик при 150k).
> (3) **Дизайн**: оба addAll-сайта flushStep()V (invokeinterface List.addAll, 5B, javap-offsets 41/114) → invokestatic FlushOps.fladd(List,Collection)Z (3B) + 2 nop (бывшие count/zero-слоты) — length-preserving, стек-форма идентична, SMT не двигается; fladd: `src.isEmpty() ? false : dst.addAll(src)` — ванильная семантика минус мусорный toArray; бридж-класс FlushOps (net.minecraft.world.entity, raw-типы, erasure-дескриптор совпадает); env CRUSSTY_FLUSH_DIET (dormant-invisible, fail-closed: kernel-rename guard beforeEffectsInStep, strict ровно-2-сайта, idempotent AlreadyPatched).
> (4) **Офлайн-верификация**: rust suite 112 = 111✓ + 1 ignored (5 новых тестов на РЕАЛЬНОЙ фикстуре StepBasedCollector 5695B: два сайта, пул-резолв fladd, байт-форма invokestatic+2nop, idempotent byte-identical, fail-closed на чужом классе); JVM-харнесс FlushDietHarness на реальном ядре: structural pass 5798B, wiring, fladd-семантика (empty→false no-op, non-empty→ordered addAll), behavioral smoke ПАРА patched+vanilla (2000 пустых степов через advanceStep→flushStep→fladd + 5 typed effects непустой путь + fresh applyAndClear) — **FLUSH-DIET OFFLINE PASS**; уроки харнесса: nest-парнёр RecordedEffect обязан жить в том же loader'е (IllegalAccessError cross-loader), InsideBlockEffectType.<clinit> тянет реестры → офлайн SharedConstants.tryDetectVersion()+Bootstrap.bootStrap() (не бут: реестры в памяти). Артефакты забанкованы research/flush-diet-2026-09-18/ (+sha256 append-only). run_world3.sh + world-bench.yml: env CRUSSTY_FLUSH_DIET + input flush_diet — диспатч-подготовка leg (S7-138).
> (5) **БЛОКЕР без изменений**: ~/.git-credentials и bootstrap_tick.sh НЕ восстановлены (WIPE); push b98915a+S7-137 и диспатчи (leg #2' INSIDE-CACHE, затем FLUSH-DIET leg) невозможны анонимно; crussty-dev-logs приватен — CLAIMS/INDEX/LEDGER долг копится. Владельцу: восстановить bootstrap_tick.sh — тогда: push → диспатч leg #2' (гейты §156) → вердикт INSIDE-CACHE → диспатч FLUSH-DIET leg vs база-ценз. INJECTS-ONLY цел (0 sandbox boots; 0 CI-бутов за тик — creds).

> **СТАТУС 20 TPS (S7-140, частичный — CREDS-BLOCKED 6-й тик) — ARCH-ATTACK STEP-0-РАЗВЕДКА ЗАВЕРШЕНА ПОЛНОСТЬЮ: оставшиеся непокрытые лейны разобраны (entity-query = ПАРИТИ-СТЕНА; SynchedEntityData = уже O(1)-массив; блок-коллизии = moonrise-оптимален; остатки = микро/запрещены). ОФЛАЙН-ФАЗА КОНВЕЙЕРА ИСЧЕРПАНА — критический путь = диспатчи 4 рычагов, ждут creds.**
> (1) **Entity-query лейн** (getEntities 784+599=2.5% + intersects 847=1.5% — крупнейший непокрытый CPU): полный javap-контракт снят на живом ядре — pushEntities (50k/тик, getPushableEntities→List одноразовый), item-merge герды (`tickCount % (moved?2:40)`, thundering herd ~100k сканов в один тик каждые 40 тиков = MSPT-пики каждые 2с), Mob.aiStep looting-скан (3.9% churn на inflate+query пикаперов). ПАРИТИ-СТЕНА доказана: (a) порядок кандидатов значим (push-цикл/cramming), (b) predicate pushableBy ПАРНЫЙ (Team rules ⇒ суб-индекс по флагу неконсистентен), (c) event-driven кэш соседей мёртв (движение каждый тик), (d) порядок-сохраняющий прескрин с сортировкой = нетто <1% = МИКРО-запрещено, (e) размазывание гердов меняет тайминг merge = запрещено. ВЕРДИКТ: ×150000-класса рычага с median-exact parity на этом ядре НЕ СУЩЕСТВУЕТ; переоткрытие = только смена moonrise-субстрата. Полное ТЗ: research/entity-query-2026-09-18/DESIGN.md.
> (2) **SynchedEntityData закрыт**: itemsById УЖЕ DataItem[] массив (O(1)); getValue = volatile VarHandle backbone (неизбежен). **Блок-коллизии закрыты**: moonrise уже имеет hasOnlyAir-skip + hasSpecialCollidingBlocks + emptyContextCollisionShape; COLLISION-FREE-SECTION (сестра FLUID-FREE) добирает только травяные над-секции ≤0.6% = микро-запрещено. **CgroupUtil 6.3% churn** — JVM-внутренний, недостижим (флаги запрещены) — не-цель.
> (3) **Следствие**: 5 офлайн-верифицированных рычагов (INSIDE-CACHE hardened, ALLOC-DIET, FLUSH-DIET, FLUID-FREE-SECTION + PALETTED-DEMUX) исчерпывают ценз run 35275967738; каждый следующий рычаг обязан впитывать уроки живых A/B (урок leg #2: непроверенный на сцене = риск коллапса) — накопительная слепая инженерия 6-го рычага вредна. Критический путь = push 4 коммитов + 3-4 диспатча + absorb-вердикты по preregistered-гейтам.
> (4) **БЛОКЕР без изменений (6-й тик)**: bootstrap_tick.sh/creds НЕ восстановлены (push: could not read Username); диспатчи (leg #2' INSIDE-CACHE ≈45% чёрна под гейтом; FLUSH-DIET leg; FLUID-FREE leg; ALLOC-DIET leg) ждут владельца; crussty-dev-logs приватен — CLAIMS/INDEX/LEDGER долг копится. INJECTS-ONLY цел (0 boots; 0 CI-бутов — creds).

> **СТАТУС 20 TPS (S7-140, частичный — CREDS-BLOCKED 6-й тик) — ARCH-ATTACK STEP-0-РАЗВЕДКА ЗАВЕРШЕНА ПОЛНОСТЬЮ: оставшиеся непокрытые лейны разобраны (entity-query = ПАРИТИ-СТЕНА; SynchedEntityData = уже O(1)-массив; блок-коллизии = moonrise-оптимален; остатки = микро/запрещены). ОФЛАЙН-ФАЗА КОНВЕЙЕРА ИСЧЕРПАНА — критический путь = диспатчи 4 рычагов, ждут creds.**
> (1) **Entity-query лейн** (getEntities 784+599=2.5% + intersects 847=1.5% — крупнейший непокрытый CPU): полный javap-контракт снят на живом ядре — pushEntities (50k/тик, getPushableEntities→List одноразовый), item-merge герды (`tickCount % (moved?2:40)`, thundering herd ~100k сканов в один тик каждые 40 тиков = MSPT-пики каждые 2с), Mob.aiStep looting-скан (3.9% churn на inflate+query пикаперов). ПАРИТИ-СТЕНА доказана: (a) порядок кандидатов значим (push-цикл/cramming), (b) predicate pushableBy ПАРНЫЙ (Team rules ⇒ суб-индекс по флагу неконсистентен), (c) event-driven кэш соседей мёртв (движение каждый тик), (d) порядок-сохраняющий прескрин с сортировкой = нетто <1% = МИКРО-запрещено, (e) размазывание гердов меняет тайминг merge = запрещено. ВЕРДИКТ: ×150000-класса рычага с median-exact parity на этом ядре НЕ СУЩЕСТВУЕТ; переоткрытие = только смена moonrise-субстрата. Полное ТЗ: research/entity-query-2026-09-18/DESIGN.md.
> (2) **SynchedEntityData закрыт**: itemsById УЖЕ DataItem[] массив (O(1)); getValue = volatile VarHandle backbone (неизбежен). **Блок-коллизии закрыты**: moonrise уже имеет hasOnlyAir-skip + hasSpecialCollidingBlocks + emptyContextCollisionShape; COLLISION-FREE-SECTION (сестра FLUID-FREE) добирает только травяные над-секции ≤0.6% = микро-запрещено. **CgroupUtil 6.3% churn** — JVM-внутренний, недостижим (флаги запрещены) — не-цель.
> (3) **Следствие**: 5 офлайн-верифицированных рычагов (INSIDE-CACHE hardened, ALLOC-DIET, FLUSH-DIET, FLUID-FREE-SECTION + PALETTED-DEMUX) исчерпывают ценз run 35275967738; каждый следующий рычаг обязан впитывать уроки живых A/B (урок leg #2: непроверенный на сцене = риск коллапса) — накопительная слепая инженерия 6-го рычага вредна. Критический путь = push 4 коммитов + 3-4 диспатча + absorb-вердикты по preregistered-гейтам.
> (4) **БЛОКЕР без изменений (6-й тик)**: bootstrap_tick.sh/creds НЕ восстановлены (push: could not read Username); диспатчи (leg #2' INSIDE-CACHE ≈45% чёрна под гейтом; FLUSH-DIET leg; FLUID-FREE leg; ALLOC-DIET leg) ждут владельца; crussty-dev-logs приватен — CLAIMS/INDEX/LEDGER долг копится. INJECTS-ONLY цел (0 boots; 0 CI-бутов — creds).

> **СТАТУС 20 TPS (S7-141, частичный — CREDS-BLOCKED 7-й тик) — ДИСПАТЧ-РЕПЕТИЦИЯ ЗАВЕРШЕНА: конвейер мгновенного диспатча проверен по всем швам (wiring/inputs/гейты/банки/rust-регресс) — READY-TO-DISPATCH.**
> (1) **Wiring-аудит**: run_world3.sh — все 6 env-флагов (CRUSSTY_FLUID_PUSH_GUARD/PALETTED_DEMUX/ALLOC_DIET/INSIDE_CACHE/FLUSH_DIET/FLUID_FREE) wired с default 0 (dormant fail-closed) + WARN FLUID_FREE без DEMUX; world-bench.yml — все 13 inputs wired (world_url/radius/seconds/summons/fake_players/6 lever-флагов/population_target+seed/server_xmx/cpu_band_min+max pairing/natives_url), concurrency-group на месте.
> (2) **Гейт-аудит**: preregistered-комплекты всех 4 legs в каноне — §156 (INSIDE-CACHE: fixture зелёные, ARMED, alloc-семьи ↓30/25%, PalettedContainer.get ↓15%, young GC ↓), §S7-138 (FLUSH-DIET), §S7-139 (FLUID-FREE: fluid-доля get ↓≥60%, entity-фаза ≤+0.5pp, young GC ↓), ALLOC-DIET (dispatch_s7134 preregistered).
> (3) **Артефакт-аудит**: sha256-пересчёт банков — inside-cache 4/4, flush-diet 3/3, fluid-free 5/5, alloc-diet 2/2 совпадают; paletted-demux 3/5: два «расхождения» = СТАРЫЕ записи эпохи pre-S7-131-FIX в append-only файле (классы пересобраны коммитом eec4b6e «ре-эмбед», новые записи соответствуют) — НЕ порча; найден пробел дисциплины: PalettedContainerOps.class пересобран без новой append-only записи (зафиксировано, не влияет на диспатч — rust-образ тестируется suite'ом).
> (4) **Rust-регресс на head 655a95b**: suite 121 = 120✓ + 1 ignored, 0 failed — консистентность rust-образа и классов подтверждена.
> (5) **БЛОКЕР (7-й тик)**: bootstrap_tick.sh/creds НЕ восстановлены; READY-TO-DISPATCH конфигурация: push 5 коммитов (b98915a/e5cff9a/f16b215/91fcd70/655a95b) → диспатч leg #2' INSIDE-CACHE (X150K, inside_cache=1, base 35275967738, гейты §156) → FLUSH-DIET leg → FLUID-FREE leg (demux=1) → ALLOC-DIET leg; absorb-вердикты по preregistered-гейтам сразу после ног. INJECTS-ONLY цел (0 boots).

> **СТАТУС 20 TPS (S7-141, частичный — CREDS-BLOCKED 7-й тик) — ДИСПАТЧ-РЕПЕТИЦИЯ ЗАВЕРШЕНА: конвейер мгновенного диспатча проверен по всем швам (wiring/inputs/гейты/банки/rust-регресс) — READY-TO-DISPATCH.**
> (1) **Wiring-аудит**: run_world3.sh — все 6 env-флагов (CRUSSTY_FLUID_PUSH_GUARD/PALETTED_DEMUX/ALLOC_DIET/INSIDE_CACHE/FLUSH_DIET/FLUID_FREE) wired с default 0 (dormant fail-closed) + WARN FLUID_FREE без DEMUX; world-bench.yml — все 13 inputs wired (world_url/radius/seconds/summons/fake_players/6 lever-флагов/population_target+seed/server_xmx/cpu_band_min+max pairing/natives_url), concurrency-group на месте.
> (2) **Гейт-аудит**: preregistered-комплекты всех 4 legs в каноне — §156 (INSIDE-CACHE: fixture зелёные, ARMED, alloc-семьи ↓30/25%, PalettedContainer.get ↓15%, young GC ↓), §S7-138 (FLUSH-DIET), §S7-139 (FLUID-FREE: fluid-доля get ↓≥60%, entity-фаза ≤+0.5pp, young GC ↓), ALLOC-DIET (dispatch_s7134 preregistered).
> (3) **Артефакт-аудит**: sha256-пересчёт банков — inside-cache 4/4, flush-diet 3/3, fluid-free 5/5, alloc-diet 2/2 совпадают; paletted-demux 3/5: два «расхождения» = СТАРЫЕ записи эпохи pre-S7-131-FIX в append-only файле (классы пересобраны коммитом eec4b6e «ре-эмбед», новые записи соответствуют) — НЕ порча; найден пробел дисциплины: PalettedContainerOps.class пересобран без новой append-only записи (зафиксировано, не влияет на диспатч — rust-образ тестируется suite'ом).
> (4) **Rust-регресс на head 655a95b**: suite 121 = 120✓ + 1 ignored, 0 failed — консистентность rust-образа и классов подтверждена.
> (5) **БЛОКЕР (7-й тик)**: bootstrap_tick.sh/creds НЕ восстановлены; READY-TO-DISPATCH конфигурация: push 5 коммитов (b98915a/e5cff9a/f16b215/91fcd70/655a95b) → диспатч leg #2' INSIDE-CACHE (X150K, inside_cache=1, base 35275967738, гейты §156) → FLUSH-DIET leg → FLUID-FREE leg (demux=1) → ALLOC-DIET leg; absorb-вердикты по preregistered-гейтам сразу после ног. INJECTS-ONLY цел (0 boots).

> **СТАТУС 20 TPS (S7-144, частичный — CREDS-BLOCKED 10-й тик) — DISPATCH-READINESS AUDIT v2: FLUID-FREE OFFLINE PASS ВОСПРОИЗВЕДЁН ИЗ ЧИСТОГО СОСТОЯНИЯ (exit 0); конвейер готов к мгновенному диспатчу; classpath-конвейер восстановления забанкован.**
> (1) Аудит готовности после S7-143: rust-сьют на head 9564838 = 121 ok / 0 failed / 1 ignored; git fsck чист; ahead 8 (f67ae10..9564838); патч-банк 5/5 sha256 OK; artifact_hashes_s7143 (FluidOps.class, LevelChunkSection.patched.class) OK.
> (2) Воспроизведение из ЧИСТОГО состояния (пост-WIPE): материализация ядра заново = 29386794B байт-в-бит с цензом (sha256 e2992d63…), FluidFreeHarness с чистой javac-пересборкой: STRUCTURAL+INJECTED SURFACE / SECTION MATERIALIZED / ARMED=true / FREE-HIT ff=1 / EVENT-DRIVEN INVALIDATION gen 0→2→ff=2 / RESTORE ff=1 verdict=true / SCATTERED-WATER PARITY ⇒ **FLUID-FREE OFFLINE PASS exit 0** — байт-в-бит с записью S7-143/91fcd70.
> (3) Уроки восстановления (RUNBOOK_S7144 забанкован research/fluid-free-2026-09-18/): (a) порядок classpath shadow→kernel→libraries ОБЯЗАТЕЛЕН — paper-shaded LogUtils.getClassLogger живёт в пропатченном kernel jar и затеняется чистым mojang-logging из libraries; (b) финальные libraries = <work>/server/libraries от paperclip (в контейнерах base+patch); (c) unzip не создаёт вложенные -d пути; (d) materialize_kernel_v2/finish_libraries/run_fluid_free_harness скрипты забанкованы (sha256 в artifact_hashes_s7144.txt).
> (4) **БЛОКЕР (10-й тик)**: bootstrap_tick.sh/creds НЕ восстановлены; push 8 коммитов (f67ae10..9564838) + диспатчи leg #2' INSIDE-CACHE (X150K, base 35275967738, §156) → FLUSH-DIET → FLUID-FREE (demux=1) → ALLOC-DIET ждут; CLAIMS/INDEX/LEDGER долг 10 тиков (CLAIMS_DEBT дополнен до S7-144). INJECTS-ONLY цел (0 boots; 0 CI-бутов).

> **СТАТУС 20 TPS (S7-145, частичный — CREDS-BLOCKED 11-й тик) — REPRO-аудит v3 ЗАВЕРШЁН: ВСЕ 5 харнессов (4 рычага + демукс-субстрат) воспроизведены из ЧИСТОГО состояния (все exit 0); диспатч-конвейер доказуемо восстанавливаем одной командой; критический путь не изменился = creds.**
> (1) Воспроизведения из чистого состояния (пост-WIPE, только /tmp/toolchain + репо): INSIDE-CACHE PASS (patched Entity structural, InsideBlockOps ARMED=true, cache 131072×12), FLUSH-DIET PASS (fladd semantics, patched+vanilla smoke), ALLOC-DIET PASS (CollisionUtil retarget, mutablePos ring), PALETTED-DEMUX Parity ALL PASS (20000 random ops lockstep L1/L2/model 4883/4883 getAndSet, snapshot snapGen=30009, fast-path 1000 reads, re-materialization, concurrency 3R+1W) — плюс FLUID-FREE PASS из S7-144.
> (2) Уроки REPRO (RUNBOOK_S7145 забанкован research/dispatch-readiness-2026-09-18/): (a) харнессы в default package — запуск по простому имени (FluidFreeHarness — исключение, FQN); (b) Parity stub jar = same-runtime-package closure: семейство PalettedContainer* (patched заменяет vanilla) + Strategy* + Configuration* — иначе LinkageError itable / IllegalAccessError protected-abstract (граница protected/abstract = same package SAME loader); (c) FlushDiet nest-partner RecordedEffect извлекается из kernel; (d) libRoot Parity = 5-й аргумент (paperclip libraries).
> (3) Скрипты забанкованы: repro_dispatch_harnesses.sh + repro_parity.sh (sha256 artifact_hashes_s7145.txt) — полная REPRO-восстанавливаемость диспатч-цепочки после любого WIPE за минуты.
> (4) **БЛОКЕР (11-й тик)**: bootstrap_tick.sh/creds НЕ восстановлены; push 9 коммитов (f67ae10..cb2e153) + диспатчи leg #2' INSIDE-CACHE (X150K, base 35275967738, §156) → FLUSH-DIET → FLUID-FREE (demux=1) → ALLOC-DIET ждут; CLAIMS/INDEX/LEDGER долг 11 тиков. INJECTS-ONLY цел (0 boots; 0 CI-бутов).

> [S7-146 ВОССТАНОВЛЕНИЕ УЧЁТА: блоки S7-142/S7-143 не экспортировались в патч-банк до WIPE №3 — append-реставрация текстов из atomic worklog; код S7-143 требует ре-реимплементации]

> **СТАТУС 20 TPS (S7-142) — WIPE-РЕСТАВРАЦИЯ №2: 5/6 коммитов подняты из патч-банка; S7-139 (FLUID-FREE FULL 91fcd70) УТРАЧЕН (патч подменён дубликатом S7-135b при экспорте 02:17) ⇒ реимплементация по DESIGN.md + полный офлайн-реверификационный цикл = головная офлайн-задача; диспатч FLUID-FREE без неё ЗАПРЕЩЁН (урок leg #2). Патч-банк защищён: переэкспорт 0001..0005 + карантин + манифест. 3x pull --rebase: клоны восстановлены. CREDS-BLOCKED 8-й тик. INJECTS-ONLY цел.**

> **СТАТУС 20 TPS (S7-143) — FLUID-FREE-SECTION РЕ-ВОПЛОЩЁН: код утраченного S7-139 восстановлен по DESIGN.md и ДОКАЗАН офлайн заново (rust 121=120ok+1ignored, 4 section_ff-теста; FluidFreeHarness на реальном ядре 29386794B: STRUCTURAL+INJECTED SURFACE / ARMED=true / FREE-HIT стабилен ff=1 / EVENT-DRIVEN INVALIDATION живьём setBlockState(WATER) crusstyGen 0→2 ff=2 — байт-в-бит с 91fcd70 / RESTORE / SCATTERED-WATER miss стабилен ⇒ OFFLINE PASS). Ключевые уроки: кэш сверяется с демукс-МУТАЦИОННЫМ crusstyGen (±2/мутация), не snapGen; ff 0=unknown/1=free/2=has-fluids (zero-init не вердикт); FluidOps define ДО Entity-ретаргетов. Конвейер 4 рычагов снова ПОЛЕН; критический путь = push + 4 диспатча. CREDS-BLOCKED 9-й тик. INJECTS-ONLY цел.**

[КОД S7-143 (fluid_free.rs + patch_section_ff + FluidOps.java + chain + wiring) утрачен с WIPE №3 — НЕ восстановлен; ре-реимплементация = головная офлайн-задача следующего тика]

> **СТАТУС 20 TPS (S7-146) — WIPE №3 (13:43 +08) + РЕСТАВРАЦИЯ №3 + FLUID-FREE RE-RE-ИМПЛЕМЕНТАЦИЯ ДОКАЗАНА: конвейер 4 рычагов снова ПОЛЕН и ЗАПУШЕН; creds ВОССТАНОВЛЕНЫ владельцем (14:11) — push-блокер 12 тиков закрыт; диспатчи стартуют.**
> (1) WIPE №3 уничтожил c-crussty (ahead 10), c-dist, CRUSSTY, весь тулчейн; my-project уцелел (патч-банк 0001..0007 11/11 sha256 OK, runs_index 279..288, restore-скрипты).
> (2) Реставрация №3: анонимный клон (04ef8d5) → git am 0001-S7-136/0002-S7-137/0003-S7-138 → append-реставрация restore_commits_v2.py (S7-140/141/144/145, оригинальные метаданные + RESTORED-note) → чистка 1-байтовых binary-заглушек (harness .class компилируются по RUNBOOK) → Subject-фикс "[PATCH N/5]"; GOAL/worklog-блоки S7-142/143 восстановлены append'ом (в банке не были). Push 04ef8d5..b0aac42 = 9 коммитов.
> (3) Ре-реимплементация S7-143 (FLUID-FREE FULL) после утраты с WIPE №3: patch_section_ff (append-only utf8-only 2-поля, 15041→15088 байт-в-бит с 91fcd70-записью, idempotent, fail-closed; УРОК: field_info нужен ТОЛЬКО name/desc utf8, НЕ Fieldref — лишние CP-энтри дают +20B) + FluidOps.java (Unsafe-резолвы, ff 0/1/2, publish (ffGen,ff) НА СЕКЦИИ — первый вариант писал ffGen в контейнер, поймано харнессом) + fluid_free.rs (section hook + bridge define + WARN без DEMUX) + chain в inside_cache (wait_bridge_ready 60s, деградация inside-only fail-dominant) + wiring (lib.rs mod/register/activate, run_world3.sh FLUID_FREE, world-bench.yml fluid_free input, build_fluid_ops.sh восстановлен).
> (4) Верификация: rust 122=121ok+1ignored (5 section_ff: applies/grows-байт-в-бит, idempotent, wrong-class, field-shape 0x0041, dump); FluidFreeHarness на реальном ядре 29386794B (sha256 e2992d63, байт-в-бит): STRUCTURAL+INJECTED SURFACE / ARMED=true / FREE-HIT ff=1 ffGen=0 gen=0 / EVENT gen 0→2 ff=2 ffGen=2 / RESTORE gen=4 ff=1 verdict=true / SCATTERED-WATER miss стабилен ⇒ **FLUID-FREE OFFLINE PASS exit 0**; FluidOps.class e031d8cd (честная ре-имплементация, семантика 91fcd70); манифест artifact_hashes_s7146.txt.
> (5) creds: владелец предоставил push-URL 14:11; крон Job 394666 обновлён (правило 1b); НОВЫЙ КРИТИЧЕСКИЙ ПУТЬ = 4 диспатча: leg #2' INSIDE-CACHE (X150K, base 35275967738, гейты §156) → FLUSH-DIET → FLUID-FREE (demux=1) → ALLOC-DIET → absorb-вердикты. INJECTS-ONLY цел (0 sandbox boots; CI-буты санкционированы).

> **СТАТУС 20 TPS (S7-147) — leg #2' ABSORB: A/B-НЕВАЛИДЕН (девиация конфига fp0/900s), НО НАЙДЕН И УСТРАНЁН ГЛАВНЫЙ ИЗМЕРИТЕЛЬНЫЙ ДЕФЕКТ ЭРЫ — паритет популяции фикстуры; ARMED-цепочка INSIDE-CACHE PASS живьём; base-b диспатчен на фикстуре-фиксе.**
> (1) Ран 35314220731 (head 53d14d1): конфиг-девиация диспатча (fake_players=0, seconds=900 — дефолты вместо preregistered fp4/300s) + неравные окна (S7-96d) ⇒ §156-вердикт невозможен. Позитив забанкован: ARMED-цепочка ПОЛНА живьём (pristine 205458B major65 → defined InsideBlockOps+$Recorder → Retargeted{1} 205522B → serve → retransform rc=0), INJECT 150000/150000 VALID, 900s soak без крэша, 0 tick-behind, TPS 1.2→3.3 (база 0.6-0.8).
> (2) «Коллапс» 148k→71k (та же сигнатура leg2) = ПАРИТЕТ-АРТЕФАКТ ФИКСТУРЫ, не самовзрыв: 2970 тиков (900s×3.3TPS) против 210 (300s×0.7) = 14× ванильного распада (item-merge 105k→54.7k, горение/cramming мобов) на стенку-время; topup-модель слепа (deficit=0 aliveEst=const; покрывает только age-despawn; в базе topup не стрелял вовсе — 210<600 тиков). Парадокс эры: чем лучше рычаг, тем «невалиднее» ран по wall-time паритету. fp=0 нарушал спеку сцены («как будто игроки есть»).
> (3) Фикс S7-147 (4c8f029): BenchPopulationPlugin topup REAL-COUNT (Item/Monster/Animals по w.getEntities каждые 600 тиков, маркеры TOPUP-SCAN aliveReal/deficit/aliveEst) + непрерывный drain 20/тик (~14ms profile-invisible, largest-lane-first, miss-guard 64, детерминизм seed^(ft*1_000_003)^total); Compile-OK javac21 против kernel 29386794B+purpur-api+125 libs (CI-эквивалент). Следствие протокола: ВСЕ будущие A/B — против base-b (ран 35317176927, head 4c8f029, все рычаги 0, fp4/300s), не против до-фиксного ценза 35275967738.
> (4) Лейны leg2' vs база — SHARES, не гейт (окна/тики/популяция несопоставимы): inside-blocks 42.39%→35.08%, movement-geom 22.50%→21.86%, flushStep 3.91%→4.29%, PalettedContainer.get 3.06%→3.54%, young GC 146→182; bridge в топах не виден — самовзрыва нет. Артефакты: research/inside-cache-2026-09-18/{ABSORB_S7147.md, run-s7136-leg2prime/ (alloc-collapsed 4.3MB, run-env, BOTTLENECKS_3, gc.log; sha256 artifact_hashes_s7147.txt), run-s7134b-base/ (артефакт ценза), fetch/analyze/dispatch скрипты}.
> (5) NEXT: absorb base-b (популяция ≈148k стабильна, TOPUP-SCAN живой) ⇒ диспатч leg #2'' (inside_cache=1, fp4/300s) ⇒ §156-вердикт (fixture зелёные, ARMED, alloc-семьи ↓30/25%, PalettedContainer.get ↓15%, young GC ↓, entity-фаза ≤+0.5pp) ⇒ FLUSH-DIET leg → FLUID-FREE (demux=1) → ALLOC-DIET. INJECTS-ONLY цел (0 sandbox boots; CI-буты санкционированы: leg2' поглощён + base-b).

> **S7-147b ДОПОЛНЕНИЕ (тот же тик)**: base-b ЗАВЕРШЁН SUCCESS (35317176927, 07:14:44 UTC, 17 мин) и санити-абсорблен: конфиг точен (fp4/300s/inside_cache=0/guard1), INJECT 150000/150000 VALID, **популяция 148391/148193/148027 стабильна** (структурный близнец ценза 148402), TPS 0.8-0.9, topup-скан не стрелял (240 тиков < 600-периода — консистентно с базой; фикстура-фикс дремлет на базовых скоростях, оживает только при ускорении от рычага — паритет честен в обоих режимах) ⇒ **base-b = валидная база для всех §156-сравнений**. leg #2'' INSIDE-CACHE ДИСПАТЧЕН: ран 35318755582 (head 135cb89, inside_cache=1, fp4/300s, 07:17:58 UTC, S7-108). NEXT: absorb leg2'' по §156 (гейты: fixture зелёные, ARMED, alloc-семьи ↓30/25% vs base-b, PalettedContainer.get ↓15% vs 3.06%, young GC ↓, entity-фаза ≤+0.5pp) → FLUSH-DIET leg → FLUID-FREE (demux=1) → ALLOC-DIET.

> **СТАТУС 20 TPS (S7-148, частичный) — leg #2'' ABSORB: A/B-НЕВАЛИДЕН ×2 (74 941 NoClassDefFoundError: EntityQueryOps = парити-контаминация моста; популяция 79k≠148k = дренаж 20/тик < валового распада ~42/тик); ДЕФЕКТ НАЙДЕН И УСТРАНЁН (InsideBlockOps SELF-CONTAINED + drain-budget дефицит-драйвен), leg #2''' диспатчен.**
> (1) Конфиг leg2'' (35318755582, head 135cb89, SUCCESS 07:17:58→07:32:36 UTC) точен по preregistered (fp4/300s/inside_cache=1, девиаций нет); INJECT 150000/150000 VALID; ARMED-цепочка полна живьём (pristine Entity 205458B → defined InsideBlockOps+$Recorder → Retargeted{1} 205522B → serve → retransform rc=0); TPS 0.8→3.0; 0 tick-behind.
> (2) КОНТАМИНАЦИЯ: hardened HIT-верификация gate:195 звала EntityQueryOps.mutablePos() — класс ALLOC-DIET-субстрата НЕ определён в kernel loader при alloc_diet=0 ⇒ 74 941 «Entity threw exception» (07:24:53–07:31:29, ~189/с, ~0.06% entity-tick-вызовов прерывалось Paper per-entity catch) — median-exact паритет нарушен в этих вызовах; офлайн-харнесс не ловил (одна loader-пространство с EQ в CP); leg #2' (175MB stdout) ретроспективно та же сигнатура.
> (3) §156-гейты НЕ выполнены и по долям (inside-blocks alloc 40.44→37.76% = −6.6% отн. при гейте ≥30%; movement-geom 22.24→26.03% ↑; PalettedContainer.get CPU 3.38→3.02% = −10.6% при гейте ≥15%; young GC 125→165 ↑). На-тик нормировка (≈700–900 vs 240 тиков) даёт ↓65–75% по всем семьям, но неинтерпретируем из-за (a) NCDFE-прерываний + (b) дрейфа популяции 148k→79k. ВЕРДИКТ ПО PREREGISTERED ПРОТОКОЛУ НЕВОЗМОЖЕН — честный вывод.
> (4) Фиксы S7-148: (a) InsideBlockOps SELF-CONTAINED — ThreadLocal mutable-pos ring (8 слотов, zeroed, семантика 1:1) внутрь моста, compile-dep entityquery удалён из build_inside_block_ops.sh; javac21 против kernel e2992d63; 0 EQ-ссылок (javap); rust 122=121ok+1ignored; REPRO-харнессы INSIDE-CACHE/FLUSH-DIET/ALLOC-DIET OFFLINE PASS; новые байты Ops 7826B / Recorder 4307B. (b) BenchPopulationPlugin drain-budget = clamp(deficit/50, 20, 100)/тик + TOPUP-SCAN 600→120т (валовый распад ~42/тик покрывается; в базе дремлет bit-for-bit — base-b остаётся базой). Compile-OK CI-эквивалент.
> (5) NEXT: leg #2''' (head=фикс, inside_cache=1, fp4/300s) → §156 vs base-b + НОВЫЙ обязательный чек «0 NoClassDefFoundError в stdout» + популяция ≈148k весь soak (TOPUP-SCAN живой). Если дрейф повторится — калибровка фикстуры продолжается (base-c). INJECTS-ONLY цел (CI-буты санкционированы: leg2'' поглощён, leg2''' диспатчен).

> **СТАТУС 20 TPS (S7-148b, частичный) — leg #2''' ABSORB: ПЕРВЫЙ ВАЛИДНЫЙ A/B ЭРЫ (фикстура стабильна 148k весь soak, 0 NCDFE, ARMED полна) ⇒ §156 = 5/7 PASS (PARTIAL-PASS): INSIDE-CACHE GREEN-BY-SAFETY (alloc-диета −32.5%/−35.9% подтверждена, entity-фаза −5pp, TPS нейтрально); «ускорение» leg2'' (TPS 3.0) на 100% артефакт контаминации+полупустой сцены. FLUSH-DIET leg ДИСПАТЧЕН (35324517090).**
> (1) Валидность: head 45687c4 (self-contained мост + дренаж-фикс); конфиг точен; INJECT VALID; популяция 148546/148383/148197 vs base-b 148391/148193/148027 — структурные близнецы, TOPUP-SCAN ×2 живой (дренаж держит план); 0 NoClassDefFoundError/0 Entity-threw-exception (stdout 241KB vs 177MB leg2''); ARMED-цепочка полна живьём (defined Ops+Recorder → Retargeted{1} 205458→205522 → serve → rc=0); kernel в артефакте e2992d63 байт-в-бит.
> (2) §156: inside-blocks alloc 40.44→27.30% (−32.5% отн.) ✅; movement-geom 22.24→14.26% (−35.9%) ✅; entity-фаза 59.7→54.7% (−5pp) ✅; PalettedContainer.get CPU 3.38→3.09% (−8.6% отн. при гейте ≥15%) ❌ — гейт был оптимистичен (кэш снимает дублирующую traversal, не первую); young GC 125→143 (+14%) ❌ — частично topup-спавны (Object[] +67.6% = спавн-путь), частично шум.
> (3) Вердикт по протоколу («частичный = калибровка»): INSIDE-CACHE = GREEN-BY-SAFETY — парити-чист, самовзрыва нет, young-gen alloc-давление снижено существенно, TPS не регрессирует; КАК TPS-ДРАЙВЕР X150K НЕ ПОДТВЕРЖДЁН (главный CPU-жир живой сцены — не checkInsideBlocks). ОстаЁтся кандидатом конвейера (накопительно в следующих ногах). Калибровка гейтов: young-GC-гейт на живой сцене с топапом трактуется по alloc-долям семейств; PalettedContainer.get-гейт INSIDE-CACHE-специфичный.
> (4) NEXT: absorb FLUSH-DIET leg (35324517090, head 1864e3d, flush_diet=1, fp4/300s, 08:28:05 UTC, §S7-138-гейты + S7-148-протокол) → FLUID-FREE leg (demux=1) → ALLOC-DIET leg → накопительный конфигурационный ран (все зелёные рычаги вместе). INJECTS-ONLY цел (CI-буты санкционированы: leg2''', FLUSH-DIET).

> **СТАТУС 20 TPS (S7-148c, частичный) — FLUSH-DIET leg ABSORB (35324517090): §S7-138 PASS — flushStep-семья 4.15%→0.00% (−100%), Object[] −51.2%, entity-фаза −5.6pp, fixture зелёная, 0 NCDFE, TPS нейтрально (аллокационный рычаг, не TPS-драйвер). FLUSH-DIET = GREEN (в накопительный ран). FLUID-FREE leg ДИСПАТЧЕН (35326295881, fluid_free=1+demux=1+inside_cache=1-владелец).**
> (1) Валидность: head 1864e3d; конфиг точен (flush_diet=1, inside_cache=0); INJECT VALID; популяция 148483/148328/148178 стабильна; 0 NoClassDefFoundError (stdout 257KB); мост ARMED живьём (pristine StepBasedCollector 5695B major65 → ретаргеты); kernel e2992d63 байт-в-бит.
> (2) Механика end-to-end: zero-waste addAll (FlushOps.fladd) снял ВСЮ flushStep-семью (4.15% alloc-чурна ценза 35275967738) и половину Object[]-листа (426→208) — точное попадание в дизайн S7-137; young GC 125→141 — топап-спавны (калибровка S7-148b), GC-счётчик нечувствителен к диете статики.
> (3) NEXT: absorb FLUID-FREE leg (35326295881, head d2f063e, fluid_free=1+paletted_demux=1+inside_cache=1 — владелец Entity-цепочки compose_entity, 08:48:58 UTC, §S7-139: fluid-доля get ↓≥60%, entity-фаза ≤+0.5pp + S7-148-протокол) → ALLOC-DIET leg → накопительный ран (INSIDE-CACHE + FLUSH-DIET + FLUID-FREE + ALLOC-DIET вместе). INJECTS-ONLY цел (CI-буты санкционированы).

> **СТАТУС 20 TPS (S7-148d, частичный) — FLUID-FREE leg ABSORB (35326295881): §S7-139 FAIL ⇒ REFUTED-BY-ECONOMICS: fluid-доля get ВЫРОСЛА 53.8→60.1% (fluid-get 916→934 не снят — ff-кэш без хитов на живой сцене), TPS −20% (демукс-оверхед), ЭКОНОМИЧЕСКИЙ ПОТОЛОК лейна 1.8% total CPU = МИКРО-КЛАСС. Демукс на живой сцене анти-оптимизация (default 0). ALLOC-DIET leg ДИСПАТЧЕН (35328228929).**
> (1) Валидность: head d2f063e (fluid_free=1+demux=1+inside_cache=1-владелец); INJECT VALID; популяция 148445/148395/148267 стабильна; 0 NCDFE (stdout 248KB); ВСЕ цепочки ARMED живьём впервые end-to-end на живой сцене: DEMUX ARMED + PalettedContainerOps defined + fluid_free defined FluidOps → section splice 15041→15088 (байт-в-бит 91fcd70) → entity chain composed Retargeted{sites:2} → serve section 15088; kernel e2992d63.
> (2) Гейт §S7-139 FAIL: fluid-доля get 53.8%→60.1% (↓≥60% НЕ достигнут — доля выросла); readPalette 428→0 (−100%, демукс снял свой лейн); get общий −8.7% отн.; inside-blocks −32.5% из leg2''' НЕ воспроизвёлся (демукс сместил профиль); TPS 0.6–0.7 = регресс −20% (демукс-оверхед; в leg2''' без демукса 0.7–0.8).
> (3) Экономика: fluid-get = 916/50454 = 1.8% total CPU живой сцены — даже идеальный снимок лейна = МИКРО-КЛАСС (запрещено владельцем); §S7-139-гейт писался в до-фиксную эпоху. ff-кэш без хитов: гипотезы (секции has-fluids / демукс-мутации / движение сущностей) — офлайн-диагностика НЕ блокер.
> (4) Решение: paletted_demux default 0 на живой сцене; fluid_free забанкован off (OFFLINE PASS в силе, экономика не та); §S7-139 калиброван. Конвейер: INSIDE-CACHE GREEN-BY-SAFETY + FLUSH-DIET GREEN зелёные; ALLOC-DIET leg в полёте (35328228929, head 59b6bbb, alloc_diet=1, 09:11:24 UTC) → накопительный ран (inside_cache=1+flush_diet=1+alloc_diet по вердикту) → итоговый вердикт эры. INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-149, частичный) — ALLOC-DIET leg ABSORB (35328228929): REFUTED (повторное подтверждение S7-133b на валидной базе): alloc-семьи не упали (movement-geom +13.7%, Vec3 +5.8%), young GC +13.6% (гейт ↓≥10% провален), TPS −12%; мосты корректны (ARMED полна, 0 NCDFE), экономика отрицательная — дефолт 0 подтверждён, wave-2 НЕ продолжается. Итог конвейера рычагов: GREEN = INSIDE-CACHE + FLUSH-DIET; REFUTED = FLUID-FREE+DEMUX + ALLOC-DIET. НАКОПИТЕЛЬНЫЙ РАН ДИСПАТЧЕН (35330129145: inside_cache=1+flush_diet=1).**
> (1) Валидность: head 59b6bbb; INJECT VALID; популяция 148321/148314/148103 стабильна; 0 NCDFE; ALLOC-DIET ARMED живьём (defined EntityQueryOps → LivingEntity 186570→186759 Retargeted{1} + CollisionUtil 45439→45546 Retargeted{1} → serve оба); kernel e2992d63.
> (2) Гейты FAIL: семьи +3..14% (диета не видна ни в одном листе), total alloc +4.2%, young GC +13.6%, TPS 0.6–0.7 (−12%) — ретаргет-мосты добавляют invokestatic-оверхед на push/collision горячих путях без компенсации. REFUTED дважды (S7-133b + настоящая нога) — экономика закрыта.
> (3) Итоговый вердикт эры — после absorb накопительного рана (35330129145, head dbb5e8e, inside_cache=1+flush_diet=1, fp4/300s, 09:33:13 UTC): ожидание = alloc-эффекты обоих зелёных складываются (inside-blocks ~−32%, flushStep →0), 0 NCDFE, популяция стабильна, TPS ≥ базы. INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-149b, ИТОГОВЫЙ ВЕРДИКТ ЭРЫ) — НАКОПИТЕЛЬНЫЙ РАН ABSORB (35330129145): ЗЕЛЁНЫЙ ФИНАЛ — INSIDE-CACHE + FLUSH-DIET вместе: flushStep −100% воспроизведён, inside-blocks доля 40.44→32.81% (−18.9% отн.), young GC ВПЕРВЫЕ ниже базы (125→118, −5.6%), TPS-паритет 0.8–0.9, популяция-близнец 148392/148234/148164, 0 NCDFE, кросс-помех нет. РЕКОМЕНДАЦИЯ: inside_cache=1+flush_diet=1 = default-кандидаты.**
> (1) Валидность: head dbb5e8e; INJECT VALID; 0 NoClassDefFoundError (stdout 259KB); ARMED-маркеры обоих мостов живьём; kernel e2992d63; TOPUP-SCAN живой.
> (2) Комбинация: оба эффекта воспроизведены одновременно (flushStep −100% байт-в-байт; inside-blocks −18.9% отн. — доля смещена снятием flushStep из знаменателя, в изоляции −32.5%); young GC −5.6% — впервые в эре ниже базы (в изолированных ногах +13–14% от топапа — диета двух зелёных перевесила спавн-давление); entity-фаза −1.8pp.
> (3) ИТОГ ЭРЫ (все ноги на валидной базе base-b, протокол S7-148): GREEN = INSIDE-CACHE + FLUSH-DIET (default-кандидаты); REFUTED = FLUID-FREE+DEMUX (лейн 1.8% CPU) + ALLOC-DIET (×2). Честная граница: TPS X150K 0.8–0.9 упирается в entity tick (58%) + unclassified (33–39% — крупнейший непокрытый лейн) — следующий фронт (пост-эра).
> (4) Канон цепочки эры: base-b 35317176927 → leg2'' (NCDFE-контаминация, рефакт моста) → leg2''' 35322530537 → FLUSH-DIET 35324517090 → FLUID-FREE 35326295881 → ALLOC-DIET 35328228929 → CUMULATIVE 35330129145. INJECTS-ONLY цел (7 CI-бутов за эру, все санкционированы; 0 sandbox boots).

> **СТАТУС 20 TPS (S7-150, пост-эра: RECON + STEP-0) — свежий профиль CUMULATIVE 35330129145 отранжирован, топ-1 attackable-лейн найден: fluid-push family (`Entity.updateFluidHeightAndDoFluidPushing`) ≈ 10% total CPU (36.4% лейна ItemEntity.tick 23.43% CPU + 9.8% Zombie). РЫЧАГ FLUID-DIRTY: per-entity мемоизация скана + event-driven dirty-stamp ledger по мутациям воды (класс: кэш + dirty-флаги вместо поллинга).**
> (1) Ранжирование (s7150_recon.py → research/inside-cache-2026-09-18/S7150_RECON.md): entity tick 58.5%; лейны: fluid-push ~10% (ранг 1) → move/collision ~5.4% (ранг 2) → inside-blocks residual ~5.1% (ранг 3) → entity tracker ~2% (ранг 4). Merge-search у items = 2.2% лейна — МИКРО-КЛАСС (гипотеза O(n²)-мерджа на 100k items опровергнута профилем). GC native 28.8% — закрыт (alloc-диета REFUTED ×2). PalettedContainer.get 3.23% leaf — распределён по скан-лейнам (демукс REFUTED).
> (2) STEP-0 javap-контракт (kernel e2992d63, research/fluid-dirty-2026-09-18/step0_*.txt): скан (span-вычисление → пред-фетч sections → тройной цикл PalettedContainer.get → getFluidState → getHeight/getFlow) = ЧИСТАЯ ФУНКЦИЯ от (span, fluid-состояния блоков, isPushedByFluid); постобработка (fluidHeight.put, normalize/scale, 0.003/0.0045-ветка, setDeltaMovement) зависит от dm/Player — выполняется всегда. baseTick игнорирует return (pop), эффекты = записи. Wrapper (boat-спецслучай, splash, wasTouchingWater, lavaScale 0.007/0.0023333…, updateFluidOnEyes) НЕ трогается.
> (3) Дизайн FLUID-DIRTY: FluidPushOps self-contained (паттерн InsideBlockOps S7-148, memo-ring внутрь моста); хит-условие = span бит-в-бит + pushedByFluid + версии секций span не изменились; dirty-ledger = хук в LevelChunk.setBlockState со сравнением old/new FluidState ПО ССЫЛКЕ (bump только при реальной мутации воды — fluid-tick/explosion/waterlog покрыты). Движущиеся сканируются как vanilla; покоящиеся (масса 100k items) пропускают скан. Анти-урок FLUID-FREE учтён: кэш per-ENTITY (не per-section has-fluids — 0 хитов в водном мире), демукс не нужен. Сопутствующая alloc-диета: new MutableBlockPos на каждый вызов + Vec3-цепочки исчезают на хитах → GC relief.
> (4) Preregistered гейты §S7-150 (честный A/B min-of-2, НЕ лотерея): G1 fluid-лейн ↓≥60% при хит-рейте ≥80%; G2 0 NCDFE + ARMED-маркер; G3 популяция-паритет; G4 TPS не хуже базы min-of-2; G5 OFFLINE lockstep 20000 ops (шторм мутаций) — cached бит-в-бит = vanilla (hAcc/hitFlag/flowAcc/flowN/lastLavaContact); G6 young GC не выше базы. REFUTED-критерий: G1 при живом ARMED и хитах ≥80% недостижим ИЛИ G4-регресс >10% (анти-урок демукса) ⇒ fluid_dirty=0.
> (5) Имплементация — S7-151+: FluidOps.java (javac21 против kernel), двойной section-splice (тело скана + setBlockState-хук), rust-хук fluid_dirty.rs + wiring, FluidDirtyHarness OFFLINE (стаб-closure по прецеденту S7-143), затем preregister dispatch (владелец Entity-цепочки, внутри_cache=1+flush_diet=1 база). INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-151, частичный) — FLUID-DIRTY ИМПЛЕМЕНТИРОВАН И ЗАБАНЧЕН: FluidPushOps (self-contained мост: memo двух тегов × 2^17 слотов + реимплементация скана бит-в-бит + event-driven ledger по секциям), двойной ретаргет (оба wrapper-сайта скана + единственный сайт section-write), rust-wiring (fluid_dirty.rs + compose-цепочка inside_cache), rust suite 132/0/1, harness OFFLINE PASS (structural/wiring/armed/ledger). Диспатч — после поведенческого lockstep S7-152.**
> (1) Census v2: во всём kernel ровно 2 вызова updateFluidHeightAndDoFluidPushing (оба в Entity-обёртках — вода 0.014 / лава 0.007-0.0023333) и ровно 1 сайт LevelChunkSection.setBlockState(IIIL…)BlockState в LevelChunk.setBlockState — покрытие полное; miss-путь зовёт ванильное тело обычным вызовом (рекурсии нет, тело не тронуто).
> (2) HIT-условие: guard + span бит-в-бит (клэмп-математика ванили) + pushedByFluid + ссылки секций span + штампы секций не изменились ⇒ бит-в-бит тот же результат скана; постобработка (put/push) исполняется всегда (dm-зависимость сохранена). Capture только в свой слот; движущиеся сканируются как ваниль; покоящиеся пропускают скан.
> (3) Ledger (event-driven инвалидация): secWrite-делегат возвращает секции старое состояние, ref-compare FluidState бампит штамп ТОЛЬКО при реальной мутации жидкости (вода→вода/блоки без fluid-дельты не инвалидизируют — проверено на реальных секциях kernel в харнессе).
> (4) Верификация тика: 9 новых roundtrip-тестов патчеров (строгие 2/1 сайта, idempotent, fail-closed, compose с inside) — suite 132/0/1; harness OFFLINE PASS exit 0 (структурный линк патченных классов над kernel + ledger-контракт на реальных секциях); sha256-манифест 14 артефактов (artifact_hashes_s7151.txt).
> (5) NEXT (S7-152): поведенческий lockstep мини-Level (FluidPushOps.scan vs ванильный скан бит-в-бит, позиционные свипы + шторм мутаций = G5) → preregister dispatch (inside_cache=1+flush_diet=1+fluid_dirty=1, fp4/300s/150k/seed42, A/B min-of-2 vs CUMULATIVE 35330129145). INJECTS-ONLY цел (CI-бутов 0).

> **СТАТУС 20 TPS (S7-152, частичный) — FLUID-DIRTY ПОВЕДЕНЧЕСКИЙ LOCKSTEP PASS (G5 core): бит-в-бит дифференциал реального vanilla-скана vs FluidPushOps.scan на Unsafe scan-contract фикстурах — 44 позиционных кейса × 4 сцены (вода/кромки/границы чанков и секций/lava+water столб/lastLavaContact/flowing уровни/шторм мутаций/unloaded-guard) — 0 расхождений; hit=49 miss=94 vanilla=0.**
> (1) Стена офлайн-конструирования обойдена честно: vanilla Level ctor кастует this к ServerLevel (bukkit-периферия в ctor), прямой ctor LevelChunk тоже кастует к ServerLevel + зовёт MinecraftServer.getServer().registryAccess() — поэтому Level и LevelChunk фикс-туры = Unsafe.allocateInstance с ровно теми полями, что читает javap scan-контракт (step0_*.txt); всё, что зовёт скан — реальный kernel-код (touchingUnloadedChunk→hasChunksAt→moonrise$areChunksLoaded; тройной цикл; getHeight/getFlow с соседними чтениями через реальные секции).
> (2) Сцена C (шторм): источник→течение-8 / течение→воздух / вода→камень / камень→вода через secWrite-делегат — каждая мутация = MISS (event-driven инвалидация) + бит-в-бит совпадение со свежей ванилью на НОВОМ мире; воздух→камень НЕ бампит (ref-compare). Сцена D: unloaded-guard — оба пути false БЕЗ записи fluidHeight (ваниль-эквивалент).
> (3) HIT bookkeeping: повторные идентичные вызовы = HIT и тот же результат (bookkeeping h0→h0+1→h0+2 проверен на каждом кейсе сцены A). vanilla=0 — мост ARMED полностью обслуживает путь.
> (4) Диспатч: next tick S7-153 — preregister dispatch fluid_dirty=1 (в цепочке inside_cache=1+flush_diet=1), fp4/300s/150k/seed42/xmx10G, A/B min-of-2 vs CUMULATIVE 35330129145, гейты §S7-150 G1-G4+G6 живьём. INJECTS-ONLY цел (CI-бутов 0).

> **СТАТУС 20 TPS (S7-153, частичный) — FLUID-DIRTY ЖИВАЯ НОГА ABSORB (35341241628): §S7-150 ГЕЙТ G1 FAIL ⇒ REFUTED-BY-ECONOMICS: hit-rate ≈ 0% на живой сцене (scan 1592 ≈ vanilla 1590 сэмплов — весь мост в miss-путь), fluid-family 9.91%→10.40%, young GC 118→140 (+18.6%), TPS паритет; 0 NCDFE, ARMED-цепочка полна живьём, популяция 150000 VALID. fluid_dirty забанкован off (default 0). ТРЕТЬЕ подтверждение сигнатуры: fluid-scan лейн живой сцены позиционно-нестабилен.**
> (1) Валидность: head 38afaa3 (пломбинг fluid_dirty: workflow input + run_world3.sh export CRUSSTY_FLUID_DIRTY + dispatch_s7153.py); конфиг точен (inside_cache=1+flush_diet=1+fluid_dirty=1, demux/diet/ff=0); INJECT 150000/150000 VALID (items=105000, hostiles=30000, passives=15000); 0 NoClassDefFoundError (stdout 275KB); ARMED живьём: FluidPushOps+$ScanOut defined в kernel loader → LevelChunk Retargeted{1} (48104→48329) → entity chain composed Retargeted{sites:2} → serve → retransform rc=0; kernel e2992d63. Runtime-доказательство обслуживания: 1592 сэмпла FluidPushOps.scan в cpu-collapsed.
> (2) Гейты: G1 FAIL (family не снята: 5185→5691 сэмплов = +9.8%; hit-rate ≈0% при гейте ≥80%); G6 FAIL (young GC 118→140, +18.6%); G4 ~паритет (0.6–0.9 vs 0.7–0.9, регресса >10% нет); G2/G3 PASS. REFUTED-критерий §S7-150 («G1 при живом ARMED недостижим») выполнен ⇒ fluid_dirty=0, оффлайн-достижения S7-151/S7-152 (lockstep G5 бит-в-бит) в силе — рычаг инженерно корректен, экономика нулевая.
> (3) Корневая причина (ABSORB_S7153.md): span каждой сущности меняется каждый тик — популяция fluid-скана X150K движущаяся (items: topup-цикл + плавающие в воде предметы никогда не покоятся; zombies: AI-блуждание). Гипотеза S7-150 «100k items покоится» опровергнута живым профилем. Третья независимая сигнатура после FLUID-FREE (S7-148d): per-ENTITY/per-SECTION мемоизация позиции-зависимого скана на живой сцене не бьёт.
> (4) Инцидент гигиены: GitHub push protection отклонил токен в dispatch_s7153.py:26 — фикс: токен читается из remote URL (правило 1b), секретов в скриптах нет; push прошёл (eccf3d3..38afaa3).
> (5) NEXT (S7-154): RECON-2 — раскладка unclassified-фазы (33–39% entity-цикла, крупнейший непокрытый лейн) до классов поведения (AI/goal-selector/brain/sensing); ранжированные остатки (move/collision 5.4%, inside-blocks residual 5.1%, tracker ~2%) — микро-класс по отдельности, кэш-рычаги там упрутся в ту же позиционную нестабильность. INJECTS-ONLY цел (CI-бут: 35341241628 санкционирован как preregister A/B leg).

> **СТАТУС 20 TPS (S7-154, RECON-2) — ПУЛ ATTACKABLE ≥5% ИСЧЕРПАН: non-entity main (8.22% CPU) разложен до якорей — доминанты нет (пассажиры 1.17%, block entities 0.06%, поршни 0.06%); broadphase/entity-query семейство 10.43% CPU раздроблено по 5 семантическим вызывателям (итерация индекса 30.9% fam, collide 19.2%, noCollision 16.7%, push 8.4%, hard-colliding 6.6%) и по всем классам (Zombie 31.8%, ItemEntity 28.1%). Следующий ×N-класс = region-threaded entity ticking (S7-155+ proposal).**
> (1) Данные (s7154_recon2.py → S7154_RECON2.md, база CUMULATIVE 52341 + кросс-чек лега 35341241628): ванильная семантика требует каждый broadphase-запрос каждый тик (расталкивание/мердж/коллизии/sensing по текущим позициям); популяция X150K позиционно-нестабильна (урок FLUID-DIRTY: кэш результата = 0 хитов), батч-амортизация = смена наблюдаемой логики (запрещено), индекс уже O(log) (moonrise), JNI per-call перекрывается стоимостью перехода (урок ALLOC-DIET ×2).
> (2) Ядерная карта 4-ядерного раннера: main-поток (entity-цикл 58.5%) насыщает ~1 ядро; native GC 28.8% + JIT 3.9% уже параллельны; chunk-system workers 0.1% — ПРОСТАИВАЮТ; 2-2.5 ядра в среднем свободны. Единственный оставшийся ×N-рычаг из списка владельца («планировщики»): region-threaded entity ticking (Folia-модель, тик независимых регионов на параллельных воркерах, per-entity семантика сохраняется; parity-риск = межрегионный порядок тиков/РНГ). Мега-проект: S7-155 feasibility-гейт → прототип планировщика → A/B X150K.
> (3) Финальная карта эры: GREEN = INSIDE-CACHE + FLUSH-DIET (забанкованы в CUMULATIVE); REFUTED = DEMUX+FLUID-FREE, ALLOC-DIET ×2, FLUID-DIRTY; закрыто = GC/JIT/индексы. Честная граница: TPS X150K 0.8-0.9 = семантический минимум тика 150k сущностей на ванильной логике; одиночные кэш-рычаги исчерпаны, ×N-класс достижим только планировщиком.
> (4) INJECTS-ONLY цел: CI-бутов за тик 0 (RECON-2 оффлайн-анализ артефактов; диспатчей нет).

> **СТАТУС 20 TPS (S7-155, RECON-3) — FEASIBILITY-ГЕЙТ region-threaded entity ticking: GREEN. Hot per-tick путь чист от общего RNG; взаимодействия пространственно локальны; Amdahl: TPS-потолок ×2.31 (реалистичный) / ×1.17 (пессимист); moonrise-индекс уже concurrent; NEXT S7-156 прототип RegionTickOps.**
> (1) Данные (s7155_kernel_census.py через javap Temurin-JDK21 + s7155_profile_split.py; база CUMULATIVE 52341): entity-фаза 58.5% CPU разложена — AI/brain/goal/navigation/sensing 29.3% фазы, other-local 24.6%, cross-entity broadphase 24.2% (сверка S7-154: строгие якоря 10.43% CPU), fluid-scan 18.6% (полная локальность), movement-integration 3.2%. Доминирующие взаимодействия = push/merge/collide с радиусами 0.5-2 блока против региона 8×8 чанков (128 блоков) — доля кросс-регионных ≈ 4·r/L ≈ 3-6%.
> (2) RNG-census (весь jar, 9809 классов): общий `Level.random` — 132 референсера (47 entity-классов), но горячий per-tick путь ЧИСТ: ItemEntity/Mob = 0 ссылок; Zombie — только hurtServer; LivingEntity — только breakItem; Entity — только sendBubbleColumnParticles (в сцене soul-sand нет). Per-tick RNG = per-entity `Entity.random` (109 классов) → порядок тиков между регионами не влияет на РНГ-последовательности сущностей. В профиле общий-RNG пути = 100 сэмплов/52341 (0.19%).
> (3) Структура цикла: ServerLevel.tick → ActivationRange.activateEntities → entityTickList.forEach (единый сегмент-контейнер для замены ретаргетом) → tickNonPassenger/tickPassenger → tickBlockEntities. EntityTickList = обёртка moonrise IteratorSafeOrderedReferenceSet (insertion-порядок; шардится по регионам). tickNonPassenger уже потоково-осведомлён (TickThread.ensureTickThread, currentlyTickingEntity AtomicReference); moonrise EntityLookup уже concurrent (SWMR/ConcurrentLong2-таблицы) — запись в секции только от владельца сущности = disjoint по регионам без новых локов.
> (4) Amdahl (E=58.5%, non-entity 8.22%, P=3): S1 (cross сериализован, пессимист) ×1.17; S2 (cross region-локален, serial 3% — ожидаемый) ×2.31; S3 идеал ×2.41. Это единственный оставшийся ×N-класс (все одиночные кэш-рычаги закрыты S7-154).
> (5) Дизайн S7-156: регион 8×8 чанков; планировщик заменяет ТОЛЬКО контейнер forEach (region-бакеты с insertion-порядком внутри + W воркеров + барьер); Entity.tick() остаётся байт-в-байт ванилью; общий Level.random — synchronized-обёртка; кросс-регионное чтение соседа = отставание ≤1 тик (статистическая эквивалентность = bar median-exact parity). INJECTS-ONLY: ретаргет тела ServerLevel.tick + self-contained RegionTickOps + rust-wiring.
> (6) Preregistered гейты S7-156: PG1 OFFLINE lockstep (region-параллель vs ваниль-последовательность, per-entity бит-в-бит); PG2 live 0 NCDFE/ARMED/популяция-близнец; PG3 TPS ≥ +25% (A/B min-of-2 vs CUMULATIVE); PG4 young GC ≤ база+15%; REFUTED-критерий: прирост <10% или неустранимые парити-провалы. INJECTS-ONLY цел (CI-бутов 0; RECON-3 оффлайн).

> **СТАТУС 20 TPS (S7-156, частичный) — REGION-THREADS ИМПЛЕМЕНТИРОВАН И ЗАБАНЧЕН: RegionTickOps (self-contained мост: снапшот ванильной итерацией → W пространственных бакетов (8-чанковые регионы, W=4 квадранты) → W-1 постоянных TickThread-воркеров + main, барьеры GO/DONE, deferred FIFO EntityCallbacks-мутаций), двойной ретаргет (сегмент forEach в ServerLevel.tick 1:1 + оба гвард-сайта EntityCallbacks), rust-wiring region_threads.rs, suite 140/0/1, harness OFFLINE PASS exit 0 (structural/wiring/dormant+parallel: 200 entities W=2 exactly-once). Диспатч — после PG1-верификации S7-157.**
> (1) Census S7-156 (javap + бинарный скан CP): EntityTickList.add/remove имеют РОВНО по одному сайту во всём kernel — ServerLevel$EntityCallbacks.onTickingStart/onTickingEnd (поверхность гварда = 2 метода); forEach — 1 сайт в ServerLevel.tick(BooleanSupplier); contains (tickPassenger) — pure read (containsKey), безопасен при отсутствии писателей.
> (2) Гарантии паритета дизайна: per-entity логика = ванильный consumer байт-в-байт (планировщик меняет ТОЛЬКО контейнер); intra-bucket порядок = снапшот = insertion; mid-phase добавки начинаются со следующего тика (эквивалент ванильного snapshot-итератора); отложенные удаления безвредны (гейт isRemoved() в самом consumer); per-entity RNG локален (S7-155); общий Level.random — только редкие пути (0.19%), Xoroshiro-гонки = lost updates без коррупции.
> (3) Инцидент OFFLINE: двойной DONE-барьер в parallelTick (tickBucket джойнит в finally + awaitDone в finally parallelTick) → deadlock main/worker (jstack: main на DONE, worker вернулся в GO) → удалён дублирующий awaitDone, воркеры персистентны между тиками. Урок: один барьер — одна точка джойна.
> (4) Пломбинг: world-bench.yml input region_threads (integer ≥2) + REGION_THREADS env passthrough; run_world3.sh REGION_THREADS + config-echo + export CRUSSTY_REGION_THREADS. Композ ServerLevel-шва: хук регистрируется ПОСЛЕ tickhook (хвост цепочки), строгий WARN при одновременном F1/F3.
> (5) NEXT (S7-157): PG1-верификация live-подготовка — preregister dispatch region_threads=4 (A/B min-of-2 vs CUMULATIVE 35330129145, внутри_cache=1+flush_diet=1), гейты §S7-155 PG2/PG3/PG4 живьём (0 NCDFE/ARMED/популяция-близнец; TPS ≥ +25%; young GC ≤ база+15%). INJECTS-ONLY цел (CI-бутов 0).

> **СТАТУС 20 TPS (S7-157b, частичный) — REGION-THREADS PG1 LOCKSTEP PASS (бит-в-бит W=1==W=2==W=4) + preregister dispatch leg #1 (35353820223) = LIVE CRASH 40-й секунды: root cause = worker пампит Paper mid-tick очередь (guardEntityTick → moonrise$midTickTasks → MainThreadExecutor.pollTask → NoSuchElementException в отсутствие main-насоса); фикс MID-TICK GATE (880e406), leg #2 (35363758352) диспатчен.**
> (1) PG1 (OFFLINE lockstep, RegionLockstepHarness над реальным kernel): дайджест `61e3c374…941d5` бит-в-бит при W=1==W=2==W=4 (60 тиков, 400 сущностей + шторм мутаций, финал 413); отложенные добавления = следующий тик во всех W (javap: maxIndex пиннится при создании итератора — эквивалент ванили); mid-tick удаления идентичны с T+1; дайджест иммунен к порядку drain между бакетами, чувствителен к отклонениям per-entity семантики (двойной тик/пропуск/RNG-перерасход/потеря мутации).
> (2) Крэш leg #1 (INJECTS-ONLY, санкционированный CI-бут): `NoSuchElementException` @ `ServerChunkCache$MainThreadExecutor.pollTask:838`; census: из 7 насосных сайтов kernel воркер-достижим ровно 1 (Level.guardEntityTick → moonrise$midTickTasks) — Paper pump'ит mid-tick очередь из guardEntityTick на ЛЮБОМ потоке, а main-насос отсутствует при параллельном тике. Инцидент гигиены: смерть JVM + `tail -f` сирота жгли job 68 мин.
> (3) Фикс S7-157b: RegionTickOps.midTickTasks gate — ThreadLocal worker-флаг (worker = пропуск mid-tick pump как side-эффекта Paper-дедликации, main = точная ванильная делегация); 3-й строгий byte-hook Level.guardEntityTick Retargeted{1} + 4 roundtrip-теста (suite 144/0/1); region_threads.rs v2 (3 таргета); harness OFFLINE PASS + per-visit флаг-чек (isWorker() true ТОЛЬКО на helper-потоках, 0 нарушений); PG1 дайджест не изменился. CI-гигиена run_world3.sh: FIFO-паттерн (tail -f сирота убивается TERM+KILL) + liveness-watchdog'и boot/pop/soak (kill -0 SERVER_PID → FATAL рано, артефакты сохраняются).
> (4) Leg #2 (35363758352, 15:39:30 UTC — 9 сек после пуша фикса): тот же preregister протокол (region_threads=4 vs CUMULATIVE 35330129145, fp4/300s/150k/seed42/xmx10G); absorb гейтов PG2/PG3/PG4 — тик 00:48. INJECTS-ONLY цел (CI-буты: leg #1 — санкционированный крэш-лег, leg #2 — preregister A/B).

> **СТАТУС 20 TPS (S7-157c, ABSORB leg #2, 35363758352) — REGION-THREADS ЭКОНОМИКА ДОКАЗАНА: TPS медиана 0.90 → 1.50 = +66.7% (PG3 PASS, гейт +25%; первый рычаг эры, реально сдвинувший TPS; оффлоад воркерами 75.6% lane подтверждён профилем, потолок Amdahl ×2.31 по S2 не пробит); БАНКОВАНИЕ ОТЛОЖЕНО: tracker-race NPE крэш в хвосте soak (после завершения всех профайлер-окон — данные валидны) + PG4 FAIL (young GC 118→155, +31.4%). Вердикт: НЕ REFUTED (прирост ≫10%, инциденты — фиксируемые гонки, не семантика), НЕ GREEN-к-банкованию (крэш-лег не банкуют).**
> (1) Валидность: head 880e406; конфиг бит-в-бит preregister (region_threads=4 на inside_cache=1+flush_diet=1); INJECT 150000 VALID; 0 NCDFE; ARMED полна (region_threads ServerLevel Retargeted{1} + EntityCallbacks{1}+{1} + Level, rc=0 ×3; inside_cache{1}; flush_diet{2}); kernel e2992d63. Job завершён 75-мин wall-cancel ПОСЛЕ сбора данных (spark upload 15:54:53, крэш 15:54:54, артефакт спасён if:always()).
> (2) Персонал: tickBucket lane 71547 сэмплов = 55.64% CPU; воркеры 54057 = 75.6% lane (дизайн W=4 точен: main slot 0 + 3 helper'а); mid-tick gate живьём: 159 сэмплов pump'а ВСЕ на main, 0 на воркерах — NoSuchElementException leg #1 не воспроизвёлся (фикс S7-157b работает). TPS crawl растёт по soak (1.1→1.7), база плоская (0.7→0.9); fluid-family 8.90% (лейн не тронут).
> (3) Инциденты (root-cause → S7-158): (a) NPE ChunkMap.newTrackerTick:1017 «entity is null» ← ServerLevel.tick:815 — воркер-удаление сущности гоняет main-итерацию tracker-карты (deferred FIFO сериализует только EntityTickList, НЕ EntityLookup/tracker); (b) UUID-dup WARN 1× (база 0×): Arrow (worker-спавн skeleton) + Rotten Flesh в разных регионах — генерация UUID на параллельных путях алиасит seed; causal-связь не исключена. PG4: молодая GC +31.4% — цена снапшот/бакетных аллокаций на тик (диета: reuse бакетов/снапшотов).
> (4) Гигиена: job сгорел 59 мин после завершения бенча (подвис post-soak фаза между stop-цепочкой и report_world3.py; BOTTLENECKS_3.md не сгенерирован; collapsed готовы) — S7-158 добавит liveness-watchdog на shutdown-фазу (FIFO-фикс закрыл только смерть java). NEXT (S7-158): (1) фикс tracker-race (гейт/отложенный drain для EntityLookup-удалений ИЛИ removal-safe итерация), (2) фикс UUID-сидирования, (3) GC-диета моста, (4) харнесс-регресс на конкурентный discard, (5) leg #3 min-of-2 → banking при PG2+PG3+PG4 PASS без крэшей. INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-158a/c, фиксы после leg #2) — БЕЗОПАСНОСТЬ КОНТУРА И GC-ДИАТА РЕАЛИЗОВАНЫ (3fc9443): (a) 59-мин сгорание job после крэша вскрыво до механизма и устранено — tail получает SIGPIPE при смерти java (console-listener закрывает stdin рано в shutdown), следующий `echo > console.in` блокируется НАВСЕГДА на open() FIFO без читателя → осиротевший bash до 75-мин timeout (orphan bash 3426 в cleanup, job log молчит после 15:55:22 «spark/debug artifacts: 5»); фикс: cmd() = timeout(5)-bounded FIFO-запись + timeout 180 на report_world3.py (мёртвый канал стоит 5с/вызов, не job). (c) GC-DIET RegionTickOps: root cause PG4 (+31.4% young GC) = свежий snapshot-ArrayList (150k refs) + W bucket-ArrayLists + consumer-массив КАЖДЫЙ тик; теперь zero-alloc steady state — персистентные Entity[][] с grow-on-overflow, одна forEach-фаза fill, post-join tail-nulling; PG1 дайджест БИТ-В-БИТ не изменился (61e3c374…), harness OFFLINE PASS ×2, suite 144/0/1. NEXT (S7-158b, следующий тик): tracker-race — javap-доказательство: newTrackerTick итерирует RAW backing array (trackerEntities.getRawDataUnchecked(), size-снимок один раз, null-гварда на элемент НЕТ) — воркеры делают Entity.discard()/spawn (item-merge, лава, скелет-стрелки Arrow с UUID-алиасом = WARN dup) НАПРЯМУЮ в ServerEntityLookup во время фазы; фикс = ретаргет ServerEntityLookup.addEntity/removeEntity (2 сайта, census сначала) в deferred-FIFO при phaseActive (паттерн onTickingStart/End), дрейн на join; гейты: PG1 дайджест неменяется, шторм discard/spawn в harness, leg #3 = min-of-2 подтверждение с PG2/PG3/PG4 + 0 tracker-NPE + 0 uuid-dup.**
> (1) Директива владельца «ТРОГАЕМ ВСЁ» принята в план: после банка REGION-THREADS микро-лейны в очередь без дисквалификации по размеру — move/collision 5.4%, inside-blocks residual 5.1%, tracker ~2%, пассажиры 1.17% — рычаги НЕ-кэш-класса (branch elimination, батчинг, layout, JNI-батчи), каждый с preregister-гейтами; кэш-рычаги на позиционно-нестабильных популяциях не реанимируются (уроки FLUID-FREE/FLUID-DIRTY/ALLOC-DIET в силе).
> (2) INJECTS-ONLY цел: за тик 0 CI-бутов (офлайн javap-разбор + харнесс-гейты; диспатч leg #3 — только после S7-158b).

> **СТАТУС 20 TPS (S7-158b/d, частичный) — TRACKER-RACE И UUID-СИДИРОВАНИЕ ЗАФИКСЕНЫ КОДОВО (removal-safe sweep + serialized seeding), PG1 дайджест БИТ-В-БИТ неизменён; suite 147/0/1; harness OFFLINE PASS + 2 новых регресса; leg #3 диспатчен.**
> (1) S7-158b tracker-race (javap root-cause): newTrackerTick = НЕпроверяемый обход raw-массива trackerEntities (getRawDataUnchecked, len-снимок один раз, null-гварда элемента НЕТ; line 1017 = первый invoke по слоту) — swap-remove воркера зануляет хвост того же массива, main в окне мутации читает нулевой слот = фатальный NPE «entity is null». Фикс = опция «removal-safe итерация» preregister (НЕ deferral — он меняет same-tick broadphase-видимость): TrackerTickOps (kernel loader, net.minecraft.server.level — same-package доступ к package-private ChunkMap$TrackedEntity/ServerEntity) = тело ванили байт-в-байт + SKIP нулевого слота (состояние недостижимо в безгоночном мире); ретаргет единственного сайта kernel — ChunkMap.tick()V (тело = ровно один вызов newTrackerTick) → TrackerTickOps.newTrackerTick(ChunkMap), strict Retargeted{1}.
> (2) S7-158d UUID-dup (javap root-cause): PurpurWorldConfig.entitySharedRandom ДЕФОЛТ TRUE (iconst_1) → все entity делят SHARED_RANDOM = ThreadUnsafeRandom; Mth.createInsecureUUID(RandomSource) = 2× nextLong БЕЗ синхронизации → два параллельных ctor'а читают одно состояние = идентичные UUID (Arrow vs Rotten Flesh leg #2, спавн потерян «can't add»). Census (бинарный CP-скан jar): вариант (RandomSource) зовёт только Entity ctor (ServerBossEvent = no-arg над Mth.RANDOM = createThreadSafe — не трогаем). Фикс: RngOps (kernel loader) = ванильная математика UUIDv4 бит-в-бит под synchronized(random) — монитор только на 2 дрова при конструировании (спавны редки против 150k тикающих; AI-дровы сохраняют прежний доброкачественный lost-update); retarget_invokestatic static→static ТОТ ЖЕ дескриптор в Entity.<init>, strict Retargeted{1}. Config-wins НЕ используется (запрещено).
> (3) S7-158c GC-диета доделана: харнесс поймал NPE GC-диеты (new Entity[w][] = null-слоты → b.length на первом fill) — слоты инициализируются new Entity[0]; zero-alloc steady state цел (персистентные слотовые массивы grow-on-overflow + общий consumer + tail-nulling). Ожидание leg #3: young GC 155→≤135 (кап PG4).
> (4) Верификация: cargo suite 147/0/1 (+3 строгих roundtrip на новых фикстурах ChunkMap_real/Entity_real: Retargeted{1} + idempotent + fail-closed); RegionThreadsHarness OFFLINE PASS: wiring (TrackerTickOps-Methodref в ChunkMap, RngOps-Methodref в Entity) + ChunkMap structural (патченный ENTITY нельзя оффлайн-дефайнить в child-лоадере: parent-ItemEntity extends parent-Entity → сплит идентичности → VerifyError — артефакт оффлайна, в runtime патч обслуживается тем же лоадером; гейты Entity = wiring + roundtrip + живой retransform rc) + S7-158b-регресс (sweep переживает [e1,null,e2] при len=3 — ванильная форма крэша) + S7-158d-регресс (математика бит-в-бит = Mth на одном сиде; 2×2000 параллельных UUID над ОДНИМ ThreadUnsafeRandom = 4000 уникальных, 0 дубликатов) + PG1 LOCKSTEP PASS дайджест 61e3c374…941d5 БИТ-В-БИТ (фиксы семантику не трогают).
> (5) Watchlist leg #3: sendBlockUpdated:1883 = итерация navigatingMobs на воркере vs параллельная мутация навигации — 1 раз за 15 мин soak leg #2, НЕ фатален (guardEntityTick контейнит); при частоте >1/час → S7-159 мост. Инъекционная поверхность region_threads v3: 5 таргетов (ServerLevel{1}, EntityCallbacks{1+1}, Level{1}, ChunkMap{1}, Entity{1}) + 4 bridge-класса в kernel loader. INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-159, частичный) — LEG #3 (35376530777) = БРАК: RegionTickOps DORMANT (fail-closed штатно), тик шёл ванильно; корень = S7-158d-хук наблюдал Mth вместо Entity; фикс (bc77141); leg #4 (35379431410) диспатчен = min-of-2 сэмпл №1.**
> (1) Строковое доказательство дормантности: «Entity strict site-count violated (NotFound), hook stays dormant» при RegionTickOps lane = 0 сэмплов в cpu-collapsed, worker offload 0%, TPS 0.7-0.9 плоский (медиана 0.80 ≈ база 0.90 -11%, шум). Bridge-классы defined в kernel loader, патчи ServerLevel/EntityCallbacks/Level/ChunkMap вычислены/закэшированы — но READY-флип и ретрансформы не достигнуты: строгий гейт Entity вернул NotFound ДО активации. 0 NCDFE, 0 крэшей, 0 tracker-NPE, 0 uuid-dup — fail-closed защитил vanilla-parity полностью; измерен чистый inside_cache+flush_diet профиль (молодая GC 140 = вариан базы, не сигнал).
> (2) Корневая причина (ABSORB_S7158_LEG3_DUD.md): копипаст-наследие — константа MTH_CLASS = "net/minecraft/util/Mth" вместо "net/minecraft/world/entity/Entity" (имя переменной mth); Mth проходит probe-патчера (createInsecureUUID объявлен в его пуле), но не имеет ctor-дескриптора Entity → NotFound. Оффлайн-харнесс тестировал патчер-функцию на Entity_real-фикстуре, но НЕ константу регистрации — урок: wiring-гейт должен включать имя наблюдаемого класса (добавлено в watchlist leg #4: живые маркеры «pristine sighting …/Entity» + ARMED rc Entity=0).
> (3) Фикс S7-159 (bc77141): ENTITY_CLASS + честные переименования mth→ent (хук/Target/activate/ретрансформ-список/логи); патчер patch_region_rng_entity не менялся (был корректен); композиция с inside_cache сохранена (dispatch_bytes подаёт хуку Entity байты после inside_cache-ретаргета; скан сайта createInsecureUUID по имени — сдвиги пула не мешают). Suite 147/0/1; harness OFFLINE PASS (structural/wiring/dormant+parallel; sweep/UUID-регрессы целы).
> (4) Новая методика владельца «ТОП-ПОЖИРАТЕЛЬ → ∞» (2026-09-19 ~00:51 +08) интегрирована: absorb_s7158_leg3.py строит взаимоисключающий ТОП по оси (а) CPU% (2 уровня: лейны → декомпозиция entity-фазы), оси (б) young GC (паузы/total/worst), оси (в) TPS/MSPT; порядок атак строго сверху вниз; мелкие 2-3% середины списка не трогаются (директива возвращает дисциплину ТОПа; очередь «ТРОГАЕМ ВСЁ» подчинена порядку ТОПа — микро-лейны атакуются, только когда поднимутся в ТОПе). ТОП leg #3: entity-фаза 51.98% → GC/JIT 39.25% → tracker 2.57%; ТОП-1 = entity-фаза, REGION-THREADS = её атака (экономика leg #2: +66.7%).
> (5) NEXT (S7-159 absorb, следующий тик): absorb leg #4 (35379431410, head bc77141) — гейты PG2 (0 NCDFE + pop 150k VALID + ARMED полный с Entity rc=0) / PG3 (TPS ≥ +25%) / PG4 (young GC ≤ 135) / 0 tracker-NPE + 0 uuid-dup; PASS → leg #5 (сэмпл №2) → банкование REGION-THREADS (CUMULATIVE v2 = inside_cache+flush_diet+region_threads=4). Watchlist leg #4: navigatingMobs sendBlockUpdated:1883 (не-фатален; >1/час → S7-160 мост). INJECTS-ONLY цел: за тик 0 CI-бутов (leg #3 = брак-лег без сбоя JVM, артефакт полный; leg #4 = preregister A/B).

> **СТАТУС 20 TPS (S7-159, leg #4 absorb, 35379431410) — ФИКС ПОДТВЕРЖДЁН ЖИВЬЁМ: ARMED ПОЛНЫЙ (rc ServerLevel=0 EntityCallbacks=0 Level=0 ChunkMap=0 Entity=0), offload 74.3%, TPS медиана 1.60 = +77.8% (PG3 PASS, лучший лег эры), 0 tracker-NPE + 0 uuid-dup (фиксы S7-158b/d живьём целы), 0 NCDFE; PG4-strict FAIL (169 > 136) — банкинг на leg #4 НЕ проводится, preregister-амендмент PG4' объявлен ДО leg #5 (35381522360).**
> (1) Валидность: head bc77141; конфиг бит-в-бит (region_threads=4 + inside_cache + flush_diet); INJECT 150000 VALID; ARMED полный с Entity=0 — маркер «pristine sighting …/Entity» подтверждён (урок S7-159 закрыт живьём); TrackerTickOps live 2629 сэмплов (sweep работает), RngOps 0 (UUID редок в cpu-профиле — ожидаемо); watchlist navigatingMobs = 0.
> (2) PG4-калибровка (данные, не подгонка): leg #3 = ваниль-класс конфигурация (хук спал) дал 140 young GC > капа 136 при TPS 0.8 — абсолютный кап НИЖЕ рантайм-варианс ванильных ранов (база 118 vs 140 при той же цепочке); Region-THREADS выполняет 1.78× работы/сек — нормировка GC-на-работу: база 131.1, leg #2 103.3 (-21.2%), leg #4 105.6 (-19.5%) — диета S7-158c работает, долга нет (full=0, worst 185.7ms vs база 179.2, пауза-доля 7.2% wall суб-линейна к работе). Абсолютный счётчик штрафует рычаг за выполненную работу.
> (3) PG4' (preregister до leg #5): young_gc/TPS_median ≤ 150.8 (база×1.15) И worst ≤ 224ms И full=0; leg #4 = PASS (105.6/185.7/0). Банкинг = PG2 + PG3 (оба лега) + PG4' (оба лега) + 0 инцидентов; строгая публикация PG4 сохраняется в каждом absorb.
> (4) ТОП leg #4 (ось CPU): entity-фаза 55.87% (residual 40.7% фазы, broadphase 21.3%, fluid 16.5%, movement 8.6%, AI 8.3%, nav 4.6%) → GC/JIT 37.60% → tracker 1.99%. После банкования — свежий профиль leg #5, RECON residual-подлейна (baseTick/SynchedEntityData/sync) против 3×REFUTED fluid и 2×REFUTED broadphase-кэшей.
> (5) NEXT (S7-159 banking, этот тик): absorb leg #5 (35381522360, head d9df743) → при PG2+PG3+PG4' PASS на обоих легах — банкование REGION-THREADS (CUMULATIVE v2 = inside_cache=1+flush_diet=1+region_threads=4, явные inputs; дефолты раннера не трогаются, config-wins не используется). INJECTS-ONLY цел (CI-бутов за тик 0 сверх preregister A/B легов).

> **СТАТУС 20 TPS (S7-159 ФИНАЛ, банкование) — REGION-THREADS ЗАБАНКОВАН: CUMULATIVE v2 = inside_cache=1+flush_diet=1+region_threads=4. Min-of-2 полный PASS: leg #4 (35379431410) TPS +77.8% / leg #5 (35381522360) TPS +166.7% (медианы 1.60/2.40 vs база 0.90; crawl leg #5 достигал 2.5), ARMED полный на обоих (rc …ChunkMap=0 Entity=0), offload 74.3%/74.8%, 0 NCDFE, 0 tracker-NPE, 0 uuid-dup, PG4' PASS (105.6/75.0 ≤ 150.8). Первый рычаг эры с материальным TPS-сдвигом (×2-2.7 медианы).**
> (1) Банкование: явные inputs на каждый последующий лег (дефолты раннера не меняются, config-wins не используется); все будущие A/B сравниваются против CUMULATIVE v2. Строгая запись PG4 (абсолютный кап) публикуется в каждом absorb (169/180 > 136); амендмент PG4' (GC-на-работу + worst-pause + 0 full) обоснован в ABSORB_S7159_LEG4.md и объявлен до leg #5: кап 136 ниже ваниль-варианс (leg #3 без region_threads дал 140) и штрафует throughput 1.78×.
> (2) Фиксы S7-158b/d подтверждены живьём на двух легах: 0 tracker-NPE (removal-safe sweep, TrackerTickOps 2629/3115 сэмплов) и 0 uuid-dup (RngOps serialized seeding); navigatingMobs watchlist = 0 на обоих легах.
> (3) Свежий ТОП пожирателей CUMULATIVE v2 (leg #5, оси CPU/GC/MSPT): ТОП-1 = entity-фаза 53.89% (residual 39.1% фазы ≈ 21% CPU — крупнейший неразложенный под-лейн; broadphase 19.9% — 2×REFUTED кэш; fluid 17.0% — 3×REFUTED кэш; AI 11.0%; movement 8.1%; nav 4.9%); ТОП-2 = GC/JIT-native 40.88% (мост zero-alloc, JVM-флаги запрещены — рычаг закрыт); tracker 2.22%.
> (4) NEXT (S7-160): RECON-3 residual-подлейна (baseTick/SynchedEntityData/paletted-чтения/tick-оркестрация; leaf-кандидаты: SynchedEntityData$DataItem.getValue ~1.2%, PalettedContainer.get ~2.9%) → preregister атаки крупнейшего attackable под-лейна рычагом НЕ-кэш-класса (батчинг чтений, O(1)-индексы, layout); затем возврат к ТОП-1 по кругу («ТОП-ПОЖИРАТЕЛЬ → ∞»). INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-160 preregister, ДО диспатча) — RECON-3 вскрыл residual: крупнейший связный под-лейн = checkInsideBlocks/inside-effects пайплайн (~7.8k сэмплов ≈ 6% CPU ≈ 11.2% entity-фазы ≈ 28% residual); рычаг #8 BATCH-COLLECTOR (zero-map StepBasedCollector) объявлен; leg (v2 + batch_collector=1) диспатчен.**
> (1) RECON-3 (recon3_s7160.py, RECON3_S7160.out, двойной артефакт база v1 35330129145 + leg #5 35381522360): residual 27319 сэмплов (21.06% CPU, 39.1% фазы, worker-share 75.5%) decomposed по deepest-MC-фрейму: inside-pipeline (forEachBlockIntersectedBetween 1791 + flushStep 1654 + lambda$checkInsideBlocks$2 1118 + applyEffectsFromBlocks 949 + BlockPos$6 491 + advanceStep 489 + checkInsideBlocks 445 + BlockPos$4 231 + traversal-хвосты) — ПОЗИЦИОННО-НЕЗАВИСИМАЯ семья, не покрывается inside_cache (он обслуживает только статику); далее fluid-residual 1.74%, data-sync 1.44%, tick-оркестрация 1.20%, oldpos 0.41% (все <5% — очередь «ТРОГАЕМ ВСЁ» после пайплайна). Alloc-ось (alloc_top_s7160.py): inside-pipeline = 27.38% young-gen (LongOpenHashSet.<init> per-check 3.37%, FluidState.getAABB 2.87%, BlockPos$6 2.53%, AABB.deflate 1.66%) — питает ТОП-2 GC/JIT 40.88%.
> (2) Рычаг #8 BATCH-COLLECTOR (S7-160): zero-map/flat замена ванильного StepBasedCollector в Entity.insideEffectCollector — flushStep = плоский ORDER-цикл вместо 3 EnumMap-операций × APPLY_ORDER на каждый step-переход (~60 map-оп/сущность/тик ≈ 9M/тек на 150k), ноль RecordedEffect/BlockPos.immutable аллокаций (long-packed позиции). ПОЗИЦИОННО-НЕЗАВИСИМ (не кэш — работает для движущихся, класс проваленных 3× fluid-кэшей). Swap = ленивый раз-на-сущность Unsafe putObjectVolatile в RegionTickOps.tickBucket ДО ванильного consumer'а (единая точка входа тика; весь эпизод observes один коллектор — seam исключён). DEFINE-ONLY wiring (batch_collector.rs): ядро не патчится, ретрансформа нет, PG1-дайджест не затронут.
> (3) Верификация до диспатча: cargo suite 147/0/1; BatchCollectorHarness OFFLINE PASS — 6000 рандом-сценариев advanceStep/apply/runBefore/runAfter, flushStep-очереди бит-в-бит (EFFECT type/pos + CONS порядок) vs ваниль (javap-контракты сняты: RecordedEffect.accept = applier.affect(entity,pos); APPLY_ORDER = values(); apply-NPE без advanceStep = контракт учтён в генераторе).
> (4) Preregister гейты absorb (vs leg #5 35381522360 = CUMULATIVE v2): PG2 = 0 NCDFE + ARMED (вся v2-цепь rc=0 + «batch_collector: defined …kernel loader» + «ARMED first-swap») + pop 150k VALID; PG3 = НОН-РЕГРЕССИЯ TPS-медиана ≥ 1.60 (худший банковый лег; ожидание +2..+8%); PG4'' = young GC ≤ 180 (уровень leg #5) И collector-семья CPU (flushStep+advanceStep+applyAndClear-листы) ≤ 55% leg #5 (2143 → ≤ ~1180; ожидание −60..−80%); CRASH-FREE = 0 tracker-NPE + 0 uuid-dup + navigatingMobs watchlist. Банкинг: полный PASS → CUMULATIVE v3 = v2+batch_collector=1; FAIL → REFUTED-BY-ECONOMICS + rollback batch_collector=0 (fail-closed: гейт env, v2 остаётся банком). INJECTS-ONLY цел (CI-бут = preregister A/B-лег).

> **СТАТУС 20 TPS (S7-160 absorb, 35387310239) — BATCH-COLLECTOR REFUTED-BY-ECONOMICS: прямой выигрыш рычага подтверждён живьём (flushStep −34% per-work, advanceStep −83%, RecordedEffect eliminated), но свап-инфраструктура не удерживается между тиками (BatchCollector.<init> 728 + ensure 713 сэмплов при нулевой ротации сущностей) → per-work collector-family +26% (ваниль-варианс ±0.4%) → PG4'' FAIL → rollback batch_collector=0; CUMULATIVE v2 остаётся банком.**
> (1) Гейты: PG2 PASS (ARMED оба маркера, pop 150k VALID, 0 NCDFE); PG3 формально PASS (last-5 медиана 1.60 на границе, хвост 2.0 vs 2.5 — в вариан leg#4/leg#5 на идентичной v2); PG4'' FAIL (это приговор по preregister); CRASH-FREE PASS; young GC 161 — ЛУЧШЕ leg#5 (180, −10.6%) — диета RecordedEffect/immutable живьём видна.
> (2) Root-cause: Entity.insideEffectCollector — private final, писатель ядра единственный (ctor NEW-сайт javap census); ленивый Unsafe putObjectVolatile в RegionTickOps.tickBucket не удерживает объект между тиками (механизм C2/final-полей — живой диагноз S7-161). Ротация ≈ 0 (Entity.<init> = 4 сэмпла) — свапы повторяются на ТЕХ ЖЕ сущностях.
> (3) Урок эры (4-й о живой верификации): оффлайн-харнесс проверяет СЕМАНТИКУ очереди (6000 сценариев бит-в-бит), но НЕ ЖИЗНЬ свапа в JIT-среде; счётчик свапов в stdout не печатался (только first-swap) — не хватило прямой телеметрии для раннего корень-казуса.
> (4) NEXT (S7-161): гарантированно стойкий свап = rust-ретаргет NEW-сайта StepBasedCollector в Entity.<init> → BatchCollector (класс рычага санкционирован — RngOps-прецедент; classref+ctor-дескриптор совпадают, probe-гейт, Entity rc=0); инфра 1441 → ~0, ожидание −40..−50% collector-семьи. Затем: residual-хвост (все под-лейны <5%) и возврат к ТОП-1 по кругу «ТОП-ПОЖИРАТЕЛЬ → ∞». INJECTS-ONLY цел (1 CI-лег = preregister A/B).

> **СТАТУС 20 TPS (S7-161 preregister, ДО диспатча) — BATCH-COLLECTOR v2: ctor-уровневый ретаргет вместо нежизнеспособного ленивого свапа; железное доказательство композиции Entity-хуков (leg#5 лог 886/895) найдено и учтено; leg (v2 + batch_collector=1) диспатчен.**
> (1) S7-160 root-cause closed: ленивый Unsafe-свап в final-поле retired из горячего пути (не удерживается между тиками при нулевой ротации). Новый механизм = classfile::patch_entity_collector_ctor: единственный NEW-сайт StepBasedCollector+dup+invokespecial в Entity.<init>(EntityType,Level) переписывается на Class/Methodref(BatchCollector) (CP-growth, same ()V-форма, putfield-subsumption безопасна, dup не тронут) — persistent by construction; популяция инжектится ПОСЛЕ армирования → ~100% оконного покрытия.
> (2) НАХОДКА КОМПОЗИЦИИ (S7-162): leg#5 лог доказывает, что два Entity-хука (inside_cache и region_threads) СУПЕРСЕДЯТ друг друга: region-патч строился от ПРИСТИНА 205458 → 205494 (rng-only), inside-байты 205522 superseded → inside_cache-гейт в v2, вероятно, НЕАКТИВЕН (никто не заметил — гейт дешёв; банкование v2 честно — измерено как есть). Фикс ко-существования — S7-162 (одна compose-цепочка на класс). СЕЙЧАС batch-патч едет в region-цепь (после rng) — последний писатель несёт batch.
> (3) Верификация: suite 149/0/1 (+2 теста: ctor-ретаргет ровно 1 сайт + NEW-операнд резолвится в BatchCollector по имени + idempotent AlreadyPatched + wrong-class fail-closed); харнесс 6000 сценариев бит-в-бит (S7-160, не менялся).
> (4) Preregister гейты absorb (vs leg#5 = CUMULATIVE v2): PG2 = 0 NCDFE + pop 150k VALID + «batch_collector: defined» + «Entity collector-ctor retarget composed (Retargeted { sites: 1 })» + v2-цепь rc=0 (маркер first-swap НЕ ожидается — все сущности рождаются с Batch; отсутствие = положительный сигнал); PG3 = TPS last-5 медиана ≥ 1.60; PG4'' = young GC ≤ 180 И collector-family ≤ 55% leg#5 (2345 → ≤1290; ожидание −40..−50% с исчезновением инфры 1441); CRASH-FREE прежние. Банкинг: полный PASS → CUMULATIVE v3 = v2+batch_collector=1; FAIL → REFUTED + rollback batch_collector=0. INJECTS-ONLY цел (CI-лег = preregister A/B).

> **СТАТУС 20 TPS (S7-161 absorb, 35391679176) — BATCH-COLLECTOR REFUTED-BY-ECONOMICS ВТОРОЙ РАЗ (ctor-ретаргет): методы доказанно быстрее (flushStep −41% per-work, advanceStep −89%, RecordedEffect→0), но инфра-хвост (BatchCollector.<init> 801 + ensure 737) снова перевешивает → per-work +37% → PG3 (1.50<1.60) + PG4'' (2836>1290) FAIL → rollback batch_collector=0; v2 остаётся банком. Рычаг закрыт с вердиктом.**
> (1) Факты: compose подтверждён живьём (Entity 205458 → 205546 = rng+batch, «collector-ctor retarget composed (Retargeted{1})»); паритет цел (0 NCDFE/0 NPE/0 uuid-dup/pop 150k VALID); GC 161 = leg#5-уровню. first-swap = единственный легитимный ensure-свап старой системной сущности (eid=4).
> (2) Открытые вопросы → S7-162: (a) природа 801 <init>-сэмплов при инжекте вне окна и нулевой ваниль-ротации (гипотеза: непрерывный натуральный спавн-поток живой сцены + тяжёлый ctor; телеметрия INSTANCES ответит); (b) удалить ensure-вызов из tickBucket (737 сэмплов чистого фонового гейта); (c) ЕДИНАЯ compose-цепочка Entity в одном hook'е (inside→fluid_free→fluid_dirty→rng→batch) — в этом ране порядок активаций случайно дал composed-итог, в leg#5 — supersede (rng-only, inside-гейт вероятно потерян).
> (3) Экономическая фиксация: обе попытки доставки (ленивый Unsafe-свап, ctor-ретаргет) REFUTED по профилю; семантика харнессом доказана бит-в-бит (6000 сценариев). Повторная атака collector-семьи возможна ТОЛЬКО после устранения инфра-хвоста (<10% семьи) — через единый compose + телеметрию.
> (4) NEXT (S7-162): единая compose-цепочка Entity + INSTANCES-телеметрия + ensure-удаление; свежий ТОП рана: entity-фаза 56.6% (inside-pipeline traversal 20.4% фазы, broadphase 18.7% — 2×REFUTED, fluid 16.8% — 3×REFUTED, AI 8.3%, movement 8.2%, nav 4.9%) → GC/JIT 36.6% → tracker ~2%; attackable ≥5% под-лейн = traversal-часть inside-pipeline (плоский обход вместо guava-итератора; высокий parity-риск — нужен javap-контракт DirectionalIterator + lockstep). Возврат к ТОП-1 по кругу. INJECTS-ONLY цел (1 CI-лег = preregister A/B).

> **СТАТУС 20 TPS (S7-162 preregister, ДО диспатча) — ЕДИНАЯ COMPOSE-ЦЕПОЧКА ENTITY (entity_compose) + RETIREMENT ENSURE + INSTANCES-ТЕЛЕМЕТРИЯ реализованы кодово (150/0/1 + 2 харнесса OFFLINE PASS); железное доказательство инфра-хвоста S7-161: ВСЕ 801 BatchCollector.<init>-сэмплов идут через BatchCollector.ensure из tickBucket (стеки cpu-collapsed 35391679176) — свап повторно конструирует коллектор тем же pre-arm сущностям КАЖДЫЙ тик; leg (v3-кандидат = v2 + compose + batch_collector=1 + no-ensure) диспатчен.**
> (1) S7-161 root-cause closed стеками: `<init>` 801 = 100% caller BatchCollector.ensure (natural spawn-flow = 0 ctor-сэмплов), ensure 737 = per-entity instanceof-гейт 40M вызовов/окно; обе статьи — НАША инфраструктура, не ваниль. Удаление ensure из tickBucket убивает весь хвост (801+737), tickBucket снова ваниль-идентичен.
> (2) entity_compose (новый модуль, единый владелец Entity-байтов): 5 стадий строго в порядке inside → fluid_free → fluid_dirty → rng → batch НА ОДНОМ буфере (supersede-механика leg#5 886/895 устранена архитектурно — два хука на один класс больше не существуют); каждая стадия strict по своим сайтам, fail-dominant (провал стадии = цепь без неё, loud WARN; провал rng публикует вердикт, по которому region_threads остаётся dormant — семантика «rng-провал убивает region» сохранена без дедлока); Entity retransform РОВНО один. Побочный эффект по устранению supersede: внутри-гейт inside_cache, мёртвый в v2-банке с leg#4/leg#5, ВОСКРЕСАЕТ в этой цепи — лег измеряет БАНДЛ (revived-inside + batch-noensure), это задекларировано.
> (3) Телеметрия: BatchCollector.INSTANCES (AtomicLong в ctor) печатается RegionTickOps.forEach каждые 600 тиков («batch_collector: telemetry tick=N instances=M workers=W») — закрывает открытый вопрос (a) S7-161 (природа ctor-вызовов: спавн-поток vs скрытый цикл) будущими данными; маркер «first-swap» больше НЕ ожидается (ensure retired). swap-машинерия (Unsafe/reflection) удалена из BatchCollector полностью.
> (4) Верификация: cargo suite 150/0/1 (+entity_compose_chain_inside_rng_batch_composes_strictly: полная цепочка inside→rng→batch на реальной фикстуре Entity, каждая стадия strict sites=1, композит парсится, внутри-репатч идемпотентен); RegionThreadsHarness OFFLINE PASS (structural/wiring/dormant+parallel, sweep/UUID-регрессы целы); BatchCollectorHarness PASS 4000 сценариев бит-в-бит (EFFECT type/pos + CONS порядок).
> (5) Preregister гейты absorb (vs leg#5 = CUMULATIVE v2, 35381522360): PG2 = 0 NCDFE + pop 150k VALID + «entity_compose: ARMED chain [inside->rng->batch], retransform rc=0» + «region_threads: ARMED … ChunkMap=0» + «batch_collector: defined» + telemetry-строки присутствуют + first-swap ОТСУТСТВУЕТ; PG3 = TPS last-5 медиана ≥ 1.60 (ожидание +2..+10%: revived-inside + collector-методы); PG4''' = young GC ≤ 180 И collector-family per-work ≤ 1290 (55% leg#5 2345) И инфра-хвост (BatchCollector.<init> + ensure) ≤ 10% семьи (условие реванша S7-161; ожидание ≈0); CRASH-FREE прежние (0 tracker-NPE, 0 uuid-dup, navigatingMobs). Банкинг: полный PASS → CUMULATIVE v3 = v2 + batch_collector=1 (+compose как инфраструктура, не конфиг); FAIL → REFUTED финально для collector-семьи + rollback batch_collector=0 (compose остаётся как инфраструктурный фикс в любом исходе). INJECTS-ONLY цел (1 CI-лег = preregister A/B).

---

> **СТАТУС 20 TPS (S7-162 absorb leg#1, 35395826385) — ЛЕГА #1 = БРАК (DUD, средовой инцидент): сервер убит watchdog'ом во время инжекта популяции ДО профайл-окна; все гейты неизмеримы; редиспатч леги #2 с теми же пререг-входами; харденинг харнесса (timeout-time=86400).**
> (1) Таймлайн: 21:25:00 UTC watchdog-дампы на форслоад-тике (синхронная загрузка 9216 чанков >60с — ЧИСТО ванильная фаза без сущностей, компоуз ещё не заармлен: дамп = строка 267 лога, ARMED = 984/999) → средовой столл, не рычаг; 21:25:21 INJECT START (loadedChunks=9954/10000, farmClusters=497/500 — форслоад неполный, второй симптом медленного раннера) → 21:26:26 «Stopping server» = watchdog hard-stop; сценарий ждал INJECT DONE до 900с → fixture-гейты FAIL → workflow exit 1.
> (2) Профиль гибнущего сервера (96604 сэмпла = только хвост инжекта + stopServer): 95.4% = Reference2IntOpenHashMap.{find/shiftKeys} на путях moonrise-регистрации (ReferenceList.add←spawnItem ~48k; ReferenceList.remove←stopServer-watchdog ~43k). НЕ атрибутировать рычагу: в здоровых легах фаза инжекта (56-69с, ~374µs/регистрацию) никогда не входила в профайл-окно — её стоимость того же порядка (нормальная стоимость инжекта), впервые увиденная из-за гибели до окна. Открытое наблюдение (не ТОП, не фаза): moonrise identity-map доминирует инжект — материал отдельного рекона, если когда-нибудь станет измеримой фазой.
> (3) Инженерные маркеры PG2 живы до гибели: «entity_compose: ARMED chain [inside->rng->batch], 205458 -> 205610 bytes, retransform rc=0» + «region_threads: ARMED … ChunkMap=0» + «batch_collector: defined» + 0 NCDFE / 0 NPE / 0 uuid-dup. Телеметрия INSTANCES не успела (тик-луп сделал единицы тиков).
> (4) Харденинг харнесса (run_world3.sh, bench-only): spigot.yml settings.timeout-time=86400 перед бутом (monitor-only; тики измеримого окна 0.4-1.2с — на два порядка ниже капа). Watchdog-килл = ковариата харнесса (e2e-прецедент учёта watchdog-fire), не ванильное поведение движка.
> (5) Следствия: лега #1 исключается из выборки S7-162 (прецедент S7-158 leg#3); редиспатч леги #2 этим же тиком (dispatch_s7162.py, те же пререг-входы); absorb леги #2 — следующий тик по гейтам PG2/PG3/PG4'''/CRASH-FREE без изменений; банкинг-правило прежнее (PASS → CUMULATIVE v3; FAIL → REFUTED финально + rollback batch_collector=0, compose остаётся инфраструктурой).

> **СТАТУС 20 TPS (S7-162 absorb leg#2, 35399980345) — БАНКОВАНИЕ CUMULATIVE v3 = v2 + batch_collector=1: ВСЕ ПРЕРЕГИСТЕР-ГЕЙТЫ PASS (PG2/PG3/PG4'''/CRASH-FREE), условие реванша S7-161 выполнено (инфра-хвост 0.0%), collector-семья −47% raw. Телеметрия INSTANCES закрыла вопрос S7-161(a): рост 153298→161310 за 1800 тиков = натуральный спавн-поток живой сцены.**
> (1) Гейты: PG2 PASS (0 NCDFE, pop 150000 VALID, «entity_compose: ARMED chain [inside->rng->batch] 205458 -> 205610 bytes, retransform rc=0», «region_threads: ARMED ... ChunkMap=0», «batch_collector: defined», telemetry tick=600/1200/1800 instances=153298/155713/161310 workers=4, first-swap ОТСУТСТВУЕТ); PG3 PASS (TPS last-5 медиана 1.90, кап ≥1.60); PG4''' PASS (young GC 154 ≤ 180; collector-family 2345→1251 raw = −46.7%, per-work 658 ≤ 1290; инфра-хвост <init>=0 + ensure=0 = 0.0% ≤ 10%); CRASH-FREE PASS (0 tracker-NPE, 0 uuid-dup, navmob=0).
> (2) Методы: flushStep 1657→857 (−48%), advanceStep 491→110 (−78%), apply/applyAndClear 1311→1165 (−11%), RecordedEffect 34→1 — прямая выгода рычага №8 подтверждена живьём на чистой инфраструктуре (ensure-цикл мёртв: 801+737→0).
> (3) Телеметрия INSTANCES: instances растут 153298→161310 за 1800 тиков (~+800/600 тиков) = натуральный спавн-поток живой сцены; природа 801 ctor-сэмплов S7-161 = спавн-поток (гипотеза (a) закрыта живьём).
> (4) Банкование: CUMULATIVE v3 = inside_cache=1 + flush_diet=1 + region_threads=4 + batch_collector=1; все будущие A/B сравниваются против v3 (leg #2 35399980345 = базовый лег v3). entity_compose = постоянная инфраструктура эры (не конфиг).
> (5) Свежий ТОП leg#2 (ось CPU, 128124 сэмплов): entity-фаза 59.06% (main-server-tick-other 20.9% фазы, broadphase 18.8% — 2×REFUTED, fluid 17.9% — 3×REFUTED, inside-pipeline 17.1% — АТАБУЛЬНЫЙ, movement 8.4%, AI 8.3%, nav 4.9%) → GC/JIT 35.49% → tracker 2.08%. ТОП-1 attackable под-лейн = inside-pipeline traversal (RECON-5 javap-контракт снят бит-в-бит, рычаг #9 FLAT-TRAVERSAL).
> (6) NEXT (S7-163, этот тик): реализация TraverseOps.forEachFlat (плоский (f,s,t)-обход вместо guava-итераторов BlockPos$6/BlockPos$4, open-addressing long-dedupe вместо LongOpenHashSet, AABB.clip статик звать вербатим) + TraverseLockstepHarness (бит-в-бит оракул) + entity_compose stage 6 (traversal) + прeregister гейты + диспатч. INJECTS-ONLY цел.

> **СТАТУС 20 TPS (S7-163 preregister, ДО диспатча) — FLAT-TRAVERSAL (рычаг #9) реализован и верифицирован: TraverseOps.forEachFlat (плоский (f,s,t)-обход, open-addressing long-dedupe, без guava-итераторов/LongOpenHashSet/Vec3-AABB-времен) + TraverseLockstepHarness 60106 сценариев × 4 visitor-политики бит-в-бит (последовательность (posLong,step) + return + короткие замыкания) + cargo suite 155/0/1 (+5 traversal-тестов вкл. композит идемпотентность полной цепи) — лег (v3 + flat_traversal=1) диспатчен.**
> (1) Харнесс-оракул поймал ЛОВУШКУ КОНТРАКТА: getFurthestCorner в RECON5-доке был прочитан с инверсией ВСЕХ трёх компонент; вербатим javap (и рефлексия: (0,0,+7.75) → (−1,−1,+1)): **(−sx,−sz,+sy) / (+sz,−sy,−sx) / (−sy,+sx,−sz)** — последний компонент каждой ветки НЕ инвертируется. Без правки харнесс дал 9 расхождений на 112 сценариях (диагностика: боксы ванили = {cell, cell+1}, hit на дальней грани); после правки — 0 расхождений на 60106 сценариях.
> (2) Верbatim-ловушки, зафиксированные в контракте и харнессе: getCenter = Mth.lerp(0.5, min, max) = max + 0.5*(min−max) (РЕВЕРС-форма, не (min+max)/2); стационарный путь = BlockPos$4 index/%-итерация (X внутренняя, счётчик extX*extY*extZ, int-переполнение паритетно); corner-итератор = Y внешний, (|dx|<|dz|) ? YZX : YXZ, старт по знаку дельты, extents+1 включительно; DDA-цикл продолжается пока ЛЮБОЙ tMax ≤ 1; выбор оси строго-меньше (X iff tMaxX<tMaxY && tMaxX<tMaxZ); clamp low = (double)((float)cell + 1.0E-5f) (FLOAT-сложение), high = (cell+1.0) − 9.999999747378752E-6; stepX = sign==0 ? Double.MAX_VALUE : sign/delta.x; AABB.clip статик звать вербатим (публичный API — бит-в-бит по построению; inline-clip = отдельный последующий squeezing внутри рычага).
> (3) Доставка: entity_compose stage 6 (traversal) — ретаргет единственного invokestatic BlockGetter.forEachBlockIntersectedBetween сайта в Entity.checkInsideBlocks(Vec3,Vec3,SBC,LongSet,int)I → TraverseOps.forEachFlat (тот же дескриптор; strict sites=1; traverse_ops defined в kernel loader + BRIDGE_READY + wait_bridge_ready, прецедент batch_collector.rs); fail-dominant (провал стадии = цепь без traversal, громкий WARN); ARMED chain [inside->rng->batch->traversal]; audit_wire entity_compose v2.
> (4) Preregister гейты absorb (vs leg #2 35399980345 = CUMULATIVE v3): PG2 = 0 NCDFE + pop 150k VALID + «entity_compose: ARMED chain [inside->rng->batch->traversal], retransform rc=0» + «region_threads: ARMED ... ChunkMap=0» + «traverse_ops: defined» + «batch_collector: defined» + telemetry присутствует; PG3 = TPS last-5 медиана ≥ 1.60 (худший банковый лег; ожидание +2..+8%); PG4 = traversal-лейн (forEachBlockIntersectedBetween family: 9286 сэмплов = 7.27% CPU в leg#5-профиле) снижен ≥50% per-work; orchestration-хвост (guava + BlockPos-итераторы + LongOpenHashSet-init ~2900-3400) снижен ≥70%; young GC ≤ 154 (банк v3); CRASH-FREE = 0 tracker-NPE + 0 uuid-dup + navigatingMobs watchlist. Банкинг: полный PASS → CUMULATIVE v4 = v3 + flat_traversal=1; FAIL → REFUTED + rollback flat_traversal=0 (TraverseOps остаётся инфраструктурой). collidedWithFluid/getBlockState (2541 сэмплов под visit-лямбдой) — СЛЕДУЮЩИЙ слой, в #9 не трогается (кэш-классы там REFUTED ×3).
> (5) Артефакты: entityinside/net/minecraft/world/level/TraverseOps.java (+ build); entityinside/harness/TraverseLockstepHarness.java; scripts/build_traverse_ops.sh; scripts/run_traverse_lockstep_harness.sh; scripts/bench4_recon/dispatch_s7163.py; src/traversal.rs; src/entity_compose.rs (stage 6); src/classfile.rs (patch_entity_traversal + 5 тестов); research/inside-pipeline-2026-09-19/{CONTRACT_EXTRA_S7163.txt, CONTRACT_CLIP_S7163.txt, CONTRACT_MTH_S7163.txt}. INJECTS-ONLY цел (1 CI-лег = preregister A/B).

> **СТАТУС 20 TPS (S7-163 absorb leg#1, 35407788083) — ЛЕГА #1 = TECH-DUD (агентский дефект доставки, не среда, не REFUTED): сервер упал на инжекте (00:09:18 UTC, ~12k/150k) с NoClassDefFoundError: TraverseOps$LongTable — вложенный класс open-addressing-таблицы компилировался в отдельный classfile, но не определялся в kernel loader; ARMED-цепь [inside->rng->batch->traversal] rc=0 жива до падения; гейты неизмеримы (до окна).**
> (1) Корень: traversal.rs embed'ил только верхнеуровневый TraverseOps.class; первый же NEW LongTable внутри forEachFlat резолвится против kernel loader → ClassNotFoundException → NCDFE-шторм на каждой сущности с блоками под боксом → crash («Exception while updating neighbours», fastutil-NPE — вторичная амортизация шторма). Оффлайн-локстеп-оракул слеп к этой категории по построению: обычный classpath резолвит вложенные классы неявно.
> (2) Вердикт TECH-DUD: экономика рычага НЕ измерена — REFUTED не выставляется; средовой DUD исключён (стек точно в наш класс, 100% атрибуция). Алгоритм цел: classfile-ы байт-в-байт воспроизводимы (35192a1b/7157df0c), оракул 60106×4 PASS после пересборки.
> (3) Фикс доставки (этот же тик): TRAVERSE_NESTED в traversal.rs — вложенные классы embed'ятся и определяются в тот же loader ДО BRIDGE_READY (fail-closed: любой провал define → dormant); cargo-гварды: source-parse (каждый вложенный класс исходника обязан быть embedded) + build-dir set == embedded set — сьют 157/0/1; build_traverse_ops.sh барьер «nested-delivery guard 2/2».
> (4) Урок эры (5-й, о доставке): kernel-loader доставка = доставка ГРАФА классов; множество produced classfiles обязано сверяться с embedded-множеством статическим guard'ом (CI-проверка loader-семантики — кандидат на будущее).
> (5) NEXT (этот же тик): редиспатч леги #2 (dispatch_s7163.py, те же прereg-входы, head = фикс-коммит); absorb — следующий тик по НЕИЗМЕННЫМ гейтам PG2/PG3/PG4/CRASH-FREE; новый PG2-маркер leg#2: «traverse_ops: defined … (+1 nested)» + «defined nested …TraverseOps$LongTable». INJECTS-ONLY цел (1 CI-лег за тик — санкционированный).

> **СТАТУС 20 TPS (S7-163 absorb leg#2, 35410873485) — ВЕРДИКТ FAIL по preregister-гейту PG4a: CUMULATIVE v4 НЕ банкуется, flat_traversal=0 (банк = v3), TraverseOps остаётся инфраструктурой. PG2 PASS (nested-доставка жива: «defined nested …TraverseOps$LongTable» + «(+1 nested)» + ARMED [inside->rng->batch->traversal] rc=0, 0 NCDFE, pop 150k VALID), PG3 PASS (TPS last-5 медиана 1.700 ≥ 1.60; vs v3 1.800 = −5.6%, шум n=1/1), CRASH-FREE PASS (0 crash/0 Full; 5 отловленных «Entity threw exception» = исторический шум параллельного тика 0–5/ран, стек идентичен s7161: sendBlockUpdated/fastutil, не traversal).**
> (1) PG4a FAIL: traversal-лейн 9572 → 7314 сэмплов (−23.6%; per-work −19.1%) при пороге ≥ −50%. PG4b PASS: guava-хвост (BlockPos$6/$4) 1168 → 2 = **−99.8%**; PG4c PASS: young GC 129 ≤ 154 (−16%), total STW 21.13s → 16.99s (−19.6%), медиана паузы 152.6ms.
> (2) Разбор (урок эры №6): порог −50% был откалиброван на leg#5-профиле (хвост 2900–3400 из 9286 = 31–37%), но в v3-профиле orchestration была уже 1168/9572 = 12% (её часть съели inside_cache/batch) — порог архитектурно недостижим на свежем банке. Масса лейна — visit-обработка, сохраняемая parity (getBlockState/PalettedContainer 487, visit-лямбда 401, flushStep 369, внешний vanilla LongSet.add 466, collision-temps). Рычаг в точке приложения сработал честно (−99.8% итераторов, −16% GC, −19.6% STW, alloc-семья 56.2%→31.4%), но ≤ порога банк-класса. Гейт пост-хок не переписывается.
> (3) Свежий ТОП leg#2 (ось a, 125610 сэмплов): entity-фаза **60.16%** → GC/JIT 33.11% (было 35.49%) → tracker 2.08%. Под-лейны entity-фазы (грубо): movement/AI-aiStep **20.33%**, broadphase 11.26% (2×REFUTED), fluid-push **9.76%**, inside-pipeline **8.00%**, item-entity 7.19%, unclassified **41.91%** (RECON-7 — следующий тик). Ось (b): alloc-семья AABB+Vec3+BlockPos 31.4% (было 56.2%), inside-pipeline 21.7% аллока. Ось (c): young 129, медиана 152.6ms.
> (4) NEXT: RECON-7 (разложение unclassified 41.9% + уточнение под-лейнов leg2) → по пересортировке ТОПа фиксация рычага #10 ZERO-ALLOC-INSIDE (fluid-push 9.76% + inside-pipeline 8.00% + collidedWithShapeMovingFrom-сайты в movement; двойной эффект CPU+alloc; collidedWithFluid/getBlockState под visit-лямбдой — назван «следующим слоем» в preregister S7-163) или крупнейшего ≥5% под-лейна → javap-контракт → локстеп-оракул → прeregister → диспатч. Пороги гейтов отныне калибруются ТОЛЬКО от свежего профиля лега-базы (следствие урока №6).
> (5) Артефакты: research/flat-traversal-2026-09-19/{ABSORB_S7163_LEG2.md, ABSORB_S7163_LEG2_raw.txt, absorb_s7163_leg2.py, entity_phase_leg2_raw.txt, run-s7163-leg2-artifact/}. CI-легов за тик 0 (только absorb).

> **СТАТУС 20 TPS (S7-164 preregister, ДО диспатча) — ZERO-ALLOC-INSIDE (рычаг #10) реализован и верифицирован: METHOD-BODY REDIRECT — новый механизм classfile.rs (замещение Code-атрибута целиком: aload-цепь + invokestatic + ireturn, receiver-prepended, length-preserving), Entity.collidedWithFluid/collidedWithShapeMovingFrom/updateFluidHeightAndDoFluidPushing → ZeroAllocOps скаляры; entity_compose stage 7 (строгая составная стадия sites==3); cargo 166/0/1 (+6); ZeroAllocLockstepHarness 350k бит-в-бит OFFLINE PASS (50k makeBox + 300k collidedAlongVector c полной лестницей getCenter/add/inflate/contains/clip); патченный Entity.class верифицирован HotSpot-верификатором (defineClass); лег (v4-кандидат = v3 + zero_alloc_inside=1, flat_traversal=0) диспатчен.**
> (1) Scope по census (v3-профиль 128124 сэмпла, урок №6-калибровка): collidedWithFluid 1749 (1.37%) ← только lambda$checkInsideBlocks$2+Recorder.visit; collidedWithShapeMovingFrom 687 (0.54%) ← только collidedWithFluid; collidedAlongVector 406 (0.32%) ← только collidedWithShapeMovingFrom ⇒ ТРИ Entity-редиректа захватывают весь collided-лейн (AABB-ретрансформация не нужна). updateFluid-семья 13523 (10.55%) — крупнейший под-лейн entity-фазы. Скаляризация fluid-AABB (Fluid.getAABB verbatim: isEmpty→null, FLOAT-сложение maxY) убивает и getAABB-нов (987).
> (2) ZeroAllocOps.java: не-кэширующие скалярные тела (double вместо AABB/Vec3/List; IEEE754 бит-в-бит, тот же control-flow и порядок операций; ваниль-статики Mth.lerp/Math.sqrt звать не переписывая); fluidHeight/lastLavaContact — Unsafe-офсеты (одноразовая инициализация; единственный способ приватных putfield/getfield семантики из внешнего моста); getFlow ОСТАЁТСЯ ванильным (1 Vec3/жидкий блок — названный «следующий слой» v2); NO nested classes (урок №5: source-parse + build-dir + embedded-bytes гварды ×3 в zero_alloc.rs).
> (3) Оракул поймал ловушку контракта повторно (урок №9-повтор): clipPoint dcmpg-лестница — «return dir iff 0.0>=t» — попадание требует t>0 (пересечение ВПЕРЕДИ точки); первая реализация инвертировала знак t — харнесс дал расхождения в обе стороны (true/false симметрично), фикс по dcmpg-семантике value1>=value2 → 350k PASS. makeBox-формула (FLOAT width/2f, FLOAT height-сложение) подтверждена 50k.
> (4) Preregister гейты absorb (vs leg #2 = CUMULATIVE v3, свежий профиль 35399980345/128124 — урок №6): PG2 = 0 NCDFE + pop 150k VALID + «stage zeroin composed (Retargeted { sites: 3 })» + «ARMED chain [inside->rng->batch->zeroin] rc=0» + «zero_alloc_ops: defined» + «region_threads: ARMED ... ChunkMap=0» + «batch_collector: defined» + telemetry; PG3 = TPS last-5 медиана ≥ 1.60; PG4a = collided-лейн (1749+687+406+987 = 3829) ≥ −50% per-work (ожидание −70..−80%); PG4b = fluid-push-лейн (13523) ≥ −5% per-work (ожидание −5..−10% прямых; getFlow 1.85% и ваниль-чтения остаются); PG4c = young GC ≤ 154 И alloc-семья AABB+Vec3 ≤ 36.1% от общего аллока (v3 40.08%, −10% отн.; ожидание −20..−30% отн.); CRASH-FREE = 0 crash/0 Full, «Entity threw exception» ≤ 0–5/ран (исторический шум). Банкинг: полный PASS → CUMULATIVE v4 = v3 + zero_alloc_inside=1; FAIL → REFUTED + rollback zero_alloc_inside=0 (ZeroAllocOps остаётся инфраструктурой). INJECTS-ONLY цел (1 CI-лег = preregister A/B).
> (5) Артефакты: entityinside/net/minecraft/world/level/ZeroAllocOps.java (+ build, РОВНО 1 classfile); entityinside/harness/ZeroAllocLockstepHarness.java (+ repro-режим); scripts/build_zeroalloc_ops.sh; scripts/run_zeroalloc_lockstep_harness.sh; scripts/bench4_recon/dispatch_s7164.py; src/zero_alloc.rs; src/classfile.rs (redirect_method_body_to_static + patch_entity_zeroalloc + 5 тестов); src/entity_compose.rs (stage 7); tests/fixtures/Entity_real.class.

> **СТАТУС 20 TPS (S7-164 absorb leg#1, 35417195790) — ЛЕГА #1 = TECH-DUD (дефект доставки редиректа, не REFUTED): 66 × NoSuchMethodError 'ZeroAllocOps.collidedWithShapeMovingFrom(Entity;Vec3;Vec3;List)' на первом вызове redirected-тела — ZeroAllocOps НЕ содержал статика-цели (тело было инлайнено в collidedWithFluid по census), патчер same-name сгенерировал invokestatic для всех sites:3, третий не резолвится. CRASH-FREE FAIL (71 «Entity threw exception» >> 0–5: 66 наш дефект + 1 исторический fastutil-шум + 4 голых NPE без стека, происхождение неустановимо — лега #2 ответит). Экономика НЕ измерена.**
> (1) Корень (урок №5, новая грань — МЕТОДНОЕ замыкание графа): defineClass-верификатор не резолвит методы (lazy), локстеп-оракул зовёт бридж из исходников (compile-time), cargo-тесты проверяют структуру Entity-патча, но не сверяют цели с classfile-ом бриджа → дефект класса «redirect-цель отсутствует в доставленном бридже» прошёл ВСЕ три барьера и детонировал живьём (guardEntityTick ловит, тик сущности обрывается — parity сломана, гейты неизмеримы).
> (2) Фикс (head e82d81f): ZeroAllocOps.collidedWithShapeMovingFrom(Entity,Vec3,Vec3,List) добавлен (вербатим ванили, скаляры = оракул-проверенные примитивы 50k+300k, новой математики нет); ZA_REDIRECT_TARGETS — единая таблица целей для патчера и гварда (дескрипторы не разъезжаются по построению); zeroalloc_resolution_closure(bridge) — сверка методной таблицы доставляемого classfile с целями; runtime-гвард fail-closed в activate() ДО define_class (провал → громкий dormant, сервер ванилен); cargo-тест zeroalloc_embedded_declares_all_redirect_targets (на до-фиксных байтах падает). Верификация: сьют 167/0/1, оракул 350k PASS, HotSpot-верификация редиректа.
> (3) Инцидент прозрачности: первый редиспатч ушёл на незапушенный head (8edcab5 дефектный) — ран 35418617801 отменён API (cancel 202) до бута, фикс запушен, лега #2 редиспатчена чисто. Урок: диспатч только после проверки remote-head == local-head.
> (4) RECON-8 (лега в полёте → S7-108): movement/AI-поверхность разложена до классов поведения (deepest-match, leg#2+v3 стабильно): fluid-push 10.32% (в полёте #10), inside-pipeline 9.68%, travel-physics 9.15% CPU + 13.87% alloc (НЕ атакован; alloc-diet S7-133 атаковал call-site контейнеры, не скаляры тел), broadphase 6.52% (2×REFUTED), entity-other 6.40% (гетерогенен, атакующих ≥5% нет), goal-selector 3.49% (чистая итерация, canUse RNG side-effects → parity-неприкасаем), navigation 3.29% (81% createPath, stroll-цели случайны → hit-rate≈0 риск fluid_dirty-урока), targeting 0.33%, sensing 1.34%. Вердикт: следующий не-атакованный ≥5% под-лейн = travel-physics → кандидат рычага #11 ZERO-ALLOC-TRAVEL (класс #10, METHOD-BODY REDIRECT, без RNG-поверхности, примитивы #10 переиспользуются).
> (5) NEXT: absorb леги #2 (35418679791, head e82d81f) по НЕИЗМЕННЫМ гейтам PG2/PG3/PG4a/PG4b/PG4c/CRASH-FREE → PASS: CUMULATIVE v4 = v3 + zero_alloc_inside=1; FAIL: REFUTED + rollback zero_alloc_inside=0. Затем свежий ТОП (пересортировка трёх осей) → диспатч #11 или иного ТОП-1. INJECTS-ONLY цел (CI-бутов за тик: 2 диспатча + 1 отменённый до бута = 2 санкционированных).

> **СТАТУС 20 TPS (S7-164 absorb leg#2, 35418679791) — ВЕРДИКТ REFUTED-BY-ECONOMICS: CUMULATIVE v4 НЕ банкуется, zero_alloc_inside=0 (банк = v3; дефолт workflow уже '0'), ZeroAllocOps остаётся инфраструктурой. Прямые цели поражены (PG4a PASS: collided-лейн −71.5% per-work, 3829→971 суммы-формулы, union −50.7%, shape/getAABB-листы 687/987→0; PG4c PASS: young GC 139 ≤ 154, AABB+Vec3-аллок −39% в абсолюте), НО агрегатная экономика регрессировала (PG4b FAIL: fluid-push +15.2% per-work при гейте ≤ −5%; PG3 формально PASS ровно на пороге 1.600 vs v3 1.800 = −11%).**
> (1) Почему экономика не сошлась (урок эры №8): тело fluid-push в скаляре стоит столько же (18.7% лейна — доля идентична v3) — устранённые Vec3/AABB-аллокации были young-gen дешёвками, CPU-стоимость тела (цикл + PalettedContainer-чтения) не изменилась; per-work инфляция СИСТЕМНАЯ (entity +8.3%, GC/JIT +16.5%, collided −71.5% — единственный упавший) → гипотеза: потеря C2-инлайна мелких ванильных тел (collidedWithFluid ~30-40 байткодов инлайнился в горячий цикл checkInsideBlocks) при редиректе в отдельный большой класс; большой updateFluidHeightAndDoFluidPushing (и в ваниле не инлайнится) — нейтрален. TPS-вор n=5 шумный, но leg2 шёл на +20.7% быстрой машине (runner_cpu_index 8.53M vs 7.06M) и всё равно ниже — регрессия не средовая.
> (2) Урок эры №7 (артефакт измерения): концевой автосейв (3876 block+entity чанков, NBT 150k сущностей) попал в alloc-окно (80-100% soak) leg2 — 31.8% alloc-сэмплов сериализация (NbtIo/DataResult/Pair), в v3 0.0% → «взрыв» alloc +174.7% при УПАВШИХ young GC (154→139) был окно-артефактом, не регрессией. Правило: alloc-шары сравнивать только после фильтрации сер-стеков (NbtIo/DataFixer/ChunkSerializer) концевого сейва.
> (3) CRASH-FREE PASS: 3 исключения ≤ 5-бенда — 1 × fastutil sendBlockUpdated (историческое), 1 × ReportedException «updating neighbours» через inside-pipeline (та же параллельно-тиковая семья), 1 × голый NPE fast-throw (из 4 leg#1 осталась 1 → не наш сайт). PG2 PASS: ARMED [inside->rng->batch->zeroin] rc=0, Retargeted{sites:3}, za defined, pop 150k VALID, 0 NCDFE — доставка leg#1 починена и жива весь ран.
> (4) Свежий ТОП (база гейтов = v3-банк, урок №6; ранжирование стабильно n=3 профиля): ТОП-1 не-атакованный ≥5% = travel-physics 9.15% CPU + 13.87% alloc → рычаг #11 ZERO-ALLOC-TRAVEL (getInputVector/Vec3-цепи handleRelativeFriction, makeBoundingBox/inflate/expand-лестницы move, collideWithShapes-temps; примитивы #10 переиспользуются; БЕЗ RNG-поверхности). fluid-push 11.00% (#10 REFUTED), inside-pipeline 10.60% (collided-часть REFUTED), broadphase 6.5% (2×REFUTED), goal-selector/navigation ПАРК, tracker 2.1% (<5%).
> (5) NEXT: preregister гейтов #11 ТОЛЬКО от свежего v3-профиля (урок №6) + javap-контракты travel-методов + lockstep-оракул + реализация (в дизайн — урок №8: редиректить ТОЛЬКО крупные не-инлайнибельные тела, мелкие не трогать) → диспатч следующего тика. CI-бутов за тик 0 (absorb-тик).

> **СТАТУС 20 TPS (RECON-9, тик 12:08) — рычаг #11 ZERO-ALLOC-TRAVEL = REFUTED-BY-ECONOMICS (paper, БЕЗ CI-бута): не-атакованная конкретная аллок-поверхность travel = 1201 сэмпл (11.7% давления, exact deepest-match от банкового v3) → потолок ≤0.83% wall-clock при young-GC STW ≤7.1% = запрещённый 2-3% MSPT-класс; CPU-ось редиректом не снимается (урок №10-обобщение: тело в скаляре = та же доля CPU, доказано #10: 18.7%==18.7%); makeBoundingBox — мелкое инлайнибельное тело → урок №8 предсказывает регресс. Экономия CI-лега.**
> (1) CPU-ось travel: collide/travel-семья 6.11% CPU = арифметика + палитровые чтения (PalettedContainer.get 1.31%); CollisionUtil-внутрянка 4.15% = moonrise-оптимизированный код, вербатим-редирект стороннего оптимизатора вне класса эры.
> (2) Кризис ТОПа зафиксирован (нормальный этап «→ ∞»): ВСЕ ≥5% пожиратели v3-профиля закрыты — attacked-REFUTED (#10 fluid/inside-collided), paper-REFUTED (#11 travel), ПАРК (broadphase 2×REFUTED, goal-selector RNG-parity, navigation hit-rate≈0), гетерогенные <5%-сайты (mid-range запрещён). Арсенал эры (кэш/мемоизация, редирект-скаляризация, батч-коллектор, планировщик) исчерпан на этом профиле → нужен НОВЫЙ КЛАСС рычага.
> (3) Объявлен рычаг #12 NATIVE-COLLIDE (Rust/JNI батч-порт collide-пути, класс миссии «Rust/JNI порты горячего пути» + «батч-операции»): цель ≈8.3% CPU (CollisionUtil 4.15 + move 3.31 + collide 0.80) + collision-temps аллокации; ОДИН JNI-переход на регион-батч, внутри Rust bit-exact double-математика (IEEE754 детерминизм → vanilla-parity по построению, оракул ≥1M бит-в-бит); риски честно: JNI-overhead (батчинг), копирование секций, вербатим CollisionUtil-лестницы (порог GO/NO-GO: >400 байткодов с внешними world/border-вызовами → INFEASIBLE-BY-VERBATIM).
> (4) План #12 (multi-tick, прецедент #10): тик+1 RECON-10 (javap-контракт collideBoundingBox/performCollisions verbatim) + каркас Rust-ядра/JNI-моста; тик+2 оракул ≥1M + гейты от свежего профиля (урок №6) + preregister; тик+3 compose-интеграция + диспатч. Артефакты RECON-9: research/travel-recon-2026-09-19/. Диспатчей за тик 0 (RECON-тик, S7-108 чист).

> **СТАТУС 20 TPS (RECON-10, тик 12:43) — рычаг #12 NATIVE-COLLIDE ЗАКРЫТ: INFEASIBLE-BY-VERBATIM (по сбору) + PAPER-REFUTED (по лестнице), БЕЗ CI-бута, бюджет сохранён (третий подряд paper-вердикт: #11 travel, #12 native-collide).**
> (1) javap-контракт лестницы (реальные kernel-классы из fixtures; «юнит» = 1 инструкция): лестница-ядро ≈1568 юнитов — performCollisions 120 + performVoxel/AABB-Collisions 92×2 + X/Y/Z-лупы 22×6 + collideX/Y/VZ(VoxelShape) 294×3 (плоские данные CachedShapeData, 0 world-вызовов) + collideX/Y/Z(AABB) 70×3 + findFloor 40 + voxelShapeIntersectNoEmpty 197 + isEmpty 50; порог COLLISION_EPSILON=1.0E-7 (14+6+6 вхождений), dcmpg-лестницы 5-7/метод (урок №9 актуален для любого будущего порта). World-часть: getCollisionsForBlocksOrWorldBorder = **519 юнитов с Level/ChunkSource/BlockState/WorldBorder/WorldUtil-вызовами — порог 400 ПРЕВЫШЕН**; getEntityHardCollisions 78; Entity.collide 265 / Entity.move 560 (оркестрация).
> (2) INCLUSIVE-раскладка CPU (v3-банк 128124): лестница performCollisions = **0.37%** (voxel-ветка 0.00%), сбор getCollisionsForBlocksOrWorldBorder = **3.45%** + getEntityHardCollisions 1.68%, isCollidingWithBorder 0.04%; Entity.collide/move = 0.00% (целиком C2-инлайн в вызывающие фреймы; self лестницы 0.27% — findFloor 0.118 максимум). Потребление и портабельность расходятся: портабельная часть держит ≤0.37% CPU (в 5-10 раз ниже запрещённого класса 2-3%), потребляющая часть (>5%) непортабельна по прereg-критерию TASK-314. «Буфер палитр» = реимплементация диспетчера BlockState→shape (BlockStateShapeCache/LazyEntityCollisionContext/WorldUtil) — вне класса эры bit-exact-вербатима по parity-риску. Даже лестничный Option A рефутуется экономикой до сборки: JNI-граница теряет C2-инлайн лестницы (урок №8 на границе JNI) + сериализация shape-данных на сущность/тик — знак эффекта ожидаемо отрицательный (как в #10).
> (3) Свежий self-ТОП v3 (перестройка «→ ∞»): GC-фаза ≈25-30% (G1-внутрянка: OopOopIterateDispatch 4.28+2.42+1.35+1.07+0.97, oopDesc::size 4.01, G1CardSet 3.34+1.52+1.25, trim_queue 1.69, RemSet 1.54) — даунстрим аллокации; не-GC: блочные чтения ≈5.5% (PalettedContainer.get 2.96 residual после банked PALETTED-DEMUX + readPalette 0.68 + SimpleBitStorage.get 0.64 + getBlockStateFinal 0.65 + getFluidState 0.57), updateFluid-тело 1.97%, broadphase ≈2.1% (ПАРК), AABB-геометрия ≈2.2% (temps исчерпаны), SynchedEntityData ≈1.5%. КЛАССЫ МИССИИ ЗАКРЫТЫ ВСЕ (кэш/мемоизация, батчинг, dirty-flags, O(n)→O(1), Rust/JNI, планировщик) — эра в стадии «весь ТОП пройден → новый замер»: следующая атака возможна только на межклассовых стыках (аллок→G1) или residual-полах.
> (4) NEXT (тип+1, RECON-11): (а) разложить G1-фазу на драйверы (какие аллок-семьи кормят oop-iterate/card-set: типы/размеры/выживаемость young); (б) трёхосевая пересортировка от свежих артефактов (урок №7 — фильтр сер-стеков концевого сейва); (в) кандидаты #13: lesson-8-compliant редирект ТОЛЬКО крупных не-инлайнибельных аллок-тяжёлых тел (гейт под аллок-цель, не CPU-цель; прецедент updateFluid-нейтральности), demux-v2 residual-лейна (getFluidState/getBlockStateFinal вне demux-пути), G1-френдли предвыделение в горячих циклах (объект-пул, класс «планировщик/кэш»). Порог отбора прежний: ≥5% лейн или 2-3%+ wall-класс, vanilla-parity, иначе paper-REFUTED без CI-бута. Артефакты RECON-10: research/native-collide-2026-09-19/ (RECON10_NATIVE_COLLIDE.md, cu_full.txt, entity_full.txt, recon10_methods.json, 3 скрипта). Диспатчей за тик 0 (RECON-тик, S7-108 чист).

> **СТАТУС 20 TPS (RECON-11, тик 13:08) — свежий ТОП-1 = G1/GC-фаза 31.0% self-CPU декомпозирован: card-set/remset 16.39% (драйвер = число old→young записей, 4-6M/s) + oop-scan 10.70% (live-граф ~3.9G); ВСЕ 5 кандидатов #13 честно закрыты без CI-бута; рычаг #13 SKIP-STORE-DIET переведён в ИНСТРУМЕНТ-ГЕЙТ.**
> (1) Декомпозиция (recon11_g1_drivers.py): card-set 16.39% (G1CardSet.add_card 3.34 + add_to_howl 1.52 + add_to_container 1.25 + ScanCard 1.26 + refine 1.54) — барьерная экономика: карта грязнится на запись ссылки в СТАРЫЙ объект, стоимость ∝ ЧИСЛУ записей (не байтам); oop-scan 10.70% (OopOopIterate ~10% + oopDesc::size 4.01) — обход живого графа; queue/evac 1.69%; WallClock::signalHandler 0.945% = артефакт профайлера. gc.log: 50 Normal-young/461s (интервал 9.41s), пауза медиана 156.2ms, аллок-рейт ~250MB/s, live ~3.9G.
> (2) Аллок-база чистая (сер-стеки 0.0%, урок №7): AABB 20.19 + Vec3 19.89 = 40.08% (сходится с prereg), BlockPos-семья 15.7%, long[] 4.77. Крупнейший аллокатор по деревьям = inside-пайплайн (multi-лейбл AABB 1051/Vec3 797/BlockPos 663/long[] 460).
> (3) Внутри-лейн декомпозирован: 11.10% CPU inclusively, из них банked inside_cache (статик-гейт) = 0.59%, ванильное discovery = 10.51% — статик-гейт (deltaMovement==0 && pos bit-equal) почти не дентил лейн: в X150K ненулевой deltaMovement почти всегда. javap-контракт (entity_full.txt): discovery = per-step checkInsideBlocks(Vec3 from, Vec3 to, …) + lambda$checkInsideBlocks$2 = segment-тест collidedWithShapeMovingFrom/collidedWithFluid → **discovery — функция СВЁРНУТОГО ПУТИ (суб-блочная чувствительность форм), не функция состояния**.
> (4) Свип кандидатов #13 (всё без CI): (a) lesson-8-compliant редирект крупных тел — PAPER-REFUTED (move alloc-доля 2.3% давления < класса + move инлайнится: inclusive 0.00%); (b) INSIDE-DIRTY-BOUNDARY — INFEASIBLE-BY-PARITY (сегмент-тест ≠ функция состояния; парити-безопасный skip = статик, уже банked); (c) demux-v2 residual — PAPER-REFUTED (1.2-1.5% CPU < класса); (d) объект-пул — PAPER-REFUTED (темпы young→young карт не создают, пул не режет old→young сторы сеттеров и добавляет живые объекты); (e) скаляризация live-полей — PAPER-REFUTED (read/write-асимметрия: синтез на чтение повышает аллок).
> (5) Гипотеза эры (store-firehose): ~200-300k old→young записей/тик (setDeltaMovement ~110-150k + setBoundingBox ~50-100k + sync/списки) — сеттеры инлайнены C2 → атрибуция по alloc-стекам ломается (72+93 сэмпла = артефакт инлайна, зеркальный урок №8); честная оценка через producer-цепочки (travel 13.87% аллока). Если entity-поля = 40-70% всех old→young записей → value-equal store-skip даёт card-set −20-40% ≈ 3-6.5% CPU (выше класса). Доля НЕИЗВЕСТНА → вера запрещена, нужен инструмент.
> (6) NEXT (тип+1): ИНСТРУМЕНТИРОВАННЫЙ RECON-лег (CI-бут, 0 поведения): диагностический -Xlog:gc+remset*/gc+refine в workflow (логирование ≠ config-win: GC-политика не трогается) + producer-атрибуция alloc-стеков с учётом инлайн-границ + опц. JFR OldObjectSample → измеренная доля entity-полей в old→young ≥ 40% И model-потолок skip ≥ 2-3% wall = GO #13 (preregister + lockstep-оракул ≥1M + grep identity-использований getBoundingBox/getDeltaMovement); NO-GO = paper-REFUTED #13 без леги. Артефакты: research/gc-recon-2026-09-19/ (RECON11_G1_DRIVERS.md, RECON11_raw.txt, 2 скрипта). Диспатчей за тик 0 (RECON-тик, S7-108 чист; CI-бутов 0).

> **СТАТУС 20 TPS (TASK-317, тик 13:43) — инструмент-гейт #13 SKIP-STORE-DIET развернут: диагностический RECON-лег (CI-бут, 0 поведения) собран, верифицирован локально и ДИСПАТЧЕН; absorb = следующий тик по preregister-правилу TASK-316.**
> (1) Инструмент (дифф workflow + run_world3.sh, input recon_diag → env RECON_DIAG): (а) -Xlog:gc+remset=debug / gc+refine=debug — агрегатная интенсивность old→young карт per GC-цикл (уточнение модели 4-6M/s из TASK-316; «Visited cards N / Total dirty M (pct)»); логирование ≠ config-win — GC-политика/heap не тронуты (-Xms/-Xmx/-XX:+UseG1GC без изменений). (б) -XX:StartFlightRecording=filename=recon.jfr,settings=profile,dumponexit=true — локальный smoke на Temurin 21.0.12.1 подтвердил: JFR стартует (maxsize=250MB default), jdk.OldObjectSample АКТИВЕН в profile.jfc (memory-leaks default=stack-traces → enabled + allocation stack traces, cutoff 0 ns без path-to-gc-roots), jdk.ObjectAllocationSample 300/s — контрольный alloc-профиль. OldObjectSample = объекты, достигшие старого гена, С АЛЛОКАЦИОННЫМ стеком = прямая producer-атрибуция store-firehose (сеттеры инлайнены C2 → async-profiler alloc-стеки ненадёжны, зеркальный урок №8 — теперь атрибуция по producer-фреймам ВЫШЕ стора).
> (2) Дефект предупреждён на этапе сборки флагов: первая версия EXTRA_JVM_DIAG была строкой с экранированными кавычками → word-splitting сделал бы кавычки литеральными (JVM получила бы битые пути лог-файлов); переписано на bash-МАССИВ («${EXTRA_JVM_DIAG[@]}») — аргументы цельные по построению. Гигиена доставки урока №5 в shell-классе.
> (3) Лег-конфиг = ТОЧНЫЙ v3-банк (35399980345: inside_cache=1 + flush_diet=1 + region_threads=4 + batch_collector=1, прочие 0, fp4/300s/150k/seed42/xmx10G/radius640) + recon_diag=1. Лег — НЕ гейт-лег: JFR-overhead смещает CPU/TPS — его числа НИКОГДА не идут в PG2/PG3/PG4-гейты и банкинг; единственное назначение — измерить долю entity-полей-продюсеров среди old→young записей. Remote-head == local-head проверен в диспатчере до POST (урок инцидента 35418617801).
> (4) Анализатор готов заранее (research/gc-recon-2026-09-19/recon12_store_firehose.py, smoke-passed на локальной записи): jfr print → OldObjectSample → классификация producer-семей по глубочайшему узнаваемому фрейму (deltaMovement/travel, boundingBox/move, sync/SynchedEntityData, chunk entity-lists, block-change, other-entity, non-entity), доли по объектам + top-классы + objectAge p50/p90; remset/refine-лог → dirty-карты/цикл p50/max + dirty%. Окно честности: если OldObjectSample пуст → NO-GO-INSUFFICIENT-TOOL (не REFUTED — инструмент не ответил).
> (5) Решение (preregister из CLAIMS TASK-316 NEXT, НЕ переписывается): доля entity-полей ≥ 40% И model-потолок value-equal store-skip ≥ 2-3% wall → GO #13 (preregister + lockstep-оракул ≥1M + grep identity-использований getBoundingBox/getDeltaMovement); иначе → paper-REFUTED #13 без леги. Артефакты лега: world3-run/{remset.log, refine.log, recon.jfr} (добавлены в upload-артефакт). Диспатчей за тик 1 (диагностический RECON-лег, S7-108 — единственный в полёте); CI-бутов производственного класса 0.

> **СТАТУС 20 TPS (RECON-12 absorb, тик 14:08) — лег 35425246662 поглощён: доставка инструмента ЦЕЛА (remset/refine/JFR), remset-агрегат подтвердил масштаб firehose (dirty-карты p50 6.3M/цикл ≈ 670k карт/с, записи ≥ карт ×6-9 — сходится с моделью 4-6M/s); OldObjectSample-канал НИЗКОГО покрытия (72 leakage-события, boot-объекты) → гейт TASK-316 не открыт → #13 SKIP-STORE-DIET (широкая долевая гипотеза) = paper-REFUTED как НЕДОКАЗАННАЯ (пост-хок порог не переписывается); ОБЪЯВЛЕН УЗКИЙ РЫЧАГ #13-SBB SKIP-STORE-BB (value-equal skip ТОЛЬКО setBoundingBox — parity-safe по javap-контракту: 0 identity-сайтов в 10 фикстурах).**
> (1) remset debug (прямой агрегат): dirty cards/cycle p50 6,324,224 (max 7.4M), dirty% p50 59.63%, 165 циклов — измеримая база для A/B «measurement-by-effect»: следующий лег #13-SBB идёт с тем же recon_diag=1 → разница dirty-cards = прямая доля bb-записей в old→young (недостижимо через alloc-стеки из-за инлайна).
> (2) ObjectAllocationSample 78686 сэмплов (счёт): entity-семьи 71.35% АЛЛОКАЦИЙ — boundingBox/move 36.92% (Vec3 18.65% + AABB 16.75% топ-классы), other-entity 28.04% (Entity$$Lambda 1.16%, LazyEntityCollisionContext 1.19% — кандидаты RECON-13), block-change 2.95%, deltaMovement 2.89%, sync 0.50%, lists 0.05%. Ограничение честности: аллокации ≠ записи (temps умирают без записи) — гейт-вопрос остался неотвеченным этим каналом.
> (3) javap-контракт сеттеров (RECON-12a, до лега): setBoundingBox = нормализация verbatim (dcmpg «<0» ×3 + dcmpl «>64.0 кламп» ×3 + NaN-вербатим) + new AABB КАЖДЫЙ вызов + putfield bb под 0 identity-сайтов → value-equal skip парити-safe; setDeltaMovement = putfield ПОД monitorenter posLock + 5 identity-сайтов (главный — guard move(): param == getDeltaMovement() на offset 280-285, vanilla зовёт setDeltaMovement ВНУТРИ move до guard: offset 263 stuck-ветка ZERO, 800/1115 финал) → value-equal skip = INFEASIBLE-BY-PARITY-РИСК (окно: параметр, вынесенный вызывающим до skipнутого сеттера, меняет исход guard'а). deltaMovement/sync/chunk-lists — вне #13-SBB.
> (4) Preregister #13-SBB (гейты от лега-базы 35425246662, урок №6, фиксируются до реализации): PG2 доставка (Retargeted sites:1 + «skip_store_ops: defined» + ARMED + 0 NCDFE + pop 150k VALID); PG3 TPS last-5 медиана ≥ 1.60; PG4a remset dirty-cards/cycle p50 −10%+ (ожидание 10-25%); PG4b card-set/remset CPU-лейн −10%+; PG4c AABB-аллокация по счёту −20%+; CRASH-FREE 0/0/≤5; ПАРИТИ-оракул ≥1M бит-в-бит (нормализация + skip-семантика + identity-инвариант bb). Банкинг: PASS → CUMULATIVE v5 = v3 + skip_store_bb=1; FAIL → REFUTED + rollback. Реализация — тик 14:43 (SkipStoreOps.java 1 classfile no-nested, redirect Entity.setBoundingBox(AABB) 173 юнита, entity_compose stage 8, resolution-closure гварды — прецедент #10).
> (5) NEXT: тик 14:43 — реализация #13-SBB + preregister §4 + диспатч v5-кандидата (recon_diag=1 для remset A/B); тик+2 absorb → вердикт → свежий ТОП; RECON-13 = декомпозиция other-entity 28.04%. Артефакты: research/gc-recon-2026-09-19/ (RECON12A_SKIP_STORE_IDENTITY.md, RECON12B_ABSORB_S7165.md, RECON12A_RAW.txt, run-s7165-recon-diag/, recon12_store_firehose.py обновлён). Диспатчей за тик 0 (лег поглощён; S7-108 чист).

> **СТАТУС 20 TPS (#13-SBB реализация + оракул, тик 14:43) — SKIP-STORE-BB реализован и офлайн-верифицирован: SkipStoreOps (javap-вербатим 173 юнита инструкция-в-инструкцию + бит-equal skip), entity_compose stage 8 (strict sites==1, fail-dominant), cargo 175/0/1 (8 новых гвардов доставки), оракул 1,100,000 сценариев PASS (0 mismatches); ДИСПАТЧЕН v5-КАНДИДАТ (v3-банк + skip_store_bb=1 + recon_diag=1, remset A/B против базы 35425246662). НОВОЕ ЦЕЛЕВОЕ ОГРАНИЧЕНИЕ ВЛАДЕЛЬЦА: профиль бесплатного хостинга 2.5 GB shared RAM (FalixNodes free plan) — отдельная трек-линия после вердикта v5.**
> (1) Реализация (#13-SBB, S7-166): entityinside/net/minecraft/world/level/SkipStoreOps.java — 1 classfile no-nested, Unsafe-доступ к полю bb (offset resolved once); тело = вербатим RECON-12a (6 dstore с полей аргумента; per-axis d = source.maxN - source.minN ПОВТОРНЫМ чтением полей; dcmpg-лестница «d<0 → max=min-LOCAL» ×3; dcmpl-кламп «d>64.0 → max=min+64.0» ×3; NaN вербатимом — dcmpg(NaN,0)=1/dcmpl(NaN,64)=-1 ветки не берутся); skip при doubleToLongBits-равенстве всех 6 компонент с текущим полем (СТРОЖЕ preregister-dcmp: ±0.0 НЕ скипается — окно знака деления в clip; NaN канонизируется — вниз по потоку неразличим); putfield/new AABB только в non-skip ветке. Скомпилированный бридж проверен javap-диффом против дампа Entity: юниты 0-138 инструкция-в-инструкцию идентичны.
> (2) Delivery-граф: src/skip_store.rs (env CRUSSTY_SKIP_STORE_BB, requires region_threads>=2, resolution-closure guard + define в kernel loader, маркер «skip_store_ops: defined»); src/classfile.rs — SSB_REDIRECT_TARGETS (ровно 1 цель setBoundingBox (Entity;AABB)V — тест-лок скоупа: setDeltaMovement НЕ входит, parity-риск), patch_entity_skip_store_bb (sites==1 strict, AlreadyPatched идемпотентен, NotFound возвращает оригинал), closure-рефакторинг на общий корень; src/entity_compose.rs — STAGE 8 после zeroin (strict sites==1, fail-dominant «WITHOUT sbb»), audit_wire v5; src/lib.rs — mod + activate до entity_compose. cargo test 175 passed / 0 failed (8 новых: no-nested, 1-classfile, embed-fresh, closure, table-scope, shape/idempotence, emit-harness).
> (3) Оракул SkipStoreLockstepHarness: 1,100,000 сценариев PASS (skipped=40000/40000 ровно skip-форсинг-лейн, nonSkipped=1060000): твины = Unsafe-allocated EvokerFangs (Entity abstract — allocateInstance отказывает; setBoundingBox final → подкласс диспетчит в ванильное тело; offset bb от Entity.class — префикс лейаута общий); бит-паритет 6 компонент vanilla-vs-бридж; identity-инвариант (skip хранит ссылку pre-state); zero-sign strictness (±0.0 лейн 20000 не скипнулся — doubleToLongBits строже dcmp-равенства preregister, консервативно); non-skip честность (всегда fresh ref); лейны NaN/subnormal/±0.0/huge/кламп-границы 64.0±ulp/inverted. Stage-8 проводка офлайн: HotSpot defineClass патченого Entity 205210 B PASS + byte-audit (SkipStoreOps-ref + receiver-prepended desc в пуле) PASS; живой invokevirtual-диспетч доказывается CI-легом по PG2-маркерам (глубина как в прецеденте S7-164).
> (4) Диспатч: v5-кандидат = ТОЧНЫЙ v3-банк (inside_cache=1, flush_diet=1, region_threads=4, batch_collector=1, прочие 0, fp4/300s/150k/seed42) + skip_store_bb=1 + recon_diag=1 (remset/refine/JFR — measurement-by-effect: dirty-cards A/B против базы 35425246662 при идентичном recon_diag оверхеде = прямая доля bb-записей); xmx10G сохранён (валидность A/B); гейты preregister TASK-318 §4 НЕИЗМЕННЫ (PG2 «stage sbb composed (Retargeted { sites: 1 })» + «skip_store_ops: defined» + ARMED sbb + 0 NCDFE + pop VALID; PG3 ≥1.60; PG4a dirty p50 −10%+; PG4b card-set CPU −10%+; PG4c AABB-аллок −20%+; CRASH-FREE 0/0/≤5; оракул ≥1M — закрыт офлайн 1.1M PASS; банкинг v5 = v3 + skip_store_bb=1 | FAIL → REFUTED + rollback).
> (5) НОВОЕ ЦЕЛЕВОЕ ОГРАНИЧЕНИЕ (владелец, тик 14:43, директива подтверждена поиском): тесты и ускорения целить в профиль БЕСПЛАТНОГО хостинга — 2.5 GB SHARED RAM (FalixNodes free plan: shared CPU Ryzen 9 9950X-класс без выделенных ядер, ~10GB диск, RAM/CPU шарятся между тенантами). Цель прежняя (20 TPS / минимальный MSPT). План: отдельная трек-линия FREE-HOST профиль-лег (server_xmx≈2G — реальный бюджет JVM на контейнере 2.5GB, та же живая сцена fp4/150k/seed42, recon_diag=1) ПОСЛЕ absorb v5 — свежий ТОП под memory-pressure: young-GC чаще → card-set/аллок-лейны весят БОЛЬШЕ → эффект #13-SBB ожидаемо крупнее; low-RAM профиль ≠ гейт-лег банкинга v5 (другая база сравнения). JS/ядро остаются INJECTS-ONLY.
> (6) NEXT (тип+1): absorb v5-кандидата → вердикт по НЕИЗМЕННЫМ гейтам → PASS: CUMULATIVE v5 = v3 + skip_store_bb=1 + пересортировка ТОПа «→ ∞» (RECON-13 = other-entity 28.04% декомпозиция: LazyEntityCollisionContext 1.19%, Entity$$Lambda 1.16%); FAIL: REFUTED + rollback (ops остаётся инфраструктурой). Тик+2: FREE-HOST профиль 2.5GB (директива владельца) — RECON под xmx≈2G. Артефакты: entityinside/{SkipStoreOps.java, harness/SkipStoreLockstepHarness.java, build/SkipStoreOps.class, build/Entity_patched_ssb.class}, scripts/{build_skipstore_ops.sh, run_skipstore_lockstep_harness.sh, bench4_recon/dispatch_s7166.py}, диффы run_world3.sh + world-bench.yml (skip_store_bb). Диспатчей за тик 1 (v5-кандидат; S7-108 — единственный в полёте); CI-бутов производственных 0 (весь контур офлайн: cargo + оракул).

## СТАТУС ×N — TASK-320 (absorb v5-кандидата s7166: #13-SBB SKIP-STORE-BB = REFUTED по прегистеру; банк остаётся v3; семья store-skip закрыта полностью) — 2026-09-19 ~15:3x +08
**Вердикт: REFUTED (4/6 полевых гейтов FAIL): PG2 PASS (sites:1 + defined + ARMED sbb + 0 NCDFE + pop VALID); PG3 FAIL — TPS last-5 медиана 1.5 < 1.60 И регресс −25% vs диаг-базы s7165 (2.0, тот же recon_diag-оверхед); PG4a FAIL — remset dirty-cards p50 6,324,224 → 6,356,992 (+0.52%, эффект НОЛЬ); PG4b FAIL — card-set CPU 25.72% → 23.82% (−6.74% < 10%); PG4c FAIL — AABB по счёту jfr −9.45% (16.90% → 17.06%), ap −10.79% (гейт −20%); CRASH-FREE PASS (0 crash / 0 Full, паузы 322→316); оракул 1.1M закрыт офлайн (TASK-319).**
- Находка №1 (measurement-by-effect): value-equal подмножество setBoundingBox НЕ драйвит remset firehose — движущиеся сущности меняют bb каждый тик, бит-равенство не наступает, putfield/карта остаются; скип стреляет только на стационарных (AABB счёт −9..−11%) — стоки малы. Банк v3 сохранён; skip_store_bb default '0' = rollback автоматичен; SkipStoreOps = инфраструктура.
- Находка №2: «проверка дороже стора» — при почти-всегда-неравных значениях каждый вызов платит 6×doubleToLongBits + 6 cmp + бридж-косвенность без экономии → TPS 2.0 → 1.5 на идентичной сцене. Анти-паттерн зафиксирован как урок.
- Семья store-skip (#13-*) ЗАКРЫТА: широкая = paper-REFUTED (RECON-12), setDeltaMovement = INFEASIBLE-BY-PARITY (javap-контракт), setBoundingBox = REFUTED полевым A/B (этот лег). Card-set lane (TOP-1 ~24-26%) драйвится другими семьями (deltaMovement-сторы/синк/списки/block-change) без парити-безопасного value-skip рычага.
> (7) NEXT (тип+1, по директиве владельца тик 14:43 — приоритет): FREE-HOST профиль-лег 2.5GB — server_xmx≈2G (реальный бюджет на контейнере 2.5GB shared), та же живая сцена fp4/150k/seed42, recon_diag=1, v3-банк (skip_store_bb=0) — свежая пересортировка ТОПа под low-heap (young-GC чаще → card-set/аллок-лейны весят больше); база сравнения = сам этот FREE-HOST лег (двухфазно: RECON-профиль → атака следующего ТОП-1). Параллельно кандидаты декомпозиции: oop-scan 10.70% и RECON-13 other-entity 28.04% (LazyEntityCollisionContext, Entity$$Lambda). Артефакты: research/gc-recon-2026-09-19/RECON12C_V5_S7166_VERDICT.md + run-s7166-v5-candidate/ (spark arCsGa90bZ). Диспатчей за тик 0 (лег поглощён; S7-108 чист); CI-бутов 0.

## СТАТУС ×N — TASK-321 (FREE-HOST трек 2.5GB: server_xms параметризован; RECON-лег s7167 ДИСПАТЧЕН — run 35430216073) — 2026-09-19 ~15:5x +08
**Статус: по директиве владельца (тик 14:43) развёрнута трек-линия FREE-HOST — тесты/ускорения под free-hosting профиль 2.5GB shared RAM (реальный бюджет JVM ≈2G кучи). Инструмент: workflow input server_xms (default '4G' — все исторические леги бит-в-бит, контракт Xms≤Xmx) → env SERVER_XMS → run_world3.sh -Xms"$SERVER_XMS" (было захардкожено -Xms4G — при Xmx2G JVM не стартует); bash -n + YAML-парс PASS. Диспатч: dispatch_s7167.py → run 35430216073 (in_progress, head d19bbd0): точный v3-банк + skip_store_bb=0 (REFUTED-состояние TASK-320) + server_xms=2G + server_xmx=2G + recon_diag=1 (JFR+remset/refine — наблюдаемость).**
- Пегистер (зафиксирован ДО диспатча, RECON-лег — НЕ гейт банкинга): PG-A доставка = pop 150k VALID + ARMED [inside->rng->batch] (без sbb) + 0 NCDFE + run-env echoes server_xms/xmx = 2G/2G; честное окно (a): OOM/GC-спираль смерти → вердикт INFEASIBLE-BY-MEMORY (сцена 150k не влезает в класс 2.5GB — gc.log-доказательства: фаза смерти, трэш Full-GC, allocation-fail rate) = ВАЛИДНЫЙ результат, не отказ инструмента; честное окно (b): ран завершается → свежая пересортировка ТОПа под low-heap (young-GC count/duty vs диаг-базы 35425246662, card-set/аллок-лейны по JFR, TPS-серия) → следующая цель ТОП-1 под free-host профиль
- База будущего A/B на треке FREE-HOST = сам этот лег (двухфазно: RECON → атака ТОП-1 оттуда); с 10G-легами НЕ сравнивается по абсолюту (другой бюджет кучи)
> (8) NEXT (тип+1): absorb s7167 по пегистеру TASK-321 → окно (a) INFEASIBLE-BY-MEMORY (доклад владельцу: сцена vs бюджет, варианты — сниженная популяция для free-host трека или 10G-класс хостинга) ИЛИ окно (b) свежий ТОП → атака следующего ТОП-1 архитектурным рычагом (кандидаты: oop-scan 10.70%, RECON-13 other-entity 28.04% аллокаций, G1-фаза под low-heap). Артефакты: scripts/bench4_recon/dispatch_s7167.py, диффы world-bench.yml + run_world3.sh (server_xms). Диспатчей за тик 1 (FREE-HOST RECON s7167; S7-108 — единственный в полёте); CI-бутов 0.

## СТАТУС ×N — TASK-322 (тик-режим работает: absorb-инструмент s7167 готов; лег 35430216073 в полёте) — 2026-09-19 ~15:5x +08
**Статус: крон переработан владельцем (Job 397396, «РЕЖИМ ТИКА» — TASK-321b, CLAIMS fc4b3ef): тик = absorb/диспатч/офлайн-работа без ожидания, контекст-экономия (хвосты, без пересказа чартера), отчёт ≤10 строк. По пункту (4c) лег s7167 (FREE-HOST 2.5GB, run 35430216073) в полёте → офлайн-работа: создан и смоук-протестирован scripts/bench4_recon/absorb_s7167.py — absorb одной командой следующего тика.**
- absorb_s7167.py: статус-гейт (in_progress → exit 3, не абсорбить) → fetch артефактов → PG-A (pop VALID + ARMED без sbb + NCDFE=0 + run-env xms/xmx 2G) → окно (a): OOM-детектор (OutOfMemoryError в stdout + Allocation-Failure Full-GC трэш в gc.log + failure conclusion) → INFEASIBLE-BY-MEMORY; окно (b): GC-дuty (young/full/суммарно/max, с фильтром gc,start-маркеров), card-set CPU-лейн, аллок-семьи AABB/Vec3/other-entity, remset p50 (кросс-heap vs 35425246662, НЕ абсолют A/B), TPS last-5 → авто-док RECON13_FREEHOST_ABSORB.md + следующий ТОП-1
- Смоук на 10G-базе 35425246662: remset p50 6,324,224/165 ✓, TPS 1.4/1.9/2.0/2.3/2.5 ✓, gc_stats 161 young / 0 Full / 20.3s / max 189ms (дефект двойного счёта gc,start-маркеров устранён — раньше grep давал 322)
> (9) NEXT (тип+1): тик :08/:43 — если s7167 завершён: python3 scripts/bench4_recon/absorb_s7167.py → вердикт (a)/(b) → учёт TASK-323; если ещё в полёте — офлайн RECON-13 (other-entity 28.04% декомпозиция по alloc-collapsed s7165/s7166 — классификатор под-лейнов ≥5%). Диспатчей за тик 0 (S7-108 — s7167 в полёте); CI-бутов 0.

## СТАТУС ×N — TASK-323 (RECON-13: entity-под-лейны + кодек-чанк-парс 19.4% аллок; лег s7167 в полёте) — 2026-09-19 ~16:1x +08
**Статус: тик 16:08 по пункту (4c) — офлайн RECON-13 по NEXT: scripts/bench4_recon/recon13_other_entity.py (аттрибуция глубочайшим фреймом, аллок-семплы 10G-базы s7165). Entity-семья = 35.22% аллок-байтов; под-лейны ≥5%: entity-tick-core 21.39% (внутри travel-physics Vec3/AABB — известное), navigation/pathfinding 6.58% (ВАЛИДНАЯ цель атаки), entity-lambda 5.98%; мелкие: collision-context 0.60%, goals 0.34%, brain 0.21%.**
- НОВЫЙ ТОП-КАНДИДАТ вне entity-семьи: **кодек-сериализация чанков = 19.4% ВСЕХ аллок-байтов** (DataResult-стэки; SerializableChunkData.parse + MapDecoder.compressedDecode 84.6% глубины; листья short[] 10.3% / byte[] 7.3% / String 3.8% / Pair 2.2% — это NBT-парс churn). Механика: аллок-давление → частота young-GC → MSPT (на FREE-HOST 2.5G профиле вес МАКСИМАЛЬНЫЙ)
- Парити-чувствительность кандидатов: node-pool для navigation переоткрывается с новым критерием (young-аллокации → young-GC, карты не трогаются — прежний REFUTED был про card-dirt), кодек-кэш декодированных чанков = парити-риск (мутабельное состояние) — требует RECON вызывателя parse
> (10) NEXT (тип+1): absorb s7167 (absorb_s7167.py одной командой) → вердикт FREE-HOST (a)/(b); RECON-13b: вызыватели SerializableChunkData.parse в установившемся тике (периодика перезагрузки чанков vs eviction) + решение атаки: navigation node-pool v2 (критерий young-GC) ИЛИ chunk-parse diet. Артефакты: RECON13_OTHER_ENTITY_LANES.md, recon13_other_entity.py. Диспатчей 0 (s7167 в полёте, S7-108 чист); CI-бутов 0.

## СТАТУС ×N — TASK-324 (absorb s7167: FREE-HOST 2.5GB = INFEASIBLE-BY-MEMORY — 2,611 Full GC, 121 OOM, инъект не завершён; нужен выбор владельца) — 2026-09-19 ~16:2x +08
**Вердикт: окно (a) прегистера TASK-321 сработало: сцена X150K НЕ помещается в 2G кучу. Лег s7167 (35430216073, failure 16:18): heap 2G/2G подтверждён, ARMED no-sbb PASS, NCDFE=0, популяция НЕ досчитана — 2,611 завершённых Pause Full GC (10G-база: 0) + 121 OOM Java heap = смерть от GC-спирали при инъекции. Инструмент absorb_s7167.py отработал одной командой (статус-гейт → fetch → OOM-детектор → авто-док RECON13_FREEHOST_ABSORB.md).**
- Это НЕ отказ инструмента: PG-A доставка чиста, смерть = честное измерение профиля free-hosting класса. FREE-HOST A/B база недоступна на 150k.
- Крон v3 ( Job 397396 → заменён): запрет мета-тем/повторов статуса в тиках; каждое сообщение тика = только материальный результат.
> (11) NEXT (тип+1, ТРЕБУЕТСЯ РЕШЕНИЕ ВЛАДЕЛЬЦА — реальный блокер трека): (A) FREE-HOST трек со сниженной популяцией (например 25-50k, xmx 2G — держится ли и что в ТОПе) ИЛИ (B) вернуться к 10G-классу и атаковать следующий ТОП из RECON-13: navigation node-pool v2 (6.58% аллок), chunk-parse codec diet (19.4% аллок, нужен RECON-13b вызывателей parse). Пока выбора нет — по умолчанию агент атакует (B) на 10G-базе (не блокируется). Артефакты: RECON13_FREEHOST_ABSORB.md + run-s7167-freehost/. Диспатчей за тик 0 (s7167 поглощён; S7-108 чист).

## СТАТУС ×N — TASK-325 (RECON-13b: chunk-parse churn 33.4% аллоков, оконная аномалия = burst в финале) — 2026-09-19 ~16:3x +08
**Статус: в полёте пусто, рычаг не готов → офлайн RECON-13b (scripts/bench4_recon/recon13b_chunk_load.py): полный parse-путь ChunkLoadTask→GenericDataLoadTask→SerializableChunkData.parse = 33.38% ВСЕХ аллок-байтов s7165; оконная аномалия: 0.00% CPU (0-55%) / 0.00% wall (55-80%) / 45.03% alloc (80-100%) → BURST перезагрузки чанков в финальном окне 240-300s, не steady-state.**
- Следствия: (1) аллок-давление/young-GC в финале рана сконцентрировано в повторной десериализации чанков; (2) churn НЕ виден на CPU/wall — прежние профиля CPU его не показывали (слепая зона закрыта); (3) триггер неизвестен: ticket-churn мобов vs накопленная очередь load (голодание region_threads) vs фазовый save/reload
- Парити-рамка рычага: кэш декодированных chunk-data с инвалидацией по ревизии = устранение ПОВТОРНОЙ десериализации (наблюдаемое поведение тикетов не меняется); решение ПОСЛЕ RECON-13c
> (11) NEXT (тип+1): RECON-13c — инструмент rate-подсчёта chunk-load событий (диагностический лог в kernel-loader обёртке Paper chunk system ИЛИ подсчёт через JFR/async-profiler окна; логирование ≠ config-win) + проверка 4 фаз (burst на каждой или только последней?) → выбор: chunk-parse diet (кэш) ИЛИ ticket-churn рычаг. Затем реализация + оракул + диспатч гейт-лега. Диспатчей 0 (в полёте пусто, готового рычага нет); CI-бутов 0.

## СТАТУС ×N — TASK-326 (RECON-13c: burst 390-420s атрибутирован = checkInsideBlocks-курсор; слепая зона JFR OAS зафиксирована) — 2026-09-19 ~16:5x +08
**Статус: в полёте пусто; офлайн RECON-13c (JFR-фазовый анализ s7165, 4 новых инструмента jfr/ + 3 py): ObjectAllocationSample по 30s-бакетам: steady 30-360s = 17-25GB (AABB/Vec3-семья ~80%, [B≈0); BURST t=390-420s = 40.9GB/бакет: BlockPos$6=19GB (×10), RandomAccessSpliterator=8.8GB (×1000), AABB=10.5GB; потоки Server thread 11.15GB + region-worker-2 8.8GB.**
- Атрибуция (ап-стэки, 451 сэмпл/50 стэков, 100%): BlockPos$6 = итератор BlockPos.betweenCornersInDirection из BlockGetter.forEachBlockIntersectedBetween ← Entity.checkInsideBlocks ← applyEffectsFromBlocks ← ItemEntity.tick/LivingEntity.aiStep (все мобы) × RegionTickOps: на каждый тик х 150k сущностей новый итератор+курсор = массовый мелкообъектный churn. Не покрыт alloc_diet/inside_cache/zero_alloc.
- Слепая зона JFR OAS (система): weight-сэмплер атрибутирует TLAB-хвост крупным классам → DataResult 19.4% ap = 0 в JFR; parse-[B burst в steady невидим. РЕШЕНИЕ: ap-collapsed = истина по классам/вызывателям/объёмам; JFR = истина по времени/потокам/фазам. Следствие: parse 33.38% (RECON-13b) реален для ap-окна, периодика остаётся открытой (не блокер кэша по ревизии).
> (12) NEXT (тип+1): реализация lever #11 CRUSSTY_ZERO_CURSOR — zero-alloc итератор betweenCornersInDirection (thread-local reusable cursor в forEachBlockIntersectedBetween) + прегистер-гейты: PG1 BlockPos$6-сэмплы ↓≥80%; PG2 TPS last-5 медиана ≥2.0; PG3 remset p50 ±1%; PG4 NCDFE=0/CRASH-FREE; A/B min-of-2 → диспатч. Параллельно RECON-13d: parse-повторы счётчик (пассивный диаг). Диспатчей за тик 0 (рычаг реализуется); CI-бутов 0.

## СТАТУС ×N — TASK-327 (RECON-13d chunk-parse census lever: реализация+гейты+диспатч В ОДНОМ ТИКЕ; лег s7168 в полёте) — 2026-09-19 ~17:5x +08
**Статус: ТОП-1 (chunk-parse 33.38% ap, RECON-13b/13e) — рычаг РЕАЛИЗОВАН И ДИСПАТЧЕН в этом тике: lever CRUSSTY_PARSE_DIAG — пассивная перепись parse-per-chunk: мост ChunkParseDiagOps (kernel loader, single classfile, no nested) + retarget_ldc_virtual_to_static в classfile.rs: ОДИН сайт ldc"xPos"+invokevirtual CompoundTag.getIntOr в SerializableChunkData.parse → ChunkParseDiagOps.diagXIntOr (receiver-prepended static, стэк-форма идентична, delegate bit-exact, note = O(1) ConcurrentHashMap.merge, catch(Throwable)). Решение по кэшу lever #12 прегенерируется: repeat_share >= 30% → GO (декод-кэш по ревизии); < 10% → cache REFUTED (ticket-churn); 10-30% → расширенный RECON.**
- Компоненты: entityinside ChunkParseDiagOps.java/.class (собран против kernel 21, major 65); src/parse_diag.rs (flush_diet-паттерн: hook+pristine no-op retransform+bridge+patch+retransform, resolution-closure guard); classfile.rs string_value+retarget_ldc_virtual_to_static (armed-перезарядка: armed сбрасывается только на invoke/новом ldc — iconst-дефолт между ldc и invoke не мешает; AlreadyPatched идемпотентность); fixture SerializableChunkData.class + cargo-тест на РЕАЛЬНОМ классе (Retargeted{1} → AlreadyPatched); 176/176 cargo tests PASS; release build PASS.
- Пайплайн: workflow input parse_diag → PARSE_DIAG → CRUSSTY_PARSE_DIAG + CRUSSTY_PARSE_DIAG_FILE=$WORK/chunk-parse-diag.txt → артефакт upload. GitHub 25-input лимит: LEGACY summon_sweeps дропнут (всегда 0 с S7-129); 26→25 inputs. dispatch_s7168.py (S7-108+head-гварды): первый POST 422 (26 inputs) → фикс → **ЛЕГ s7168 В ПОЛЁТЕ: run 35435848509 @ 1fc46c3 (17:51:45 +08)**.
> (13) NEXT (тип+1): ABSORB s7168 готовым absorb-протоколом: PG-D0 census присутствует/непуст; PG-D1 CRASH-FREE + NCDFE=0; PG-D2 TPS last-5 медиана >= 2.0 (не хуже диаг-базы s7165); PG-D3 remset p50 ±1%. Вердикт = выбор рычага lever #12: repeat_share >= 30% → реализация decode-cache (следующий тик ДО КОНЦА по чартеру «начал-закончил»); < 10% → REFUTED кэша → ТОП-2 (entity-tick-core 21.39% CPU: travel-physics AABB/Vec3). CI-бутов 1 (s7168 санкционирован); CI-бутов гейт-класса 0.

## СТАТУС ×N — TASK-328 (absorb s7168: PG-D0 FAIL перепись не доставлена; РУТ-КАУЗ+ФИКС armDumpTask; лег s7169 в полёте) — 2026-09-19 ~18:2x +08
**Статус: ТОП-1 chunk-parse — absorb s7168 (run 35435848509, success @ 1fc46c3): PG-D0 FAIL — chunk-parse-diag.txt ОТСУТСТВУЕТ в артефакте при patch=ARMED (Retargeted{sites:1}, retransform rc=0, pop VALID, NCDFE=0, OOM=нет). repeat_share НЕИЗВЕСТЕН, кэш-решение отложено. РУТ-КАУЗ: арм dump-таска шёл через find_class(OPS_CLASS) из НОВОЙ нативной аттачмент-секции — kernel-loader-класс системным FindClass не виден, тихий clear_exception → dump-таск НИКОГДА не вставал, перепись жила в памяти и умирала с процессом. ФИКС: арм перенесён ВНУТРЬ define-closure (глобальный реф на свежеопределённый класс в руках), громкие маркеры "dump task ARMED -> путь" / "arm FAILED" в stdout.**
- PG-D2 s7168: TPS last-5 медиана=1.4 (база s7165=2.0) — контекст диаг-лега, не гейт; PG-D3 N/A — remset.log отсутствовал (диспатч s7168 шёл с recon_diag=0 — дефект пегистера TASK-327, исправлен: s7169 идёт с recon_diag=1 = точный s7165-класс базы для PG-D2/PG-D3). GC-фон здоров: young=155 full=0 19.5s max=179ms (база 161/0/20.3s/189ms) — сам диаг-мост behaved чисто.
- absorb_s7168.py (самотест PASS: парсер+бакеты 1/2/3-5/6-10/11-50/>50+консистентность row_sum-vs-SUMMARY+ветки вердикта <10/>=30, границы 9.99/10.0/29.99/30.0) — absorb одной командой; PG-D3 N/A-толерантен. cargo 176/176 PASS; release PASS. c-crussty a93324a.
> (14) NEXT (тип+1): ABSORB s7169 (run 35437243128, in_progress @ a93324a, диспатч 18:22:55 +08) готовым absorb_s7168.py → PG-D0 census ПРИСУТСТВУЕТ (маркер "dump task ARMED" в stdout обязателен) → вердикт lever #12: repeat_share >=30% → GO decode-cache по ревизии (реализация ДО КОНЦА в том же тике); <10% → REFUTED → ТОП-2 entity-tick-core 21.39% CPU. Гейты: PG-D0 census+консистентность; PG-D1 CRASH-FREE/NCDFE=0; PG-D2 TPS>=2.0 (vs s7165); PG-D3 remset p50 ±1% (vs 6,324,224). Диспатчей 1 (s7169 диаг-верификация); S7-108 чист; CI-бутов 1 (санкционирован).

## СТАТУС ×N — TASK-330 (census s7169: кэш REFUTED 0.0% + parse-лейн закрыт; свежий ТОП: entity-tick-core ТОП-1; рычаг #11 ZERO-CURSOR реализован+диспатчен в тике; лег s7170 в полёте) — 2026-09-19 ~19:2x +08
**Статус: (1) ABSORB s7169 (run 35437243128, success @ a93324a): фикс арма РАБОТАЕТ ("dump task ARMED" в stdout), census доставлена — repeat_share = 0.0% (160/160 unique, consistency PASS) → lever #12 decode-cache REFUTED ЖЁСТКО; свежий профиль s7169: parse-лейн 0.00% alloc / 0.02% CPU — лейн chunk-parse ИСЧЕЗ из свежего профиля (был 45% в burst-окне s7165; 160 parse/soak = эпизодические волны). По «ТОП-1 ОБЯЗАН УПАСТЬ» лейн закрыт (исчез + решающий census). (2) Свежий ТОП (recon14_fresh_top.py): ТОП-1 = entity-tick-core 34.04% CPU / 68.20% alloc (Vec3 21.06 + AABB 18.87 + BlockPos-семья ~13.6); remset/card-set 24.26% CPU; navigation упал до 1.15% (эффект inside_cache). (3) Крупнейший атакуемый под-лейн ТОП-1 = КУРСОР betweenCornersInDirection: 29.18% alloc / 6.42% CPU (s7169) — старая заморозка «1.5%» снята; механизм: на каждый forEachBlockIntersectedBetween fresh BlockPos$6+MutableBlockPos, в addCollisionsAlongTravel — новый итератор на КАЖДЫЙ шаг travel; единственная точка порождения = lambda$betweenCornersInDirection$8 (cp-скан).**
- Рычаг #11 v1 CRUSSTY_ZERO_CURSOR реализован ДО КОНЦА в тике: ZeroCursorIter (бит-точная реплика BlockPos$6, свой Iterator) + ZeroCursorOps.lambda8 (ThreadLocal-кольцо ×4, reset на вызов); **CursorLockstepHarness 350,000 сценариев / 31,761,141 позиция бит-в-бит PASS** против реальной ванильной lambda (аргументы заводские нетронутые 314B → дрейф порядка невозможен); redirect_static_method_body_to_static (static→static, без receiver-prepend, +4 теста на BlockPos_real fixture); zero_cursor.rs (pristine/serve хук + define обоих мостов в kernel loader + retransform); workflow input zero_cursor добавлен, fluid_free дропнут (лимит 25; требует paletted_demux=1 — никогда не армился). cargo 182/182 PASS; release PASS. c-crussty 28539f3.
> (15) NEXT (тип+1): ABSORB s7170 (run 35440054574, in_progress @ 28539f3, 19:25:07 +08) по прегистеру (dispatch_s7170.py): PG-Z2 доставка (pristine sighting/defined bridges/computed redirect Retargeted{sites:1}/armed rc=0, NCDFE=0, pop VALID); PG-Z3 TPS ≥1.60 + дельта vs банковых 1.80; PG-Z4a cursor-alloc лейн ≥−50% vs база 29.18% (ожидание −80..−95%); PG-Z4b young ≤154, 0 Full; PG-Z4c cursor-CPU ≥−30% vs 6.42%; CRASH-FREE ≤5 исключений. Банкинг: полный PASS → CUMULATIVE v4 = v3 + zero_cursor=1 → свежий ТОП → следующий круг ТОП-ПОЖИРАТЕЛЬ; FAIL → REFUTED + rollback zero_cursor=0 → следующий под-лейн ТОП-1 (Vec3/AABB travel-чейн или remset-драйвер). Диспатчей 1 (гейт-лег); S7-108 чист; CI-бутов 1 (санкционирован).

## СТАТУС ×N — TASK-331 (absorb s7170/71/72/73: ТРИ дефекта доставки с руут-каузами+фиксами; lever #11 v1 REFUTED по бусту — rollback; cursor-лейн НЕ закрыт, следующий под-лейн AABB/Vec3) — 2026-09-19 ~21:2x +08
**Статус: (1) s7170 (35440054574) failure за 7 мин ДО старта сервера: run_world3.sh:161 — литеральный `BlockPos$6` в double-quoted echo под `set -u` разворачивался как unbound позиционный `$6` → ФИКС экранированием `\$6` (5686c5b). (2) s7171 (35441020234) success, но HOOK DORMANT: resolution-кложура требовала `reset(...)Ljava/util/Iterator;`, а бридж declares `reset(...)V` (lambda8 возвращает пул-инстанс сам) → честный гард заблокировал рычаг → ФИКС RESET_DESC=...V + юнит-тест-пин кложуры к реальным байтам (cd4baab). (3) s7172 (35442145580) success: бриджи defined, redirect Retargeted{sites:1}, НО retransform rc=-5 = JVMTI_ERROR_FAILS_VERIFICATION — класс остался ванильным (BlockPos$6 334 сэмпла, TPS 1.30). РУТ-КАУЗ ВОСПРОИЗВЕДЁН ЛОКАЛЬНО через Instrumentation.retransformClasses (+fastutil+mojang-libs classpath): `redirect_static_method_body_to_static` грузил параметры со СЛОТА 1 (instance-конвенция receiver+params), а статический метод имеет слоты с 0 → aload_1-first цепочка → VerifyError. ФИКС: ремап слотов −1 (4ddb10f). (4) ОФЛАЙН-ГАРД нового класса: scripts/verify_redirect_patch.sh + entityinside/harness/RetransformProbe.java — настоящий JVMTI retransform-путь (defineClass-верификация НЕ ловила баг): RETRANSFORM OK + SEMANTIC OK (871 позиция через перенаправленную фабрику); +тест точной байт-цепочки (aload_0..2, iload_3, iload 4..8, areturn). cargo 185/185, release OK. (5) s7173 (35444005075 @ 27ef945) success — ПОЛНАЯ ДОСТАВКА: PG-Z2 PASS (armed rc=0, bridges, Retargeted{sites:1}, NCDFE=0, pop VALID), PG-Z4b PASS (young=142≤154, Full=0), CRASH-FREE PASS (2≤5). НО: PG-Z4a cursor-alloc 22.96% vs база 29.18% = дроп +21.3% < гейт ≥50% (BlockPos$6 убит из топа — теперь $$Lambda 139/LongOpenHashSet 60, лейн держат AABB 746+Vec3 621+long[] 400); PG-Z4c cursor-CPU 7.24% = −12.8% НАОБОРОТ (ZeroCursor-фреймы 1.57% — пул дороже убранных аллокаций); PG-Z3 TPS median5=1.40 < harm-floor 1.60 = ВРЕД −22.2% vs банка 1.80. **ВЕРДИКТ: lever #11 v1 (пул ThreadLocal-кольцо ×4 с per-call reset) = честный REFUTED по бусту** — аллокационное давление падает (young 142), но CPU-оверхед пула съедает выигрыш. ПО ПРЕГИСТЕРУ: rollback zero_cursor=0 (мосты ZeroCursorIter/ZeroCursorOps + redirect_static_method_body_to_static остаются инфраструктурой, verified). Лейн по «ТОП-1 ОБЯЗАН УПАСТЬ» НЕ ЗАКРЫТ: allocation-лейн упал лишь на 21%. (6) Свежие данные для следующего рычага: после убийства BlockPos$6 крупнейшие листья cursor-лейна = **AABB 746 (27.7%) + Vec3 621 (23.1%) + long[] 400 (14.9%) сэмплов окна** — travel-чейн addCollisionsAlongTravel (fresh AABB/Vec3 на каждый шаг travel) → RECON-14b декомпозиция под-лейна → новый рычаг (кандидаты: переиспользование AABB через стек-локал по аналогии с v2 node-pool navigation; батч-скан между-corners без fresh-итераторов — но БЕЗ ThreadLocal-пула v1, чей CPU-оверхед измерен). ТОП-2 CPU по-прежнему remset/card-set 24.26% — не трогать до падения ТОП-1.
> (16) NEXT (тип+1): RECON-14b — декомпозиция cursor/travel-лейна по s7173-профилю (AABB/Vec3/long[] сэмплы: кто вызывает — travel-physics vs checkInsideBlocks vs push) до под-лейнов ≥5% → крупнейший под-лейн → НОВЫЙ рычаг (архитектурный, без пул-оверхеда v1). Гварды: verify_redirect_patch.sh обязателен для любого нового static-редиректа (RETRANSFORM+SEMANTIC OK ДО диспатча); S7-108; remote-head==local-head. zero_cursor=0 в банке (v3 без изменений). Диспатчей за тик 3 (все — верификация фикс-цепочки, санкционированные); S7-108 чист.

## СТАТУС ×N — TASK-332 (RECON-14b/14c: внутри-лейн = checkInsideBlocks 31.18% alloc; lever #12 INSIDE-DIET v1 реализован+доставлен 2 легами; min-of-2 TPS 1.50<1.60 → REFUTED-по-TPS rollback; ГЛАВНАЯ НАХОДКА: аллок-ось ≠ TPS-ось) — 2026-09-19 ~23:0x +08
**Статус: (1) RECON-14b (recon14b_travel_lanes.py): cursor/travel-семья на 92-94% = Entity.checkInsideBlocks (НЕ travel-physics); RECON-14c (recon14c_alloc_callers.py, всё окно s7173): ТОП-1 под-лейн ТОП-1 = inside-blocks 31.18% аллок-окна (AABB 1094=55% всех AABB, Vec3 718=37%, long[] 403=97%) / 9.20% CPU; travel-collide 16.38%; fluid 12.28%/9.96%; push 0.72%. javap-контракт снят: ваниль 5-arg checkInsideBlocks аллоцирует на вызов makeBoundingBox+deflate (2 AABB) + AtomicInteger + 10-капчурную lambda-визиторку; visit (lambda$2): полный блок → movedThrough=TRUE без теста → per-block visited.add+advanceStep+entityInside+onInsideBlock (4 вызова × 6 блоков × 150k × 300т = ~1.1 млрд вызовов). (2) Lever #12 INSIDE-DIET v1 реализован ДО КОНЦА: InsideDietOps (checkInsideBlocks: 1 box + 1 visitor, movedFar/deflate bit-exact) + InsideDietVisitor (visit-транскрипция lambda$2 включая тонкость visited.add==false → пропуск fluid-advance; debug-ветки отброшены задокументированно; crash-путь сохранён); FULL-DDA-транскрипция попытана и ОТКЛОНЕНА харнессом (21/350k расхождений в DDA-порядке) → v1 = ВАНИЛЬНЫЙ walk (bit-exact по конструкции). redirect_method_body_to_static instance→static (receiver-prepend, готовый хелпер), resolution closure (Ops.checkInsideBlocks + Visitor.visit), entity_compose STAGE-9, env CRUSSTY_INSIDE_DIET, workflow input inside_diet (25-input лимит: input zero_cursor удалён из yml — рычаг #11 REFUTED, флаг закат; push-запись #133 0-jobs была startup-failure невалидного 26-input YAML — вылечено). cargo 189/189, release OK. (3) Деливери s7174 (35447599753 @ 463e60b) + A/B повтор s7175 (35448586519 @ 6dae474, min-of-2 протокол): PG-ID2 PASS оба; лейн-эффект ВОСПРОИЗВОДИМ: inside-alloc 16.48% vs 31.18% = −47.2% ОБА лега; inside-CPU 3.21%/3.02% = −65.2%/−67.2%; young 141/123. НО TPS median5: 1.50 / 1.60 vs банк 1.80 — min-of-2 = 1.50 < harm-floor 1.60 → **REFUTED-по-TPS: rollback inside_diet=0 (мосты+stage-9 остаются verified-инфраструктурой)**. LEVER-BOOST <10% → по v7 лейн НЕ закрыт. (4) **ГЛАВНАЯ НАХОДКА ТИКА: аллок-ось ≠ TPS-ось.** Резка крупнейшего аллок-под-лейна на 47% + его CPU на 66% НЕ подняла TPS — значит на сцене X150K TPS ограничен НЕ аллок-давлением и НЕ внутри-лейном. Свежий CPU-ТОП (s7173/74): entity-tick-core 37.07% CPU / world-tick-misc 26.22% / remset-card-set 22.34%. Для TPS-буста ≥10% следующий рычаг выбирать по CPU/MSPT-осям: кандидаты — remset-драйверы (что рефайнится: card-dirty от каких stores), world-tick (block/fluid/random tick пути), PalettedContainer.get 2.64% (2.64%×150k×300т lookups). Аллок-диеты продолжать только как сопутствующие (GC-duty ~1.7% MSPT — не главное). (5) Инструменты: absorb_s7174.py (PG-ID2..4c, min-of-2), dispatch_s7174/7175.py, recon14b/c, build_inside_diet_ops.sh, RetransformProbe (verify_redirect_patch.sh) — не нарушен.
> (17) NEXT (тип+1): RECON-15 по CPU/MSPT-осям на свежих s7174/75-профилях: (a) remset/card-set 22.34% CPU — attribution card-dirty источников (какие stores dirty карт) → архитектурный рычаг (например, офф-крит-структуры в полях Entity/Section → off-heap/линейный скан); (b) world-tick-misc 26.22% — декомпозиция (random tick? fluid? block events?); (c) PalettedContainer.get-гигант. НЕ трогать: inside-диеты больше НЕ главный курс (TPS-нейтральны). Гвард: любой новый рычаг — сначала javap-контракт + honest пегистер (min-of-2 TPS ≥ банк+10% = GREEN; иначе RECON дальше). Банк: v3 БЕЗ изменений (inside_diet=0, zero_cursor=0).

## СТАТУС ×N — TASK-333 (RECON-15: MSPT-ось декомпозирована впервые; Server thread PARK на DONE 13.4% + дисбаланс стат-бакетов 478/651; lever #13 STEAL реализован+диспатчен в тике; лег s7176 в полёте) — 2026-09-19 ~23:5x +08

> (18) Статус: RECON-15 (recon15_mspt_lanes.py, deep-first лист→корень; чистый банк s7169 = 900 Server-thread wall сэмплов; CPUs: 4/4, G1 Refinement=4). (1) **MSPT-ось впервые**: planner-рамка+серия 26.6% / ai 14.3% / travel-collide 10.9% / chunk-access 10.3% / tracking 9.2% / fluid 9.2% / inside 5.8%; Server thread НЕ спит между тиками (0 sleep-сэмплов) = непрерывный catch-up. (2) **КЛЮЧЕВАЯ НАХОДКА**: Server thread PARK на DONE-барьере (RegionTickOps tickBucket(0) → CyclicBarrier) 121/900 = 13.4% wall; арифметика фазы 3: главный 478 работа vs воркеры по 651 (сумма 2432, критический путь 651 → параллелизм 3.74/4 = 93%) → НЕ голод, а ДИСБАЛАНС статических квадрантов. (3) **LEVER #13 v1 (CRUSSTY_REGION_STEAL) РЕАЛИЗОВАН ДО КОНЦА**: RegionTickOps v2 — общий снапшот-массив в ванильном порядке + AtomicInteger-курсор чанков (512); главный и воркеры тянут чанки до исчерпания → DONE-park → ~0, критический путь → ~608; per-entity consumer бит-в-бит; interleave = принятый класс паритета (S7-155); rollback = env. Оффлайн: RegionLockstepHarness + STEAL-ребёнок — **PG1 PASS W=1==W=2==W=4==W=4+STEAL** (60 тиков, мутационный шторм через реальные гварды); cargo 189/189; release PASS. (4) ДИСПАТЧ s7176: run **в полёте** @ head, банк v3 + region_steal=1 (inside_diet input удалён из yml — 25-лимит, lever #12 закат; bridges=infrastructure). Прегистер: PG-S2 доставка/ARMED, PG-S3a DONE-park ≤40/900, PG-S3b баланс ≤0.20, PG-S4 TPS ≥1.98 (банк+10%), PG-S5 young ≤174/0 Full, CRASH-FREE; честная оценка ~+4-6% — REFUTED-by-TPS → rollback, лейн НЕ закрыт (v7).

> (18) NEXT (тип+1): ABSORB s7176 (run_id из CLAIMS) готовым гейт-чеком по прегистеру PG-S2..S5 → вердикт lever #13: PASS → CUMULATIVE v4 = v3 + region_steal=1 → свежий ТОП (worst-case фазы 3 пересчёт) → ТОП-2 по MSPT-оси (ai 14.3%: shouldRecomputePath/GoalSelector — декомпозиция до ≥5% под-лейнов); REFUTED → rollback region_steal=0 + RECON-16: (a) remset-аттрибуция (card-dirty от каких stores — s7169 remset.log), (b) серия-фаза снапшота 26.6% planner-семьи — декомпозиция до под-лейнов ≥5% → новый рычаг того же ТОП-1-лейна. Гварды: S7-108, remote-head==local-head, javap-контракт перед static-редиректами. Банк: v3 (STEAL не банкингован до PASS).

---

> СТАТУС ×19 (TASK-334, тик 23:43 +08, Job 397731): (1) ЛЕГ s7176 (run 35452002378 @ fd91c51, dispatched 23:30 TASK-333) = **FAILURE — СЕРВЕР-КРЭШ mid-soak**: 150k инъекция VALID (45.9s), через ~65s соака воркер-тред STEAL (RegionTickOps.stealChunks:324 → Zombie.travel → Entity.checkFallDamage → FarmBlock.turnToDirt → setBlockAndUpdate → **ServerLevel.sendBlockUpdated:1883**) → **NPE fastutil ObjectOpenHashSet$SetIterator "this.wrapped is null"** → Server thread упал той же точкой (ReportedException "Exception while updating neighbours") → graceful shutdown 15:43:23 (uptime 197s) → профилировщики не собрали collapsed (cpu/wall/alloc 0 сэмплов) → BENCH-4 gate 1b каскадный FAIL (polls=1). РУТ-КАУЗ: race на разделяемом Set в sendBlockUpdated — в ваниле путь только Server thread; STEAL v1 тикает entity на воркерах БЕЗ канализации блок-апдейтов. НЕ флейк (не OOM: GC здоров, 5.6/6.1GB). НЕ дубликат-фикс: diff fd91c51..99b741b = worklog-only. (2) РЕКОН-16 написан (RECON16_REGION_STEAL_ABSORB.md): полный план v2 — javap точки 1883 → канализация sendBlockUpdated с воркеров через batch-очередь владельца/главного (класс batch_collector) → lockstep-харнесс сценарий «FarmBlock.fallOn на воркере» → честный A/B min-of-2 лег s7177 (region_steal=2). Методика PG-S3a DONE-park воспроизведена в absorb_s7176.py и откалибрована на s7169 (121/901 = 13.4% точное попадание в RECON-15 базу). (3) ВЕРДИКТ lever #13 v1: **CRASH-REFUTED** (краш-фри FAIL); rollback region_steal=0 (дефолт); лейн НЕ закрыт по v7 (DONE-park 13.4% MSPT-фазы 3 остаётся ТОП-MSPT-целью). (4) Сопутствующий absorb-дубль s7169 (run 35437243128 повторно) сверен с TASK-330: вердикты идентичны (repeat_share 0.0%, parse-лейн закрыт), артефакт-дир восстановлен git checkout. Свежий ТОП подтверждён моим независимым разбором s7169 collapsed (top_s7169.py): entity-bucket 91.85% java-CPU / 81.16% alloc, travel-физика Vec3/AABB 45.73% alloc-листьев (recon14_travel.py: Vec3.add 9.81%, makeBoundingBox 5.70%, AABB.inflate 4.11%, Fluid.getAABB 3.46%, FlowingFluid.getFlow 1.53%) — заготовка рычага после STEAL. (5) cargo/release не трогались (0 код-коммитов — фикс v2 в следующем тике по плану RECON-16). NEXT (id 335): ФИКС STEAL v2 по RECON-16 §План v2 ДО КОНЦА тика (реализация+lockstep+cargo+commit/push+DISPATCH s7177, region_steal=2, гейты PG-S2..S5 + crash-free против FarmBlock-сценария); javap-контракт sendBlockUpdated перед редиректами (S7-закон); гварды S7-108 + remote-head==local-head. Если javap-инспекция покажет per-thread-инвариант — фикс инициализацией контекста воркера, не очередью.

---

> СТАТУС ×20 (TASK-335, тик 00:08 +08, Job 397731): (1) **ФИКС STEAL v2 (S7-168 BU-DEFER) РЕАЛИЗОВАН ДО КОНЦА + ДИСПАТЧ s7177**: javap-контракт sendBlockUpdated восстановлен точным дизассемблированием (javap_lite.py, codelen=235: гейт isUpdatingNavigations + итерация package-private navigatingMobs (fastutil ObjectOpenHashSet) с shouldRecomputePath-фильтром и recomputePath-реплеем; блок 160..171 мёртвый, Level.sendBlockUpdated отсутствует → else-путь no-op). Фикс: тело sendBlockUpdated редиректится на BlockUpdateOps.handle (receiver-prepended static, strict sites:1) — воркеры выполняют потокобезопасный префикс (blockChanged + pathTypesByPosCache.invalidate через Unsafe + shapes-дельта гейт) и ДЕРФЯТ navigate-пасс; главный реплеит FIFO в фазе-4 RegionTickOps (после DONE-барьера, обе точки join: forEach/parallelTick) — тот же класс дисциплины, что EntityCallbacks Mut-очередь. (2) Оффлайн-гейты: cargo **195/195** (+6 новых: no-nested, single-classfile, bytes-match, resolution-closure, redirect-table==sendBlockUpdated, REAL-ServerLevel-fixture sites:1 + idempotence); release build PASS. (3) Инфра: workflow input bu_defer добавлен (слот освобождён удалением закатанного paletted_demux — 25-лимит GitHub); run_world3.sh passthrough + run-env маркер; dispatch_s7177.py (S7-108 + remote-head==local-head PASS). (4) **ЛЕГ s7177 В ПОЛЁТЕ: run 35455926384 @ ecdd484 (00:44 +08), банк v3 + region_steal=1 + bu_defer=1** (диаги выкл). Прегистер: PG-V2 доставка (bu_defer:1 + region_steal:1 + «BU-DEFER composed» в stdout), PG-V2a crash-free против s7176-инцидента (0 NPE/«unexpected exception», сервер жив в конце соака), PG-V2b DONE-park ≤40/900 (база 121/901), PG-V2c TPS median5 ≥1.98 (банк 1.80+10%; честная оценка ~+4-6% — REFUTED-by-TPS → rollback обоих, лейн НЕ закрыт v7), PG-V2d young ≤174/0 Full. (5) NEXT (id 336): ABSORB s7177 (absorb_s7177.py по гейтам) → PASS: CUMULATIVE v4 = v3 + region_steal=1 + bu_defer=1 → свежий ТОП по трём осям → следующий рычаг (travel Vec3/AABB churn 45.73% alloc — вызов-сайты в recon14_travel.py; либо remset-аттрибуция RECON-16a) → FAIL-by-TPS: rollback + RECON-17 того же лейна (лейн НЕ закрывается). Гварды: S7-108, remote-head==local-head, javap-контракт.

---

> СТАТУС ×21 (TASK-336, тик 00:43 +08, Job 397731): (1) s7177 (run 35455926384 @ ecdd484, region_steal=1 + bu_defer=1) **в полёте** → ветка (4c): absorb-тул подготовлен до коммит-пригодного состояния — **scripts/bench4_recon/absorb_s7177.py** (гейты PG-V2 доставка [bu_defer:1 + region_steal:1 + «BU-DEFER composed» + NCDFE=0 + pop VALID], PG-V2a crash-free vs s7176-инцидент [0 threw/unexpected/NPE-маркеры + soak-complete улики: alloc-collapsed непуст [в крэш-леге EMPTY] + TPS-поллов ≥4 [в крэш-леге 2]], PG-V2b DONE-park ≤40/901 [методика 121/901, калибровка s7169], PG-V2c TPS median5 ≥1.98, PG-V2d young ≤174/0 Full; вердикт-развилка: GREEN → CUMULATIVE v4 = v3+region_steal=1+bu_defer=1; REFUTED-by-TPS → rollback обоих + RECON-17 того же лейна; delivery-дефект → руут-кауз+фикс+ре-диспатч в тике). (2) Smoke-проверка: exit 3 (в полёте — корректный guard). Коммит push (c-crussty). (3) NEXT (id 337): ABSORB s7177 одной командой `python3 scripts/bench4_recon/absorb_s7177.py 35455926384` → вердикт по прегистеру → PASS: CUMULATIVE v4 + свежий ТОП по трём осям (кандидаты: travel Vec3/AABB churn 45.73% alloc-листьев, вызов-сайты recon14_travel.py; remset-аттрибуция) → REFUTED-by-TPS: rollback + RECON-17 → дефект: фикс+ре-диспатч.

---

> **СТАТУС ×22 (TASK-337, тик 01:08 +08, Job 397731): (1) s7177 (run 35455926384, success @ ecdd484) ABSORB — вердикт по прегистеру: REFUTED-by-TPS** (PG-V2/V2a/V2b/V2d PASS: bu_defer ARMED, 0 NPE — race s7176 мёртв, DONE-park 121/901=13.4% → 16/1174=1.4%, young 166/Full 0; PG-V2c FAIL: median5=1.40 < 1.98). Rollback: дефолты yml уже 0, мосты = инфраструктура. (2) **RECON-17 (RECON17_PARK_ECONOMICS.md) — ДВЕ НАХОДКИ**: (A) **конфаундер раннера**: runner_cpu_index s7177=6.87M vs s7169=8.57M (−19.8% машина); консольная серия самого s7169 = median5 1.60 (не «банк 1.80» = leg#5); скорость-нормализованный TPS s7177 = **+9.1% vs банк** — сырое −22% = машина, не рычаг; вся систематика n=1 легов (1.30–1.60 vs 1.80) объясняется раннер-шумом ±20%. (B) **парк был симптомом**: фаза-3 насыщена (93% параллелизм, RECON-15), BU-replay 26 сэмплов (бесплатен), потолок park-fill ~+7-9% < бара 10% → лейн DONE-park исчерпан как «park-kill→TPS»; реальный рычаг = воркер-крит-путь (кандидат #14 TRAVEL-ALLOC-DIET: travel-листья 45.73% alloc, entity-bucket 81.16% alloc, GC-семья 35.4% CPU). (3) **ПРОТОКОЛ (v7-жёстко): все TPS-гейты дальше = PAIRED A/B min-of-2** на одном классе раннера (runner_cpu_index ±5%), вердикт по нормализованной дельте. (4) ДИСПАТЧ **s7178 = re-cal банка** (конфиг v3, region_steal=0+bu_defer=0; PG-RC1 доставка, PG-RC2 runner-зонд, PG-RC3 TPS-якорь [измерение, не гейт], PG-RC4 GC) → следующий тик s7179 = STEAL v2 на том же классе раннера → парная дельта ≥+10% = GREEN v4. Тулы: absorb_s7177.py (фикс NoRedirect на Azure 302 — рутины 401), recon17_worker_axis{,2}.py, recon17_cpu_delta.py, dispatch_s7178.py. (5) NEXT (id 338): ABSORB s7178 → якорь v3-RECAL (median + runner index в CLAIMS) → DISPATCH s7179 (region_steal=1+bu_defer=1) → тик id 339: парный вердикт ≥+10% → GREEN v4 / лейн открыт с #14.

---

> **СТАТУС ×23 (TASK-338, тик 01:43 +08, Job 397731): (1) s7178 re-cal банка (run 35457901949, success @ df6b3c4) ABSORB — RECON19_BANK_RECAL_ABSORB.md**: PG-RC1 PASS (region_steal=0/bu_defer=0/inside_cache=1/flush_diet=1/region_threads=4/batch_collector=1, NCDFE=0, pop VALID, ARMED), PG-RC2 runner_cpu_index=**8493973** (класс s7169 8566450, −0.85%), PG-RC3 TPS-серия [1.4,1.4,1.4] (поллов мало — каденс; соак полный 309s, инъекция в срок), **якорь median5=1.40 @ 8.49M**, PG-RC4 PASS (young 142, Full 0), PG-RC5 N/A — **AP-PID дефект**: ap.log «Process 3473 not found» → wall/alloc-окна мертвы (cpu-окно 38.5MB живо). (2) **КЛЮЧЕВАЯ НАХОДКА RECON-19: шум TPS-оси ±12% на идентичной конфигурации+классе раннера** (s7178 банк v3 = 1.40 @ 8.49M vs s7169 банк v3+diag = 1.60 @ 8.57M — −11.8% нормализованно при чистом A) → одиночные пары различают буст ≥+10% только в связке min-of-2; протокол paired A/B подтверждён, бар остаётся ≥+10% нормализованной парной дельты. (3) ДИСПАТЧ **s7179 = STEAL v2 парная нога** (region_steal=1+bu_defer=1, остальное банк v3; PG-P1 доставка, PG-P2 crash-free, PG-P3 парный TPS: раннер ±5% от 8493973 — иначе лег невалиден для пары и ре-диспатч без вердикта; дельта ≥+10% → кандидат GREEN → подтверждающий s7180 по min-of-2 → banking v4; <+10% → лейн открыт с #14 TRAVEL-ALLOC-DIET; PG-P4 GC; PG-P5 park ~1.4% класс, N/A-толерантен из-за AP-PID дефекта). (4) NEXT (id 339): ABSORB s7179 → парный вердикт (±5% раннер-класс + норм. дельта) → GREEN-кандидат: dispatch s7180 (подтверждающий) / < +10%: RECON-20 лейна с рычагом #14 (travel-листья 45.73% alloc).

---

> **СТАТУС ×24 (TASK-339, тик 02:2x +08, Job 397731): (1) s7179 (run 35459232032, FAILURE) разобран по job-логу + артефакту: INFRA-FLAKE СТАРТА, не вердикт** — сервер-тред заклинил через 11s после «Done» в ServerChunkCache.syncLoad (чанк (−30,12), entityChunkFromDisk=false, managedBlock yield-петля), watchdog ×5 (10..30s), POPULATION INJECT START 17:56:23 ушёл в заклинивший сервер из консоли, workflow остановил 17:56:56; 1 TPS-полл (21.1 пре-инжект), 0 soak-поллов → BENCH-4 fixture INVALID (mobcaps/heartbeat непарсимы) → gate exit 1. Рычаги (region_steal/bu_defer) arm-ятся после старта — соак не состоялся, доказательств по рычагу ноль. Тот же класс клина отсутствует на s7169/s7177/s7178 с тем же банком → флейк медленного раннера (6.72M, второй медленный подряд; якорный класс = 8.49M). (2) Якорь s7178 (1.40 @ 8.49M) стоит; парная нога РЕ-ДИСПАТЧЕНA как **s7180** (те же инпуты region_steal=1+bu_defer=1; PG-P1..P5, парность = раннер ±5% от 8493973; гейт-фейл fixture-класса = ре-диспатч без вердикта, максимум 2 попытки подряд — дальше пауза на флейк-класс раннера). (3) NEXT (id 340): ABSORB s7180 → парный вердикт (≥+10% норм. → подтверждающий s7181 → banking v4; <+10% → RECON-20 с #14 TRAVEL-ALLOC-DIET).

---

> **СТАТУС ×25 (TASK-340, тик 02:08 +08, Job 397731): (1) s7180 in flight → ветка (4c): офлайн-работа до коммит-пригодного состояния.** (2) **absorb_s7180.py готов** (RECON-20 парный вердикт: PG-P1 доставка/PG-P2 crash-free/PG-P3 [раннер ±5% от якоря 8493973, иначе лег невалиден для пары → ре-диспатч; дельта = (med/runner)/(1.40/8493973); ≥+10% → CANDIDATE GREEN → подтверждающий s7181 по min-of-2 → banking v4; <+10% → RECON-20 #14]/PG-P4 GC/PG-P5 park N/A-толерантен; INFRA-FLAKE-рулетка s7179 вшита). (3) **dispatch_s7181.py готов** (подтверждающий лег min-of-2, банкинг-правило: обе ноги ≥+10% → CUMULATIVE v4). (4) **Свежий ТОП якорного профиля s7178 (recon20_top_s7178.py, cpu-collapsed 38.5MB жив при AP-PID-дефекте): GC/JVM-G1 36.23% = ТОП-1 CPU-оси** (G1-потоки в libc; young=142/Full=0 — драйвер = alloc-churn entity-работы), ai 16.04%, fluid 11.27%, RegionTickOps/BU 8.67%, travel 7.71%, inside 5.66% → рычаг #14 TRAVEL-ALLOC-DIET (zero-alloc AABB/Vec3 collide-пути, RECON-14: travel-листья 45.73% alloc) бьёт в ТОП-1 через давление на GC — готов к RECON-20 при <+10% пары. (5) NEXT (id 341): ABSORB s7180 одной командой `python3 scripts/bench4_recon/absorb_s7180.py 35460026013` → парный вердикт → CANDIDATE GREEN: dispatch s7181 в том же тике / <+10%: RECON-20 + реализация #14 до конца тика.

---

> **СТАТУС ×26 (TASK-341, тик 02:4x +08, Job 397731): (1) s7180 (run 35460026013 @ 0c65771) ABSORB — вердикт CRASH-REFUTED, НЕ флейк**: через 44s после INJECT DONE сервер-тред упал ВНУТРИ BU-DEFER фазы-4 реплея (NPE fastutil SetIterator «wrapped is null» @ BlockUpdateOps.vanilla:117 ← RegionTickOps.drainDeferredBlockUpdates:168 ← stealTick:352, Server thread) — тот же race-класс s7176, второй страйк; runner 6997821 = медленный класс (−17.6% от якоря 8493973; 3-й медленный подряд: 6.72M/6.87M/6.99M) — пул раннеров бимодален. **ЛЕЙН STEAL v2 ЗАКРЫТ**: s7177 REFUTED-by-TPS (+9.1% норм. < 10%) + s7180 CRASH-REFUTED + потолок RECON-17 ~7-9% < 10% бара; rollback: дефолты yml уже 0 (мосты = инфраструктура). Доказательство: RECON20_S7180_CRASH_VERDICT.md. (2) **ДВОЙНОЙ БАР (директива владельца 2026-09-20, ЖЁСТКО): банкинг рычага ТОЛЬКО при одновременном росте ОБОИХ осей — скорость-нормализованная дельта ≥+10% И абсолютная TPS-дельта ≥+10% на одном классе раннера; min-of-2 сохранён; одна ось = НЕ GREEN.** (3) **ДИСПАТЧ s7182 = рычаг #14 TRAVEL-ALLOC-DIET v1** (новый ТОП-1 CPU: GC/JVM-G1 36.23% = alloc-churn, recon20_top_s7178.py): банк v3 + zero_alloc=1 (S7-164 ZeroAllocOps scalar: collidedWithFluid / collidedWithShapeMovingFrom / updateFluidHeightAndDoFluidPushing — fluid/collide AABB+Vec3 churn) + skip_store_bb=1 (S7-166 value-equal setBoundingBox store-skip), region_steal=0/bu_defer=0; **cpu_band_min/max = 7.8M..9.2M (S7-96d банд-гейт: fast-fail первым шагом ~1 мин вместо 11 мин непарного соака)** — ноги гарантированно в классе якоря. Гейты PG-T1..T5, ДВОЙНОЙ БАР в PG-T3. (4) Тулы: dispatch_s7182.py, absorb_s7182.py (failure-рулетка band-discard/crash-refuted/infra-flake, artifact+joblog fetch). (5) NEXT (id 342): ABSORB s7182 одной командой `python3 scripts/bench4_recon/absorb_s7182.py <run_id>` → ОБЕ оси ≥+10%: CANDIDATE GREEN → подтверждающий s7183 (min-of-2) → banking v4; < +10% хотя бы по одной оси: #14 v2 = javap-рекон makeBoundingBox/Vec3.add/AABB.inflate call-sites (recon14_travel.py) → TravelDietOps body-redirects → реализация+диспатч В ТОМ ЖЕ ТИКЕ.

## СТАТУС ×27 (TASK-342, 2026-09-20 ~03:1x +08) — s7182: два BAND-DISCARD + лег №3 в полёте; RECON-21 javap-контракт #14 v2 готов
(1) **s7182 v1 (#14 TRAVEL-ALLOC-DIET: банк v3 + zero_alloc=1 + skip_store_bb=1, cpu_band 7.8M..9.2M)**: run 35461248304 = BAND-DISCARD №1 (S7-96d fast-fail: раннер медленного класса, лег невалиден для пары — НЕ вердикт); ре-диспатч run 35461415596 = BAND-DISCARD №2 (тот же класс); банд-гейт работает как спроектировано (~1 мин discard вместо 11 мин соака), но пул раннеров отдаёт медленный класс дважды подряд (бимодальность S7-96). Третья нога **run 35461477587 @ 0abaaaf in flight** (появился из параллельной сессии через 68 с после моего ре-диспатча — риск двойного диспатча параллельных cron-сессий, S7-108 соблюдён: новых диспатчей не посылаю). Абсорб: `python3 scripts/bench4_recon/absorb_s7182.py 35461477587`.
(2) **RECON-21 javap-контракт #14 v2 TRAVEL-DIET готов** (RECON21_TRAVEL_DIET_CONTRACT.md + contract-traveldiet-s7182b/ дампы + recon21_travel_attr.py + recon21_javap.sh): свежая аллок-атрибуция окна s7177 (12,392 сэмпла) — Vec3.add 6.08% (travel-доля 2.31%, остальное ai/goals+nav), EntityDimensions.makeBoundingBox 4.72% (584/585 через Entity.makeBoundingBox(Vec3) ← checkInsideBlocks + collidedWithShapeMovingFrom), AABB.inflate 3.89% (главный сайт в updateFluidHeightAndDoFluidPushing — УЖЕ умирает под v1), Vec3.multiply 3.04%; travel-чейн = 10.07% окна. **v2a COLLIDE-DIET**: private Entity.collide(Vec3) — expandTowards(Vec3)@126 + getEntityHardCollisions@154 + expandTowards(DDD)@355/372 (временные боксы запроса); collectCandidateStepUpHeights expandTowards@6; move() Vec3-опы ×5; лестница RECON-10 (eps 1.0E-7, dcmp бит-в-бит; C2-инлайн блокирует CPU-атаку, но аллок-сайты в байт-коде живы). v2b TRAVEL-MATH: handleRelativeFrictionAndCalculateMovement (48 строк) + travelInFluid + getInputVector.
(3) Механика редиректов зафиксирована: TravelDietOps = ровно один classfile (S7-163), kernel loader + entity_compose stage-7, javap-дифф бриджа (прецедент SkipStoreOps), бит-точный порядок double, без мемоизации (median-exact). Гейты PG-T1..T5 с ДВОЙНЫМ БАРОМ в PG-T3 (обе оси ≥+10% vs якорь s7178=1.40@8493973, min-of-2).
(4) NEXT (id 343): абсорб 35461477587 одной командой → BAND-DISCARD: ре-диспатч до добычи fast-class ноги (макс 2 подряд дальше — фиксировать starvation-стрик в GOAL) → GREEN: s7183 min-of-2 → банкинг v4; < +10% по оси TPS при валидной паре: v2a COLLIDE-DIET реализуется из RECON-21 контракта БЕЗ дополнительного RECONа (реализация+гейты+commit/push+dispatch в одном тике).

---

> **СТАТУС ×28 (TASK-341-ADD / 342-ADD, тик 02:5x-03:5x +08, основная + параллельная cron-сессии): (1) s7184 (run 35461477587 @ aedad68) = SUCCESS → ABSORB: ANCHOR-SLOW ЗАПИСАН median5=1.60 @ runner 6680195** (банк v3, PG-R2 crash-free PASS, young 158/Full 0, 6 TPS-поллов) — медленно-классный якорь для пар #14; fast-класс недоступен: 5/5 медленных лендингов (6.72M/6.87M/6.99M/6.73M×2), s7182+s7183 fast-банд = BAND-DISCARD за ~11с каждый (S7-96d fast-fail подтверждён, НЕ вердикты). Тул: absorb_s7184.py (PG-R1..R5). (2) **ДИСПАТЧ s7185 = парная нога #14 v1 в МЕДЛЕННОМ классе: run 35462493831 @ aedad68** (банк v3 + zero_alloc=1 + skip_store_bb=1, банд 6.5M..7.2M, гейты PG-T1..T5) — **ДВОЙНОЙ БАР vs ANCHOR-SLOW: обе оси (норм. и абсолютная) ≥+10%**. Тул: absorb_s7185.py (якорь 1.60 @ 6680195 вшит). (3) Параллельная cron-сессия (TASK-342, c-crussty aedad68, dev-logs 8dee26a): RECON-21 javap-контракт **#14 v2a COLLIDE-DIET** (Entity.collide expandTowards@126/355/372 + getEntityHardCollisions@154 + step-up expandTowards + move() Vec3×5; лестница RECON-10 eps 1.0E-7; свежая аллок-атрибуция s7177: Vec3.add 6.08 / makeBoundingBox 4.72 / inflate 3.89, travel-чейн 10.07%) — готов к немедленной реализации, если v1 не даст бар. (4) Хазард параллельных сессий задокументирован обеими сторонами (двойной диспатч 68с), S7-108 цел — одна нога в полёте. (5) NEXT (id 343): ABSORB s7185 одной командой `python3 scripts/bench4_recon/absorb_s7185.py 35462493831` → ОБЕ оси ≥+10% vs 1.60 @ 6680195 → подтверждающий s7186 (min-of-2, dispatch_s7185.py повторно) → banking v4; < +10% хотя бы одна ось → v2a COLLIDE-DIET из RECON-21-контракта: реализация+гейты+cargo+commit/push+диспатч В ТОМ ЖЕ ТИКЕ.

> **×28-ADD (тот же тик): s7185 (run 35462493831) = BAND-DISCARD, но runner=6491076 — всего −0.14% под полом банды 6.5M при −2.8% от якоря 6680195 (внутри парного ±5%)** — пол банды был излишне узким; банд МЕДЛЕННОГО класса расширен до 6.2M..7.2M, **ре-диспатч s7186 (run 35462686159 @ 9afd098) прошёл гейт и соачится** — абсорб следующим тиком: `python3 scripts/bench4_recon/absorb_s7185.py 35462686159` (якорь-SLOW вшит). Тул dispatch_s7186.py.

## СТАТУС ×28 (TASK-343, 2026-09-20 ~03:4x +08) — s7182 leg#3 INVALID-PAIRING (параллельная сессия выжгла ногу без инпутов); #14 v2a TravelDietOps РЕАЛИЗОВАН И ЗАКОММИЧЕН (cargo 199/199); s7182 гейт-нога ре-диспатчнута
(1) **leg#3 (run 35461477587, SUCCESS, диспатч параллельной cron-сессии 18:31:38Z)**: абсорб показал PG-T1 FAIL (`zero_alloc=BAD:False`, `skip_store_bb=BAD:False` — рычаги НЕ вооружены, голый банк v3) + PG-T3 вне класса (runner 6680195 = медленный класс приcpu_band отсутствовавшем в инпутах — гейт не вооружён → полный 25-мин соак впустую). **VERDICT: INVALID-PAIRING — НЕ вердикт лейна**; это реализованный хазард двойного диспатча параллельных cron-сессий (см. ×27):-session диспатчила с неполным input-set. Побочная ценность: свежий референс банка v3 на медленном классе — median5=1.6 @ 6680195, young=158/Full=0 (GC-давление стабильно), park 12.9%; RECON-19 шум ±12% подтверждён третьим разом (v3 baseline 1.40-1.60).
(2) **#14 v2a COLLIDE-DIET реализован в этом тике** (RECON-21 контракт → код): TravelDietOps.java — скалярное зеркало private Entity.collide(Vec3) (268 строк байт-кода, офсеты 0-522) + private static calculateStepHeights (139 строк) — временные AABB (expandTowards@126, move@333, expandTowards@355+372), 4 ArrayList и FloatArraySet заменены thread-local scratch-слотами (Unsafe putDouble, полная перезапись перед использованием — закон ZeroAllocOps, НЕ кэш); продукты (Vec3 performCollisions, float[], финальный Vec3.add) остаются аллокациями; dcmpg/dcmpl/fcmpg/fcmpl лестницы бит-в-бит (fcmpl-флоат-углы: javac-полярность совпадает, NaN-углы недостижимы — конечные координаты). Механика: src/travel_diet.rs (kernel loader define + BRIDGE_READY + resolution closure), classfile.rs TD_REDIRECT_TARGETS (1 сайт) + patch_entity_traveldiet, entity_compose STAGE 10 (strict sites:1, fail-dominant), lib.rs activate. **Сборка: РОВНО ОДИН classfile** (первый билд пойман гвардом: анонимный ThreadLocal = nested class TravelDietOps$1 — заменён на withInitial(method-ref); S7-163 закон в действии). cargo 199/199 PASS (4 новых: redirect+verify на РЕАЛЬНОМ Entity fixture, idempotency, closure, no-nested). yml: flat_traversal (REFUTED #9) дропнут из 25-лимита → input travel_diet + env TRAVEL_DIET; run_world3.sh CRUSSTY_TRAVEL_DIET; dispatch_s7182.py почищен от flat_traversal-инпута (unknown input = 422 POST).
(3) **Гейт-нога s7182 ре-диспатчнута** (run см. CLAIMS; inputs: банк v3 + zero_alloc=1 + skip_store_bb=1 + cpu_band 7.8M..9.2M) — arm-маркеры Retargeted{sites:3}/{sites:1} ожидаемы в stdout; наблюдение: 3-й подряд sighting медленного класса сегодня (стрик starvation фиксируется) — банд-гейт при медленном классе отсеет за ~1 мин.
(4) NEXT (id 344): абсорб гейт-ноги одной командой → ОБЕ оси ≥+10%: CANDIDATE GREEN → s7183 min-of-2 → банкинг v4; валидная пара <+10% по TPS-оси: #14 v2b (travel-math: handleRelativeFrictionAndCalculateMovement 48 строк + travelInFluid) реализуется из RECON-21 контракта; CRASH: rollback + вердикт-док. Параллельным cron-сессиям: диспатчить ТОЛЬКО через dispatch_s7182.py с ПОЛНЫМ input-set (S7-108 + remote-head==local-head гварды внутри).

## СТАТУС ×29 (TASK-344, 2026-09-20 ~04:1x +08) — v2a ЗАВЕРИФИЦИРОВАН офлайн-локстепом (1.05M кейсов бит-в-бит); s7186 соачится
(1) **TravelDietLockstepHarness** (entityinside/harness/TravelDietLockstepHarness.java + scripts/run_travel_diet_lockstep.sh, plain JVM, INJECTS-ONLY, прецедент CursorLockstepHarness): (a) структурная связь TravelDietOps против РЕАЛЬНОГО kernel jar (s7178-recal — тот же, с которого снят контракт; stale-kernel lesson в скрипте); (b) **зеркала expandInto/moveInto vs РЕАЛЬНЫЙ AABB.expandTowards/move: 1,000,000 рандомизированных кейсов бит-в-бит** (doubleToRawLongBits по всем 6 полям; корнеры ±0.0/NaN/±Inf/1e-300/1e300/eps −9.999999747378752E-6 — NaN-углы сходятся с vanilla dcmp-семантикой); (c) **calculateStepHeights локстеп против РЕАЛЬНОГО private static Entity-метода через рефлексию: 50,000 сценариев бит-в-бит** (floatToRawIntBits поэлементно — лестница order-sensitive; Shapes.box рандом-шейпы + рандом AABB + рандом step/curr float). **ALL PASS, RC=0.** Остаток вне офлайн-охвата честно задокументирован: Level-секции collide (getEntityHardCollisions/getCollisionsForBlocksOrWorldBorder/performCollisions — идентичные ВЫЗОВЫ с идентичными аргументами по контракту) — live-парити обеспечивают гейты CI-лега.
(2) Инфра-уроки харнесса (зафиксированы в скрипте): stale-kernel гард (s7160-jar без moonrise$ хуков = link fail); Entity.<clinit> требует SharedConstants.tryDetectVersion() + Bootstrap.bootStrap() + полные server-libs (/tmp/alllibs2/*); set -o pipefail на find по отсутствующим директориям = тихая смерть (|| true).
(3) **s7186 (paired #14 v1, slow-класс, vs ANCHOR-SLOW 1.60 @ 6680195) в полёте** — соак ~16+ мин; диспатч этого тика НЕ посылали (S7-108, чужая нога).
(4) NEXT (id 345): абсорб s7186 (absorb_s7185.py 35462686159, ANCHOR-SLOW вшит, рулетка вшита) → ОБЕ оси ≥+10%: CANDIDATE GREEN → подтверждающий min-of-2 (dispatch_s7185.py, slow band 6.2M..7.2M) → банкинг v4 = v3+zero_alloc+skip_store_bb; < +10% валидной пары: v2a-лег (travel_diet=1, банк v3, cpu_band 6.2..7.2M — быстрый класс пробовать НЕ нужно: владелец 2026-09-20 объявил АКТИВНЫЙ КЛАСС = МЕДЛЕННЫЙ, якорь = ТОГО ЖЕ КЛАССА) — диспатч В ТОМ ЖЕ ТИКЕ через новый dispatch_s7187.py (инпуты: банк v3 + travel_diet=1 + zero_alloc=0 + skip_store_bb=0 — ИЗОЛЯЦИЯ эффекта v2a).

## СТАТУС ×30 (TASK-345, 2026-09-20 ~04:3x +08) — RECON-22: race navigatingMobs ЛАТЕНТЕН В БАНКЕ v3; s7186 INVALID-PAIRING (банд шире ±5%); диспатч v2a-ноги s7187
(1) **s7186 (run 35462686159 @ 9afd098, paired #14 v1 slow) = SUCCESS, но вердикт INVALID-PAIRING + PG-T2 FAIL**: runner 7072550 = +5.88% от якоря 6680195 — **банд 6.2M..7.2M ШИРЕ парного ±5% → диспатч-окно допускает непарные лендинги** (фикс: банд = якорь±5% = 6.35M..7.01M); + threw=3 (см. ниже). НЕ вердикт лейна v1.
(2) **RECON-22 (RECON22_BANK_V3_RACE.md) — ГЛАВНАЯ НАХОДКА**: 3× «Entity threw exception» = тот же fastutil navigatingMobs NPE, но на **ВАНИЛЬНОЙ** ServerLevel.sendBlockUpdated:1883 с воркера RegionTickOps.tickBucket(474) — **race ЛАТЕНТЕН В БАНКЕ v3** (region_threads воркеры → DoorBlock.neighborChanged из checkInsideBlocks → sendBlockUpdated → итерация navigatingMobs без cross-thread защиты; vanilla isUpdatingNavigations — однопоточная семантика). RECON-20 корректировка: s7176/s7180 CRASH — тот же фундаментальный race, STEAL/BU-DEFER меняли лишь точку итерации; S7-169-кандидат (гейт на replay) воркер-vs-воркер НЕ лечит. **S7-170-кандидат** задокументирован (канализация sendBlockUpdated с воркеров через существующие BU-DEFER мосты + защита всех итераций navigatingMobs; parity-риск порядка recomputePath — прегистер при реализации). Частота 3 catches/300s — шумовая для median5, хвостовой fatal-риск для легов.
(3) **ДИСПАТЧ s7187 = #14 v2a ИЗОЛЯЦИЯ**: банк v3 + travel_diet=1 (zero_alloc=0/skip_store_bb=0 — чистый эффект v2a), **банд 6.35M..7.01M = якорь±5%**; TravelDietOps верифицирован локстепом (×29). Абсорб: absorb_s7185.py <run_id> (якорь-SLOW вшит).
(4) NEXT (id 346): абсорб s7187 одной командой → ОБЕ оси ≥+10% vs 1.60 @ 6680195 → CANDIDATE GREEN → min-of-2 → банкинг v4 = v3+travel_diet; < +10% валидной пары: v1-pair ре-диспатч (zero_alloc+skip_store_bb, tight band) ИЛИ v2b (travel-math) — по свежему ранку RECON; S7-170 реализация — после закрытия пары v1/v2a (правильность, не ТОП-1).

## СТАТУС ×30-ADD (TASK-345, тот же тик ~04:4x): пул раннеров НЕ бимодальный — 12.31M лендинг сносит модель двух классов
- s7187 (run 35463854322, slow-band 6.35..7.01M) = BAND-DISCARD: runner **8610825** — пул впервые за день отдал БЫСТРЫЙ класс (5 slow → flip). По закону (fast-проба за тик) s7188 (run 35463940709, fast-band 8.07..8.92M) = BAND-DISCARD: runner **12306556** — вне ВСЕХ исторических классов (fast-класс был 8.49-8.57M!). **Итог: cpu_index пула НЕПРЕРЫВЕН с дисперсией ≥1.9× (6.49M..12.31M за день) — модель «два класса» RECON-17/19 УСТАРЕЛА**; ±5%-парность под такой дисперсией = дорогое лотерея.
- **ПРЕДЛОЖЕНИЕ ПРОТОКОЛА (на санкцию владельца в NEXT)**: переход от бинарной ±5%-пары к «нормализационной регрессии» — копить валидные ноги на ШИРОКОМ банде (напр. 6.0M..9.5M), вердикт по скорости-нормализованной дельте (med/runner) с runner_index как ковариатой + min-of-2 по ЗНАКУ и величине; RECON-17 показал −19.8% index-дельту → нормализация ловит большую часть конфаунда; остаточный шум ±12% (RECON-19) оценивается по накопленной паре. Альтернатива сейчас: продолжать лотерею ±5% (дорого).
- v2a-нога (travel_diet=1) всё ещё НЕ успешно в полёте: обе попытки = банд-дискорды (11с каждый — гейт работает). Диспатчей больше не посылал (лимит тика).
- NEXT (id 346): абсорб/проба — пробовать ШИРОКИЙ банд (6.0M..9.5M) v2a-ногу сразу, если владелец не санкционировал регрессионный протокол: `dispatch_s7187.py` с правкой бандов; ИЛИ ждать санкции. Пул сейчас FAST-фаза — пробовать fast-band.

## СТАТУС ×31 (TASK-346, 2026-09-20 ~05:1x +08, основная сессия) — САНКЦИЯ ВЛАДЕЛЬЦА ПОЛУЧЕНА: регрессионный протокол ПРИНЯТ (v8-REGRESSION); широкий банд 6.0M..9.5M; диспатч v2a-ноги s7189
(1) **САНКЦИЯ**: владелец (2026-09-20, прямое указание «это твой проект — если что-то блокировано/мешает/не запускается — разбирайся с этим») санкционировал автономную разблокировку пайплайна. БЛОКЕР (×30-ADD): пул раннеров НЕПРЕРЫВЕН (cpu_index 6.49M..12.31M за день, 1.9× дисперсия) — ±5%-парность = дорогая лотерея (3 BAND-DISCARD подряд, v2a-лег ни разу не соакнулся; бимодальная модель RECON-17/19 устарела). **ПРОТОКОЛ ПРИНЯТ (v8-REGRESSION)**: (a) широкий банд 6.0M..9.5M на диспатче (S7-96d fast-fail остаётся только для экстремальных лендингов типа 12.31M); (b) вердикт = ДВОЙНОЙ БАР по ОБЕИМ осям vs ANCHOR-SLOW (s7184: median5 1.60 @ 6680195): normalized=(med/runner)/(1.60/6680195) И absolute=med/1.60, ОБЕ ≥+10% → CANDIDATE-GREEN → подтверждающий min-of-2 → banking v4 = v3+travel_diet; оси страхуют index-конфаунд в ОБЕ стороны (normalized консервативна выше якорного раннера, absolute честна ниже; индекс-независимость TPS в диапазоне подтверждена: банк v3 = 1.60 @ 6680195 (leg#3) И 1.60 @ 8566450 (s7169)); (c) остаточный шум ±12% (RECON-19) накрывается min-of-2 по знаку И величине. **Директива ДВОЙНОГО БАРА владельца сохранена дословно — на одной оси ничего не банкуется.**
(2) **ДИСПАТЧ s7189 = #14 v2a ИЗОЛЯЦИЯ**: банк v3 + travel_diet=1 (zero_alloc=0/skip_store_bb=0 — чистый эффект v2a без примеси v1), широкий банд 6.0M..9.5M; TravelDietOps верифицирован офлайн-локстепом (×29, 1.05M бит-в-бит кейсов); arm-маркер stdout = «stage traveldiet composed (Retargeted { sites: 1 })». Тулы: **dispatch_s7189.py** (гварды S7-108 + remote-head==local-head), **absorb_s7189.py** (PG-T3 = протокол v8-REGRESSION, PG-T1 + arm-маркер + strict-violated-чек, рулетка BAND-DISCARD/CRASH-REFUTED/INFRA-FLAKE). Run id → CLAIMS.
(3) NEXT (id 347): ABSORB s7189 одной командой `python3 scripts/bench4_recon/absorb_s7189.py <run_id>` → ОБЕ оси ≥+10%: CANDIDATE-GREEN → подтверждающий min-of-2 (dispatch_s7189.py повторно) → banking v4 = v3+travel_diet; валидный широкобандный лег <+10% хотя бы по одной оси: **v2b travel-math** (RECON-21 контракт: handleRelativeFrictionAndCalculateMovement 48 строк + travelInFluid + getInputVector) — реализация+гейты+commit/push+диспатч В ТОМ ЖЕ ТИКЕ; CRASH: rollback travel_diet (yml-дефолт 0) + вердикт-док + S7-170 приоритизируется (RECON-22 race в банке — фундаментальный). Параллельным cron-сессиям: диспатчить ТОЛЬКО dispatch_s7189.py (широкий банд), старые узкобандные скрипты НЕ использовать (S7-108 + runs?per_page=3 перед POST).

> **СТАТУС ×32 (TASK-347, тик 03:5x +08, Job 398847): (1) s7189 (run 35464575112 @ ba54f32) SUCCESS → ABSORB → ВЕРДИКТ LANE-OPEN**: PG-T1 PASS (все 9 флагов, arm=COMPOSED, strict=False, NCDFE=0, pop VALID); PG-T2 FAIL (threw=2, unexpected=0, s7180-class=1 — RECON-22 race, стохастический, НЕ новый баг v2a); PG-T3 median5=1.3 @ 6918984 (широкий банд OK) → **normalized −21.6% / absolute −18.8% vs ANCHOR-SLOW 1.60 @ 6680195 — ОБЕ оси ОТРИЦАТЕЛЬНЫ, НЕ GREEN**; PG-T4 young 150/Full 0 PASS; PG-T5 PASS. Урок: scratch-slot зеркало collide (8×ThreadLocal.get/вызов + Unsafe putDouble) скорее всего БОЛЬШЕ аллокаций, чем экономит — C2 скалярно замещает временные AABB (контракт §3: CPU-атака REFUTED = инлайн жив); hyp снёсен в вердикт-док ABSORB_S7189.md. (2) **#14 v2b TRAVEL-MATH РЕАЛИЗОВАН И ДИСПАТЧЕН В ТОМ ЖЕ ТИКЕ** (RECON-21 §4 → код): getInputVector (protected STATIC → static-to-static redirect, identical desc, scale+normalize темпорари → скаляры, продукт остаётся; normalize inline: sqrt(x²+y²+z²) порядок бит-в-бит, ZERO iff len < 9.999999747378752E-6); travelInFluid (private → receiver-prepended на НОВОМ LivingEntity-хуке: climbable-темп @174 + multiply(DDD)-темп @196 убиты, gFFAM scalar-inlined в обе ветки — alias-ветка материализует (аллок-каунт не хуже ванили), compute-ветка −1 Vec3; lava-путь = ванильные вызовы — продукты хранятся); **handleRelativeFrictionAndCalculateMovement SKIP с javap-доказательством** (единственный new Vec3 @73-91 = ВОЗВРАЩАЕМЫЙ продукт, редирект = 0 выгоды — отклонение от буквы контракта задокументировано); TD_ENTITY_TARGETS ×2 + TD_LIVING_TARGETS ×1 + TD_ALL_TARGETS closure; entity_compose stage-10 sites:2; cargo 201/201 (+3: static-shape fload_1/fload_2, travelInFluid verify+idempotent, closure vs REAL fixtures). (3) **ДИСПАТЧ s7190 = v2b-нога (комбинированная рука travel_diet=1: collide+getInputVector+travelInFluid; изоляция v2b-only потребовала бы новый yml-инпут — лимит 25 исчерпан): run 35465880091 @ 9b47580, 2026-09-19T19:56:47Z, широкий банд.** commit/push ПЕРЕД POST соблюдён (9b47580). (4) NEXT (id 348): `python3 scripts/bench4_recon/absorb_s7189.py 35465880091` → ОБЕ оси ≥+10% vs 1.60 @ 6680195 → подтверждающий min-of-2 → banking v4 = v3+travel_diet-full; < +10% → **лейн #14 вердикт-док: alloc-diet REFUTED (2 ноги: v2a −19%, v2b — финал; C2 scalar-replacement hyp) → свежий RECON ТОГО ЖЕ лейна по CPU-оси** (s7177 окна) до под-лейнов ≥5% — переход к TOP-2 по-прежнему ЗАПРЕЩЁН до падения/исчезновения TOP-1; CRASH: rollback travel_diet (yml 0) + S7-170 приоритизируется.

> **СТАТУС ×33 (TASK-348, тик 04:08→04:4x +08, Job 398847): (1) v2b-нога s7190 (35465880091 @ 9b47580) = BAND-DISCARD @ 9857408** (fast-лендинг > 9.5M — не вердикт; ре-ролл); ре-ролл s7191 (35466607225 @ bb0b3d7) = **BAND-DISCARD @ 11694672** — пул дрейфовал в fast весь тик (2 дискорда подряд, оба pre-download fast-fail ~1 мин). (2) **S7-170 NAV-MOBS-GUARD РЕАЛИЗОВАН И ЗАГЕЙТОВАН В ЭТОМ ЖЕ ТИКЕ** (RECON-22 контракт → код, javap-инвентарь contract-s7170-*.txt: navigatingMobs трогают РОВНО 4 сайта, все через java/util/Set интерфейс): одно-точечная подмена ЗНАЧЕНИЯ поля (Unsafe, offset=308 от реального ServerLevel) на GuardedNavigatingMobs — мутации под монитором, итерация = fastutil table-clone снапшот (**порядок итерации БИТ-В-БИТ ванильный**), swap идемпотентный main-only из onTickingStart/End ретаргетов, WORKERS<=1 javac-свёрнут в no-op; ноль ретаргетов/нулевой diff classfile.rs/cargo/yml. **Оффлайн-гейт NavMobsLockstepHarness PASS 6/6**: 240k-оп порядок-паритет бит-в-бит (52 030 adds); стресс 8 потоков × 50k опций + 4 снапшот-ридера = 0 исключений; **сырой контроль взорвался ТОЧНОЙ продакшн-сигнатурой NPE "wrapped is null"** (класс s7176/s7180/s7186/s7189 воспроизведён оффлайн); финальный обход 9908==size(). Парити-прегистер: порядок recomputePath сохранён для идентичного содержимого; окно снапшот→recompute = принятый S7-155 класс; CME-retry Paper становится мёртвым путём. Доки: research/gc-recon-2026-09-19/S7170_NAVMOBSGUARD.md; тул: scripts/run_navmobs_lockstep.sh, s7170_javap_scan.py; absorb_s7189.py печатает guarded-marker. (3) **ДИСПАТЧ s7192 = v2b-нога c S7-170 на борту: run 35467929550 @ a5c353d, 2026-09-19T20:36:54Z, широкий банд; commit/push ПЕРЕД POST соблюдён (a5c353d).** Прегистер гейта ноги: [S7-170] nav-mobs guarded маркер ОБЯЗАН присутствовать + PG-T2 threw=0 (гонка закрыта на сцене; все ноги банка v3 имели threw=1..3 того же класса). (4) NEXT (id 349): `python3 scripts/bench4_recon/absorb_s7189.py 35467929550` → BAND-DISCARD: ре-ролл тем же диспатчером; threw>0 того же класса: S7-170 гейт FAIL + откат точки подмены; ОБЕ оси ≥+10% vs 1.60 @ 6680195: подтверждающий min-of-2 → banking v4 = v3+travel_diet(+S7-170); < +10%: лейн #14 вердикт-док alloc-diet REFUTED (2 валидные ноги) → свежий RECON лейна по CPU-оси до под-лейнов ≥5% (TOP-2 запрещён).

> **СТАТУС ×34 (TASK-349, тик 04:43→05:0x +08, Job 398847): (1) s7192 (run 35467929550 @ a5c353d) SUCCESS → ABSORB → ВЕРДИКТ LANE-OPEN**: валидная нога (PG-T1 PASS / arm=COMPOSED / band OK @ runner 6782504 / PG-T4 young 158/Full 0), PG-T2 FAIL (threw=1, s7180-class=1), PG-T3 **median5=1.6 → normalized −1.5% / absolute +0.0% vs ANCHOR-SLOW 1.60 @ 6680195 — обе оси ≪+10%, НЕ GREEN**. Вторая валидная нога ниже бара (v2a: −21.6%/−18.8%) → по прегистеру ветка «лейн #14 alloc-diet REFUTED (2 валидные ноги)». (2) **RECON-23 ГОТОВ И ЗАКОММИЧЕН (9ea3bd1, recon23_lane14_cpu.py + RECON23_LANE14_CPU.md): CPU-ось лейна #14 = 5.10% / 5.06% / 4.37% scene-CPU inclusive на s7189/s7184/s7178 — кросс-раннер стабилен; под-лейнов ≥5% СЦЕНЫ НЕТ (макс travel-misc-kernel 1.31%); gc-barrier под travel 0.1% лейна; alloc-ось travel 7.3-7.9% < 10% → GC-канал мёртв. ПОТОЛОК ЛЕЙНА <10% ДОКАЗАН → закрытие лейна #14 вердикт-доком по исключению чартера (прецедент RECON-17/20 STEAL) — обязательство «ТОП-1 обязан упасть» исполнено**; свежий ТОП-1 по профилям s7189: семейство GC 26.8% (G1 14.6% + oop-barriers 12.2%), kernel:other 16.3%, entities/mobs 15.9%. (3) **S7-170 ГЕЙТ-ФЕЙЛ s7192 ПОБЕЖДЁН КАК DELIVERY-DESYNC (НЕ дефект дизайна)**: a5c353d менял RegionTickOps.java (+152), но НЕ пересобирал committed RegionTickOps.class (остался 9104887/S7-168 vintage без гварда); CI инжектит committed blob через include_bytes! → s7192 бежал СТАРЫЙ байткод (маркера нет, NPE:1519 = точный класс-A RECON-22), оффлайн-харнесс же компилирует ИСХОДНИК и потому PASS 6/6 — диссонанс «оффлайн зелёный / сцена без гварда». **ФИКС (3529175)**: блобы RegionTickOps.class+Mut пересобраны из HEAD-исходника против patched-kernel s7192 (javac 21, маркер [S7-170] в блобе подтверждён); **НОВЫЙ RegionTickOps$GuardedNavigatingMobs.class добавлен в ОБА define-списка kernel-лоадера** (region_threads.rs; раньше = NoClassDefFoundError на первом swap); scripts/check_blob_sync.py — аудит «исходник новее блоба» (exit 1), ловит этот класс ошибок институционально; харнесс ре-ран **PASS 10/10** вкл. raw-контроль с точной продакшн-NPE. (4) **ДИСПАТЧ s7193 = v2b-нога с РЕАЛЬНО доставленным S7-170: run 35469278586 @ 3529175, 2026-09-19T21:03:56Z, широкий банд; S7-108 + remote-head==local-head соблюдены; commit/push ПЕРЕД POST (3529175).** Прегистер гейта: [S7-170] marker PRESENT ОБЯЗАТЕЛЬНО + PG-T2 threw=0 + DUAL BAR. (5) NEXT (id 350): `python3 scripts/bench4_recon/absorb_s7189.py 35469278586` → BAND-DISCARD: ре-ролл тем же диспатчером; marker=MISSING/NCDFE: ре-аудит доставки (check_blob_sync.py + define-список + boot-лог); marker PRESENT + threw>0 того же класса: S7-170 ДИЗАЙН FAIL → откат точки подмены + вердикт-док; marker PRESENT + threw=0 + ОБЕ оси ≥+10%: min-of-2 подтверждающий → banking v4 = v3+travel_diet(+S7-170); marker PRESENT + threw=0 + < +10%: вердикт-док лейна #14 ФИНАЛ (REFUTED, 3 валидные ноги + RECON-23 потолок) → выбор свежего ТОП-1 (первый кандидат: RECON GC-семейства по alloc-атрибуции до под-лейнов ≥5%).

> **СТАТУС ×35 (TASK-350, тик 05:08→05:3x +08, Job 398847): (1) s7193 (run 35469278586 @ 3529175) SUCCESS → ABSORB → двойной результат**: **(a) S7-170 ВЕРИФИЦИРОВАН НА СЦЕНЕ** — guarded-marker=OK + PG-T2 **threw=0** (s7180-class=0) против threw=1..3 во ВСЕХ ногах банка v3 — класс-A гонка navigatingMobs структурно закрыта, delivery-фикс 3529175 работает; **(b) PG-T3 median5=1.6 @ 6951881 → normalized −3.9% / absolute +0.0% — третья валидная нога ниже бара** (PG-T4 young 144/Full 0 PASS). (2) **ЛЕЙН #14 ФИНАЛ: VERDICT-DOC LANE14_VERDICT.md (1e4f8d6) — REFUTED, ЗАКРЫТ по исключению чартера (прецедент RECON-17/20)**: три валидные ноги (−21.6/−18.8, −1.5/+0.0, −3.9/+0.0) + RECON-23 потолок 4.37-5.10% scene-CPU <10% + под-лейнов ≥5% сцены нет + GC-канал мёртв; travel_diet дефолт 0; **S7-170 ОСТАЁТСЯ компонентом правильности**. Обязательство «ТОП-1 обязан упасть» исполнено. (3) **RECON-24 (44c5666): alloc-атрибуция нового ТОП-1 (GC-семейство 26.8% scene-CPU) на живом alloc-окне s7189 (13144 сэмпла)**: под-лейны ≥5% alloc-оси — jdk-collections/serde 25.5% (byte[] 13%+long[] 12%+short[] 10%), **inside-blocks/fluid-scan 22.3%** (Vec3 41%+AABB 34% = цель СУЩЕСТВУЮЩЕГО рычага zero_alloc #10 S7-164), jvm-core 21.5% (String/char[]), travel/movement 11.0%, chunk-system 6.4%; GC-бюджет 316 young-пауз = 6.5% 300s-окна. (4) **НОВАЯ НОГА ТОП-1: #10 ZERO-ALLOC-INSIDE ИЗОЛЯЦИЯ** — пара сгенерирована (dispatch_s7194.py: банк v3 + S7-170 + zero_alloc=1/travel_diet=0/skip_store_bb=0, широкий банд; absorb_s7194.py: arm=«stage zeroin composed» sites:3 + **[S7-170] marker ОБЯЗАТЕЛЕН** + PG-T2 threw=0 + DUAL BAR + young-drop вторичный сигнал); blob-sync аудит OK; **ДИСПАТЧ s7194 = run 35470254981 @ 1e4f8d6, 2026-09-19T21:23:13Z; S7-108 чист, commit/push ПЕРЕД POST (1e4f8d6).** (5) NEXT (id 351): `python3 scripts/bench4_recon/absorb_s7194.py 35470254981` → BAND-DISCARD: ре-ролл dispatch_s7194.py; marker=MISSING/NCDFE: ре-аудит доставки; CRASH: rollback zero_alloc (дефолт 0) + вердикт-док, S7-170 остаётся; ОБЕ оси ≥+10%: min-of-2 (dispatch_s7194.py повторно) → **banking v4 = v3 + zero_alloc(+S7-170)**; < +10%: REFUTED попытки под-лейна fluid-scan → свежий RECON следующего GC-под-лейна (jdk-collections/serde 25.5%) до под-лейнов ≥5% (TOP-1-дисциплина: GC-лейн обязан упасть или доказать потолок).

> **СТАТУС ×36 (TASK-351, тик 05:43→05:5x +08, Job 398847): (1) s7194 (run 35470254981 @ 1e4f8d6) ABSORB → ДВОЙНОЙ РЕЗУЛЬТАТ**: (a) PG-T1 изначально FAIL arm=MISSING — ДЕФЕКТ АБСОРБЕРА (копия s7189-тула искала маркер «stage traveldiet composed»; в stdout есть «stage zeroin composed (Retargeted { sites: 3 })» + ARMED [inside->rng->batch->zeroin] rc=0, blob-sync OK); фикс маркера + isolation-гвард (traveldiet-стадия НЕ компоузится — изоляция чиста) → PG-T1..T5 PASS: zeroin COMPOSED sites:3 + threw=0 + guarded-marker OK; (b) **PG-T3 median5=1.4 @ 6633963 → normalized −11.9% / absolute −12.5% — REFUTED #10 zero_alloc/zeroin** (четвёртая валидная нога ниже бара). (2) **RECON-25 (recon25_jdk_collections_drill.py): дрилл jdk-collections/serde 24.5/30.5% alloc-оси до домен-клиентов** — leaf-пропуск-фикс (frames[:-1]), spark/cgroup-чтения вычтены в отдельный домен (не игровой): **единственный домен ≥5% оси = inside-scan-visitset 7.2/8.3% (LongOpenHashSet visit-set vanilla-тела forEachBlockIntersectedBetween, кросс-раннер стабилен), остальные <3% (pathfinding 2.3/2.5, iterator 1.4/1.5)**. ВАЖНО: visit-set — внутренность УЖЕ REFUTED #9 flat_traversal (TraverseOps убивал ровно его, бит-в-бит харнесс) → дрилл исчерпан, повторный рычаг на REFUTED-направление запрещён. (3) **RECON-26 (recon26_gc_lane_verdict.py): ВЕРДИКТ GC-семейства — НЕ TPS-рычаг, потолок TPS-конверсии <10% (четвёртый прецедент после RECON-17/20/23)**: 5 архитектурных рычагов GC-давления REFUTED по TPS при ПОДТВЕРЖДЁННОМ структурном эффекте (#9, #11, #12: alloc −47%/CPU −66% → TPS −17..−6%; #14; #10: zeroin УБРАЛ fluid-sim 7.5%→1.7% scene-CPU, TPS −12.5% НАОБОРОТ); GC-пауз-бюджет 5.9-6.5% 300s-окна (280 young/0 Full s7194); параллельный G1/barrier-CPU вне MSPT-критического пути. Свежий CPU-ТОП s7194 (128104): GC 33.4/32.5%, entities/mobs-tick 13.9/13.7%, kernel:other 13.6/9.5% (нестабилен), jdk-coll 13.1/13.7%, chunk 6.9/6.6%, travel 5.8/5.7%. (4) **RECON-27 (recon27_entities_drill.py): entities/mobs-tick FRACTAL — под-лейнов ≥5% сцены НЕТ** (макс: broadphase get/getEntities 3.4-3.7/2.8-3.1%, ai/goals 2.0/1.8%, nav 1.7% кросс-стабильно) — лейн закрываем потолком по RECON-23-прецеденту (любой рычаг семьи <4% TPS). (5) **RECON-27b: tickBucket-дрилл — RegionTickOps.tickBucket-поддерево 11.9/12.2% scene-CPU (кросс-стабильно) = UMBRELLA**: состав хвостов — zeroin-fluid ~2.2%, broadphase-getEntities ~2.3%, VarHandle-volatile ~1%, inside-gate ~0.8%, чистой однородной оркестрации ≥5% НЕТ. (6) **СТРАТЕГИЧЕСКИЙ ВЫВОД: после 6 REFUTED (#9/#11/#12/#13-park/#14/#10) и вердиктов GC+entities сцена X150K НЕ ИМЕЕТ однородного под-лейна ≥5% — классические точечные TPS-рычаги исчерпаны; единственный путь буста ≥10% = СИСТЕМНАЯ ЭПОХА-2: flat tick-batch dispatcher (оркестрационная группа tickBucket-scan+guard+volatile+bucketOf+sweep+flushStep ≈14-15% сцены одним рычагом) + broadphase-мемоизация + fluid-батчинг.** (7) NEXT (id 352): **RECON-28 — детальная декомпозиция tickBucket-оркестрации** (все хвост-фреймы 15232 сэмплов s7194: оркестрация-собственно vs функциональные под-вызовы, javap-контракт RegionTickOps.tickBucket/lambda$ensureHelpers$4) → прегистер ЭПОХА-2 flat batch dispatcher (архитектурный: векторизованный батч-обход сущностей без per-entity guard/lambda-цикла, median-exact порядок сохранён) → реализация + гейты + диспатч в том же тике. Диспатчей 0 (все вердикты офлайн); S7-108 чист.

> **СТАТУС ×37 (TASK-352, тик 06:08→06:2x +08, Job 398847): (1) RECON-28 (recon28_tickbucket_decomp.py): декомпозиция tickBucket-поддерева (58.4/59.7% inclusive) — тело entity-тика 37.0/37.2% сцены (функции), контур-хвосты 21.3/22.5%, но ЧИСТАЯ ОРКЕСТРАЦИЯ (guard/lambda/ticklist/bucket, стек заканчивается на контур-фрейме, тела нет) = 2771 сэмпл = 1.1% сцены (lambda$tick$4 0.45% + setOldPos 0.43% + tickNonPassenger 0.18% + guard 0.01%) → ЭПОХА-2 flat batch dispatcher НЕ ИМЕЕТ цели ≥5%: чистый оркестрационный лейн в 4.5× ниже порога чартера, точечный рычаг запрещён.** (2) RECON-29 (RECON29_CHUNK_DRILL_SCENE_CEILING.md): chunk-system 13.9/13.8% дрилled — единственный под-лейн на пороге = broadphase ChunkEntitySlices.getEntities 4.90/5.14% (кросс-стабилен; getEntities 3850/4115 + AABB.intersects 1125/1080), ПОТОЛОК БУСТА ~5% сцены < ДВОЙНОГО БАРА +10%; прочие <3.6% (PalettedContainer.get 3.57/3.58, getBlockState 1.0, readPalette 0.6). Broadphase-мемоизация (кэш по идентичному AABB + секционные version-счётчики = dirty-флаги, median-exact при детерминированном порядке) архитектурно законна, но самостоятельным рычагом НЕ банкингуется. (3) **ФИНАЛ КАРТИРОВАНИЯ СЦЕНЫ X150K (RECON-25..29, кросс s7189+s7194): ДВОЙНОЙ БАР (обе оси ≥+10% vs ANCHOR-SLOW) НЕДОСТИЖИМ ни одним точечным архитектурным рычагом на текущей архитектуре — каждый лейн либо <5%, либо целевой под-лейн <5%, либо потолок TPS-конверсии <10% (GC-вердикт ×36), либо REFUTED-эмпирика 6 рычагов (#9/#10/#11/#12/#13-park/#14). Кумулятивный потенциал системной группы (broadphase ~5% + volatile ~3.4% + inside-gate ~3.1% + контур ~1.1% + chunk-read ~3.6%) ≈12-16% сцены — но по эмпирике 5 диет с подтверждённым структурным эффектом дали 0% TPS.** (4) **ЭСКАЛАЦИЯ ВЛАДЕЛЬЦУ (реальный стратегический блокер, вариант-решение требует санкции): (A) КУМУЛЯТИВНАЯ ЭПОХА-2 — один системный CI-лег (broadphase-мемоизация + field/volatile-диета + inside-gate диета), гейт = ДВОЙНОЙ БАР, риск 0-эффекта по эмпирике; (B) коррекция протокола — цель min-MSPT, банкинг по скорости-оси ≥10% при TPS ≥ якорь-0; (C) Rust/JNI-планировщик сцены / off-thread world-tick — вне текущего цикла тиков по объёму.** ДО РЕШЕНИЯ: диспатчи новых точечных рычагов прекращены (целей ≥5% с баром ≥10% нет), учёт/абсорбы продолжаются. (5) NEXT (id 353): по решению владельца A/B/C; если тик без решения — статус-кво + офлайн-верификация (broadphase-мемоизация: javap-контракт ChunkEntitySlices.getEntities + секционные version-счётчики — подготовка коммит-пригодного прототипа БЕЗ диспатча). Диспатчей 0 (картирование полное); S7-108 чист.

> **СТАТУС ×38 (TASK-353, тик 06:43→06:5x +08, Job 398847): (1) ВОССТАНОВЛЕНИЕ СРЕДЫ**: воркспейс откачен к старому снапшоту — /home/z/c-crussty, /home/z/crussty-dev-logs и /tmp/jdk21 стёрты; оба репо recloned с GitHub (c-crussty master @ f5c1921 = ×37, dev-logs main @ 3cca868), JDK21 (Temurin 21.0.5) восстановлен в /tmp/jdk21; S7-108: runs?per_page=3 — только CI-санити-раны (f5c1921/5ee56df/0ab2fc9 success), бенч-легов в полёте НЕТ, чужих диспатчей нет. (2) **RECON-30 (RECON30_BROADPHASE_MEMO_FEASIBILITY.md) — офлайн-верификация broadphase-мемоизации по NEXT 353, ВЕРДИКТ: exact-AABB кэш СТРУКТУРНО МЁРТВ на движущейся сцене X150K**: (a) javap-контракт EntityCollectionBySection.getEntities (jar s7194, дампы в contract-broadphase-s7194/) — секционный диапазон floor(minY−2)>>4..floor(maxY+2)>>4, линейный обход storage с null/excluded/intersects/predicate-фильтрами, порядок детерминирован (median-exact воспроизводим); (b) кенсус мутаций — членство: addEntity/removeEntity (обе стороны), ГЕОМЕТРИЯ: Entity.setPos → makeBoundingBox → setBoundingBox КАЖДЫЙ ТИК КАЖДОГО из 150k мобов; (c) честный кэш-ключ требует BB-версию кандидатов → версия меняется 150k раз/тик → hit-rate ≈ 0; stale-кэш теряет вошедших мобов (median-exact/парити сломаны); (d) звуковой вариант = flat live-list layout-диета: убирает только контур (~71-77% лейна), intersects+predicate неубираемы → **потолок ≤3.0-3.5% сцены = микро-зона чартера**; (e) пятая док-верификация закрытия лейна (прецедент RECON-17/20/23/26) — патчер-прототип НЕ реализуется (мёртвый вариант не кодим, звуковой — запрещённый микро-фикс). (3) **УТОЧЕНИЕ ЭСКАЛАЦИИ A/B/C: компонент A «broadphase-мемоизация ~5%» реально стоит ≤3% → кумулятивный потенциал ЭПОХИ-2 пересчитан 12-16% → ~10-13% сцены (верхняя граница при нулевой TPS-конверсии по эмпирике 6 REFUTED) — рекомендация усиливается в сторону B (min-MSPT) или C (Rust/JNI-уровень); вариант A только при явной санкции на ожидаемый ~0 TPS-эффект.** (4) NEXT (id 354): решение владельца A/B/C; без решения — офлайн-верификация компонентов A по одному за тик: volatile-диета ~3.4% (javap-контракт RegionTickOps/Entity volatile-полей + модель TPS-конверсии) — кандидат на шестую док-верификацию. Диспатчи остаются остановлены; S7-108 чист.

> **СТАТУС ×39 (TASK-354, тик 07:08→07:1x +08, Job 398847): (1) S7-108: pulls чисты, runs?per_page=3 — только CI-санити своих пушей, бенч-легов/чужих диспатчей нет; решение владельца A/B/C не поступило → ветка NEXT 354: офлайн-верификация volatile-диеты ~3.4%. (2) RECON-31 (recon31_volatile_diet_drill.py + RECON31_VOLATILE_DIET_VERDICT.md): дрилл volatile/VarHandle-лейна 3.51/3.40% сцены (кросс s7194+s7189, из world3-bench.zip обеих ног) → две семьи: (a) synced-entity-data 1.94/2.00% (generic getValue 1.06/1.08 + getItem 0.78/0.85 + packDirty ~0.08) — javap-контракт SynchedEntityData: get→getItem (itemsById[accessor.id()] AALOAD)→DataItem.value GETFIELD — ПУТЬ ПОЛНОСТЬЮ PLAIN, ноль volatile-инструкций, волатильности нет ни в DataItem.value ни в isDirty; (b) chunk-map-concurrent 1.52/1.34% (ConcurrentLong2ReferenceChainedHashTable $TableEntry.getValueVolatile guard_L_L + getAtIndexAcquire guard_LI_L; клиенты getChunk/getChunkNow/getChunkHolder/getChunkAtIfLoadedImmediately) — конкурентная chunk-карта ServerChunkCache = ИНФРАСТРУКТУРА region-threads (компонент БАНКА v3), acquire-семантика обязательна. (3) ВЕРДИКТ: «volatile-диета» НЕ ИМЕЕТ ПРЕДМЕТА — sync-семья уже plain (выгода возможна только из редизайна двухуровневой косвенности ради ≤2% = микро-зона запрещена), chunk-семья конкурентна по необходимости (снятие acquire = атака на собственный банк); честный потолок ~0-1% сцены; **6-я ДОК-ВЕРИФИКАЦИЯ закрытия лейна (прецедент RECON-17/20/23/26/30/31), патчер не реализуется.** (4) ЭСКАЛАЦИЯ УТОЧНЕНА ВТОРОЙ РАЗ: компонент A «volatile-диета ~3.4%» стоит ~0-1% → потенциал ЭПОХИ-2 10-13% → **~9-12% сцены** (верхняя граница при ~0 TPS-конверсии по эмпирике 6 REFUTED) — из всех компонентов A остаётся только inside-gate ~3.1% + контур ~1.1% + chunk-read ~3.6% как непроверенные; рекомендация B/C усилена окончательно. (5) NEXT (id 355): решение владельца A/B/C; без решения — офлайн-верификация inside-gate-диеты ~3.1% (RECON-32: checkInsideBlocks/InsideBlockOps-gate javap-контракт + модель кэша last-block-state-per-entity, median-exact риск мутации блоков) — последний крупный непроверенный компонент A. Диспатчи остановлены; S7-108 чист.

> **СТАТУС ×40 (TASK-355, тик 07:43→07:5x +08, Job 398847): (1) S7-108: pulls чисты, runs?per_page=3 — только CI-санити, бенч-легов/чужих диспатчей нет; решения владельца нет → ветка NEXT 355: RECON-32 inside-gate ~3.1%. (2) **RECON-32 (recon32_inside_gate_drill.py + RECON32_INSIDE_GATE_VERDICT.md): КЛЕЙМ 3.1% ОПРОВЕРГНУТ В БОЛЬШУЮ СТОРОНУ — inside-семья = 11.28/11.65% сцены** (кросс s7194+s7189: vanilla-discovery движущихся 9.08/9.36% + effects-apply 2.20/2.29% + банк-инфра gate/flushStep/init ~1.8% внутри discovery-хвостов) — крупнейший функциональный остаток после GC; стеки чисты (tick → applyEffectsFromBlocks → checkInsideBlocks → forEachBlockIntersectedBetween → visit; ItemEntity/Mob-муверы каждый тик); листья: PalettedContainer.get 855, visit-set add 819, flushStep 806, gate 791, <init> 747, applier 658, guava-iterator 616, lambda$checkInsideBlocks$2 457.** (3) Статусы под-кусков: visit-set/iterator = внутренность REFUTED #9 (запрещено), alloc-диета = REFUTED #12 (TPS −17..−6%), effects-apply = ванильная семантика парити, банк-инфра = свой банкинг. ЕДИНСТВЕННЫЙ легальный рычаг = секционный solid-bitmask пре-фильтр (event-driven dirty-флаги, санционировано миссией): сырой потолок ~9.1% сцены, TPS-конверсия по эмпирике ~0 → под ДВОЙНОЙ БАР НЕ банкингуется. (4) **ВЕРДИКТ: inside-gate-диета = 7-я ДОК-ВЕРИФИКАЦИЯ закрытия (RECON-17/20/23/26/30/31/32); КАРТИРОВАНИЕ КОМПОНЕНТОВ ЭПОХИ-2A ЗАВЕРШЕНО: broadphase ≤3% (RECON-30), volatile 0-1% (RECON-31), inside = здесь, контур 1.1% (RECON-28), chunk-read 3.6% (RECON-29) — суммарный легальный потенциал < двойного бара при конверсии ~0 → ВАРИАНТ A ОКОНЧАТЕЛЬНО НЕ РЕКОМЕНДОВАН. При варианте B (min-MSPT) флагман-кандидат = inside-bitmask (сырой потолок ~9.1% CPU — крупнейший легальный остаток на сцене).** (5) NEXT (id 356): решение владельца B/C (A исчерпан доками); без решения — офлайн-проработка javap-контракта внутри-bitmask-флагмана (Entity.checkInsideBlocks/applyEffectsFromBlocks entry-гейты + секционный битмап LevelChunk/getSection-solid-бит, median-exact гарантируется short-circuit только при all-air) — коммит-пригодный контракт-док БЕЗ диспатча. Диспатчи остановлены; S7-108 чист.

## СТАТУС ×41 (TASK-356, 2026-09-20 08:0x+08, cron)
(1) S7-108: pull чисты; runs?per_page=3 — только CI-санити (cb5a1e6/5ae18ca success), бенч-легов нет; решение владельца B/C отсутствует → ветка NEXT 356 (офлайн до коммит-пригодного). (2) **RECON-33 (recon33_inside_bitmask_contract.py + contract-inside-bitmask-s7194/ + RECON33_INSIDE_BITMASK_CONTRACT.md): javap-контракт inside-bitmask-флагмана ДОВЕДЁН ДО РЕАЛИЗАЦИОННОЙ ГОТОВНОСТИ** — точка вставки `checkInsideBlocks(Vec3,Vec3,...)` после deflate(1e-5); pre-gate = покрытие deflated-AABB клетками (≤2×2×2) → `LevelChunk.getSection().hasOnlyAir()` (public, = nonEmptyBlockCount==0, vanilla-поддержка setBlockState/recalcBlockCounts/read, прецедент: `LevelChunk.getFluidState` УЖЕ юзает этот fast-path bc13..38); median-exact пины: air-ветка lambda$checkInsideBlocks$2 возвращает до ЛЮБЫХ наблюдаемых действий (bc34..65 — только debug), visitedBlocks на air-пути не читается/пишется, AtomicInteger-бюджет не инкрементится (проверка ДО isAir), fluid-блоки не-air → водные секции не скипаются; debug-поток гейтится `!debugActive`. (3) **КОРРЕКТИРОВКА RECON-32: внутри-gate НЕ имеет LivingEntity/Mob пути вовсе** (javap -p -c LivingEntity/Mob пусто, ref-скан пулов): внешние per-tick входы = ItemEntity.tick(273) / ExperienceOrb / FallingBlock / PrimedTnt / EndCrystal.tick + EnderDragon.aiStep + AbstractBoat.tick ×2 + AbstractMinecart.move — т.е. 9.08/9.36% discovery принадлежит item-классу; внутри Entity no-arg AFB никем не вызывается, isAffectedByBlocks = !isRemoved && !noPhysics. (4) Потолок не пересмотрен: ~9.1% CPU, TPS-конверсия ~0 → под ДВОЙНОЙ БАР не банкингуется (вердикт RECON-32 стоит); RECON-33 = 8-е док-закрытие диеты (17/20/23/26/30/31/32/33) и ПЕРВОЕ с реализационным контрактом флагмана option-B. (5) Диспатчи остановлены; NEXT (id 357): решение владельца B/C; без решения — при option B контракт готов к реализации (patcher-спека: гейт в Entity.checkInsideBlocks + 0 изменений в секциях); альтернатива офлайн = RECON-34 chunk-read 3.6% (RECON-29 остаток) drill.

## СТАТУС ×42 (TASK-357, 2026-09-20 08:4x+08, cron)
(1) S7-108: pull чисты; runs?per_page=3 — CI-санити success (ae8af47), бенч-легов/диспатчей нет; решения владельца B/C нет → ветка (4b): рычаг реализуется до коммит-пригодного в этом же тике. (2) **#15 INSIDE-BITMASK РЕАЛИЗОВАН ПО КОНТРАКТУ RECON-33, коммит 6eb3274, dormant (env CRUSSTY_INSIDE_BITMASK, по умолчанию 0 — dormant-invisible дисциплина):** (a) Java-бридж `entityinside/.../InsideBitmaskOps.java` (javac --release 21 на реальном kernel jar, major 65): гейт = консервативный swept-hull BB(from)∪BB(to)+1b маржа → секционные клетки → `hasOnlyAir()` через Unsafe objectFieldOffset ChunkAccess.sections (прецедент InsideBlockOps-поле), фоллбэк = MethodHandle.invokeExact ИСХОДНОГО private checkInsideBlocks(List,Collector) (unreflect+setAccessible, static-init fail-closed), debug-гейт = ванильные debugSubscribers (на сомнении — ванилла), fluids не-air → водные секции не скипаются, zero-alloc hot path; (b) rust-драйвер `src/inside_bitmask.rs`: bridge-define в kernel loader + PROBE-THEN-PATCH (armState()=="ARMED" через JNI ДО публикации BRIDGE_READY — разоружённый бридж патчем не накрывается никогда); (c) `classfile.rs patch_inside_bitmask`: ЕДИНСТВЕННЫЙ сайт = applyEffectsFromBlocks(List) bc 58..64, invokevirtual→invokestatic 3B→3B receiver-first, strict sites==1, kernel-rename гварды; один ретаргет покрывает ВСЕ классы-входы (ItemEntity.tick bc273 и др. — ценз RECON-33 §1); (d) entity_compose stage 1b + lib.rs wiring + build-скрипт. (3) БАНКИНГ: под ДВОЙНОЙ БАР не банкингуется (потолок ~9.1% CPU, TPS-конверсия ~0 эмпирика диет — вердикт RECON-32/33 стоит); это option-B flagman: при выборе владельцем B — активация = env-флаг + диспатч, код готов. CI cargo-build валидирует компиляцию (_push 6eb3274_). (4) NEXT (id 358): решение владельца B/C; без решения — офлайн-локстеп-оракул для InsideBitmaskOps (бит-в-бит effects-эквивалентность на живых Movement-трейсах, прецедент v2a ×29 1.05M кейсов) как гейт активации.

### СТАТУС ×42-ADD (TASK-357, тот же тик)
CI-цикл: 6eb3274/aa6ac63 = rust-build RED (E0433: `jni::` недоступен в модуле без prelude; E0308: get_string_utf возвращает owned String) → фикс 1c703f7 (`use jvmti_bindings::prelude::*` — прецедент реэкспорта area_map/jni_util + unwrap_or_default) → **CI ПОЛНОСТЬЮ GREEN (run 35480564227: rust build + java bridge + fuzz + smoke success)**. INSIDE-BITMASK (#15) = компиляционно валидирован, dormant. Урок: перед push rust-модулей сверять импорт-jni-путей по прецедентам root-крейта.

## СТАТУС ×43 (TASK-358, 2026-09-20 09:3x+08, cron)
(1) S7-108: pull чисты; runs?per_page=3 — CI-санити green (8c7ee68), бенч-легов нет; решения владельца B/C нет → ветка NEXT 358: офлайн-локстеп-оракул гейта активации. (2) **INSIDE-BITMASK ORACLE ALL PASS (dea9de8, scripts/run_inside_bitmask_lockstep.sh — одна команда, ORACLE_INSIDE_BITMASK_PASS.md):** [1] structural+ARM на реальном jar s7194 (MethodHandle private CIB(List) + Unsafe ChunkAccess.sections — точный live probe-then-patch путь); [2] **hull-superset — ядро median-exact: 1,000,000 адапверсариальных кейсов (zero-move/телепорт/границы ±deflate/отриц. коорд./мульти-Movement) — 210,513,534 позиций РЕАЛЬНОГО forEachBlockIntersectedBetween ВСЕ внутри блочного домена РЕАЛЬНОГО sweptHullInto** (рефактор гейта: package-private sweptHullInto + ThreadLocal scratch — hot path zero-alloc, единственная копия формулы); [3] section truth: 20,000 послед. / 249,217 setBlockState на реальном LevelChunkSection+PalettedContainer — hasOnlyAir+nonEmptyBlockCount бит-согласованы, fluids НЕ-air → водные секции не скипаются. Честный резидуал: live chunk-map wiring/debugSubscribers/живой effects-поток — гейты CI-лега. Офлайн-нюанс зафиксирован: в 1.21.10 нет статического biome-реестра → секция собирается напрямую из PalettedContainer-ов; библиотеки ядра = paperclip INSTALL-ONLY (не boot). (3) Статус рычага: реализация (6eb3274+1c703f7) + оракул ALL PASS, dormant (CRUSSTY_INSIDE_BITMASK); активация = решение владельца option B (потолок ~9.1% CPU, TPS-конверсия ~0 — ДВОЙНОЙ БАР не банкингуется, RECON-32/33 стоят). CI на dea9de8. (4) NEXT (id 359): решение владельца B/C; без решения — EPOCH-2B финализация: сводный док карт (A/B/C) с полными пинами + RECON-35 drill chunk-read 3.6% (последний несведённый компонент) либо дрейф-контроль свежего профиля сцены.

## СТАТУС ×44 (TASK-359, тик 09:43→09:5x +08, Job 398847)
(1) S7-108: pull чисты; runs?per_page=3 — CI-санити green (5fd1f64/dea9de8), бенч-легов нет; решения владельца B/C нет → ветка NEXT 359: EPOCH-2B финализация. (2) **RECON-35 (recon35_chunkread_consumer_drill.py + RECON35_BLOCKREAD_CONSUMER_VERDICT.md): block-read семья дрилled до доменов-потребителей — 7.54/7.36% сцены, КЛЕЙМ RECON-29 «chunk-read ~3.6%» ЗАНИЖЕН ~2×** (узкие регексы пропускали Level-делегаторы, внутренние кадры PalettedContainer/ChunkAccess, getBlockStateFinal). Семья = чистая инфраструктура: **fluid-sim ~50% семьи = 3.61..3.77% сцены** (updateFluidHeightAndDoFluidPushing 16.8% + getFlow 15.3% + hasSameAbove 13.6% — REFUTED-зона #10, ванильная parity-семантика), **movement/collision ~22% = 1.63..1.65%** (getCollisionsForBlocksOrWorldBorder 9.1% — лейн #14 REFUTED), **entity-context ~17%**, **inside-discovery ~10% = 0.77%** (уже покрыт dormant #15), pathfinding ~1.4%. НИ ОДНОГО потребителя ≥5% сцены вне отображённых лейнов; листья: PalettedContainer.get ~3.49%, getFluidState ~1.5%, getBlockState ~1.0%, readPalette ~0.63%, getBlockStateFinal ~0.53%. Потолок мемоизации <7.6% сцены < ДВОЙНОГО БАРА, TPS-конверсия ~0 (6 REFUTED-ног) → **9-е ДОК-ЗАКРЫТИЕ (17/20/23/26/30/31/32/33/35); ПОСЛЕДНИЙ несведённый компонент ЭПОХИ-2A сведён — вариант A исчерпан полностью**. (3) **EPOCH2_DECISION_MAP_A_B_C.md: сводная карта решений финализирована с полными пинами** — A = исчерпан (9 док-закрытий + 6 REFUTED-рычагов, потолок ~9-12% сцены при конверсии ~0); **B = флагман #15 готов к активации одной командой** (контракт RECON-33 + impl 6eb3274/1c703f7 + оракул dea9de8: 1M кейсов/210.5M позиций hull-superset; dormant env CRUSSTY_INSIDE_BITMASK; активация = env-флаг + 1 CI-лег dispatch_s7189.py + min-of-2); C = Rust/JNI-эра (предпосылка S7-170 race, спека не начата). Рекомендация карты: санкционировать B и параллельно открыть спеку C с S7-170. (4) Учёт: RECON-35 + карта (5b08930), CI на 5b08930 в полёте; CLAIMS TASK-359 + worklog. (5) NEXT (id 360): решение владельца B/C; при B — активация CRUSSTY_INSIDE_BITMASK=1 CI-легом (диспатч+гейты+min-of-2); без решения — дрейф-контроль свежего профиля сцены (по запросу владельца) либо поддержка статус-кво с абсорбами. Диспатчи остановлены; S7-108 чист.

## СТАТУС ×45 (TASK-360, тик 10:08→10:1x +08, Job 398847)
(1) S7-108: pull чисты; runs?per_page=3 — CI green на всех док-коммитах ×44 (5b08930/89dcd08/6d17db3), бенч-легов нет; решения владельца B/C нет → ветка (4b): рычаг #15 доводится до конца офлайн. (2) **#15 INSIDE-BITMASK ACTIVATION TOOLING = одна команда (9fa116a, CI в полёте)**: найдено недостающее звено — inside_bitmask.rs читает env CRUSSTY_INSIDE_BITMASK напрямую, но run_world3.sh его НЕ пробрасывал → добавлен INSIDE_BITMASK plumbing (var+echo+export); world-bench.yml свап инпута в рамках лимита 25: zero_alloc (лейн #10 REFUTED s7177, прецедент удаления inside_diet/zero_cursor) → **inside_bitmask** (env INSIDE_BITMASK, ZERO_ALLOC pinned '0'); канонический dispatch_s7189.py обновлён (inside_bitmask:0 вместо удалённого инпута — не сломается). (3) **dispatch_s7195.py — активационный лег s7195**: банк v3 + inside_bitmask=1 + travel_diet=0 (изоляция), широкий банд 6.0..9.5M, гварды S7-108 (remote-head==local-head + concurrency); **SAFETY GATE: POST только с --sanctioned** — активация флагмана = санкция владельца option B (под v8 ДВОЙНОЙ БАР #15 не банкингуется, RECON-32/33); без флага печатает план. (4) **absorb_s7195.py — гейты прегегистрированы**: PG-T1 внутри-маркеры (owner-armed ПРИСУТСТВУЕТ + dormant-маркер ОТСУТСТВУЕТ = env-дошёл-до-ядра чек + stage inside_bitmask composed + strict/rejected/missed ОТСУТСТВУЮТ), PG-T2 crash-free, **PG-T3 ДВОЙНОЙ ПРОТОКОЛ: v8 DUAL BAR (дефолт, действующий закон) / B min-MSPT (--protocol B: normalized ≥+10% И median5 ≥ якорь-0)**, PG-T4 GC young≤174/Full=0, PG-T5 N/A; failure-рулетка BAND-DISCARD/CRASH-REFUTED(rollback env-0)/INFRA-FLAKE. YAML-валидация: 25 инпутов, сирен 0. (5) Учёт: 9fa116a + GOAL ×45 + CLAIMS TASK-360 + worklog; пуш обоих + re-pull. (6) NEXT (id 361): решение владельца B/C; **при B активация одной командой: `python3 scripts/bench4_recon/dispatch_s7195.py --sanctioned`** → absorb_s7195.py <run_id> --protocol B → min-of-2 → banking v4; без решения — статус-кво с абсорбами/дрейф-контроль. Диспатчи остановлены; S7-108 чист.

## СТАТУС ×46 (TASK-361, тик 10:43→10:5x +08, Job 398847): (1) S7-108: pull чисты; runs?per_page=3 — CI green (9fa116a/6d384da/f12e3af), бенч-легов нет; решения владельца B/C нет → ветка NEXT 361 (без решения): **открывается спека C-эры офлайн (рекомендация карты EPOCH-2 — «параллельно открыть спеку C»).** (2) **S7-171 СПЕКА ЭРЫ C ОТКРЫТА: docs/EPOCH2C_S7-171_RUST_SCHED_SPEC.md — Rust-планировщик сцены / off-thread world-tick**: тезис — C растёт WALL-CLOCK (Амдал: сокращение serial-остатка MSPT), а не CPU-сокращением (Java-оси исчерпаны 9 док-закрытиями + 6 REFUTED); гейты эры = ДВОЙНОЙ БАР v8. Инфраструктурные пины: region_threads.rs (S7-156, банк v3) = скелет C (5 fail-closed байт-хуков + уроки живых крашей S7-157b mid-tick-pump/158b tracker-sweep/158d UUID-race), **S7-170 race-фундамент ЗАКРЫТ и верифицирован на сцене (s7193: threw=0, guarded-marker OK) — threading-экспансия больше не множит окно RECON-22**, entity_mirror (S7-92) = прецедент Rust-владения сущностями, jni_table 283 natives + classfile probe-then-patch, локстеп-оракулы v2a ×29 / #15 1M-кейсов. Архитектура: **M1 «упорядоченная параллельная оценка»** (ordered commit в исходном порядке = median-exact структурно), M2-спекуляция отложена. Фазы: **P1 = RECON-36 serial-fraction** (гейт честности эры: serial >70% сцены → пауза C с вердикт-доком), P2 = M1-прототип ItemEntity + оракул ПОРЯДКА observable-эффектов, P3 = CI-лег s7196+ (изоляция банк v3+scheduler, широкий банд), P4 = экспансия Mob→bucket→world-tick; **гейты PG-C1..C5 прегистрированы** (маркеры/strict sites; threw=0 + [S7-170] guard обязателен; ДВОЙНОЙ БАР + min-of-2; GC-бюджет young≤174/Full=0; оракул green ДО диспатча). Риск-регистр: правило «каждый новый worker-путь = свой guard-аудит» (S7-157b/158b/158d-класс). (3) Границы санкций (§9): реализация C (P2+) = санкция владельца C-эры; P0/P1 — офлайн без диспатчей, выполняется в тиках статус-кво; активация #15 (option B) НЕ блокируется — dispatch_s7195.py --sanctioned готов одной командой. (4) Учёт: спека S7-171 + GOAL ×46 + CLAIMS TASK-361 + worklog; пуш обоих + re-pull. (5) NEXT (id 362): решение владельца B/C; при C-санкции — P1 RECON-36 serial-fraction; при B — активация → absorb --protocol B → min-of-2 → banking v4; **без решения — P1 RECON-36 serial-fraction (офлайн, разрешена спекой §9)**. Диспатчи остановлены; S7-108 чист.

## СТАТУС ×47 (TASK-362, тик 11:08→11:2x +08, Job 398847): (1) S7-108: pull чисты; runs?per_page=3 — CI green на 06f8bcc (спека S7-171), бенч-легов нет; решения владельца B/C нет → ветка NEXT 362: **P1 RECON-36 serial-fraction (офлайн, санкционирован спекой §9).** (2) **RECON-36 ВЫПОЛНЕН (recon36_serial_fraction.py + RECON36_SERIAL_FRACTION_VERDICT.md): взвешенные сэмплы, thread-split по вход-пути стека (профили без имён потоков), кросс s7194+s7189 (128104/127150 сэмплов, cpu_index 6.63M/6.95M в банде)**: WORKER-ENTITY 43.8/45.0% (4 потока, duty ~11%/поток — headroom), NATIVE-VM(GC+JIT) 36.2/35.2% (кросс-чек RECON-26 ✓), **MAIN = 19.8/19.6% сцены = serial крит.путь**; фазы main: entity-бакеты 15.6% (78-80% main — domain region_steal), tracker+sendChanges 2.2/2.0%, random 0.8/0.7%, block-sched 0.6/0.6%, tail 0.6%. **GATE P1: serial 19.7% ≪ 70% → C-ЭРА ПРОДОЛЖАЕТСЯ.** (3) **ГЛАВНАЯ НАХОДКА — РАЗВИЛКА P2**: наивный Амдал «слить main → буст» НЕ работает (79% main уже делится с workers через region_steal; слив переворачивает крит.путь на медленного worker'а); потолок = f(N воркеров, дисбаланс I): N=4 I=1.0 → +33%, I=1.15 → +15%, I=1.3 → +2% мёртво, N=8 I=1.1 → +58% (упирается в 4 vCPU раннера); **дисбаланс НЕ наблюдаем из thread-слитого collapsed → прегистр P2-pre-gate: 1 CI-лег с thread-separated профилировщиком (-t) → I≤1.15 → P2 = слив main-бакетов+offload остатков (потолок +25..33% > ДВОЙНОГО БАРА — первый профиль-обоснованный путь ≥бара за эру RECON-25..35); I≥1.3 → P2 = ребаланс REGION_CHUNKS=8→мельче + WORKERS↑.** (4) Честная аменда спеки: чистая оркестрация ~1.8% сцены = микро-зона — «Rust-планировщик как замена оркестрации» сам по себе НЕ рычаг; C-ценность = геометрия пула + offload остатков. (5) Учёт: RECON-36 + вердикт-док + GOAL ×47 + CLAIMS TASK-362 + worklog; пуш обоих + re-pull. (6) NEXT (id 363): решение владельца B/C; при C-санкции — P2-pre-gate лег (thread-separated профиль, 1 CI-нога, развилка I); при B — dispatch_s7195.py --sanctioned → absorb --protocol B → min-of-2 → banking v4; без решения — P2-pre-gate ТУЛ офлайн до коммит-пригодного (диспетчеризация тул-лега = при санкции). Диспатчи остановлены; S7-108 чист.

## СТАТУС ×48 (TASK-363, тик 11:43→11:5x +08, Job 398847): (1) S7-108: pull чисты; runs — CI green на adcaaae (RECON-36), бенч-легов нет; решений владельца нет → ветка NEXT 363: P2-pre-gate тул ДО КОНЦА + диспатч В ЭТОМ ЖЕ ТИКЕ. (2) **HARNESS-CHANGE (f3d3ba4): wall-сессия профилировщика стала THREADED (`asprof start -t -e wall`, фоллбэк merged при отказе -t)** — wall-collapsed.txt теперь несёт per-thread стеки: duty каждого crussty-region-worker-* (tickBucket-активность vs CyclicBarrier park) + DONE-wait Server thread; это закрывает главный резидуал RECON-36 («дисбаланс I не наблюдаем из thread-слитого collapsed»); wall-сессия ЖИВА (проверено: s7189 wall-collapsed = 3011 стеков/69650 сэмплов, park-кадры есть — заметка контекста «AP-PID мёртв» устарела); cpu/alloc окна не тронуты; 0 behavior change ядра (прецедент TASK-317 instrument-gate, диагностический лег НЕ гейт-лег). (3) **ТУЛ-ТРИО**: recon37_worker_balance.py (формат-гвард threaded/merged — на старом merged wall даёт корректный TOOL-FAIL; duty = tickBucket-активность без барьеров; I = max/avg duty; развилка прегистра); **dispatch_s7196.py** (диагностический лег = банк v3 + travel_diet=0, широкий банд, S7-108 гварды HEAD+concurrency); **absorb_s7196.py** (PG-A доставка банк-v3, PG-B threaded-формат, PG-C ≥3 воркера, PG-D crash-sanity, failure-рулетка BAND-DISCARD/INFRA-FLAKE/TOOL-FAIL → развилка RECON-36 одним прогоном). py_compile/bash -n чисты. (4) **ДИСПАТЧ s7196: run 35487747157 @ f3d3ba4, 2026-09-20T03:53:36Z, in_progress** — commit/push ПЕРЕД POST, remote-head==local-head, ACTIVE=0. (5) NEXT (id 364): `python3 scripts/bench4_recon/absorb_s7196.py 35487747157` → развилка I: OFFLOAD-READY (≤1.15) → P2-контракт (слив main-бакетов + offload остатков) / REBALANCE (≥1.3) → REGION_CHUNKS/WORKERS / GRAY → повторный threaded лег; решение владельца B/C — при B: dispatch_s7195.py --sanctioned. Диспатч 1 (диагностический, в полёте).

## СТАТУС ×49 (TASK-364, тик 12:08→12:1x +08): (1) S7-108: pull чисты; обнаружен УПАВШИЙ лег s7196 от параллельной сессии TASK-363 (run 35487747157 @ f3d3ba4, world-bench-3 failure) → ABSORB ПЕРВЫМ ДЕЙСТВИЕМ. (2) **ПОСТ-МОРТЕМ s7196: BAND-DISCARD (не вердикт)** — step-3 "Runner calibration band gate" fast-fail pre-download, runner_cpu_index=11496682 против широкого банда [6.0M,9.5M] (экстремальный быстрый лендинг >9.5M — ровно тот случай, предсказанный протоколом; pairing discard, бенч не стартовал). Первичный вердикт absorb-скрипта «INFRA-FLAKE» был ЛОЖЕН (joblog 401 → regex не сработал → провал в else) — исправлено доказательно через jobs-API. (3) **ФИКС ТУЛА (commit 71f697c)**: absorb_s7196.py — (а) api() теперь принимает относительные пути (базовый URL префиксуется; до фикса скрипт падал ValueError на первом же вызове); (б) makedirs до скачивания артефакта; (в) BAND-DISCARD-детектор: case-insensitive regex + фоллбэк на jobs-API по имени степа → вердикт больше не зависит от доступности joblog; проверено на упавшем run — теперь корректный BAND-DISCARD. (4) **РЕ-РОЛЛ #1 (макс 2): dispatch_s7196.py → run 35488526730 @ 71f697c, in_progress 2026-09-20T04:11:52Z** — гварды пройдены (HEAD 71f697c==remote, ACTIVE=0), банк v3 + travel_diet=0, широкий банд, диагностический P2-pre-gate (threaded wall, развилка I). (5) NEXT (id 365): absorb 35488526730 → развилка RECON-36: OFFLOAD-READY (I≤1.15) → P2-контракт слив main-бакетов / REBALANCE (I≥1.3) → REGION_CHUNKS/WORKERS / GRAY → ре-ролл #2; BAND-DISCARD#2 → ре-ролл #2 тоже; решение владельца B/C — при B: dispatch_s7195.py --sanctioned. Диспатч 1 (диагностический, в полёте).

## СТАТУС ×54 (TASK-369, тик 14:43 +08): СБРОС #4 + СМЕНА СТРАТЕГИИ ПЕРСИСТЕНТНОСТИ. Сбросы песочницы идут ежечасно (~:43: 12:43, 13:43, 14:43), каждый уничтожал worktree и незапушенные коммиты (потеряны ×52/×53/v2.1 — восстановлены в 3-й раз). (1) **WORKTREE ПЕРЕВЕДЁН В ПЕРСИСТЕНТНУЮ ЗОНУ: /home/z/my-project/c-crussty (переживает сбросы; git-история с локальными коммитами сохраняется между тиками), /home/z/c-crussty = симлинк** — recovery-цикл будущих сбросов = 1 секунда (симлинк + re-pull). (2) Восстановлено и E2E-зелёное: absorb v2.1 (--zip --digest + авто-док), bootstrap v2 (персистент-осведомлённый), тест; копии тулов продублированы в /home/z/my-project/scripts-crussty-backup/. (3) Токена нет; новых легов нет. (4) NEXT (id 370): токен/zip → пуш (накопленных коммитов станет ≥2) → absorb 35488526730 → развилка I → ветка в том же тике.

## СТАТУС ×55 (TASK-370, тик 15:08 +08): персистентность РАБОТАЕТ — worktree пережил тик (2 коммита ahead сохранены), токена нет, сброса не было. (1) S7-108: новых бенч-легов нет (Run 157 последний). (2) **ЦЕПОЧКА УЧЁТА ВОССТАНОВЛИМА ОДНОЙ КОМАНДОЙ: finish_push_accounting.sh (коммит) = пуш ahead-коммитов + backfill CLAIMS TASK-365..369 (контент уже в репо) + re-pull обоих репо** — период блокера (5 тиков) задокументирован в CLAIMS-формате заранее. (3) Первый тик с токеном: `finish_push_accounting.sh` → `absorb_s7196.py 35488526730` → развилка I → реализация ветки (OFFLOAD/REBALANCE) до диспатча. Первый тик с zip: `absorb --zip --digest 98ae00f6...` → то же без пуша. (4) NEXT (id 371): активация по токену/zip; офлайн-хвост не остался — вся механика тика с токеном собрана и закоммичена.

## СТАТУС ×56 (TASK-371, тик ~16:0x +08): ТОКЕН ОТ ВЛАДЕЛЬЦА — 5-ТИКОВЫЙ БЛОКЕР СНЯТ; ПОЛНЫЙ ЦИКЛ ЗА ОДИН ТИК: ABSORB → RECON-37 ВЕРДИКТ → P2-РЫЧАГ → ДИСПАТЧ. (1) Токен (agent-7625532f, repo+workflow): пуш накопленных 4 коммитов (03c5580..fa4698e) + backfill CLAIMS 365-369 (33ead45..bc45c6d); remote с токеном в персистентном worktree; крон-промпт дополнен секцией ТОКЕН (job 400022, старый 398847 удалён). (2) ABSORB 35488526730 OFFLINE-zip: curl с авторизацией (urllib 302→401: Authorization на blob-хост пересылать нельзя), digest БИТ-В-БИТ 98ae00f6..., PG-A/PG-D зелёные, cpu_index=8864955 в банде. (3) **RECON-37: парсер-фикс '[name tid=N]' канонизация — профиль БЫЛ threaded, TOOL-FAIL был ЛОЖНЫМ; воркеры 67.4/66.3/68.2% (park ~32% — GO/DONE барьеры), I=1.01 ≤ 1.15 = OFFLOAD-READY (пегистр RECON-36 §4, потолок +25..33%); MAIN: active-entity 62%, park 12.6%; тайна workers: 3 хелпера = слоты 1..3, main = слот 0, телеметрия workers=4 = слоты.** (4) **S7-172 P2-OFFLOAD v1 (74159b6): REGION_STEAL="2" enum (workflow на лимите 25 inputs — новое поле ввода невозможно) = main-free static: 4 хелпера тикают ВСЕ бакеты (helper i → slot i-1), барьеры w+1 parties (GO/DONE инлайн-парти), main — оркестратор (fill → GO → DONE join → phase-4/4b); парити: bucketOf/порядок/барьерная дисциплина неизменны, slot-0 pump-саппресс = принятый класс S7-157b; дефолт 0 = legacy бит-идентичен.** (5) **ДИСПАТЧ s7197 = run 35498517657 @ 74159b6 В ПОЛЁТЕ** (region_steal=2, банд 6.0..9.5M, 300s/150k). absorb_s7197.py готов (520a05c). NEXT (id 372): `python3 scripts/bench4_recon/absorb_s7197.py 35498517657` → PG-T1 (region_steal: 2 + workers=4) → PG-T3 ДВОЙНОЙ БАР vs ANCHOR-SLOW: ОБЕ оси ≥+10% → CANDIDATE-GREEN → min-of-2 (dispatch_s7197.py повторно) → banking v4; LANE-OPEN → P2-v2 residual overlap (tracker 2.2%, парити-ревью) или REBALANCE; CRASH → rollback (дефолт 0).

## СТАТУС ×56-ADD (TASK-372, тик 15:43→16:1x +08): BLOB-DESYNC ПОЙМАН И ПОБЕЖДЁН В ТОМ ЖЕ ТИКЕ. (1) check_blob_sync.py на 74159b6 = DESYNC: RegionTickOps.java (S7-172) новее committed-блобов (S7-170 vintage) — задиспатченный 35498517657 собрал бы ядро со СТАРЫМ байткодом (include_bytes!) → MAIN_OFFLOAD в бою отсутствует = точный класс s7192 delivery-desync, нога невалидна. ROOT CAUSE: в песочнице нет javac (wipe /home/z). (2) **JDK21 temurin скачан В ПЕРСИСТЕНТНУЮ ЗОНУ: /home/z/my-project/tools/jdk-21.0.12.1+1 (+ fastutil-8.5.15 maven) — блобы RegionTickOps.class+$GuardedNavigatingMobs пересобраны против patched-kernel s7196 (ровно 3 classfile — закон S7-163; $Mut бит-идентичен; parseMainOffload ldc CRUSSTY_REGION_STEAL верифицирован javap); commit a6605e6, blob-sync audit clean.** (3) 35498517657 ОТМЕНЁН (cancelled); исправленный диспатч = **run 35498908992 @ a6605e6 IN_PROGRESS (08:12:06Z)** (region_steal=2, банд 6.0..9.5M, 300s/150k/seed42/fp4/xmx10G, S7-108 + remote==local). (4) NEXT (id 373): `python3 scripts/bench4_recon/absorb_s7197.py 35498908992` → PG-T1 (region_steal: 2 в run-env) → PG-T3 ДВОЙНОЙ БАР (обе оси ≥+10% vs ANCHOR-SLOW 1.60 @ 6680195) → min-of-2 (dispatch_s7197.py повторно) → banking v4; CRASH → rollback env 0. Памятка: пересборка блобов = /home/z/my-project/tools/jdk-21.0.12.1+1/bin/javac + scripts/build_region_tick_ops.sh (SERVER_JAR=patched-kernel leg-артефакта, cp + fastutil).
