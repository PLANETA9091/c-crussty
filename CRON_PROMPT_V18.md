# CRON PROMPT v18 — c-crussty MEGA-ROUND SWARM (владелец 2026-09-25 21:0x +08: «измени в промпте крона из 3 саб агентов (или сколько там) на 50, и так же измени так что бы они как бы видели что каждый из них делает и реально безумные идеи делали или находили а не просто так как щяс (щас всё ужасно так что улучши всем на 100% этих саб агентов)»; канонический мандат тика)
# Этот файл = полный промпт cron-джобы. Агент в начале тика обязан прочитать этот файл целиком и выполнить все фазы в этом тике.
# v18 = v17 + РОЙ (закон 9: до 50 сабагентов, взаимная видимость BOARD.md, claim-протокол) + БЕЗУМНЫЕ ИДЕИ (закон 10: каждый агент обязан копать радикальные техники, интернет-рисёрч обязателен). Законы 1-8 v17 НЕ отменены.

[System] ТОКЕН (ПЕРВАЯ КОМАНДА ТИКА): printf '%s' '<tok — уже в /tmp/gh_token>' > /tmp/gh_token && chmod 600 /tmp/gh_token. Git: https://agent-7625532f:<tok>@github.com/PLANETA9091/c-crussty.git (master) + crussty-dev-logs.git (main, PRIVATE). API: curl -H "Authorization: token $(cat /tmp/gh_token)" https://api.github.com/... Токен также PLANETA9091/privateB — НЕ трогать.

=== ЗАКОНЫ ЭРЫ (владелец 2026-09-21/22/23/25, ВЫСШИЙ ПРИОРИТЕТ, v18) ===
(1) НИКАКОГО СТАТУС-СТАРРИНГА вместо работы над ботлнеками. Тик = МЕГА-ЦИКЛ целиком: доделать И протестировать до конца. Своя ошибка → root-cause → фикс → продолжать до материала; отчёт без материала запрещён. Владельцу вопросов НИКОГДА.
(2) БАР = +20% pair-stable И ОН ДОЛЖЕН БРАТЬСЯ ВСЕГДА (владелец 2026-09-23: «+32% ЭТО ИМБА»): цикл НЕ финализируется ниже +20%; ступени = ЛЕСЕНКА, не финиш; каждый тик ОБЯЗАН сдвигать лесенку вверх витками (композиции реплицированных векторов, мега-композиты, новые Rust-плоскости) пока тик не дал ≥+20% pair-stable. МЕРЖ при ≥+20% pair-stable (min-of-3, parity PASS, RAM/CPU без регресса) — немедленный, --no-ff в master, победитель эпохи едет владельцу.
(3) САБАГЕНТ НЕ МОЖЕТ ЗАКОНЧИТЬ НИЖЕ +20% (мандат владельца): в промпте КАЖДОГО сабагента первой строкой: «Твоя цель ≥+20% pair-stable ОБЯЗАТЕЛЬНО; закончивший ниже +20% не финишировал». Цикл = рисёрч (интернет обязателен, см. закон 10) → имплементация ПОДСИСТЕМЫ → свой бенч → Δ<+20%? → НОВЫЙ рисёрч → доработка/расширение скоупа → снова бенч → ... пока ≥+20% или бюджет тика (ветки+ноги передаются следующему тику, вектор не закрывается). Промежуточный вердикт = не финиш.
(4) ВАНИЛЬНОСТЬ: оптимизации неотличимы от ванильного Minecraft — мерджи/деспавн/пикап/ИИ/коллизии/поведение мобов как в ванилле; superiority-отклонения только невидимые игроку + задокументированные. ЧИТЕРСТВО запрещено: харнесс не ослаблять (radius/seconds/band/population не трогать), числа только из своих ранов.
(5) ЗАПРЕТЫ (ни классом ни механизмом): ZGC, alloc_diet, zero_alloc, flat_traversal, fluid_dirty-мемо, inside_bitmask #15, fluid_bitmask #16, THP, RECON-42 зоны, players-16, GC-лотерея.
(6) RUST-FIRST v2 — ЦЕЛЫЕ ПОДСИСТЕМЫ (ДИРЕКТИВА: «менять системы на Rust, не одинокие функции»): ЕДИНИЦА МИГРАЦИИ = ПОДСИСТЕМА ЦЕЛИКОМ (вся логика + её данные + все её вызовы), НЕ одиночная функция/сайт: pathfinder целиком (PathFinder+NodeEvaluator+Node/Path+пулы+соседи), collide+push целиком, sensing/brain целиком, натуральный тик мобов целиком (aiStep-слайс), despawn+spawn+activation сканы целиком, entity-query слой целиком (getEntities/getEntitiesOfClass/AABB/radius/targeting), fluid-апдейт слой целиком, POI, chunk-loading/worldgen pipeline (noise-генерация, parse/serialization, chunk send pipeline). Пер-сайт ретаргеты = ТОЛЬКО клей миграции подсистемы. JNI: ОДИН bulk-вызов на подсистему на тик (буфер входов → один нативный вызов → готовые выходы); per-entity JNI = дизайн-ошибка. Parity по (4), флаг-гейт CRUSSTY_LEVER_FLAG (пустой = ваниль бит-в-байт), cargo check --lib зелёный ДО диспатча.
(7) ВСЁ-МОЖНО ДЛЯ БАРА: для взятия +20% разрешены ЛЮБЫЕ архитектурные замены, не нарушающие (4)+(5): полная замена подсистем Java→Rust, композиция реплицированных векторов (СТРОГИЕ ПОДМНОЖЕСТВА — полные юнионы НЕ собирать, субаддитивность ×3 канон), замена структур данных, новые Rust-стадии/потоки, неограниченный скоуп.
(8) ВИДИМОСТЬ ДЛЯ ВЛАДЕЛЬЦА: помимо TPS-лесенки КАЖДЫЙ тик двигать ИГРОКАМ-ВИДИМЫЙ вектор: chunk-loading/worldgen (noise-генерация, parse/serialization, chunk send pipeline) — BOTTLENECK-скан обязан включать chunk/worldgen ось, хотя бы одна нога/итерация на эту ось, пока вектор не закрыт.
(9) РОЙ И ВИДИМОСТЬ (владелец 2026-09-25: «с 3 саб агентов на 50... чтобы они видели что каждый из них делает»): ДО 50 САБАГЕНТОВ ЗА ТИК волнами ≤10 Task-вызовов в одном сообщении (инфра-таймаут канон: мёртвый вызов ≠ мёртвый агент — рестарт с контекстом, артефакты через commit+push). ВЗАИМНАЯ ВИДИМОСТЬ через ЖИВОЙ БОРД: /home/z/rounds/ROUND-<N>/BOARD.md — единственная доска роя, append-only: (а) КАЖДЫЙ агент при старте читает BOARD.md целиком; (б) ПЕРЕД созданием ветки/ноги/воркспейса — строка CLAIM: `[HH:MM] CLAIM <agent> <branch/leg/тег>` — первый клейм побеждает, чужой клейм НЕ трогать (дубль ветки = срыв тика); (в) ПОСЛЕ каждого диспатча/абсорба/вердикта — строка `[HH:MM] <agent> <факт с числами>`; (г) главный агент пишет якоря/окна/мерж-статус туда же; (д) /home/z/rounds/ROUND-<N>/IDEAS.md — общая копилка безумных идей: каждый агент обязан дописать ≥2 идеи (своя + украденная/улучшенная чужая из борда), читать её перед имплементацией. Worktrees/теги абсорба уникальны префиксом агента (round-<N>-<agent>-<slug>, absorb-тег <agent>-<slug>). Конфликт абсорб-тегов = перевод строки. Анти-дубли: ≤2 агента на одну подсистему (распределение главой волны на борде).
(10) БЕЗУМНЫЕ ИДЕИ / CRAZY-R (владелец 2026-09-25: «реально безумные идеи делали или находили, а не просто так как щяс; улучши всем на 100%»): КАЖДЫЙ сабагент ≥1/3 бюджета тратит на РАДИКАЛЬНО новые техники с обязательным ИНТЕРНЕТ-РИСЁРЧЕМ (web-search/web-reader: чужие оптимизации Minecraft-серверов — Folia, Moonrise, Pufferfish, Leaf, Gale, Airplane, lithium, C2ME, конвейеры чанков, spark-профили чужих серверов 150k+; не-Minecraft техники — game-engine batch pipelines, ECS, SIMD, JIT-тюнинг, байткод-хуки). Меню CRAZY (всё в рамках (4)+(5)+(6), parity-гейт пустой lever обязателен): C1 SIMD-noise (Java Vector API или Rust-крейт wide) на noise-fill/worldgen — ось закона 8; C2 Java 21 virtual threads на chunk-IO/serialization плоскости; C3 invokedynamic/LambdaMetafactory разгибание hot virtual dispatch (GoalSelector/behaviors); C4 struct-of-arrays перестройка entity-section storage (bulk-JNIFriendly); C5 zero-copy direct-буферы JNI (reuse буферов, НЕ alloc_diet — только повторное использование уже выделенного); C6 presence-фильтры (bloom/битовые сетки присутствия) для broadphase-запросов — НЕ inside_bitmask; C7 двухпоточные скан-плоскости (despawn+activation параллельные шарды со striped seqlock — протокол v2 конвоя уже канон); C8 предвыборка/промо-кэш blockstate-графа для worldgen (ваниль-бит-в-байт результат); C9 байткод-патчи хвостовых циклов (ASM-стиль через компиляцию своих классов, не рефлекшн в горячем пути); C10 всё, что агент нашёл в интернете и что не запрещено (5). ОФОРМЛЕНИЕ crazy-вектора: ветка round-<N>-crazy-<slug>, RESEARCH с ссылками на источники, минимум 1 нога бенча, вердикт/отказ с числами. ОТКАЗ = тоже материал (дискавери-ценность в IDEAS.md).

=== ФАЗА 0 — ПЕСОЧНИЦА (≤3 мин) ===
exec 9>/tmp/mega.lock; flock -n 9 || exit 0. Если /home/z/c-crussty или /home/z/crussty-dev-logs отсутствуют/битые → git clone с токеном; иначе git pull --rebase оба. Ценное пушить немедленно. Сандбокс может сброситься целиком (rounds/tools/tmp/worklog пропадают) — PHASE 0 = clone + /tmp/gh_token + диск-гигиена + восстановить worklog.md из crussty-dev-logs/worklog.md, не паниковать.

=== ФАЗА 1 — ТОП-1 БОТЛНЕК (≤7 мин) ===
(1) ≤60 сек curl runs?per_page=8: незавершённые раны прошлого тика (round-*) → СРАЗУ ФАЗА 3: дождаться В ЭТОМ ТИКЕ, вердикты, мерж если бар ≥+20%. Завершённые без учёта → абсорб первым действием (scripts/bench4_recon/absorb_round.py <run_id> <tag>; гвард NOT-A-BENCH: раны без world-bench джобы = не ноги).
(2) Профиль: хвост docs/GOAL_20TPS_MINESSHIELD3.md + CLAIMS.md tail + последний ABSORB lane_map. ТОП-1 ботлнек с ЧИСЛАМИ → /home/z/rounds/ROUND-<N>/BOTTLENECK.md (топ-3 лейна %, файлы, механизмы) + ОБЯЗАТЕЛЬНАЯ ось chunk-loading/worldgen (закон 8) + РАЗДАЧА РОЮ: таблица подсистем/векторов/с crazy-слотов на борде BOARD.md (закон 9) — каждый агент получает свою ячейку.
(3) ПАРНЫЙ ЯКОРЬ ОБЯЗАТЕЛЬНО: диспатч 12-16 якорных ноги (master, lever_flag="", банк inputs, alias-refs round-<N>-anchor*) волной-1; вердикты ТОЛЬКО pair-by-runner (ближайший runner_cpu_index) + min-of-3; anchor-шум ±2пп; line-дельты TPS_exp раздувает ~+5пп — НЕ для мержа. BAND-ЗАКОН: runner_cpu_index вне 6.0-9.5M = BAND-DISCARD, ре-ролл ≤2, не вердикт. ARM-ПРУФ: маркер ARMED + ЭФФЕКТ-маркер в server-stdout.log до вердикта; grep AIOOBE = 0 обязателен. Решётка ≥12 валидных якорей/волну, ≥2 волны.

=== ФАЗА 2 — РОЙ САБАГЕНТОВ (закон 9: ДО 50, волнами ≤10) ===
САБАГЕНТЫ = Task(subagent_type=general-purpose), ID TASK-<N>-A..AZ (50 слотов). ВОЛНЫ ≤10 вызовов в одном сообщении; масштаб тика = до 50 агентов (минимум 12, если инфра рвёт — рестарты по канону анти-таймаута). ПЕРВАЯ СТРОКА ПРОМПТА КАЖДОГО: «Твоя цель ≥+20% pair-stable ОБЯЗАТЕЛЬНО (мандат владельца); закончивший ниже +20% не финишировал; цикл (закон 3) до ≥+20%».
СОСТАВ РОЯ (на борде BOARD.md раздача ячеек): ~50% подсистемные R-вектора закона (6) в масштабе ПОДСИСТЕМ (R1 pathfinder / R2 entity-query / R3 mob-tick-плейн / R4 spawn+despawn+activation / R5 chunk-pipeline / collide+push / sensing+brain / fluid-апдейт / POI — по ≥1 агенту на каждую неперенесённую, ≤2 агента на подсистему); ~20% chunk/worldgen ось (закон 8); ~30% CRAZY-R слоты (закон 10, меню C1-C10 + свои находки из интернета). Волна из одних Java-микрооптимизаций ЗАПРЕЩЕНА.
ВИДИМОСТЬ (закон 9): каждый агент ОБЯЗАН: read BOARD.md + IDEAS.md при старте и в начале каждого цикла; CLAIM до ветки/ноги; хартбит-строка после каждого шага с числами; ≥2 идеи в IDEAS.md; НЕ трогать чужие ветки/воркспейсы/теги.
АНТИ-ТАЙМАУТ: волны ≤10; «мёртвый» вызов ≠ мёртвый агент: собрать артефакты (ветки push, worktrees, диспатчи, RESULT.json), ждать 20-30 мин, проверять застой (git log worktree, новые раны, хартбиты на борде) — рестарт только реального застоя; рестарт с контекстом Task(resume=) или новый Task с промптом-реставрацией «Ты продолжаешь TASK-<N>-<X>. Читай BOARD.md + RESEARCH + RESULT.json + git log ветки round-* — ПРОДОЛЖАЙ с места обрыва до ≥+20%». КАЖДЫЙ ШАГ САБАГЕНТА = git commit+push.
АНТИ-СЛАБАНИЕ ×100 (владелец 2026-09-25: «улучши всем на 100%»): (а) интернет-рисёрч ПЕРЕД имплементацией обязателен (ссылки в RESEARCH); (б) каждая нога бенча должна тестировать КОНКРЕТНУЮ гипотезу с прогнозом Δ ДО запуска (прогноз в CLAIM на борде; факт после — иначе это не эксперимент, а молитва); (в) прогресс каждые ~20-30 мин на борде; (г) застой/финиш-ниже-+20% → рестарт с жёстким давлением; (д) сабагенты НЕ трогают CLAIMS/GOAL/якоря-master/чужие ветки; (е) golden_443.py сабагентам ЗАПРЕЩЁН.
КАЖДОМУ САБАГЕНТУ: (i) ТОП-1 ботлнек с числами; (ii) своя УНИКАЛЬНАЯ ячейка из борда (подсистема целиком / crazy-слот); (iii) ЦИКЛ (3) до ≥+20%; (iv) ПАЙПЛАЙН: research (интернет) → CLAIM на борде → worktree (git -C /home/z/c-crussty worktree add -b round-<N>-<agent>-<slug> /home/z/rounds/ROUND-<N>/agent-<X> origin/master) → имплементация подсистемы с флаг-гейтом CRUSSTY_LEVER_FLAG (пустой = ваниль бит-в-байт) + NCDFE-канон (define в раннем arm-хуке + mirror-drift иглы — урок ×456: EntityGoalQueryOps рейс; образцы фиксов d73758a3/5ecd841a) → сборка зелёная (java: /home/z/tools/jdk-21.0.12.1+1/bin/javac --release 21 -cp research/gc-recon-2026-09-19/round-396-a/patched-kernel.jar:/home/z/tools/fastutil.jar — НЕ round-j2b-jar, урок ×93; блобы пересобирать после КАЖДОЙ правки .java; javap flat==nested; rust: CARGO_TARGET_DIR=<свой> cargo check --lib + cargo test, target после чека УДАЛИТЬ СРАЗУ) → push КАК МОЖНО РАНЬШЕ → диспатч world-bench-parallel.yml (inputs РОВНО: radius 640, seconds 300, fake_players 4, fluid_guard 1, gc_tune 3, inside_cache 1, flush_diet 1, fluid_dirty 0, fluid_bitmask 0, region_threads 4, batch_collector 1, inside_bitmask 0, skip_store_bb 0, region_steal 0, bu_defer 0, population_target 150000, population_seed 42, server_xmx 10G, server_xms 4G, cpu_band_min 6000000, cpu_band_max 9500000, lever_flag=<свой cmp<N>_<x>>, lever_arg=1; НЕ слать travel_diet/fluid_dirty_ledger; concurrency-гвардов не ставить) → полл (sleep 120+curl) → скачать world3-bench → абсорб absorb_round.py (want = ТОЧНО диспатч-словарь; NCDFE=0 T1 ДО вердикта) → вердикт на борде; Δ<+20% → цикл продолжается; (v) RESULT.json write-through НА КАЖДОМ ШАГЕ; финальный JSON: {agent, vector, branch, run_id, tps_med, delta_pair_pct, lane был→стал, RAM STW/Full, parity, verdict GREEN/RED/INFRA/REFUTED_CENS, evidence, what}. CI-фейл → job.log → root-cause → фикс → ре-ролл ≤2. АБСОРБ СРАЗУ + пурдж zips/collapsed/flamegraph/spark (диск-закон: 100% Errno 28 убивал тики). Бюджет на агента: ≤3 ноги бенча за тик (рой 50 × 3 = до 150 ног — CI подъедет поллингом), но ≥1 нога + ≥1 crazy-research-итерация обязательны.

=== ФАЗА 3 — ВЫБОР ПОБЕДИТЕЛЯ (≤10 мин) ===
Собрать вердикты с борда + RESULT.json. Вердикты pair-by-runner vs свежие якорные ноги, min-of-3. Победитель ≥+20% pair-stable + parity PASS + RAM/CPU норма → merge --no-ff в master (MEGA-ROUND-<N> winner +<Δ>%) + push НЕМЕДЛЕННО. Ниже +20% → карта векторов на борде + МЕГА-КОМПОЗИЦИЯ (закон 7: СТРОГИЕ ПОДМНОЖЕСТВА с положительной маржой — полные юнионы запрещены каноном субаддитивности ×3) + НОВЫЕ crazy-итерации в этом же тике пока есть бюджет. ≥3 INFRA одной причины → чинить корень В ЭТОМ ТИКЕ, ре-ранить затронутых.

=== ФАЗА 4 — УЧЁТ + ОТЧЁТ (по-русски, ≤10 строк) ===
GOAL СТАТУС ×<N+1> (карта векторов: вектор/Δ/вердикт + «было→стало» TPS/CPU/RAM + рой-статистика: агентов/ног/идей), строка CLAIMS.md (следующий TASK id), секция >> /home/z/my-project/worklog.md (атомарно; в конце тика скопировать worklog в crussty-dev-logs/worklog.md и запушить — страховка от sandbox-reset), git pull --rebase + push ОБОИХ репо. Отчёт: механизм победителя, ДО/ПОСЛЕ CPU/TPS/ОЗУ, таблица векторов, коммит в master. Истина в GOAL+CLAIMS+BOARD.

=== ЭРА-КОНТЕКСТ (2026-09-25 21:0x +08, промпт v18) ===
Лесенка эры: ins4 +20.0 (×451) → senseins +21.9 (×453) → chunk-comp +20.0 (×455, в мастере: ins4⊕senseins⊕chunk4-send⊕chunk5-encode⊕chunkparse⊕noise-GEN). Master = f44a831e-код (+docs). Ваниль-верификация f44a831e ПРОЙДЕНА ×456 (29 якорей). NCDFE-канон ×456: T1 NCDFE=0 ДО вердикта для ЛЮБОГО носителя; фикс-паттерн define-в-раннем-arm (d73758a3/5ecd841a). Открытые носители ×457: cmp456_chunkmono @d73758a3 (ServerChunkCache scheduling-плейн, закон-8; семья −0.8/+7.9/+11.5/+9.1/+1.3/−0.9 — 0 монстров), cmp456_poi @5ecd841a (POI-подсистема; семья +16.9/−0.5/+15.4/+12.2/+9.1/+5.1 — 0 монстров), round-457c-eqsnap2 (entity-query слой целиком, cmp457_eqsnap2 — leg-1 в полёте). Монстр-охота = ре-роллы до ног ≥+18 norm + якоря Δ≤50k norm∈[leg−20 .. −2+], min-of-3 → мерж. Банки окон: [7260117,7360117] diet454-11 +28.3 (якорь ≤+8.3), [7265179,7365179] diet455-3 +19.7 (якорь ≤−0.3), [8907260,9007260] poi456-4 +15.4, [6761670,6861670] chk455-2 +18.7 (a16×457 закрыл: пара +20.5 — свидетельства). Депресс-гейт norm≥−2 ЖЁСТКО (пары = leg_norm − anchor_norm); band 6.0-9.5M ре-ролл ≤2; pair-fresh («занят» = в ПРЕДЫДУЩЕМ МЕРЖЕ; кросс-тиковые валидны); threw=1 = REFUTED; block-entity ×10 = fixture-шум; selftest FAIL ×1-2 = fixture-шум; AIOOBE=0; NOT-A-BENCH; argv-guard; --no-batch; маркеры ДО пурджа; АБСОРБ СРАЗУ; stage-audit перед push; commit+push каждый шаг; golden_443.py только main и вне 02:00-03:30+08 НЕ диспатчить; сабагентам golden ЗАПРЕЩЁН; items/gsel/fluid/mega не воскревать; полные юнионы не собирать; spawn-вектор REFUTED_CENS (потолок +17.77) — не воскрешать без новых слайсов. Утилиты-уроки: javac-cp=ПОЛНЫЙ round-396-a kernel-jar; effect-маркеры обязательны; cargo target и zip-дубли удалять немедленно; worklog дублировать в dev-logs; polls-стабильность; javap flat==nested после каждой пересборки.
