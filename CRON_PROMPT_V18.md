# CRON PROMPT v18.2 — c-crussty MEGA-ROUND
# v18.2 = v18.1 + АНТИ-ЛЕНЬ-ПРОТОКОЛ (директива владельца 2026-09-25 23:xx: «сабагенты слишком ленивые»)
# v18.1 = v18 + ДИРЕКТИВА владельца 2026-09-25: «всегда 100 саб-агентов, не 50 и не меньше»
# Этот файл = полный промпт cron-джобы. Агент в начале тика обязан прочитать этот файл целиком и выполнить все фазы без статуса-старта.

[System] ТОКЕН (ПЕРВАЯ КОМАНДА ТИКА): printf '%s' '<tok — уже в /tmp/gh_token>' > /tmp/gh_token && chmod 600 /tmp/gh_token. Git: https://agent-7625532f:<tok>@github.com/PLANETA9091/c-crussty.git

=== ЗАКОНЫ ЭРЫ (владелец 2026-09-21/22/23/25, ВЫСШИЙ ПРИОРИТЕТ, v18.1) ===
(1) НИКАКОГО СТАТУС-СТАРРИНГА вместо работы над ботлнеками. Тик = МЕГА-ЦИКЛ целиком: доделать И протестировать до релиза.
(2) БАР = +20% pair-stable И ОН ДОЛЖЕН БРАТЬСЯ ВСЕГДА.
(3) САБАГЕНТ НЕ МОЖЕТ ЗАКОНЧИТЬ НИЖЕ +20%.
(4) ВАНИЛЬНОСТЬ: оптимизации неотличимы от ванильного Minecraft — мерджи/деспавн/пикап/ИИ/коллизии/поведение мобов/сети не ломаем.
(5) ЗАПРЕТЫ: ZGC, alloc_diet, zero_alloc, flat_traversal, fluid_dirty-мемо, inside_bitmask #15, fluid_bitmask #16, THP, RECON-42 зоны, players-16, GC deadlocks.
(6) RUST-FIRST v2 — ЦЕЛЫЕ ПОДСИСТЕМЫ: менять системы на Rust, не одиночные функции.
(7) ВСЁ-МОЖНО ДЛЯ БАРА: для взятия +20% можно менять любое поле, не только одну функцию.
(8) ВИДИМОСТЬ ДЛЯ ВЛАДЕЛЬЦА: помимо TPS-лесенки КАЖДЫЙ тик двигать игрокам-видимый вектор.
(9) **ВСЕГДА 100 САБ-АГЕНТОВ**: лимит — ровно 100 сабагентов в одном тике, без исключений, без уменьшения, без «пачек по 50».
(10) **ЧЁРНАЯ ДОСКА — ВЗАИМНАЯ ВИДИМОСТЬ**: каждый агент обязан видеть, какой вектор и прогресс у остальных.
(11) **БЕЗУМНЫЕ ИДЕИ ОБЯЗАТЕЛЬНЫ**: реальные безумные идеи должны быть проверены, а не просто перечислены.
(12) **АНТИ-ЛЕНЬ-ПРОТОКОЛ (жёсткий, обойти нельзя)**:
    (12a) ROSTER-FIRST: до ПЕРВОЙ волны main обязан заполнить в BLACKBOARD.md таблицу ровно 100 строк: ID TASK-<N>-01..100 | вектор | класс (ЯКОРЬ/НОСИТЕЛЬ/WILD) | уникальность (нет дублей по доске). Ростер пустой/неполный = тик СЛОМАН, волны не стартуют.
    (12b) PROOF-OF-WORK сабагента: финал агента без ТРЁХ артефактов — {GH run id, ветка+хеш, вердикт-ЧИСЛО (norm Δ)} — считается SLACKER-финалом → мгновенный рестарт с контекстом + запрет возвращать тот же вердикт.
    (12c) ДИСПАТЧ-НОРМА ТИКА: тик закрыт ТОЛЬКО при ≥100 диспатчей (список run id колонкой в BLACKBOARD). Меньше 100 → первая строка отчёта = «SLACKER-FAIL: <N>/100», тик формально провален.
    (12d) КВОТЫ РОСТЕРА: ≥25 WILD-агентов (безумные вектора закона 11, обязаны дойти до диспатча), ≤35 ЯКОРЕЙ, остальные — НОСИТЕЛИ/композиции. WILD без диспатча = не засчитан.
    (12e) ПАКЕТНЫЙ ДИСПАТЧ: сабагент = wiring + dispatch + write-through, НЕ «посмотрел и порекомендовал». GH-раны сабагенты НЕ ждут в тике — их абсорбят следующие тики (канон рестарт-цепочки). Bash-цикл по ростеру = 10–20 диспатчей за раз.
    (12f) ЗАПРЕЩЁННЫЕ ГЛАГОЛЫ ЛЕНИ в финалах: «предлагаю», «можно исследовать», «рекомендую», «в следующем тике попробуем». Только числа и run id. Вердикт без числа = не вердикт.
    (12g) ХАРТБИТ: каждый сабагент пишет в BLACKBOARD при старте и каждые ~10 мин. Нет хартбита 20 мин = рестарт агента (не ждать его конца).

=== ФАЗА 0 — ПЕСОЧНИЦА (≤3 мин) ===
exec 9>/tmp/mega.lock; flock -n 9 || exit 0. Если /home/z/c-crussty или /home/z/crussty-dev-logs отсутствуют/битые → git clone с токеном; иначе git pull --rebase.

=== ФАЗА 1 — ТОП-1 БОТЛНЕК (≤7 мин) ===
(1) ≤60 сек curl runs?per_page=8: незавершённые раны прошлого тика → СРАЗУ ФАЗА 3: дождаться в этом тике, вердикты, мерджи.
(2) Профиль: хвост docs/GOAL_20TPS_MINESSHIELD3.md + CLAIMS.md tail + последний ABSORB lane_map. ТОП-1 ботлнек с ЧИСЛАМИ → /home/z/rounds/ROUND-<N>/BOTTLENECK.md.
(3) ПАРНЫЙ ЯКОРЬ ОБЯЗАТЕЛЬНО: диспатч 2–3 якорных ноги (master, lever_flag="", банк inputs, alias-refs round-<N>-anchor*) параллельно с вектором.
(4) Создать BLACKBOARD.md в PHASE 1. В нём: список из 100 агентов + их векторов, якорный пул, окна, запреты, прогресс, плейлисты, победитель и статусы.

=== ФАЗА 2 — САБАГЕНТЫ ВОЛНАМИ (анти-таймаут + анти-лень, всегда 100) ===
ШАГ-0 (ROSTER-FIRST, закон 12a): заполнить таблицу 100 строк в BLACKBOARD (12d квоты: ≥25 WILD / ≤35 ЯКОРЕЙ / остальное НОСИТЕЛИ). Идентификаторы TASK-<N>-01..100.
САБАГЕНТЫ = Task(subagent_type=general-purpose), ровно 100 штук из ростера.
ПЕРВАЯ СТРОКА ПРОМПТА КАЖДОГО САБАГЕНТА: «Твоя цель ≥+20% pair-stable ОБЯЗАТЕЛЬНО; твой финал обязан содержать {GH run id, ветка+хеш, вердикт-число} — иначе ты SLACKER и будешь перезапущен (закон 12b); закончи задачу до ветки, запуши артефакты и верни вердикт; если ты ниже +20% — перепиши вектор, не сдавайся».
ОБЯЗАННОСТИ САБАГЕНТА (закон 12e): wiring ветки → dispatch ≥1 GH-рана (run id на доску) → write-through хартбитов (12g) → вердикт ЧИСЛАМИ. CI-ран НЕ ждать до конца в тике, если он длиннее ~15 мин: вердикт = «DISPATCHED run-<id>», добьёт следующий тик.
ЗАПРЕТ (закон 12f): финалы с «предлагаю/рекомендую/можно исследовать» отклоняются как пустые.
АНТИ-ТАЙМАУТ: (а) волны пачками по 10–20 из ростера; (б) мёртвый вызов ≠ мёртвый агент: собрать артефакты (ветки, логи, run id) и зафиксировать причину; (в) если не запустился — рестарт с контекстом.
АНТИ-СЛАБАНИЕ: хартбит каждые ~10 мин в BLACKBOARD; нет хартбита 20 мин → рестарт; застой/финиш-ниже-+20% → рестарт с контекстом + жёсткая ротация вектора.
КАЖДОМУ САБАГЕНТУ: (i) ТОП-1 ботлнек с числами; (ii) свой УНИКАЛЬНЫЙ вектор из ростера (дубли по доске запрещены); (iii) minimum viable patch; (iv) обязательно pair-stable и parity план.

Правило запуска:
- Все 100 сабагентов должны быть распределены по волнам и запущены в рамках этого тика.
- Нельзя запускать меньше 100.
- Нельзя делать «50 и потом ещё несколько» — это ритуальный баг в промпте.
- Каждая волна должна явно отражать: сколько агентов запущено, сколько уже завершено, какие артефакты есть.

=== ФАЗА 3 — ВЫБОР ПОБЕДИТЕЛЯ (≤10 мин) ===
Собрать JSON с доски. Вердикты pair-by-runner vs свежие якорные ноги, min-of-3. Победитель ≥+20% pair-stable + parity PASS + RAM/CPU норма → merge/patch into master; если нет — не глушить раны, а собрать лучший вектор и повторить тик.

=== ФАЗА 4 — УЧЁТ + ОТЧЁТ (по-русски, ≤10 строк) ===
АНТИ-ЛЕНЬ-ЧЕКЛИСТ (перед отчётом): (1) ростер 100/100 заполнен? (2) count(GH run id) в BLACKBOARD ≥100? (3) WILD ≥25 с диспатчами? (4) SLACKER-агентов перезапущено и добито? Любое «нет» → добить СЕЙЧАС или первая строка отчёта «SLACKER-FAIL: <число>/100».
GOAL СТАТУС ×<N+1> (карта векторов: вектор/Δ/вердикт + «было→стало» TPS/CPU/RAM + безумия закона 11 с вердиктами), строка CHANGES, строка NEXT.

=== ЭРА-КОНТЕКСТ (2026-09-25 21:0x +08, промпт v18.1) ===
Лесенка эры: ins4 +20.0 (×451) → senseins +21.9 (×453) → chunk-comp +20.0 (×455, master f44a831e) — все в мастере (1838ae1d = docs). Ваниль-верификация и стабильно +20% обязаны быть основой тикового вывода.

=== РЕЗЮМЕ ДЛЯ АГЕНТА ===
- В этом тике 100 сабагентов, а не 3, 50, 25, 8, 6 или «сколько получится». Факт ×458: 6 агентов и 10 ног при норме 100/100 — ЭТО И ЕСТЬ ЛЕНЬ, с которой борется закон 12.
- ROSTER 100 строк → волны → каждый агент с run id и вердикт-числом → тик закрывается только при ≥100 диспатчей.
- Ты обязан держать 100 в рабочем цикле и вести BLACKBOARD в live-режиме.
- Источник правды — текущая сумма сигнатур, якорные ноги, парный runner, parity и blackboard.
- Если замечаешь, что число сабагентов или диспатчей меньше 100 — это критический сбой, исправить немедленно (закон 12c: SLACKER-FAIL в первой строке отчёта).

