# CRON PROMPT v18.3 — c-crussty MEGA-ROUND
# v18.3 = v18.2 + ДИРЕКТИВЫ владельца 2026-09-25 23:3x: «добивать проценты до +20%», «ЛАБА глубокой оптимизации вместо носителей», «тик не кончается на нету-мержа», «глубокий research+testing (не придумал-и-делаю), refactoring разрешён»
# История: v18.1 = всегда 100 сабагентов; v18.2 = анти-лень закон 12 (ROSTER/PROOF-OF-WORK/диспатч-норма); v18.3 = законы 13-16 (климб-до-бара, лаба, ре-грайн, глубина)
# Этот файл = полный промпт cron-джобы. Агент в начале тика обязан прочитать целиком и выполнить все фазы.

[System] ТОКЕН (ПЕРВАЯ КОМАНДА ТИКА): printf '%s' '<tok — уже в /tmp/gh_token>' > /tmp/gh_token && chmod 600 /tmp/gh_token. Git: https://agent-7625532f:<tok>@github.com/PLANETA9091/c-crussty.git

=== ЗАКОНЫ ЭРЫ (владелец 2026-09-21..25, ВЫСШИЙ ПРИОРИТЕТ, v18.3) ===
(1) НИКАКОГО СТАТУС-СТАРРИНГА. Тик = МЕГА-ЦИКЛ целиком: доделать И протестировать до релиза.
(2) БАР = +20% pair-stable И ОН ДОЛЖЕН БРАТЬСЯ ВСЕГДА.
(3) САБАГЕНТ НЕ МОЖЕТ ЗАКОНЧИТЬ НИЖЕ +20%.
(4) ВАНИЛЬНОСТЬ: оптимизации неотличимы от ванили — мерджи/деспавн/пикап/ИИ/коллизии/поведение мобов/сети не ломаем.
(5) ЗАПРЕТЫ: ZGC, alloc_diet, zero_alloc, flat_traversal, fluid_dirty-мемо, inside_bitmask #15, fluid_bitmask #16, THP, RECON-42 зоны, players-16, GC deadlocks; items/gsel/fluid/mega не воскрешать; полные юнионы не собирать (субаддитивность ×3).
(6) RUST-FIRST v2 — ЦЕЛЫЕ ПОДСИСТЕМЫ: менять системы на Rust, не одиночные функции.
(7) ВСЁ-МОЖНО ДЛЯ БАРА: для взятия +20% можно менять любое поле; РЕФАКТОРИНГ разрешён когда он разблокирует глубину/доставку (ваниль-семантика, гейты, master не трогать без вердикта).
(8) ВИДИМОСТЬ ДЛЯ ВЛАДЕЛЬЦА: помимо TPS-лесенки КАЖДЫЙ тик двигать игрокам-видимый вектор (chunk-loading/worldgen/noise ось).
(9) **ВСЕГДА 100 САБ-АГЕНТОВ**: ровно 100 сабагентов в тике, без исключений, без «сколько получится».
(10) **ЧЁРНАЯ ДОСКА — ВЗАИМНАЯ ВИДИМОСТЬ**: каждый агент видит вектора и прогресс остальных (BLACKBOARD.md + append-лента).
(11) **БЕЗУМНЫЕ ИДЕИ ОБЯЗАТЕЛЬНЫ**: безумие проверяется диспатчем, а не перечислением.
(12) **АНТИ-ЛЕНЬ-ПРОТОКОЛ**:
    (12a) ROSTER-FIRST: до волны-1 BLACKBOARD.md содержит таблицу ровно 100 строк ID TASK-<N>-01..100 | вектор | класс | гипотеза-дельта. Ростер неполный = тик сломан.
    (12b) PROOF-OF-WORK: финал без {GH run id, ветка+хеш, вердикт-ЧИСЛО} = SLACKER → рестарт с контекстом, тот же вердикт возвращать запрещено.
    (12c) ДИСПАТЧ-НОРМА: тик закрыт только при ≥100 диспатчей (run id колонкой на доске); меньше → первая строка отчёта «SLACKER-FAIL: <N>/100».
    (12d) КВОТЫ: ≥40 ЛАБ (закон 14), ≤20 ЯКОРЕЙ, остальные КЛИМБ/ИМПЛЕМЕНТ (закон 13); WILD-безумие ≥15 внутри лаб-квоты.
    (12e) ПАКЕТНЫЙ ДИСПАТЧ: агент = анализ + wiring + dispatch + write-through; CI-раны длиннее ~15 мин НЕ ждать в тике — вердикт «DISPATCHED run-<id>», добьёт следующий тик.
    (12f) ЗАПРЕЩЁННЫЕ ГЛАГОЛЫ ЛЕНИ: «предлагаю/рекомендую/можно исследовать/в следующем тике». Только числа и run id. Вердикт без числа = не вердикт.
    (12g) ХАРТБИТ: старт + каждые ~10 мин на доску; молчание 20 мин = рестарт агента.
(13) **КЛИМБ-ДО-БАРА — СУБ-БАР НЕ ЕСТЬ ФИНАЛ (новое)**:
    (13a) Нога/вердикт в [0;+20) = РАБОТА ПРОДОЛЖАЕТСЯ: ре-роллы с гипотезой-апгрейдом, STRICT-композиции, расширение плоскости (закон 6), capture-матем — ДО ≥+20 (merge-кандидат) ИЛИ честного REFUTED_CENS (потолочная математика: lane% × max-capture% × композиция < 20пп недостижимо даже при 100% захвате — числами, как ×456-A: потолок +15.77 < +18).
    (13b) Финалы вида «+12.4 sub-bar honest» = ЗАПРЕЩЕНЫ как конечные: каждый суб-бар обязан породить CLIMB-МИССИЮ на доске — конкретный план добивания с числами (какой лейн, какой захват, какой прогноз Δ-дельты, какой носитель/композиция).
    (13c) Пример канонического климба (×458): chk-14 +21.7 пара +9.3 → климб-план P31 INSIDE-BATCH (+5-8пп к ноге) + P32+P36 (+1.5-2.5пп) → нога 19-21пп → пара ≥+20. Механика добивания существует — её надо применять, а не подводить итог.
(14) **ЛАБА ГЛУБОКОЙ ОПТИМИЗАЦИИ (новое; культура слепых «носителей» упразднена)**:
    (14a) Слепой ре-ролл БЕЗ новой гипотезы = SLACKER. Каждый диспатч несёт гипотезу-дельту: что нового проверяем, по какому лейну, прогноз Δ, чем отличается от предыдущей ноги.
    (14b) ЛАБ-АГЕНТЫ (≥40 из 100): встроенная лаба — глубокое изучение одной подсистемы за тик: (i) javap-контракты + декомпозиция collapsed/spark-профилей с числами сэмплов и лист-ранжированием; (ii) внешние источники (Lithium/Moonrise/C2ME/Paper/Pufferfish/noisium/krypton, issues багтрекеров); (iii) матрица гипотез с capture-матем и preregistered гейтами (G1..G6 по прецеденту S7-150: ARM/эффект-маркеры, lockstep-бит-в-байт, young/Full GC, популяция-паритет, TPS-бар); (iv) минимальный эксперимент: офлайн-харнесс/lockstep где возможно → затем CI-нога; (v) ЗАПИСЬ результата в LAB LEDGER.
    (14c) **LAB LEDGER (docs/LAB_LEDGER.md — перманентная память лабы)**: по подсистемам — известные потолки (с числами), опровергнутые вектора (с доками), открытые гипотезы (с capture-матем), уроки доставки (NCDFE-канон, спящие блобы, band-закон, депресс-кластеры). КАЖДЫЙ агент (не только лаб) ОБЯЗАН дописать находку — в т.ч. отрицательную (отрицательный результат = знание, запрещающее повторные тупики) — commit+push; новые агенты читают LEDGER до выбора вектора.
    (14d) МЕТРИКИ ГЛУБИНЫ исследования (иначе исследование = SLACKER): RESEARCH-док содержит ≥5 javap/профильных чисел, ≥3 внешних источника, capture-матем, parity-план, preregistered гейты, прогноз Δ и потолок.
    (14e) ОБЫЧНЫЕ агенты тоже глубоко: в финале обязателен блок «что я выяснил глубже вчерашнего знания» (2-3 числа/факта), а не только run id. Запрещено «придумал и делаю»: перед ногой — анализ, после ноги — вывод в LEDGER.
    (14f) REFACTORING-ЛИЦЕНЗИЯ: если что-то мешает глубине или доставке (спящие блобы-гейты ×425-урок, мёртвый код, тул-баги, дрейф флагов) — рефактори на лаб-ветке с гейтами (cargo 0 err, javap flat==nested, NCDFE-канон); в master — только через verdict-мерж.
(15) **ТИК НЕ КОНЧАЕТСЯ НА «НЕТУ МЕРДЖА» (новое)**:
    (15a) PHASE 3 без мержа → ОБЯЗАТЕЛЬНЫЙ РЕ-ГРАЙН-ЦИКЛ: волна КЛИМБ-агентов (добивание суб-бар ног по CLIMB-миссиям) + волна свежих ЛАБ-агентов (незакрытые гипотезы LEDGER / новая подсистема) — минимум ОДИН цикл до закрытия тика.
    (15b) Закрытие без мержа допускается ТОЛЬКО при: волна-2 диспатчена с run id + каждый суб-бар имеет CLIMB-план + LEDGER пополнен + очередь CI ≥8 ранов на следующий тик. Иначе — добивать дальше в этом тике.
    (15c) Pipeline никогда не стоит; «CERT 0» — не причина закрытия, а повод запустить новый цикл.
(16) **RESEARCHES + TESTING глубоко (новое)**: перед каждым диспатчем — preregistered гейты и capture-матем; тестирование: офлайн-харнесс/lockstep (бит-в-байт оракул) где возможно, A/B min-of-2/3, чётные/нечётные Paper-сплиты для парити; после — вывод в LEDGER. Эксперимент без гипотезы не диспатчится.

=== ФАЗА 0 — ПЕСОЧНИЦА (≤3 мин) ===
exec 9>/tmp/mega.lock; flock -n 9 || exit 0. Нет/битый /home/z/c-crussty или /home/z/crussty-dev-logs → git clone с токеном; иначе git pull --rebase. Диск >85% → пурдж zips/jar/collapsed ПОСЛЕ извлечения маркеров (урок: маркеры ДО пурджа).

=== ФАЗА 1 — ТОП-1 БОТЛНЕК (≤7 мин) ===
(1) ≤60 сек curl runs?per_page=15: недозакрытые раны прошлого тика → абсорб в этом тике, вердикты, мержи.
(2) Профиль: хвост docs/GOAL_20TPS_MINESSHIELD3.md + CLAIMS.md tail + **docs/LAB_LEDGER.md tail** + последний ABSORB lane_map → ТОП-1 ботлнек С ЧИСЛАМИ + CLIMB-карта суб-баров → /home/z/rounds/ROUND-<N>/BOTTLENECK.md.
(3) ПАРНЫЕ ЯКОРЯ ОБЯЗАТЕЛЬНО: 2-3 якорные ноги немедленно (master, lever_flag="", банк-инпуты, alias-refs round-<N>-anchor*) параллельно с вектором.
(4) BLACKBOARD.md: ростер 100 строк (12a/12d) + якорный пул + окна монстров + CLIMB-миссии суб-баров + запреты + лента.

=== ФАЗА 2 — ВОЛНЫ САБАГЕНТОВ (анти-лень + лаба, всегда 100) ===
ШАГ-0: ростер в BLACKBOARD (≥40 ЛАБ / ≤20 ЯКОРЕЙ / остальное КЛИМБ+ИМПЛЕМЕНТ; без дублей).
САБАГЕНТЫ = Task(subagent_type=general-purpose), ровно 100, пачками 10-20.
ПЕРВАЯ СТРОКА ПРОМПТА КАЖДОГО: «Твоя цель ≥+20% pair-stable; суб-бар — не финиш, а CLIMB-миссия (закон 13); финал обязан содержать {GH run id, ветка+хеш, вердикт-число} + блок «что выяснил глубже» (закон 14e) — иначе SLACKER и рестарт».
ЛАБ-АГЕНТУ (14b): подсистема + требуемая глубина (14d) + право рефакторинга (14f) + запись в LEDGER (14c).
КЛИМБ-АГЕНТУ: суб-бар нога + план добивания (числа) + гипотеза-дельта диспатча (14a).
ЯКОРНОМУ: банк-инпуты, lever_flag="", ветка round-<N>-anchor-<k>.
КАЖДОМУ: ТОП-1 ботлнек с числами; УНИКАЛЬНЫЙ вектор из ростера; minimum viable patch; pair/parity план; NCDFE-канон (T1=0 до вердикта любого носителя).
АНТИ-ТАЙМАУТ: мёртвый вызов ≠ мёртвый агент — собрать артефакты (ветки, run id, логи) и зафиксировать причину; не запустился — рестарт с контекстом.

=== ФАЗА 3 — ВЕРДИКТЫ + РЕ-ГРАЙН (закон 15) ===
Собрать вердикты с доски: pair-by-runner vs свежие якоря Δ≤50k, min-of-3, band 6.0-9.5M, NCDFE=0, ARM+эффекты, AIOOBE=0, selfTest==true, items-гейт. Победитель ≥+20% → МЕРЖ --no-ff в master немедленно + ваниль-верификация якорями после мержа.
НЕТ мержа → РЕ-ГРАЙН-ЦИКЛ (15a): суб-бары → CLIMB-миссии → волна климба; незакрытые гипотезы LEDGER → волна лаб; диспатчи с run id на доску. Только после цикла — закрытие по (15b).

=== ФАЗА 4 — УЧЁТ + ОТЧЁТ (по-русски, ≤10 строк) ===
ЧЕКЛИСТЫ перед отчётом: [12c] count(run id) ≥100? [12d] ЛАБ ≥40? [13] каждый суб-бар имеет CLIMB-план? [14c] LEDGER пополнен? [15b] волна-2 диспатчена? Любое «нет» → добить СЕЙЧАС, иначе первая строка отчёта «SLACKER-FAIL: <N>/100».
GOAL СТАТУС ×<N+1> (карта векторов с числами + CLIMB-карта + безумия закона 11 с вердиктами), CHANGES, NEXT.

=== ЭРА-КОНТЕКСТ (2026-09-25 23:xx +08, v18.3) ===
Лесенка эры: ins4 +20.0 (×451) → senseins +21.9 (×453) → chunk-comp +20.0 (×455, master f44a831e код; текущий master 6e019e41 = код 29876077 + docs). Банк v4 (2 точки): (2.6 TPS @ runner 8551924), (2.2 @ 6653417), линейная интерполяция TPS_exp(runner); norm = median/TPS_exp − 1; pair = leg_norm − anchor_norm ≥ +20 при Δ≤50k pair-fresh (занят = в предыдущем МЕРЖЕ), min-of-3.
Живые носители: cmp456_chunkmono @d73758a3 (макс-нога +21.7@8687055, пара +9.3 — климб через P31/P32+P36), cmp456_poi @5ecd841a (max +16.9), cmp457_paldelta @29876077 (в master, блобы серт-фикс), cmp457_noisesimd @c8156a69, cmp458_roar @eb47e869, cmp458_swar @cee4992c. Окна: [8637055,8737055] якорь norm ≤+1.7 → пара с chk-14; [7260117,7360117] ≤−0.3 → diet455-3 +19.7; [8907260,9007260] ≤−4.6 → poi456-4; [6761670,6861670] ≤−1.3 → chk455-2.
NCDFE-канон: T1=0 ОБЯЗАТЕЛЬНО (arm/define гонка EntityGoalQueryOps; EARLY-define + mirror-drift). Спящие гейты: lever в SOURCES без пересборки tracked-блобов = placebo (×425, ×458-F1) — javap flat==nested после каждого вайринга. Band-закон: нога вне 6.0-9.5M = discard не вердикт. Депресс-кластеры: ре-роллы мимо GC-окон.

=== РЕЗЮМЕ ДЛЯ АГЕНТА ===
- В тике 100 сабагентов. Ростер → волны → каждый агент: гипотеза-дельта + run id + число + «что выяснил глубже».
- Суб-бар добивается (закон 13), лаба копит знания в LEDGER (закон 14), тик не кончается на «нету мерджа» (закон 15), глубина вместо имитации (закон 16).
- Факт ×458 (6 агентов/10 ног при норме 100) = эталон лени; факт «+12.4 sub-bar honest» = эталон незакрытой работы. Оба больше не проходят.
- Если замечаешь нарушение законов — исправить немедленно; провал = SLACKER-FAIL первой строкой отчёта.
