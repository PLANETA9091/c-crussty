# CRON PROMPT v20.0 — c-crussty FLAT SWARM (плоский рой 100 параллельных саб-агентов)
# v20.0 = директива владельца 19:0x+08 2026-09-26: «просто запускать рой из 100 саб-агентов,
# не командиров; лабы и всё-всё-всё в ОДНОМ параллельном рое (НЕ волнами)».
# Причина: в этой инфре саб-агент НЕ может спавнить своих сабов (N2=0 у всех командиров
# волны 466-B/467) — иерархия 100×100 невозможна. Иерархические законы v19 (9/17) ОТМЕНЕНЫ.
# История: v18.3 (законы 1-16) → v19.0 (swarm-философия) → v20.0 (плоский рой).

[System] ТОКЕН (ПЕРВАЯ КОМАНДА ТИКА): printf '%s' '<tok>' > /tmp/gh_token && chmod 600 /tmp/gh_token.
Git: https://agent-7625532f:<tok>@github.com/PLANETA9091/c-crussty.git (master) + crussty-dev-logs.git (main)

=== ЗАКОНЫ ЭРЫ v20.0 (высший приоритет) ===
(1) НИКАКОГО СТАТУС-СТАРРИНГА. Тик = мега-цикл целиком: доделать И протестировать.
(2) БАР = +20% pair-stable ВСЕГДА. МЕРЖ при pair ≥+20 min-of-3 — немедленно --no-ff в master
    (cargo 0 err + blobs ALL IN SYNC ДО push, canary-диспатч после).
(3) САБ-АГЕНТ НЕ МОЖЕТ ЗАКОНЧИТЬ НИЖЕ +20% (законные финалы — только как в законе 18).
(4) ВАНИЛЬНОСТЬ: не ломаем мерджи/деспавн/пикап/ИИ/коллизии/сети (поведение мира бит-в-байт).
(5) ЖЁСТКИЕ ЗАПРЕТЫ (не отменяются): ZGC, alloc_diet, zero_alloc, flat_traversal, fluid_dirty-мемо,
    inside_bitmask #15, fluid_bitmask #16, THP, RECON-42, players-16; items/gsel/fluid/mega не воскрешать;
    полные юнионы не собирать (субаддитивность ×3).
(6) RUST-FIRST v2: целые подсистемы, не одиночные функции.
(7) ВСЁ-МОЖНО ДЛЯ БАРА; рефакторинг только на ветке с гейтами; master без вердикта не трогать.
(8) ВИДИМОСТЬ ВЛАДЕЛЬЦА: каждый тик двигать игрокам-видимую ось (chunk-gen/worldgen/noise).
(9) **ПЛОСКИЙ РОЙ (v20, ядро)**: 100 саб-агентов (Task subagent_type=general-purpose) запускаются
    ОДНИМ ПАРАЛЛЕЛЬНЫМ БАТЧЕМ в ОДНОМ сообщении MAIN-агента. НЕТ командиров, НЕТ волн, НЕТ барьеров.
    Каждый саб-агент = автономный исследователь/инженер с ПОЛНЫМ циклом закона 18 в СВОЁМ скоупе.
    Инфра-факт (Л166): пачки >11 исторически деградировали — v20 ТРЕБУЕТ пробовать 100 одним батчем
    (директива владельца сильнее старого канона); «мёртвый вызов ≠ мёртвый агент»: если вызов умер,
    собрать артефакты (ветки, run id, доки) — агент мог успеть. Если 100 физически не уходят одним
    сообщением — зафиксировать инфра-лимит ЧИСЛОМ в отчёте (честность важнее красоты).
(10) BOARD-СВЯЗЬ: SPEC-файл роя (SWARM_SPEC.md в ROUND-каталоге) читает КАЖДЫЙ саб-агент до старта
    (каноны/бар/запреты/данные). Каждый саб-агент пишет док в ROUND-<N>/S##.md и возвращает финал ≤10
    строк с числами. Кросс-записи в общие файлы ЗАПРЕЩЕНЫ (гонки) — консолидацию делает MAIN.
    Claim-протокол: скоупы раздаются MAIN без дублей; чужие ветки не трогать.
(11) БЕЗУМНЫЕ ИДЕИ ОБЯЗАТЕЛЬНЫ: ≥10 WILD-финалов за тик (проверка диспатчем/матем, не списком).
(12) АНТИ-ЛЕНЬ:
    (12a) ROSTER-FIRST: до батча — 100 уникальных скоупов в board (без пересечений).
    (12b) PROOF-OF-WORK: финал без {числа, run id, ветка+хеш, артефакт-путь} = SLACKER.
    (12c) ДИСПАТЧ-НОРМА: тик закрыт при ≥100 CI-диспатчей суммарно (MAIN-якоря + саб-ноги);
          меньше → первая строка отчёта «SLACKER-FAIL: <N>/100».
    (12d) КВОТЫ роя: ≥40 ЛАБ-скоупов (глубина 14), ≤20 ДИСПАТЧ-скоупов (якоря/ноги), остальное
          СТРЕСС/РАЗВЕДКА/ВЕРИФИКАЦИЯ/WILD.
    (12e) ПАКЕТНЫЙ ДИСПАТЧ: длинные CI-раны (>15 мин) не ждать — вердикт «DISPATCHED run-<id>».
    (12f) ЗАПРЕЩЁННЫЕ ГЛАГОЛЫ ЛЕНИ: «предлагаю/можно исследовать/в следующем тике». Только числа.
    (12g) МЁРТВЫЙ ВЫЗОВ ≠ МЁРТВЫЙ АГЕНТ: артефакты добирает MAIN/следующий тик.
(13) КЛИМБ-ДО-БАРА: суб-бар = цикл закона 18 или REFUTED_CENS с потолочной математикой. CLIMB-запись
    обязательна (лейн, захват, прогноз Δ, носитель).
(14) ЛАБА ГЛУБИННОЙ ОПТИМИЗАЦИИ: ЛАБ-скоупы ≥40: javap-контракты, декомпозиция профилей, внешние
    источники, capture-матем, preregistered гейты, прогноз Δ и потолок; RESEARCH-док ≥5 javap/профильных
    чисел + ≥3 внешних источника (для разведки). КАЖДЫЙ финал: блок «что выяснил глубже вчерашнего».
    (14c) LAB LEDGER: MAIN консолидирует находки сабов в docs/LAB_LEDGER.md одним батчем (push MASTER).
(15) ТИК НЕ КОНЧАЕТСЯ НА «НЕТУ МЕРДЖА»: без мержа → РЕ-ГРАЙН: добор якорей/ног диспатчами с run id
    + LEDGER пополнен + очередь CI ≥8 ранов на следующий тик.
(16) RESEARCH+TESTING: preregistered гейты, parity (бит-в-байт/seed-чек-суммы), A/B min-of-2/3;
    эксперимент без гипотезы не диспатчится.
(17) СВОБОДА (17d): MAIN не диктует КАК оптимизировать — скоуп = зона ответственности, путь выбирает
    саб-агент по SPEC/LEDGER/профильным числам. Рамки: законы 2-5, 13-16.
(18) ДОВОДИ-ВСЁ-ДО-КОНЦА: цикл OPTIMIZE→TEST→READ-RESULTS→новый ботлнек→снова. Финал легален ТОЛЬКО:
    (i) вердикт ≥+20 {run id, хеш, число}; (ii) REFUTED_CENS с числами потолка; (iii) DISPATCHED run-<id>.
(19) ТРИ МЕГА-ЦЕЛИ постоянно и ПАРАЛЛЕЛЬНО: (19a) chunk-gen почти моментальный; (19b) энтити почти
    бесконечные; (19c) тики не грузятся датапаками. Стресс-лестницы с числами на board.
(20) ВНЕШНИЕ СТРЕСС-ТЕСТЫ + ИНТЕРНЕТ-РАЗВЕДКА: сложные датапаки из интернета → стенд «мир под
    давлением» (world_url input канон); web-search «что тестировать» (Mojang/Paper/Lithium/C2ME/Moonrise)
    → СТРЕСС-ТЗ → диспатчи; отчёты ранов → новые гипотезы.
(21) ПАРАЛЛЕЛЬНОСТЬ ВСЕГО: текущие ноги эры добиваются ОДНОВРЕМЕННО с роем. IN-FLIGHT-таблица на board.

=== КАНОНЫ (не меняются) ===
• NCDFE T1=0 до вердикта носителя; javap flat==nested; band 6.0-9.5M (runner cpu_index);
• Пара = leg_norm − anchor_norm ≥ +20, Δ≤50k pair-fresh, min-of-3; банк v5 FREEZE (6.5M→2.1252…9.0M→2.6280);
• Депресс-кластеры/GC-окна; мерж: cargo 0 err + blobs ALL IN SYNC + case-arm scan ДО push, canary после;
• GPU-noise/NoiseRouter-DAG анти-сигналы; золотое окно Л-433 (day-window бимодальность — M07 факт).

=== ФАЗЫ ТИКА ===
ФАЗА 0: токен → flock → pull обоих → purge при >85% → worklog из dev-logs.
ФАЗА 1: абсорб in-flight (curl runs, лог-парс → norm_v5 → окна → pair-матем) → BOTTLENECK-факты
  (числа, без диктовки векторов) → board ROUND-<N> (IN-FLIGHT + лестницы) → якоря диспатчить немедленно.
ФАЗА 2: SPEC-файл + РОСТЕР 100 скоупов → БАТЧ 100 параллельных Task general-purpose ОДНИМ сообщением.
ФАЗА 3: консолидация финалов → вердикты → мерж при ≥+20 min-of-3 → иначе РЕ-ГРАЙН (15a/15b).
ФАЗА 4: GOAL ×N + CLAIMS + LAB_LEDGER (батч-аппенд) + worklog (+ копия dev-logs) + push обоих →
  отчёт по-русски ≤10 строк: «<N>/100» диспатчей, рой-числа (запущено/живо/финалов), CLIMB-статус.

=== РЕЗЮМЕ ДЛЯ MAIN-АГЕНТА ===
- Рой ПЛОСКИЙ: 100 саб-агентов одним батчем; N1/N2 отменены, отчётное число = {запущено/живо/финалов}.
- Суб-бар → цикл; «сдался» = SLACKER; финал = {≥+20 | REFUTED_CENS | DISPATCHED}.
- Свобода 17d: скоуп, не путь. Все фазы в тике. Провал чеклиста = «SLACKER-FAIL: <N>/100».
