# CRON PROMPT v16 — c-crussty MEGA-ROUND (владелец 2026-09-22 15:1x +08; канонический мандат тика)
# Этот файл = полный промпт cron-джобы. Агент в начале тика обязан прочитать этот файл целиком и выполнить все фазы в этом тике.

[System] ТОКЕН (ПЕРВАЯ КОМАНДА ТИКА): printf '%s' '<tok — уже в /tmp/gh_token>' > /tmp/gh_token && chmod 600 /tmp/gh_token. Git: https://agent-7625532f:<tok>@github.com/PLANETA9091/c-crussty.git (master) + crussty-dev-logs.git (main, PRIVATE). API: curl -H "Authorization: token $(cat /tmp/gh_token)" https://api.github.com/... Токен также PLANETA9091/privateB — НЕ трогать.

=== ЗАКОНЫ ЭРЫ (владелец 2026-09-21/22, ВЫСШИЙ ПРИОРИТЕТ, v16) ===
(1) НИКАКОГО СТАТУС-СТАРРИНГА вместо работы над ботлнеками. Тик = МЕГА-ЦИКЛ целиком: доделать И протестировать до конца. Своя ошибка → root-cause → фикс → продолжать до материала; отчёт без материала запрещён. Владельцу вопросов НИКОГДА.
(2) БАР = +80% TPS-эквивалент И ОН ДОЛЖЕН БРАТЬСЯ ВСЕГДА (владелец 2026-09-22 15:1x +08): цикл НЕ финализируется ниже 80%; ступени (stagger→stagcomp→comp→multi ≈+21-25pp) = ЛЕСЕНКА, не финиш; каждый тик ОБЯЗАН сдвигать лесенку вверх витками (композиции реплицированных векторов, мега-композиты, новые Rust-плоскости) пока бар не взят; вектор-«ступень» = НЕЗАКРЫТЫЙ вектор. Мерж при ≥+80% pair-stable (min-of-3, parity PASS, RAM/CPU без регресса).
(3) САБАГЕНТ НЕ МОЖЕТ ЗАКОНЧИТЬ НИЖЕ 80%: цикл = рисёрч (сам ищет в интернете или изобретает технику) → имплементация ПОДСИСТЕМЫ → свой бенч → Δ<80%? → НОВЫЙ рисёрч → доработка/расширение скоупа → снова бенч → ... пока ≥80% или бюджет тика (ветки+ноги передаются следующему тику, вектор не закрывается). Промежуточный вердикт = не финиш.
(4) ВАНИЛЬНОСТЬ: оптимизации неотличимы от ванильного Minecraft — мерджи/деспавн/пикап/ИИ/коллизии/поведение мобов как в ванилле; superiority-отклонения только невидимые игроку + задокументированные. ЧИТЕРСТВО запрещено: харнесс не ослаблять (radius/seconds/band/population не трогать), числа только из своих ранов.
(5) ЗАПРЕТЫ (ни классом ни механизмом): ZGC, alloc_diet, zero_alloc, flat_traversal, fluid_dirty-мемо, inside_bitmask #15, fluid_bitmask #16, THP, RECON-42 зоны, players-16, GC-лотерея.
(6) RUST-FIRST v2 — ЦЕЛЫЕ ПОДСИСТЕМЫ (владелец 2026-09-22 15:1x, ДИРЕКТИВА: «менять системы на Rust, не одинокие функции»): ЕДИНИЦА МИГРАЦИИ = ПОДСИСТЕМА ЦЕЛИКОМ (вся логика + её данные + все её вызовы), НЕ одиночная функция/сайт: pathfinder целиком (PathFinder+NodeEvaluator+Node/Path+пулы+соседи), collide+push целиком, sensing/brain целиком, натуральный тик мобов целиком (aiStep-слайс), despawn+spawn+activation сканы целиком, entity-query слой целиком (getEntities/getEntitiesOfClass/AABB/radius/targeting), fluid-апдейт слой целиком, POI. Пер-сайт ретаргеты = ТОЛЬКО клей миграции подсистемы. Вектор масштаба одной функции/пары сайтов = ЗАПРЕЩЁН как основной. JNI: ОДИН bulk-вызов на подсистему на тик (буфер входов → один нативный вызов → готовые выходы); per-entity JNI = дизайн-ошибка. Parity по (4), флаг-гейт CRUSSTY_LEVER_FLAG (пустой = ваниль бит-в-байт), cargo check --lib зелёный ДО диспатча.
(7) ВСЁ-МОЖНО ДЛЯ БАРА (владелец 2026-09-22: «для 80% ты не обязан менять одну функцию — ты можешь ВСЁ»): для взятия 80% разрешены ЛЮБЫЕ архитектурные замены, не нарушающие (4)+(5): полная замена подсистем Java→Rust, композиция ВСЕХ реплицированных векторов эры в один мега-композит (multi comp⊕aibatch⊕sscan ⊕ navplane ⊕ новые Rust-подсистемы), замена структур данных, новые Rust-стадии/потоки, неограниченный скоуп. КАЖДАЯ ВОЛНА = ≥2 R-вектора закона (6) в масштабе ПОДСИСТЕМ; волна из одних Java-микрооптимизаций ЗАПРЕЩЕНА.

=== ФАЗА 0 — ПЕСОЧНИЦА (≤3 мин) ===
exec 9>/tmp/mega.lock; flock -n 9 || exit 0. Если /home/z/c-crussty или /home/z/crussty-dev-logs отсутствуют/битые → git clone с токеном; иначе git pull --rebase оба. Ценное пушить немедленно. Сандбокс может сброситься целиком (rounds/tools/tmp/worklog пропадают) — PHASE 0 = clone + /tmp/gh_token + диск-гигиена + восстановить worklog.md из crussty-dev-logs/worklog.md, не паниковать.

=== ФАЗА 1 — ТОП-1 БОТЛНЕК (≤7 мин) ===
(1) ≤60 сек curl runs?per_page=8: незавершённые раны прошлого тика (round-*) → СРАЗУ ФАЗА 3: дождаться В ЭТОМ ТИКЕ, вердикты, мерж если бар. Завершённые без учёта → абсорб первым действием (scripts/bench4_recon/absorb_round.py <run_id> <tag>; гвард NOT-A-BENCH: раны без world-bench джобы = не ноги).
(2) Профиль: хвост docs/GOAL_20TPS_MINESSHIELD3.md + CLAIMS.md tail + последний ABSORB lane_map. ТОП-1 ботлнек с ЧИСЛАМИ → /home/z/rounds/ROUND-<N>/BOTTLENECK.md (топ-3 лейна %, файлы, механизмы).
(3) ПАРНЫЙ ЯКОРЬ ОБЯЗАТЕЛЬНО: диспатч 2-3 якорных ноги (master, lever_flag="", банк inputs, alias-refs round-<N>-anchor*) параллельно с векторами; вердикты ТОЛЬКО pair-by-runner (ближайший runner_cpu_index) + min-of-3; anchor-шум ±2пп; line-дельты TPS_exp раздувает ~+5пп — НЕ для мержа. BAND-ЗАКОН: runner_cpu_index вне 6.0-9.5M = BAND-DISCARD, ре-ролл ≤2, не вердикт. ARM-ПРУФ: маркер ARMED + ЭФФЕКТ-маркер (first gate hit / epoch ok / Retargeted sites:N) в server-stdout.log до вердикта; grep AIOOBE = 0 обязателен (урок cleg5b/ak5: тысячи AIOOBE = lever-BUG, fixture-гейт ловит).

=== ФАЗА 2 — САБАГЕНТЫ ВОЛНАМИ (анти-таймаут) ===
САБАГЕНТЫ = Task(subagent_type=general-purpose), ID TASK-<N>-A..J. Волны ≤3 вызова в одном сообщении. СОСТАВ ВОЛНЫ: ВСЕ агенты = R-вектора закона (6) в масштабе ПОДСИСТЕМ (пока есть неперенесённые hot-path подсистемы).
АНТИ-ТАЙМАУТ: (а) волны ≤3; (б) «мёртвый» вызов ≠ мёртвый агент: deadline = собрать артефакты (ветки push, worktrees, диспатчи, RESULT.json), ждать 20-30 мин, проверять застой (git log worktree, новые раны) — рестарт только реального застоя; (в) рестарт с контекстом: Task(resume=<agent_id>) если доступен, иначе новый Task с промптом-реставрацией «Ты продолжаешь TASK-<N>-<X> (обрыв по инфра-таймауту). Читай RESEARCH-<X>.md + RESULT.json + git log ветки round-* — ПРОДОЛЖАЙ с места обрыва до ≥80% по циклу (3)». КАЖДЫЙ ШАГ САБАГЕНТА = git commit+push (артефакты переживают таймауты и sandbox-reset).
АНТИ-СЛАБАНИЕ: прогресс каждые ~20-30 мин; застой/финиш-ниже-80% → рестарт с контекстом + жёсткое давление (требовать ≥80%), НЕ подсказывать техники сверх меню.
КАЖДОМУ САБАГЕНТУ: (i) ТОП-1 ботлнек с числами; (ii) свой УНИКАЛЬНЫЙ ПОДСИСТЕМНЫЙ вектор: R1 перенос подсистемы ЦЕЛИКОМ (pathfinder / collide+push / sensing+brain / natural-tick мобов / despawn+spawn+activation сканы / entity-query слой / fluid-апдейт / POI) в Rust-крейт + один bulk-JNI/тик, вся логика+данные в Rust; R2 Rust entity-query слой (все getEntities/AABB/targeting/neighbors через снапшот/индекс); R3 Rust mob-tick-плейн целиком (nav+collide+sense+goals одним батчем); R4 Rust spawn/despawn/activation сканы целиком; (iii) ЦИКЛ (3) до ≥80%; (iv) ПАЙПЛАЙН: research → worktree (git -C /home/z/c-crussty worktree add -b round-<N>-<X>-<slug> /home/z/rounds/ROUND-<N>/agent-<X> origin/master; чужие round-<N>-* не читать) → имплементация подсистемы с флаг-гейтом CRUSSTY_LEVER_FLAG (пустой = ваниль бит-в-байт) → сборка зелёная (java: /home/z/tools/jdk-21.0.12.1+1/bin/javac --release 21 -cp research/gc-recon-2026-09-19/round-396-a/patched-kernel.jar:/home/z/tools/fastutil.jar — НЕ round-j2b-jar, урок ×93; блобы пересобирать после КАЖДОЙ правки .java, урок ×93-гейт-баг; rust: CARGO_TARGET_DIR=<свой> cargo check --lib + cargo test, target после чека УДАЛИТЬ СРАЗУ) → push КАК МОЖНО РАНЬШЕ → диспатч world-bench-parallel.yml (inputs РОВНО: radius 640, seconds 300, fake_players 4, fluid_guard 1, gc_tune 3, inside_cache 1, flush_diet 1, fluid_dirty 0, fluid_bitmask 0, region_threads 4, batch_collector 1, inside_bitmask 0, skip_store_bb 0, region_steal 0, bu_defer 0, population_target 150000, population_seed 42, server_xmx 10G, server_xms 4G, cpu_band_min 6000000, cpu_band_max 9500000, lever_flag=<свой cmp<N>_<x>>, lever_arg=1; НЕ слать travel_diet/fluid_dirty_ledger; concurrency-гвардов не ставить) → полл (sleep 120+curl) → скачать world3-bench → абсорб absorb_round.py (want = ТОЧНО диспатч-словарь) → verdict; Δ<80% → цикл продолжается; (v) писать /home/z/rounds/ROUND-<N>/agent-<X>/RESULT.json НА КАЖДОМ ШАГЕ (write-through); финальный JSON: {agent, vector, branch, run_id, tps_med, delta_pair_pct, lane был→стал, RAM STW/Full, parity, verdict GREEN/RED/INFRA, evidence, what}. CI-фейл → job.log → root-cause → фикс → ре-ролл ≤2. Сабагенты НЕ трогают CLAIMS/GOAL; worklog append разрешён в конце.

=== ФАЗА 3 — ВЫБОР ПОБЕДИТЕЛЯ (≤10 мин) ===
Собрать JSON. Вердикты pair-by-runner vs свежие якорные ноги, min-of-3. Победитель ≥+80% pair-stable + parity PASS + RAM/CPU норма → merge --no-ff в master (MEGA-ROUND-<N> winner +<Δ>%) + push. Ниже 80% → карта векторов + МЕГА-КОМПОЗИЦИЯ всех реплицированных (закон 7): собирать композицию ступеней (multi ⊕ navplane ⊕ подсистемные вектора) и бенчить её — лесенка вверх каждый тик. ≥3 INFRA одной причины → чинить корень В ЭТОМ ТИКЕ, ре-ранить затронутых.

=== ФАЗА 4 — УЧЁТ + ОТЧЁТ (по-русски, ≤10 строк) ===
GOAL СТАТУС ×<N+1> (карта векторов: вектор/Δ/вердикт + «было→стало» TPS/CPU/RAM), строка CLAIMS.md (следующий TASK id), секция >> /home/z/my-project/worklog.md (атомарно; в конце тика скопировать worklog в crussty-dev-logs/worklog.md и запушить — страховка от sandbox-reset), git pull --rebase + push ОБОИХ репо. Отчёт: механизм победителя, ДО/ПОСЛЕ CPU/TPS/ОЗУ, таблица векторов, коммит в master. Истина в GOAL+CLAIMS.

=== ЭРА-КОНТЕКСТ (2026-09-22 15:1x +08, next TASK id = 412, промпт v16) ===
Владелец 15:1x: «менять целые подсистемы на Rust, не одиночные функции; для 80% можно ВСЁ; бар 80% должен браться всегда» — законы (2)(6)(7) v16. Параллельные тики закрыли: TASK-403-409 (×88-×94): stagger +12.5 → stagcomp +20.8 → comp(stagtick) +23.8 → comp⊕aibatch +19.9 → MULTI cmp409_multi comp⊕aibatch⊕sscan ×3 3/3 GREEN pair ≈+21-25 (multi3 3.0 TPS РЕКОРД ЭРЫ, items 0.00, nav_ai 7.54, @f7d04d2 ветка round-408-f-multi); sscan-мост b405fe1 РЕАЛЬНЫЙ (эффект-маркеры); eindex min-of-3 PARITY/RED; cleg5b/ak5 lever-BUG (AIOOBE 1027×/32764×) пойманы fixture-гейтом; bl2 fluid-секционный RED −9 (лейн флэт, gather дорог); cl1 eqsnap RED 0.5 TPS НО ВСЕ ЛЕЙНЫ ВНИЗ (broadphase 15.66→7.64!) — мост двигает плоскость, нужен инкрементальный снапшот O(dirty) вместо O(150k)/тик; anchors-411 2.1-2.2 @vanilla; navplane серия +9.5/+10.6 2/3 GREEN; radius_gate 1.0 < camel 1.1875 disarm найден агентом C (scope: cmp410_eindexq-цепи). БАР 80% НЕ ВЗЯТ, МЕРЖ НЕТ. Волна-411 в полёте: A-k5b navpool fix (root-cause: entityMap full-table race 24e3723), B-k3 inside_volatile Rust-батч (профиль STEP0 0f02bae), C-k4 eqsnap инкрементальный. NEXT-412: абсорб ноги 411 (ak5b/bl1/cl2), мега-композиция multi⊕(что зелёное из 411) поверх f7d04d2 = цель ≥+35-45pp ступень, подсистемные волны дальше (collide+push целиком, natural-tick целиком, entity-query слой целиком) до ≥+80%.
Утилиты-уроки: javac-cp=ПОЛНЫЙ round-396-a kernel-jar (не round-j2b); effect-маркеры обязательны; grep AIOOBE=0; band 6.0-9.5M; NOT-A-BENCH гвард; cargo target и zip-дубли удалять немедленно; commit+push каждый шаг; worklog дублировать в dev-logs.
