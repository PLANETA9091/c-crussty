# RECON-17 — TASK-337 (тик 01:08 +08, Job 397731). Освобождение лейна DONE-park:
# почему park-kill не конвертируется в TPS, и конфаундер раннера на TPS-оси.

База: absorb s7177 (RECON18_STEAL_V2_ABSORB.md) + свежие профили
run-s7169-parse-diag (банк v3) и run-s7177-steal-v2 (region_steal=1+bu_defer=1).

## 1. Вердикт s7177 по прегистеру — REFUTED-by-TPS (без изменений)

- PG-V2/V2a/V2b/V2d PASS, PG-V2c FAIL: median5=1.40 < 1.98 → REFUTED-by-TPS,
  rollback (дефолты yml region_steal/bu_defer = 0; мосты BU-DEFER/STEAL v2
  остаются инфраструктурой — фикс race-NPE s7176 сохранён для любых будущих
  region_steal-легов). Лейн НЕ закрыт (v7).
- Пост-гейт-находки НИЖЕ не двигают прегистер-вердикт (честный A/B).

## 2. НАХОДКА A — конфаундер раннера (runner_cpu_index), TPS-ось была слепа к машине

- run-env: s7169 runner_cpu_index = 8 566 450; s7177 = 6 874 223 → s7177
  работал на −19.8% более медленном/контендед раннере (фиксированный 6M-step
  LCG-зонд уже в инфраструктуре, но никогда не использовался в вердиктах).
- Собственная консольная серия s7169 из артефакта [22.8, 1.5, 1.6, 1.6, 1.8,
  2.2] → median5 = 1.60, а НЕ «банк 1.80» (1.80 = leg #5 35381522360, CLAIMS
  S7-163; RECON13-док записал для s7169 другой ряд [1.1,1.2,1.5,1.4,1.9]).
- Скорость-нормализованный TPS: (1.40/6.874M)/(1.60/8.566M) = **+9.1% vs банк**
  — сырое −22% было в основном конфаундером машины, а НЕ регрессией рычага.
- Систематика подтверждает: все n=1 леги s7172..s7177 дают 1.30–1.60 «против
  банка 1.80» — полоса раннер-шума ±20% маскировала всё. Все прошлые вердикты
  по TPS-оси (REFUTED-by-TPS класс) несут этот конфаундер (n=1, CLAIMS S7-163
  прямо пишет «шум n=1/1»).

## 3. НАХОДКА B — структурная победа без конверсии: парк был симптомом, не ресурсом

RECON17 worker-ось (wall-collapsed, deep-first, main=spin-стеки,
worker=RegionTickOps-корни):

- MAIN: банк 900 сэмплов (park 121 = 13.4%) → v2 1174 (park 16 = 1.4%).
- WORKER: банк 3233 (park 1175 = 36.3%) → v2 3593 (park 767 = 21.3%).
- BU-replay: 26 wall-сэмплов на main — ничтожен (канализация бесплатна).
- GC: young 166 (≤174), Full 0, GC-CPU −1.4% — стабилен.
- CPU-семейства (окно): GC 35.4→33.3%, ai 15.5→17.2%, fluid 11.1→11.1%,
  travel 7.7→8.7%; CPU-сумма 127k→133k (+4.9% при −22% TPS = +35% CPU-работы
  на тик — отражает и(pop-динамику при медленном раннере), и оверхед STЕAL).

Интерпретация: фаза-3 была уже насыщена (RECON-15: параллелизм 3.74/4 = 93%,
крит-путь 651 воркер vs 478 main). Парк main = ожидание воркер-крит-пути, а
не свободное ядро. Steal заливает ≤0.26 ядра → потолок ~+7-9% (пегистер-оценка
+4-6% была верна), измеренные +9.1% (норм.) в потолке. Лейн DONE-park как
«убить парк → TPS» исчерпан: потолок < 10% бара чартера.

## 4. Следствие для лейна — реальный рычаг = воркер-крит-путь, не парк

Лейн (Server-thread фаза-структура) жив, но вход смещается: крит-путь = работа
воркеров (entity-тик). Топ CPU-семейств банка: GC/JVM 35.4% (alloc-churn),
ai/goals 15.5%, fluid 11.1%, travel 7.7%. Рычаг #14-кандидат: TRAVEL-ALLOC-DIET
(zero-alloc AABB/Vec3 в collide-пути; RECON-14: travel-листья 45.73% alloc,
entity-bucket 81.16% alloc) — режет И воркер-крит-путь, И GC-давление.

## 5. Исправление протокола — PAIRED A/B min-of-2 (обязательно с этого тика)

1. Все будущие TPS-гейты: парные леги A/B на одной классе раннера
   (runner_cpu_index в ±5%), вердикт по нормализованной дельте, min-of-2.
2. Шаг 1 (s7178, диспатчится этим тиком): re-cal банка — конфиг банка v3
   (region_steal=0, bu_defer=0, остальное v3), гейты доставки + фиксация
   runner_cpu_index + TPS-серия; назначение: свежий якорь банка на текущем
   классе раннеров.
3. Шаг 2 (s7179, следующий тик): STEAL v2 leg на той же классе раннера →
   парная дельта ≥ +10% (норм.) = GREEN banking v4; иначе лейн остаётся
   открытым с рычагом #14.

## 6. Артефакты тика

- absorb_s7177.py: фикс fetch_artifact (NoRedirect на Azure-blob 302 — auth
  не форвардить) — рут-кауз 401; absorb выполнен, вердикт по прегистеру.
- recon17_worker_axis.py, recon17_worker_axis2.py, recon17_cpu_delta.py:
  worker-ось, парки, BU-кадры, CPU-дельта (банк vs v2).
- RECON18_STEAL_V2_ABSORB.md (вердикт), этот док (RECON-17).
