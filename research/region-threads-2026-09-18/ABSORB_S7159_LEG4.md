# ABSORB S7-159 leg #4 (35379431410) — ФИКС ПОДТВЕРЖДЁН ЖИВЬЁМ: ARMED ПОЛНЫЙ (Entity=0), TPS +77.8%, 0 инцидентов; PG4-strict FAIL (калибровка), PG4' PASS

**Run:** 35379431410, head bc77141 (фикс S7-159), 18:18-18:36 UTC (18 мин, SUCCESS), артефакт полный.
**Конфиг:** inside_cache=1 + flush_diet=1 + region_threads=4 (прочие 0) — бит-в-бит preregister.

## Живое доказательство фикса S7-159
```
[crussty-plugin] region_threads: ARMED, retransform rc ServerLevel=0 EntityCallbacks=0 Level=0 ChunkMap=0 Entity=0
```
- RegionTickOps lane = 73912 сэмплов (57.50% CPU), worker offload = **74.3%** (54941; W=4 дизайн точен — идентично leg #2)
- TrackerTickOps live = 2629 сэмплов (2.05%) — removal-safe sweep живьём; **0 tracker-NPE** (leg #2 инцидент не воспроизвёлся)
- RngOps live = 0 сэмплов (UUID только на спавнах — редки против 128k тик-сэмплов); **0 uuid-dup WARN** (leg #2 инцидент не воспроизвёлся)
- watchlist navigatingMobs sendBlockUpdated:1883 = 0 вхождений

## Гейты (preregister §S7-158)
| Гейт | Значение | Вердикт |
|---|---|---|
| PG2 (0 NCDFE + pop 150k VALID + ARMED полный) | NCDFE=0, pop=150000, rc все 0 | **PASS** |
| PG3 (TPS ≥ +25%) | медиана 0.90 → **1.60 = +77.8%** (crawl 1.1→1.6, база плоская 0.7-0.9) | **PASS** (лучше leg #2: 1.50) |
| PG4-strict (young GC ≤ 136) | 118 → **169** (+43.2%) | **FAIL** |
| CRASH-FREE (0 NPE, 0 uuid-dup) | 0 / 0 | **PASS** |

**Вердикт leg #4: PARTIAL (PG4-strict FAIL ⇒ банкинг на этом лего НЕ проводится; экономика и безопасность рычага подтверждены повторно).**

## Калибровочный анализ PG4 (основание амендмента ДО leg #5)
Эмпирические точки (young GC / TPS-медиана / пауза-total):
- CUMULATIVE база: 118 / 0.90 / 13.46s → GC-на-работу = 131.1
- leg #3 (БРАК, но = чистый vanilla+inside+flush профиль): **140 / 0.80 / 16.84s → 175.0 (+33.5% к базе!)**
- leg #2: 155 / 1.50 / 20.40s → 103.3 (-21.2%)
- leg #4: 169 / 1.60 / 21.57s → 105.6 (**-19.5%**)

Два систематических дефекта строгой записи PG4 (абсолютный кап base+15% = 136):
1. **Кап ниже рантайм-варианс ванильных ранов**: leg #3 с НЕактивным region_threads (ванильный тик) дал 140 > 136 при той же конфиг-цепочке, что и база (118). Гейт не отделяет «мост аллоцирует» от «ран просто шумит».
2. **Гейт штрафует throughput**: Region-THREADS выполняет 1.78× тик-работы в секунду; young-GC число при равной аллокации-на-тик растёт пропорционально работе. Нормировка на работу: leg #4 = -19.5% (диета S7-158c работает: ноль-аллок steady state), leg #2 = -21.2%. Прирост absolute GC — следствие роста выполненной работы, не аллокационного долга.
3. Долг-спираль отсутствует: full GC = 0; worst pause 185.7ms (база 179.2); пауза-доля soak wall 7.2% vs 4.5% базы — суб-линейно к работе 1.78×; TPS-медиана УЖЕ включает GC-паузы (конечная метрика).

## Preregister-амендмент PG4' (объявлен ДО leg #5, GOAL СТАТУС S7-159)
PG4' = (young_gc / TPS_median) ≤ (base 131.1) × 1.15 = 150.8 **И** worst-pause ≤ base_worst × 1.25 (224ms) **И** full GC = 0.
- leg #4 под PG4': 105.6 ≤ 150.8, worst 185.7 ≤ 224, full=0 → **PASS**
- Строгая запись PG4 продолжает публиковаться в каждом absorb (прозрачность); банкинг = PG2 + PG3(оба лега) + PG4'(оба лега) + 0 инцидентов.
- Запрет на пост-хок пересмотр остаётся: амендмент обоснован данными leg #3/#4, объявлен и закоммичен до диспатча leg #5.

## ТОП пожирателей leg #4 (методика «ТОП-ПОЖИРАТЕЛЬ → ∞», ось CPU, взаимоисключающие лейны)
1. entity-фаза 55.87% (71814) → декомпозиция фазы: residual 40.7%, broadphase/collision 21.3%, fluid 16.5%, movement 8.6%, AI 8.3%, navigation 4.6%, inside-blocks 0%
2. GC/JIT-native 37.60% (48333)
3. tracker 1.99% (2556; sweep живой) | chunk-workers 0.76% | infra 0.83%
После банкования REGION-THREADS следующий круг ТОПа = свежий профиль leg #5 → крупнейший под-лейн entity-фазы (residual требует RECON-декомпозиции: baseTick/SynchedEntityData/sync/paletted-чтения) против broadphase/collision (уже 2×REFUTED по кэш-рычагам) и fluid (3×REFUTED).
