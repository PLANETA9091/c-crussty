package net.minecraft.world.level.levelgen.synth;

/**
 * Smoke-test stub of the kernel ImprovedNoise (1.21.10 shape is irrelevant
 * here: the bridge treats it as an opaque identity key in the HANDLES map).
 * public only so the default-package smoke driver can build instances —
 * the real kernel class keeps its own access; Lives ONLY in
 * noise/src-new/test — never shipped, never embedded.
 */
public final class ImprovedNoise {
    int i;
}
