# RESEARCH-458-P — закон-11 idea-пакеты 2-4 (agent-P, развитие RESEARCH-457-H)

Ветка: round-458p-ideas @ 96cc2704 (origin/master). Роль: как agent-H ×457 (его ПАКЕТ-1
broadphase/query выложен; пакеты 2-4 не успели выйти) — я достраиваю ОСТАЛЬНУЮ ПОЛКУ.
Фильтры: закон 4 (пустой lever = ваниль), закон 5 (запреты: …inside_bitmask/alloc_diet/
zero_alloc/flat_traversal… — не воскрешать), закон 6 (подсистема целиком, ОДИН bulk-JNI/тик),
закон 8 (chunk/worldgen ось), закон 11 (безумие + интернет-рисёрч, реальные URL).
АНТИ-ДУБЛЬ-КАРТА: agent-M = дельта-сериализация чанков (НЕ трогаю); agent-N = paldelta
PalettedContainer.get demux (НЕ трогаю); agent-D = noisesimd fill-family bulk-JNI (НЕ трогаю);
agent-H ×457 H01-H07 = broadphase/query-ось (развиваю только с пометкой «что взял»);
agent-I/J/K = SWAR/SAP/Hilbert/roaring (позаимствованы ПО ССЫЛКЕ с указанием источника).
Метод: z-ai web_search, 22 запроса, сырые JSON: /tmp/458p/q*.json.

## ДАННЫЕ (absorb round-chkmono457-11/12, runner 7040413/7053858, обе RED −3.6/−3.7)
Лейн (базлайн 115655 сэмплов → нога 104163/104260):
- inside_volatile 12.01→16.63/16.30 (+4.62пп доли); АБСОЛЮТНО: 13 890 → 17 322/16 994 = **+22-25%** —
  НЕ только механика делителя: лейн реально толстеет на серт-стеке (fluid для сравнения −13% абс).
- java_util 7.01→8.96/8.73 (+1.95пп); АБСОЛЮТНО 8 108 → 9 333/9 102 = **+12-15%**.
- items 31.17→0.00; broadphase 15.66→8.82/9.73; nav_ai 14.16→2.75/3.30; paletted 6.41→5.58/5.18; fastutil 8.54→6.73/6.44.
- GC: young=96-101, **Full=9** (банк 7), total 22.1/19.8s (банк 18.8s), avg 180-211ms, max 2696-2789ms —
  GC-долг растёт на серт-ногах: GC-debt relief остаётся стабильным механизмом закона 8.
Гипотезы роста inside_volatile: (A) механическая — умерли items/broadphase/nav (−49пп доли), знаменатель
сжался; (B) абсолютная +25% — invalidation-driven: chunk6-sched меняет порядок/батчинг тиков чанков →
всплески setBlockState → secWrite-бампы ген-эпох inside_snap → MISS-путь (ванильные volatile-чтения
PalettedContainer) чаще; CHM.get snap-сервиса кормит java_util. Проверка — P37 (probe).

---

## ПАКЕТ-2 — CHUNK/WORLDGEN ОСЬ (закон 8) [ID-P21..P27]
Гейт оси: GEN-ось инертна на преген-фикстуре (0.0% soak-CPU ×421-C) → noise-идеи помечены
«GC-debt relief carrier», не soak-плоскости. Уже занято другими: paldelta demux (N), chunk-дельта (M),
noisesimd fill (D), parse section-cache cmp420_chunk2 (в мастере), chunk4/5 send snapshot+encode (мастер).

- [ID-P21] Parse-cache widen: biomes+light-секции | ARCH | chunk-parse/serialization 33.38% burst-alloc (RESEARCH-F) | РАЗВИТИЕ cmp420_chunk2: ВЗЯЛ cache-first decode + twin-lambda MISS; ДОБАВИЛ покрытие biomes-секции (сейчас twin lambda$parse$7 = ваниль-путь БЕЗ кэша) + light-секций: гомогенные biome-секции прегена дают hit-rate ~90%, template.copy() вместо полного codec/MapDecoder/DataResult прогона | src/chunk_parse.rs, chunkparse/net/minecraft/world/level/chunk/storage/ChunkParseOps.java, src/classfile.rs | тот же структурно-equal tag-ключ + template.copy(); онлайн-самотест re-decode 1/100, miss → ванильный twin-лямбда путь | +1-2пп на reload-сценах + GC-debt relief (ShortArrayList/DataResult мусор burst ↓) | tag-hash коллизии → ключ (codec-identity, tagHash, len) | https://github.com/RelativityMC/C2ME-fabric + https://github.com/mojang/datafixerupper (/tmp/458p/q20_dfu.json)
- [ID-P22] Chunk-tick eligibility flat-плоскость (ServerChunkCache/ChunkMap scheduling) | ARCH | ServerChunkCache scheduling 4.6-5.2 (профиль ×456/H) | java собирает плоские массивы предикатов тика чанков (dist, flags, playerXZ) → ОДИН bulk-JNI → битмаска eligible-чанков → java строгий хвост в ваниль-порядке | новый chunksched/…ChunkSchedOps.java, src/region_threads.rs, src/stagger.rs, src/classfile.rs | superset-предикаты бит-в-байт (Rust решает только математику решений, порядок итерации java); начинать с distance-гейта only | 4.6-5.2 × 25-40% → +1.2-2пп; закон-8 носитель вне GEN → живой на soak | предикатов много — фазный widen, v1 = 1 ретаргет | https://github.com/Tuinity/Moonrise + https://bugs.mojang.com/browse/MC/issues/MC-310372 (/tmp/458p/q04_chunktick.json)
- [ID-P23] RegionFile sector-read pool + read-ahead | ARCH | RegionFile-IO burst (RESEARCH-F) + GC-debt | ВЗЯЛ у chunk_parse знание формата секций; ДОБАВИЛ IO-плоскость: пул 4KiB-секторных буферов + read-ahead соседних чанков региона, батч-вызов на группу запросов; java читает из пула, miss → ванильный FileChannel | новый src/region_io.rs, chunkparse/…ChunkParseOps.java (точка входа) | байты бит-в-байт (len+CRC контроль), пул НЕ алиасится (одна копия на выдачу) | +0.5-1пп reload + GC-debt relief; инертно на soak-CPU → carrier-член оси | FileChannel в нативе опасен — v1 только read-ahead в java-heap пул | https://github.com/Tuinity/Moonrise + https://wiki.vg/Region_File_format (/tmp/458p/q07_region.json)
- [ID-P24] Noise octave scratch-pool (GC-debt relief, ИНЕРТНО на soak ×421-C) | moonshot | gen-alloc (0% soak-CPU; GC Full=9 vs банк 7) | ВЗЯЛ дисциплину lever_scoped_allow у noisesimd; НЕ дублирую fill-family (agent-D ускоряет вычисление — этот убирает аллокацию): thread-лок пул флет-аккумуляторов ImprovedNoise/PerlinNoise/NormalNoise (октавные интерполяционные массивы переиспользуются между октавами/колонками) | src/noise_fill.rs (гейт), src/improved_noise.rs, src/perlin_noise.rs, noise/ kernel-shapes | значения бит-в-байт: буфер заполняется ЦЕЛИКОМ до чтения; fill-family lever не затрагивается | GC-debt механизм +6-8пп (канон ×421-C) на ген-сценах; monotone-член оси на прегене | escape-analysis мог уже скаляризовать — сначала alloc-профиль | https://github.com/Steveplays28/noisium (/tmp/458p/q05_noisium.json)
- [ID-P25] Noise-router 2D-cache (C2ME flat cache; GC-debt relief) | moonshot | gen-axis (инертно на soak) | флет-кэш 2D-срезов континентальность/эрозия/температура (XZ→double[]), переиспользуемый между 3D-колонками одной чанк-колонки; чистый java-мемо + epoch-инвалидация | src/proto_blend_cache.rs (паттерн кэша), src/improved_noise.rs, noise/ | бит-в-байт значения (тот же порядок вызовов шума); шум не мутируется миром → инвалидация только seed/размер | burst-генерация −20-30% шума; резервный GC-debt член пары | кэш-ключ по колонке — тривиально | https://github.com/RelativityMC/C2ME-fabric (/tmp/458p/q06_c2me.json)
- [ID-P26] Chunk-send burst coalescing (развитие cmp437_chunk4+cmp444_chunk5) | ARCH | chunk-send ось (players_packets 0.01% — monotone-член) | ВЗЯЛ snapshot-sender + encode-кэш; ДОБАВИЛ оконное слияние: очередь PlayerChunkSender coalesced — N чанков/окно одним flush, свет-пакеты группой; байты пакетов НЕ меняются (chunk5 гарантирует), меняется межтайминг flush | chunksend/net/minecraft/server/network/{ChunkSendOps,ChunkPacketEncodeOps}.java, src/chunk_send.rs, src/chunk_send5.rs | порядок отправки = ванильный порядок очереди; anti-xray shouldModify bypass как в chunk4; события per-send сохранены | +0.3-0.8пп netty/syscall хвост burst; carrier-зависимый, monotone | flush-латентность клиенту — константа окна ≤1 тик | https://github.com/CaffeineMC/krypton (flush consolidation) + https://github.com/Tuinity/Moonrise (/tmp/458p/q22_krypton.json). НЕ дублирует agent-M: дельты пакетов он НЕ делает, я тоже.
- [ID-P27] Serialization scratch-arena (pooled section buffers, MISS-путь chunk4/5) | moonshot | serialization alloc burst | арена-пул per-thread для section-буферов + heightmap-NBT scratch: MISS-путь chunk4/5 заполняет арену вместо свежих byte[]/NBT-объектов; одна защитная копия на выдачу | src/chunk_send.rs, chunksend/…ChunkPacketEncodeOps.java, src/palette_gather.rs (скэттер-дисциплина) | байты те же; полная перезапись до отправки + длина-контроль; selftest re-encode | burst alloc −15-25% → GC-debt relief; monotone | алиасинг при удержании ссылки кодеком — копия на выход | https://github.com/real-logic/agrona (flyweight/buffer pool) + https://foojay.io (object reuse) (/tmp/458p/q14_arena.json, q17_flyweight.json)

## ПАКЕТ-3 — INSIDE_VOLATILE 16.6% РОСТ (TOP-1 остаток компо-носителя chunkmono) + JAVA_UTIL 9% РОСТ [ID-P31..P37]
Анализ: рост ДОЛИ — механика (мёртвые лейны), рост АБСОЛЮТНЫЙ +22-25% — реальное утолщение:
главная гипотеза (B) invalidation-driven (bампы ген-эпох inside_snap от chunk-sched всплесков
setBlockState → ванильный volatile MISS-путь чаще, CHM.get кормит java_util). Все идеи-лечения
целят в: (1) сократить число/цену volatile-чтений на MISS, (2) дешёвый HIT/реестр вместо CHM,
(3) вынести discovery в один bulk-JNI. inside_bitmask (закон 5) НЕ используется нигде.

- [ID-P31] INSIDE-BATCH plane: discovery тика одним bulk-JNI | ARCH | inside_volatile 16.6 TOP-1 | java-мост собирает батч ВСЕХ checkInsideBlocks-кандидатов тика (entity id, x/y/z, bb-флет, section-ключи) → ОДИН JNI → Rust возвращает битмаску секций-кандидатов (superset: отсекает заведомо пустые/не-влияющие) → java строгий ванильный хвост visit-обхода по кандидатам; закон 6: подсистема целиком (gate+discovery+tail) | entityinside/net/minecraft/world/entity/InsideBlockOps.java (гейт-паттерн), новый src/inside_batch.rs, src/inside_cache.rs (bridge), src/classfile.rs (Entity stage) | candidate-superset → strict-хвост бит-в-байт; oracle-харнес entityinside/harness расширяется; dirty-list для секций-мутантов между сборкой и тиком (паттерн entity_index.rs) | захват 30-50% discovery-части 16.6пп → **+5-8пп к ноге chunkmono**: ноги +11.5/+9.1 → 16-19пп, в паре с a15 +9.7/a26 +12.4 (Δ≤50k) → **пара ≥+20 — самый прямой путь волны** | секции-мутанты — dirty-list обязателен; NCDFE-канон (EARLY-define) | https://github.com/CaffeineMC/lithium/issues/125 (suffocation/inside optimizations) + https://bugs.mojang.com/browse/MC/issues/MC-260743 (/tmp/458p/q01_inside.json)
- [ID-P32] SNAP sidecar-реестр: CHM<LevelChunkSection,Snap> → флет-индекс | ARCH | java_util 9 РОСТ + inside HIT-путь | РАЗВИТИЕ inside_snap: ВЗЯЛ снапшот-плоскость (сервинг palette[] HIT, ген-эпоха secWrite); ДОБАВИЛ sidecar: per-section стабильный int-индекс (Rust-регистрация при создании секции) → Snap по флет-массиву вместо CHM.get; epoch-проверка одним int-cmp | src/inside_snap.rs, entityinside/…InsideSnapOps.java, src/entity_index.rs (паттерн перехвата создания) | тот же объект Snap, та же инвалидация; miss-секция → fail-closed fallback на CHM; selfTest count раз/100 | java_util рост −40-60% (CHM-часть) → +0.8-1.2пп; HIT-путь дешевеет → inside −1-2пп косвенно | секции вне перехваченных сайтов создания — fallback | https://www.burnison.ca (CHM internals) + docs.oracle.com/javadocs (ConcurrentHashMap) (/tmp/458p/q03_chm.json)
- [ID-P33] Volatile-demotion: getAcquire на остаточных volatile-чтениях MISS-пути | ARCH | inside_volatile фенс-хвост | javap-таргет: в retarget-теле inside-сайта (Level.getBlockState→InsideSnapOps.snapGet уже есть) + PalettedContainerOps-классе читать поля через VarHandle.getAcquire (JMM-эквивалент volatile-read по наблюдаемости; x86 = plain load, C2-перестановки контролируемы) | src/inside_snap.rs, paletted/net/minecraft/world/level/chunk/PalettedContainerOps.java (класс уже определён), src/classfile.rs | getAcquire (НЕ opaque) в v1 — JMM-канон; single-writer region-модель; рандом-поп оракул | фенс-хвост MISS 2-4пп × 30% → +0.6-1.2пп | JMM-тонкости дольных полей — только acquire-режим до оракула | https://gee.cs.oswego.edu/dl/html/j9mm.html + https://brooker.co.za/blog/2012/09/10/volatile.html + https://jpbempel.github.io/2015/05/26/volatile-and-memory-barriers.html + https://stackoverflow.com/questions/58336714 (/tmp/458p/q02_volatile.json, q16_fence.json)
- [ID-P34] Inside-cache квант-гейт: K-тик-покойные мобы (развитие inside_cache) | ARCH | inside_volatile 16.6 | ВЗЯЛ static-гейт (deltaMovement==0 && позиция бит-равна кэшу) + флет-слоты replay; ДОБАВИЛ класс «квант-покой»: |Δ|<ε за K тиков — сервинг из флет-слотов с инкрементальным replay ванильных effect-вызовов | src/inside_cache.rs, entityinside/…InsideBlockOps.java, src/classfile.rs | replay = бит-в-байт (тот же контракт, что static-гейт); ε/K константы оффлайн-оракула; включение класса — только после 10k-сцен; фоллбэк чистый ваниль | подвижные-но-медленные ~30-50% движущихся → захват 2-4пп из 16.6 | ε-критерий меняет тайминг stateful-эффектов — оракул-гейт | https://github.com/CaffeineMC/lithium/issues/125 (/tmp/458p/q01_inside.json)
- [ID-P35] StepBasedCollector гибрид-v2 (развитие inside_diet; разбор провала DDA 21/350k) | moonshot | inside alloc 31.18% alloc-семплов лейна (RECON14C) | ВЗЯЛ inside_diet ONE box+ONE visitor и факт отказа full-DDA (21/350k); ДОБАВИЛ v2-гибрид: ванильный walk = источник истины, visit-транскрипция идёт ПАРАЛЛЕЛЬНО с выборочной верификацией 1/200 (offline 1M-сцен + online), расхождение → one-shot disarm-латч (паттерн batchOk nav_plane) | entityinside/…{InsideDietOps,InsideDietVisitor}.java, src/inside_diet.rs | наблюдаемая разница 0 (disarm раньше); верификатор сэмплирован | 1 visitor+2 AABB на вызов ↓ → java_util/GC-debt −; нога +0.5-1пп | верификатор жрёт CPU — 1/200 и только на ноги-прогоны | https://github.com/CaffeineMC/lithium/issues/125 + https://foojay.io (/tmp/458p/q01_inside.json, q14_arena.json)
- [ID-P36] Inside-epoch fast-gate (комбо к P32) | ARCH | inside/java_util хвосты | per-section счётчик мутаций (bump уже есть в secWrite) → флет-массив эпох; checkInsideBlocks сначала сравнивает int-эпоху секции с эпохой слота кэша — несовпадение → полный путь | src/inside_snap.rs, entityinside/…InsideSnapOps.java, src/inside_cache.rs | miss → ваниль; hit-инвариант самтестом; эпоха 64-бит (ABA/wrap) | −0.5-1пп java_util/inside хвостов в связке с P32 | тривиальный | q02/q03 (/tmp/458p/)
- [ID-P37] java_util attribution probe (инструментальный, evidence-first) | инструмент | java_util 9 РОСТ | разовый прогон серт-ноги с async-profiler --classes/--lines по java_util-сэмплам → таблица CHM vs PriorityQueue vs ArrayList vs.lambda → адресные таргеты для P32/P42/P43; NOT-A-BENCH маркер, 0 эффект на ваниль | harness-инфра банка, entityinside/harness | read-only, маркер NOT-A-BENCH | не нога; уточняет прогнозы P32/P42/P43 ±30% | нет | https://github.com/jvm-profiling-tools/async-profiler + q13_gc (/tmp/458p/q13_gc.json)

## ПАКЕТ-4 — NAV_AI 2.75-3.2 ОСТАТОК + GOALOPS/BRAIN ХВОСТЫ [ID-P41..P47]
Контекст: nav_plane (cmp405) уже батчит shouldRecomputePath на block-update; sense-плоскость +
eqsnap2 (agent-C) + H03 Hilbert-таргетинг (agent-J) заняты — НЕ дублирую; остаток = MoveControl/
Navigation.tick/goal-selector/brain-обходы.

- [ID-P41] Path-node neighbor cache (Lithium nether-pathfinding style) | ARCH | nav_ai остаток 2.75-3.2 | NodeEvaluator-соседи кэшируются флет-таблицей (posKey→neighborMask byte), повторные запросы узла внутри пути — из кэша; MISS = ваниль | src/nav_plane.rs, src/nav_pool.rs, новый navpath/…PathOps.java, src/classfile.rs | маска = superset соседей; порядок обхода ванили при miss; STRICT-off до оракула 10k путей; инвалидация по chunk-ревизии | 2.75-3.2 × 20-30% → +0.6-1пп | pathfinder-мутации — инвалидация | https://modrinth.com/mod/lithium (changelog: nether pathfinding optimizations) (/tmp/458p/q19b_lithpath.json)
- [ID-P42] Goal canUse-пре-гейт: sense-memo (развитие sense+goalops) | ARCH | goalops/brain хвосты 1-2пп | ВЗЯЛ SenseOps-плоскость и GoalOps flat-priorities (iter-2); ДОБАВИЛ memo-хэш требуемых сенсов на goal: canUse пропускается если сенсы не менялись (инвалидация по sense-событиям — супerset) | goalops/net/minecraft/world/entity/ai/goal/GoalOps.java, sense/…/SenseOps.java, src/goal_selector.rs, src/mobs_sense.rs | ложный-отрицательный запрещён: все пишущие сайты сенсов перехвачены sense-плоскостью; STRICT-off до оракула; selfTest-инвариант | 1-2 × 30-40% → +0.4-0.8пп | скрытые мутации сенсов вне плоскости — самотест | https://docs.pufferfish.host (DAB goal tick-rates) (/tmp/458p/q10_dab.json)
- [ID-P43] Brain flat-memory registry (развитие brainhook + паттерна goal_selector) | moonshot | brain-хвост 1-1.5пп | ВЗЯЛ flat-паттерн iter-2 и BrainOps-snapshot (brainhook); ДОБАВИЛ реестр Memories: MemoryModuleType.ordinal → флет-массив значений, snapshot БЕЗ Map-обхода; Map = источник истины, флет = read-зеркало после мутаций тика | src/brainhook.rs, randomtick/src/BrainOps.java, goalops/…GoalOps.java, src/mobs_ai.rs | read-зеркало строится после мутаций; STRICT-off до оракула (tie-break isEmpty-порядка); disarm-латч на расхождение | 1-1.5 × 25-35% → +0.3-0.5пп | зеркал-расхождение — disarm | q11_brain.json + https://docs.pufferfish.host
- [ID-P44] MoveControl/Navigation детерминированная математика батчем (navmath-плоскость) | ARCH | nav_ai остаток (MoveControl+Navigation.tick = 60-70% остатка) | плоские массивы (operationType, posDelta, rot, speedMod) всех мобов → ОДИН bulk-JNI → готовые решения (desiredRotation, operation, forward) → java применяет в порядке set-итерации; только MoveControl.tick в v1 | src/nav_plane.rs (паттерн navDecide+ERR-ladder), src/mobs_soa.rs, новый navmath/…MoveOps.java, src/classfile.rs | IEEE754-пошаговая javap-транскрипция (канон nav_plane.rs, строгий контракт); fallback decideJava бит-в-бит; NCDFE-канон | остаток × 30-40% → +0.8-1.2пп; с P41 → +1.5-2пп | много сайтов — фазный widen; v1 = 1 ретаргет | https://modrinth.com/mod/c2me-fabric (батч-дисциплина) + q12_move.json
- [ID-P45] sendBlockUpdated navigatingMobs pre-gate (widen nav_plane #2) | ARCH | nav_ai block-update хвост 0.5-1пп | ВЗЯЛ у agent-K ID-H04 roaring-occupancy паттерн: chunk→navigatingMobs флет-маппинг из home-chunk mirror; block-update в чанке БЕЗ навигирующих мобов → zero-work до батча | src/nav_plane.rs, src/entity_index.rs (mirror), NavPlaneOps.java | пустой чанк = superset-гейт (нет мобов — нет запросов) из тех же mirror-вызовов; selfTest count | 0.5-1 × 40-60% → +0.3-0.6пп дёшево | mirror-расхождение — самотест | https://docs.rs/roaring (позаимствовано у ID-H04)
- [ID-P46] PathNavigation tick-deadband (stuck-check батч) | ARCH | nav_ai хвост | shouldRecomputePath-стак-детекция раз/тик/моб → батч: тот же мост navmath решает stuck-флаги всех мобов одним JNI (входы: delay-счётчики, node-дистанции) | navmath/…MoveOps.java, src/nav_plane.rs, src/classfile.rs | как P44: javap-транскрипция + decideJava fallback | +0.3-0.5пп | меньше P44 | https://modrinth.com/mod/lithium + q12_move.json
- [ID-P47] GoalSelector transition-diff batch (goal_selector iter-3) | ARCH | goalops хвост | ВЗЯЛ iter-2 flat-проходы tick/tickRunningGate; ДОБАВИЛ: stop/start переходы батчами с отложенным пересчётом флагов управления (1 пересчёт/тик вместо per-goal), insertion-order стоп/старт сохранён | src/goal_selector.rs, goalops/…GoalOps.java | порядок stop/start = ваниль insertion-order; флаги пересчитываются в той же точке; оракул-харнес GoalOps; чётные/нечётные тики Paper-сплит тестом | +0.3-0.6пп goalops-хвост | Paper-сплит tickRunningGoals(Z) — регрессия чётность/нечётность | https://docs.pufferfish.host (/tmp/458p/q10_dab.json)

---

## §БЕЗУМИЕ-ТОП — 3 идеи с лучшим шансом ≥+20 пары (прогноз Δ до ноги)
1. **[ID-P31] INSIDE-BATCH plane** — TOP-1 остаток компо-носителя 16.6пп, живой лейн
   (абсолютный рост +25% на обеих ногах chkmono457-11/12). Захват discovery-части 30-50% →
   **Δ +5-8пп к ноге**; chunkmono ноги уже +11.5/+9.1 → нога-прогноз 16-19.5пп; банк якорей
   a15 +9.7@6813110 / a26 +12.4@8671791 (Δ≤50k) → пара ≥+20 — самый прямой путь волны.
   Почему шанс лучший: parity-паттерн candidate-superset + strict-хвост уже канон
   (nav_plane/collide_batch/entity_index — 3 прецедента), оракул-харнес entityinside/harness есть.
2. **[ID-P32+P36] SNAP sidecar-реестр + epoch fast-gate** — комбо-нога на inside_snap (код на
   70% существует: CHM, secWrite-бамп, снапшот-сервис). Δ +1.5-2.5пп суммарно + бьёт java_util
   РОСТ (9%) и часть inside; поверх P31 доводит ногу к 19-21пп → шанс дожать пары ≥+20.
   Почему: минимальный parity-риск (fallback CHM fail-closed), monotone, дешёвый имплементационно.
3. **[ID-P22] Chunk-tick eligibility flat-плоскость** — единственный живой на soak закон-8 сайт
   вне GEN (ServerChunkCache scheduling 4.6-5.2пп; GEN-идеи P24/P25 — только GC-debt carriers,
   инертны ×421-C). Δ +1.2-2пп + Moonrise-подтверждённая важность scheduling; в связке с
   GC-debt relief (P21/P24/P27) даёт второй базис ноги пары на chunkmono-оси.
   Честная пометка: ось тянет на 2-4пп суммарно — пары ≥+20 только как добавка к носителю с
   ногами +11.5/+9.1 и якорю банка, НЕ самостоятельный монстр.

Не-кандидаты на топ: P33 (JMM-тонкости до оракула), P41-P47 (хвосты <1.2пп каждый — только
комбо), P24/P25 (soak-инертны по определению).
Гигиена: golden_443.py не использовался; CLAIMS/GOAL не тронуты; запрещённые лейны не
воскрешались (inside_bitmask/alloc_diet/zero_alloc — нет ни в одной идее; P27/P35 — scratch-формы
в существующих семействах chunk5/inside_diet, не levers).
